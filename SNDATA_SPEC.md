# SNDATA 逆向规范 — `TAIKOU2_SCENARIO` / `TAIKOU2_SAVEFILE`

> 来源：脱壳后的 `F:/Games/Taikou2/SNDATA1.TR2` / `SNDATA2.TR2`（剧本定义，各 40856B）与 `SAVEDATA.TR2`（完整场景状态，328088B）
> 引擎代码依据：`scripts/_unpacked_mem.bin`（VA 基址 0x400000，脱壳产物，2MB）
> 反汇编工具：capstone 5.0.7（venv 安装）
> 关联脚本：`scripts/_sndata_*.py` 系列、`scripts/sndata_records.json`

---

## 1. 文件总览

| 项 | 值 |
|----|----|
| `SNDATA1/2.TR2` | 剧本定义，各 **40856 B**（0x9F98）；Magic `"TAIKOU2_SCENARIO"`（16B @0x00）|
| `SAVEDATA.TR2` | 完整场景状态，**328088 B**（0x50198）；Magic `"TAIKOU2_SAVEFILE"`（16B @0x00）|
| 双剧本关系 | 非副本：逐字节差异 96.1%（39271/40856）→ 两个不同剧本初始化数据 |
| 文件表引用 | 引擎以 `"F:SNDATA1.TR2"`(`0x5095a0`)/`"F:SNDATA2.TR2"`(`0x5095b0`) 间接加载；SAVEDATA 用 `"A:SAVEDATA.TR2"`(`0x509590`) |
| 加密 | SNDATA 自 0x598 起为 **XOR 单字节加密流**（密钥见 §3）；SAVEDATA **不加密**（首 4K 字节 distinct=217，无主导字节）|

> ⚠️ **架构澄清**：`SAVEDATA.TR2` 在偏移 **0x198** 处内嵌了一个完整的 `TAIKOU2_SCENARIO` 场景块（与 SNDATA 文件同格式，且 0x198+0x14=0x1ac 起有 700B 武将登场标志，与 SNDATA 的 0x14 偏移完全对应）。即：SAVEDATA = SAVEFILE 头(0..0x197) + 完整场景块(0x198 起) + 运行时状态区。

---

## 2. SNDATA 顶层字节布局（✅ 由 0x47f350 主解析器实证）

```
0x0000   16B   "TAIKOU2_SCENARIO" 文件头 magic
0x0010    2B   obj[0x90]   —— 16-bit LE 校验和（解密后流字节累加和 mod 0x10000）；0x47f4da 处 cmp [esi+0x92],[esi+0x90] 校验（sc1=0xb84b, sc2=0x8013；**续202 emu 端到端坐死**）
0x0012    2B   obj[0x94]   —— XOR 密钥种子；key = byte[0x12]^byte[0x13]，喂 0x47d960 整段 XOR 解密（sc1=0x0c, sc2=0x0a；**续202**）
0x0014  700B   写入全局 0x519288  —— 700 武将各 1 字节「登场/激活」标志（值域 0/1，354/346 分割）
0x02d4  700B   写入第二缓冲（0x4eb5c0 分配）—— 旗标/状态码（值域 0/255/1）
0x0598  ~39424B  加密流（XOR 解密，详见 §3）—— 含全局状态段 + 49B 实体记录区
0x9F81   23B   尾区
```

- `0x519288` 的 700B **不是城池归属**，而是「武将是否登场」布尔数组（已交叉验证：武将#13 信长=1、#16 藤吉郎=1、#27 庆次=0）。
- **`0x598` 处的字节非 `0x39`**（sc1=0x0c, sc2=0x0a），故 0x47f350 不会额外读 49B 到 0x519640。

### 2.1 「833×49」的真相（重要纠偏）
文件 40856 = 16 + 833×49 + 23 的整除吻合**属实**，但那 833 条 49B 记录**不是从 0x10 明文排列**。
- `0x47d890`（49B 记录访问器）的 `offset = idx*49 + 0x10` 是**文件明文视图**的读取；
- `0x47f350`（主解析器）走的是**从 0x598 起的解密流**，两者访问同一文件的不同逻辑层。
- 解密流（0x598 起 39424B）实测包含：~608B 全局状态段 + 实体记录区（详见 §4）。

---

## 3. XOR 单字节加密流机制（🚨 本回合核心突破）

- `0x47f350` 把对象[0x94] 改写为 `byte0 ^ byte1`（sc1=0x0c, sc2=0x0a），此即**解密密钥**。
- `0x47d960`（解密缓冲初始化）：分配 **0x2000（8192）字节**滑动缓冲 → 从文件 0x598 读 0x2000 密文 → 对每字节 `xor 密钥` 解密 → 用尽后 `0x47d9b0` 再读下一块 0x2000。
- 读取原语：
  - `0x47da10` = 从解密流读 1 字节（缓冲满则重填+XOR）
  - `0x47da50` = 从解密流读 2 字节（小端 word）
  - `0x47da80` / `0x47dac0` = 写 1/2 字节（存档方向）
- 密钥同时是「默认填充哨兵」：sc1 用 0x0c 填充、sc2 用 0x0a 填充（解释此前所有「0c0c/0a0a 填充族」现象）。
- **解密后**：值域从窄分布展宽为 0–255 全分布（distinct=256），证实解密正确。

---

## 4. 49 字节记录格式与扇出（✅ 0x47d890 / 0x47fc60 实证 · 2026-08-31 续159 精修）

### 4.0 访问器 `0x47d890` = `read_record(ctx, idx, dst)`（续159 坐实）

```
lea  ecx, [eax + eax*2]        ; idx*3
shl  ecx, 4                    ; idx*48
lea  edx, [ecx + eax + 0x10]   ; idx*49 + 0x10   ← stride=49, 文件头=16B
call dword ptr [0x4fb0a8]      ; lseek(h, off, SEEK_SET)
push 0x31                      ; 49              ← 记录长
call 0x4411b0                  ; read(ctx, dst, 49)
```

⇒ **stride=49 / 文件头 16B / 记录长 49B** 三数一次坐实；数据源 = SNDATA 文件**按索引直接 seek+read**，非内存表。

### 4.0.1 `0x47fc60` 扇出布局（续159 精修：是 **strcpy** 不是块拷贝）

局部帧 212B（`sub esp, 0xd4`，零填充）。扇出：

| 目标 | 源 | 机制 | 长度 |
|---|---|---|---|
| `*arg2` | `word[rec+0]` = id_word | `mov` | 2B |
| `*arg3` | `word[rec+2]` = sub_word | `mov` | 2B |
| `*arg4` | `word[rec+4]` = flag ‖ rel_word<<8 | `mov` | 2B |
| `0x522c88` | `&rec[6]`（payload 起点 0）| **strcpy** | 至首个 null |
| `0x522c60` | `&rec[0x13]`（payload 起点 13）| **strcpy** | 至首个 null |
| `0x522c70` | `&rec[0x20]`（payload 起点 26）| **strcpy** | 至首个 null |

⚠️ **续158 旧表字节数有误**：`0x522c60`/`0x522c70` 不是「单字节」，而是 payload 内**起点 13 / 26 的重叠后缀视图**。payload 内无 null 时，三视图长度恰为 **43 / 30 / 17**（零填充帧保证不越界）。

### 4.0.2 `0x4ebfc0`=strlen / `0x4ebfe0`=strcpy（续159 逐指令验证）

- `0x4ebfc0` = **strlen(s)**：`test ecx,ecx` 空指针返 0 → `mov dl,[ecx]` 逐字节 `test dl,dl` → `inc eax` 计数。
- `0x4ebfe0` = **strcpy(dst, src)**：`mov esi,[esp+0xc]`(src) + `mov eax,edx`(返回 dst) → `mov [edx],cl` 循环 → 末尾 `mov byte[edx],0`。

🔴 **推翻续158**：`0x4ebfe0` 不是「块拷贝助手」，且**没有 size 参数** ⇒ 续158「仍未知② size 参数来源」**前提不成立**。

### 4.0.3 `0x47ff50` = 记录文本格式化器（续159）

头三字全 0 → `strcpy(arg1, [0x509648])` 提前返回；否则 `0x4ec010(dest,src)` 追加 + `0x4ebe60` 数值格式化 + `0x49f120` 取字段 `cmp ax,2; jge` 分支；`0x522c88` 补齐到 **14**、`0x522c70` 补齐到 **18**（`0x49f0b0` 填充）。

### 4.0.4 🔴 P0 证伪：不存在 type→handler 函数指针表（续159）

全镜像连续代码指针游程扫描：≥24 项者 7 张、**≥128 项者仅 1 张**（`@0x501004`，153 项全为 `jmp` thunk，**0/153 触及记录缓冲**）；`0x522c50–0x522d00` 字面引用仅 33 处 / 16 函数，其中 `sbb/xor/adc eax, 0x522c52` 等属**算术立即数假阳性**。
⇒ **分派不是「按类型选 handler」**；类型作为数据参与头字比较/索引。P0 计划 (A)「搜函数指针表建 type→handler 全映射」**此路不通**。

### 4.0.5 数据侧：payload 本质是二进制，非文本（续159）

833 条中仅 **14 条**含 ≥20 个 null；主要类型（`0x0c`×249、`0xf3`×111、`0x3e`×15、`0x08`×12、`0x0d`×11、`0x8c`×10）「三段均含 null」比例 **0%**；带 null 的只有 type `0x00`/`0x01`（布尔开关）。扫描出的 `'H:D!8`、`麶H2:<K` 之类「GBK 文本」全是随机字节巧合解码，**非真文本**。

### 4.0.6 主循环 + 分派管线（⭐ 续160 · 2026-08-31）

🔑 **分派是代码级「匹配器 + 谓词 + 值开关」管线，不是 type→handler 函数指针表**——从「看到真实分派代码」角度二次坐实 §4.0.4 的 P0 证伪。参考实现 `scripts/sndata_dispatch_ref.py`（**22/22**）。

**① 主循环 `0x4e8604` / `0x4e8625`（每记录一次，回边在 `0x4e8604`）**
```
0x4e8604  call 0x480240        ; 取记录索引 (esi)，-1=结束哨兵
0x4e860b  cmp  si, -1  → je 0x4e8774
0x4e8615  lea eax,[esp+0xa] / lea ecx,[esp+8] / lea edx,[esp+0xa]   ; 4 参设置
0x4e8624  push esi → 落入 0x4e8625
0x4e8625  call 0x47fc60        ; 扇出（见 §4.0.1）
0x4e862d  cmp [esp+6]/[esp+8]/[esp+0xa], 0   ; 头三字全 0 判定
```
⚠️ **偏移纠正（续159 旧记）**：`push` 之间 `esp` 下移 4，故 arg2=`[E+0xa]`=**id_word**、arg3=`[esp+8]`=**sub_word**、arg4=`[E+6]`=**flag_rel**（续159 记的「arg2/arg3/arg4 偏移」需以此为准）。

**② 分派管线（代码级，二次证伪 P0）**
```
头三字全 0 → call 0x47b160(0x50d820) → 跳回        ; 空/哨兵记录
否则      → call 0x47b390(0x50d834)  ; 匹配器(薄壳→0x47b2e0, 返回 eax 0/1)；eax==0 跳回
           call 0x47fb80            ; 谓词(内部 call 0x47f350)；eax==0 跳回
           mov  ax, [0x5205fe]      ; 全局「当前记录类别/类型」选择器
           sub eax,0 → je 0x4e86e7   ; 簇0: 0x492e20/0x493140/0x48cc20/0x48d350/0x48e690/0x4a0b20
           dec eax   → jne 0x4e870f  ; 簇1: 0x492ed0/0x4931f0/0x4ac9c0/0x4ae380/0x4a0b70
           ; 否则(else) 簇: 0x524740/0x491e70/0x4873b0/0x491f90/0x492050/0x499050
```
⇒ `0x5205fe` 是全局选择器（被 `0x418459`/`0x44edaa` 等写入）；`0x4e8625` **只处理匹配模板 `0x50d834` 的类别**，其余类别由其它 handler（如 `0x4e89cd` 链）处理。

**③ 🔧 纠正续158 两处过度断言**
- (a) 续158 称「`0x47ff68` = 按记录类型(ax)分派的 dispatcher」——**错**。`0x47ff68` = 先 `call 0x47fc60` 扇出，再判定 `word[esp+0x18]/[esp+0x64]/[esp+4]` 全 0 → 全 0 则 `strcpy(0x509648 默认名池)` 拷默认名。**是「头全 0 → 默认名」助手，非分派器**。
- (b) 续158 称「记录循环不在 `0x47f350` 静态调用图内」——**误导**。`0x47fb80`(记录谓词) 直接 `call 0x47f350`；`0x47f350` **在记录路径内**，只是它不直接引用 49B 缓冲（0x522c88/60/70），而是解析记录引用的子表（实体/城/国/名）。

**④ payload 解析基础设施（type→field schema 真相所在）**
- handler 簇**不直读 0x522c88**；经 `0x4802e0`/`0x492800`/`0x4ec8c0` 等读兄弟缓冲 **`0x522ca0`**（同族：0x522c88/0x522c60/0x522c70/0x522c98/0x522ca0/0x522cc0），把 payload 写入游戏表（0x524978/0x524918/0x524740）。
- ⇒ **完整 type→field schema 须追 `0x4802e0`/`0x492800`/`0x4ec8c0`（payload 解析引擎）+ 各 handler 表写入**；master 注册间接分派器无静态 xref，**宜用 emu 提取全 833 条映射**。

<details><summary>旧表（续158 及以前，保留作历史对照）</summary>

| 记录偏移 | 目标 | 说明 |
|----------|------|------|
| `[0:2]`  | 0x509648 系（10B 区，push 0xa）| 经 `0x4ebe60` 反序列化 |
| `[2:4]`  | 另一 10B 区，经 `0x4ebe60` | |
| `[4:6]`  | 经 `0x49f120` 比较/分类 | |
| `[0x13=19:]` | 0x522c60 | |
| `[0x20=32:]` | 0x522c70 | |
| `[6:49]`（43B）| **0x522c88** | 主体 payload，经 `0x4ebfe0` 整体拷贝 |

</details>

- `0x522c88` = 43B 实体结构缓冲；`0x522ce4` = `0x522c88 + 0x5c(=92)` → **每城 1 字节的 92 城数组**（城数强吻合）。
- ⚠️ 续158 称「`0x47ff68` 是单条记录处理器」**表述不准**：`0x47ff68` 实为函数 `0x47ff50` 内部的一条 `call 0x47fc60` 指令，真正的处理器是 **`0x47ff50`**（见 §4.0.3）。

### 4.0.7 记录「类别 → 资源表 → payload 选择器」schema 重构（⭐ 续161 · 2026-08-31）

🔑 **833 条 SNDATA 记录不是「统一 43B payload 字段描述符」，而是「剧本资源 / 资产 + 数据初始化指令」**。证据来自续160 的「handler 簇经 `0x4802e0` 读 `0x522ca0` 写游戏表」线索 + 本轮解码各 handler 簇持有的**资源文件名表**。参考实现 `scripts/sndata_template_ref.py`（**19/19**）。

**① 类别 handler 簇持有的不是字段描述符，是资源文件名表（16B/条，NUL 终止）**
| 资源表基址 | 所属类别 handler 簇 | 资源名（6 项） |
|------------|---------------------|----------------|
| `0x506b20` | type-0 簇 `0x492e20` | `B:MAPCHIP.LZW` / `B:MAPCHAR.LZW` / `B:SHOP_BG.LZW` / `B:SHOP_OBJ.LZW` / `B:SHOP_MSK.LZW` / `B:ANMSEQ.LZW` |
| `0x506ba0` | type-1 簇 `0x492ed0` | `C:TOWNCHIP.LZW` / `C:TOWNCHAR.LZW` / `C:HEXMES.LZW` / `C:KOSENGRP.LZW` / `D:FACE.LZW` / `A:EXTFACE.PK8` |

→ 记录语义模型升级为 **「类别作用域」**：`type → 类别 handler → 资源表(0x506b20…) → payload 选择器字段 → 具体资源 + 参数`**，不再是跨类型统一的 43B 字段布局。

**② payload 选择器 = 定长构造器 `0x4ec8c0`**
```
and al, 0xfb                      ; 掩码
cmp eax, 3 ; ja <default>         ; 4 路 switch
jmp dword ptr [eax*4 + 0x4ec948]  ; 派发表
push 0 / push 1 / push 2 / push 0x1000   ; 三态尺寸
call dword ptr [0x4fb07c]         ; 登记/分配器（注册该选择器）
```
资源经 `0x492800` → `0x4f40b0`（3 参转发）登记。⇒ 每条记录携带「选哪个资源 + 该资源尺寸 + 写入目标游戏表」的参数，由类别 handler 执行。

**③ `0x50d820` / `0x50d834` 是 GBK 消息串，非类型模板**
- `0x50d820` = 「这个进度无法使用。」、`0x50d834` = 「读取游戏进度，\n可以吗？」
- ⇒ 记录路径还含**消息 / 事件指令**维度；并提示续160「匹配器 `0x47b390(0x50d834)`」标签可能需重审——`0x50d834` 更可能是进度读取对话的消息串，而非类型分派键（待续161 下一步(C) 确认）。

**④ 全镜像资源表阵列（5 张，16B 对齐、NUL 终止、`.LZW/.PK8` 扩展名）**
| 阵列基址 | 项数 | 首几项 |
|----------|------|--------|
| `0x5030d8` | 3 | `C:HBMAP.LZW` / `C:HBCHAR.LZW` / `C:HBCHAR2.LZW` |
| `0x5034c0` | 5 | `C:HJMAP.LZW` / `C:HJCHAR.LZW` / `C:HKMAP.LZW` |
| `0x506ac0` | 12 | `B:MMLDATA.LZW` / `B:GRPDATA.LZW` / `B:MESSAGE1.LZW` … `B:ANMSEQ.LZW` |
| `0x506b90` | 7 | `C:PK8DATA.LZW` / `C:TOWNCHIP.LZW` … `A:EXTFACE.PK8` |
| `0x509590` | 5 | `A:SAVEDATA.TR2` / `F:SNDATA1.TR2` / `F:SNDATA2.TR2` |

⚠️ **关键布局事实**：type-0 表 `0x506b20` 是 `0x506ac0` 阵列的内嵌条目 [6..11]；type-1 表 `0x506ba0` 是 `0x506b90` 阵列的内嵌条目 [1..6]。⇒ handler 引用的基址**未必是阵列起点**，扫表验证须断言「字节区间覆盖」而非「起点相等」。

⚠️ **工具坑（续161）**：资源文件名表解码窗口须取 **15 字节**（`B:SHOP_OBJ.LZW` / `B:SHOP_MSK.LZW` = 14 字符，13 字节窗口会截断致正则失败提前 break）；扩展名 `.IDX`（`C:NPKDATA.IDX`）不匹配 `.LZW/.PK8` 正则，阵列在其处自然断开（索引文件非资源，不计入资源表）。

→ **仍未知 / 下一步**：① 每类别 payload 选择器精确偏移（追 `0x4ec8c0`/`0x47f350` 对 `0x522ca0` 的字段布局）；② 全类别资源表枚举 + 类别↔handler↔表全表（5 张阵列逐一归属到 handler 簇）；③ 重审续160「匹配器模板 0x50d834」；④ emu 插桩 `0x47fc60` 出口抓全 833 条 type→handler 映射。详见 `BREAKTHROUGHS.md` 续161。

### 4.0.8 「payload 解析引擎」实为资源加载管线（⭐ 续162 · 2026-08-31）

🔑 **续161「类别 handler 持资源文件名表 + 选择器构造器」只是冰山一角。本轮追 `0x4802e0`/`0x492800`/`0x4ec8c0` 对 `0x522ca0` 的逐字段布局，证明所谓「payload 解析引擎」实为「资源加载管线」**——不是把 49B 记录的字节映射到游戏表字段，而是「把资源文件名表拷入 0x522ca0 暂存 → 由选择器按记录偏移挑具体资源 → 经文件 I/O 读入 → 写入游戏表」。参考实现 `scripts/sndata_payload_engine_ref.py`（**33/33**）。

**① 四函数管线（从底层到上层）**
| 函数 | 角色 | 关键形态 |
|------|------|----------|
| `0x4f40b0` | `memmove`（重叠感知） | `rep movsd` + 边界判定 `cmp edi,esi`/`cmp edi,eax` + `jbe`/`jb` |
| `0x492800` | 3 参转发壳 | `push` + `call 0x4f40b0` |
| `0x4802e0` | 资源表 → 暂存缓冲 | `push 0x20; push 0x522ca0; call 0x492800`（`memmove(0x522ca0, 资源表基址, 0x20)`）+ `push 0x522ca0; call 0x4ec8c0`（选择器构造）+ `movsx ecx, word ptr [esp+0x18]`（取记录算出的资源表偏移作 selector 尺寸） |
| `0x4ec8c0` | 资源选择器构造器 | `strcpy` 剥 `X:` 盘符 + `and al,0xfb` 掩码 + `cmp eax,3; ja` 4 路 switch 跳表 `0x4ec948`（尺寸 0/1/2/0x1000）+ `call dword ptr [0x4fb07c]`（登记） |

**② 资源读取走文件 I/O（非内存拷贝）**
- 读取器 `0x441330` / `0x441360` 经 `call dword ptr [0x4fb0a8]`（同 `read_record` 的 lseek/read 派发；`0x441360` 经 `mov edi,[0x4fb0a8]; call edi` 间接调用），再 `call 0x441170`（读原语）写入游戏表 `0x524978` / `0x524918`。
- 失败路径 `0x47bde0` = `push 0x5094d0`（「资源未找到」类消息串）+ `call 0x47ae80`（日志/报错）。

**③ 类别 = 一组资源加载器（每个簇 handler 引用资源表不同条目）**
- type-0 簇三 handler：`0x492e20` → `push 0x506b20`（entry0 `B:MAPCHIP.LZW`）、`0x493140` → `push 0x506b30`（entry1 `B:MAPCHAR.LZW`）、`0x492f80` → `push 0x506b40`（entry2 `B:SHOP_BG.LZW`）。
- 各自 `call 0x4ec8c0` 构造 selector 并写游戏表 `0x524978`/`0x524918` ⇒ **6 资源各对应一个 handler 入口**（续161 资源表 6 项逐一映射）。

**④ `[0x4fb07c]` 是运行期填入的加载器回调槽（非代码）**
- 静态值 `0x003000`（< BASE 0x400000，非本映像代码），与 `[0x4fb0a8]`（文件 I/O 派发槽）同构——运行时由初始化阶段写入真实函数指针，按资源类型分派加载器。

⚠️ **结论修正（推翻续161「扁平字段映射」残留表述）**：所谓「payload 解析引擎对 0x522ca0 逐字段偏移」并非「记录字段→游戏表字段」的扁平映射，而是「资源文件名表(16B 数组)拷入 0x522ca0 暂存 → selector 按记录算出的偏移挑具体资源 → 文件 I/O 读入 → 写游戏表」。**`0x522ca0` 是资源表暂存缓冲，不是记录字段视图**。

→ **仍未知 / 下一步**：① 每记录「资源表偏移」取自 payload 哪一段（追 `0x492e20` 如何由 49B 记录算出 `[esp+0x18]`）；② `[0x4fb07c]` 真实加载器（emu 抓初始化写入点）；③ 资源文件（`B:MAPCHIP.LZW` 等）解包后如何映射到 `0x524978` 逐字段（解 LZW/PK8，续156 同类）；④ emu 插桩 `0x47fc60` 出口抓全 833 条 type→handler 映射。详见 `BREAKTHROUGHS.md` 续162。

### 4.0.9 记录 payload 在资源加载层「不参与资源选择」（⭐ 续163 · 2026-08-31）

🔑 **续162 仍未知① 假设「0x4802e0 的 [esp+0x18] 是记录派生偏移」被推翻**。该值经栈帧数学确认是 `0x4802e0` 的 **第 2 个栈参数(arg1=尺寸)**，且资源基址在绝大多数站点为硬编常量。参考实现 `scripts/sndata_rec2offset_ref.py`（**17/17**）。

**① 形参布局（栈帧数学）**
- `0x4802e0(arg0=资源表基址 @[esp+4], arg1=尺寸 @[esp+0x18])`：`mov eax,[esp+4]`（arg0）、`movsx ecx,word ptr [esp+0x18]`（arg1）、`ret 8`（2 栈参数）。
- 证明 `[esp+0x18]` 是 arg1：本函数 `push esi` 一次 + `call 0x492800`(memmove) 时 `push 0x20/eax/0x522ca0` 三次；读 `[esp+0x18]` 时 esp = 入口-16 ⇒ 该地址 = 入口+8 = arg1，**绝非记录缓冲偏移**。

**② 全镜像 44 个 call 0x4802e0 分类（base=最后 push=arg0，size=倒数第二 push=arg1）**
| 维度 | 结果 |
|------|------|
| 尺寸(arg1) | type-0 簇 `0x492e20`/`0x493140`/`0x492f80` 与绝大多数站 = **硬编 `4`**；少数站为参数派生（`0x47d741` size=`[esp+0xc]`、`0x47d7d4` size=`eax`） |
| 基址(arg0) | **36/40 可分类站 = 常量 VA**（如 `0x506b20`/`0x506ba0`/`0x506b40`/`0x5030d8`/`0x5030e8`/`0x506bd0`…），每 handler 加载固定资源集 |
| 寄存器派生基址 | 仅 4 站：`0x43379d`(esi=`[esp+0x194]` 参数)、`0x47d7d4`(ecx=`[esp+0x2c]` 参数)、`0x47fab9`、`0x47d741`(base=`0x5095a0/0x5095b0` 由 `ax` 选，实为常量) |
| 直读记录缓冲 | **0/44 站** 直接读 `0x522c88/0x522c60/0x522c70` ⇒ 记录 payload 不在本层参与资源选择 |

**③ 结论（推翻续162 仍未知①前提）**
- 记录 43B payload 在 `0x4802e0` 层**不参与资源选择**；资源身份由「记录类型 → 簇 handler」固定决定（续160/161）。`0x492e20` 进函数即 `push 4; push 0x506b20; call 0x4802e0`（无先读记录）。
- ⇒ **43B payload 的角色是「场景初始化 / 事件数据」**，由谓词/匹配逻辑（续160 `0x47fb80`/`0x47b390`）消费，而非资源选择器。
- 🔧 修正续161「6 资源各一 handler」过度断言：单 handler `0x492e20` 实际加载 3 张资源表（`0x506b20`/`0x506ba0`/`0x506b40`），资源数 ≠ handler 数。

→ **仍未知 / 下一步**：① 寄存器派生基址站（`0x43379d`/`0x47d7d4`/`0x47fab9`/`0x47d741`）的 base 最终溯源——是否 `read_record`/`fanout` 层由记录字段派生；② 43B payload 在谓词/匹配逻辑中被读的具体字节（字段语义）；③ 资源文件解包后 byte→field（续162 仍未知③）；④ emu 插桩 `0x47fc60` 抓全 833 条映射。详见 `BREAKTHROUGHS.md` 续163。

### 4.0.10 payload 字节语义模型：6B 头(3 word) + 43B payload(3 重叠视图)（⭐ 续164 · 2026-08-31）

🔑 **续163 证明 payload 不参与资源选择后，本续定位「43B payload 到底被谁消费、怎么结构化」。答案：扇出 `0x47fc60` 把它拆成头 3 字 + 3 个重叠视图，由谓词经序列化解析器(`0x47d860`/`0x47f350`)消费。参考实现 `scripts/sndata_payload_bytes_ref.py`（**19/19**）。

**① 头部 schema（续159 实锤 + 本续精修）**
| 偏移 | 宽度 | 字段 | 去向 |
|------|------|------|------|
| rec[0..1] | word | `id_word` | *arg2（分发帧） |
| rec[2..3] | word | `sub_word` | *arg3 |
| rec[4..5] | word | `flag`‖`rel<<8` | *arg4 |
| rec[6..48] | 43B | **payload** | 见下 3 视图 |

**② 43B payload 以 3 个重叠视图暴露（修正续159「0/13/26」→ `6/19/32`）**
| 视图缓冲 | strcpy 源 | 起始偏移 | 最大长度（无 NUL） |
|----------|-----------|----------|---------------------|
| `0x522c88` | `&rec[6]` | rec+6 | 43 |
| `0x522c60` | `&rec[0x13]` | rec+19 | 30 |
| `0x522c70` | `&rec[0x20]` | rec+32 | 17 |

（扇出另有 +4 位移副本 `&rec[10]`/`&rec[23]`/`&rec[36]` 写入其它缓冲，属同一 payload 的错位视图。）

**③ 谁消费这些视图（枚举结论）**
- 匹配器 `0x47b390`/`0x47b2e0`：**0 处读 `0x522c..`** → 模板/状态门控，非字段匹配（印证续161「0x50d834 是消息串」）。
- 谓词 `0x47fb80`：取记录指针 `edi=[esp+0xac]`，`push edi` 传给序列化解析器 `0x47d860`，并 `call 0x47f350` → **43B payload 被当「3 段重叠子结构模板/覆盖块」解析**（续160「模板+稀疏覆盖」模型坐实）。
- 簇 handler（6 个：0x492e20/0x493140/0x492f80/0x492ed0/0x4931f0/0x524740）：**0 处读 `0x522c..`** → 印证续163：payload 不参与资源加载。

⇒ **完整 payload 模型**：`49B = 6B 头(3 word) + 43B payload(3 重叠视图 rec+6/19/32)`；资源身份由记录类型固定（续163），payload 是场景初始化/事件数据，由谓词经 `0x47d860`/`0x47f350` 解析为子结构。

→ **仍未知 / 下一步**：① 3 视图各自对应什么子结构（`0x47d860`/`0x47f350` 逐字段解析）；② 头 3 字→簇 handler 路由（`0x5205fe` 写入方）；③ 资源文件解包 byte→field（续162/163 仍未知③）；④ emu 插桩 `0x47fc60` 抓全 833 条映射。详见 `BREAKTHROUGHS.md` 续164。

### 4.0.11 3-文本视图消费路径 + 0x47f350 载入剧本主解码器（⭐ 续165 · 2026-08-31）

🔑 **承接续164「下一步① 反汇编 0x47d860/0x47f350 对 3 视图逐字段解析」——结果推翻续164③ / 续160③「43B payload 被当 3 段重叠子结构交给 0x47f350 解析」假设**。参考实现 `scripts/sndata_scenario_decode_ref.py`（**ALL PASS：C1=18 子解码器 / C2=表映射 / C3=3 视仅 3 引用且作字符串 / C4=stride**）。

**① 3 视缓冲全镜像仅 3 处引用，且被当字符串消费（非二进制子结构）**
- 窗口扫描(0x400000–0x5A0000，规避数据段反汇编停滞)：`0x522c88`/`0x522c60`/`0x522c70` 字面引用恰 **3 处**，全在 `0x480000` 区域（`0x48006a`/`0x4800a8`/`0x4800f8`，均 `push` 作实参）。
- 该 `0x480000` 函数把视图：`strcpy`(0x4ebfe0) → `strlen`(0x4ebfc0) → **空格对齐补到宽度 14/18/18**(`0x49f0b0`，`0xe`/`0x12`/`0x12`) → `strcat` 进输出缓冲 `0x509740`/`0x5038d0`/`0x50973c`/`0x509738`。
- ⇒ **3 视图 = 记录(rec+6 / rec+19 / rec+32)的 3 个文本列**（名称/描述/类别？须对照游戏），**不是二进制子结构**；续164③「43B payload 被当 3 段重叠子结构模板/覆盖块由 0x47f350 解析」**前提错**（0x47f350 全镜像 0/3 处引用 0x522c..）。

**② 0x47f350 = 「载入剧本(scenario)」主解码器，与 49B 记录路径独立**
- 入口：取模式 `0x5205fe` + XOR 密钥字节 `0x520604` + `read 700B`(0x519288) + `read 8KB`(0x524978) → `0x47d960` XOR 解密(密钥=`[esi+0x94]`) → 再 `call 18 个子解码器`。
- 18 子解码器程序化枚举（0x47f350 内对 0x47d000..0x47f600 的 call，剔除守卫 `0x47f5b0` 与解密器 `0x47d960`）：恰 **18** 个：`0x47dae0,0x47dce0,0x47e130,0x47e3a0,0x47e440,0x47e5a0,0x47e770,0x47ea80,0x47ebb0,0x47ecb0,0x47ed10,0x47ed70,0x47ee50,0x47ef00,0x47f050,0x47f0a0,0x47f1b0,0x47f210`。
- 各用 `0x47d910`(1B)/`0x47d930`(2B) setter 写全局游戏表；循环（370/200/49…）在子解码器内部编码，故「setter 调用点计数」是小值，不代表流总字节（见⑤纠偏）。

**③ 18 子解码器 → 目标表权威映射（★ 续166 修正：以 §5.2 emu 实测为权威）**
- ⚠️ **续165 C2 的 base 自动探测有缺陷**：`analyze_sub` 取「所有字面 push 全局地址的最小值」，而子解码器普遍把 `0x516610`/`0x5203c0`/`0x517c70` 等**暂存/scratch 基址**与真实目标表一起 push，最小值恒取 scratch ⇒ 续165 把 `0x47e3a0/0x47e440/0x47e5a0` 错并到 `0x516610`、`0x47ecb0/0x47ed10/0x47ed70` 错并到 `0x5203c0`、`0x47ee50..0x47f210` 错并到 `0x517c70`。**真实映射见 §5.2（Unicorn 实跑 `0x47f350` 逐段挂钩计数，续99）**。
- 「setter 调用点计数」(续165 C2 列) 是 **下限**：子解码器内 370/200/49 循环使真实流字节远大于该数（见 §5.2 真实长度列）。

| # | 子解码器 | 续165 C2 setter调用点 | §5.2 真实长度 | §5.2 基址 | 语义（§5.2 / §3.x） |
|---|----------|----------------------|--------------|-----------|---------------------|
| S0 | `0x47dae0` | 91B | 22 | — | 全局/头部 |
| S1 | `0x47dce0` | 88B | 21830 | `0x519868` | 武将实体池(370×59) |
| S2 | `0x47e130` | 74B | 5200 | `0x51eb88` | 城/町表(200×26, §3.17.6) |
| S3 | `0x47e3a0` | 98B | 245 | `0x519548` | 国情基表(49×5) |
| S4 | `0x47e440` | 112B | 539 | `0x5179b8` | 49国政治/关系表(49×11, §3.18.6) |
| S5 | `0x47e5a0` | 128B | 180 | `0x5197b0` | 6槽武将表(6×30；S5=60000钳制字段归属，续138) |
| S6 | `0x47e770` | 120B | 46 | `0x516610` | 玩家/事件上下文 S6(46B, §3.20) |
| S7 | `0x47ea80` | 90B | 3200 | `0x516a28` | 每城运行时状态表(200×16, 续155/§3.22) |
| S8 | `0x47ebb0` | 80B | 360 | `0x517850` | 台词表(30×12, MSGX 1000..1029/700..728) |
| S9 | `0x47ecb0` | 69B | 80 | `0x519238` | 台词表(20×4, MSGX 2000..2019/696..699) |
| S10 | `0x47ed10` | 66B | 120 | `0x5176a8` | 台词表(30×4, MSGX 3000..3029) |
| S11 | `0x47ed70` | 62B | 3800 | — | 物品段(189×19, §3.19) |
| S12 | `0x47ee50` | 57B | 160 | `0x517728` | 物品副池(20×12, 续98/续76) |
| S13 | `0x47ef00` | 50B | 2280 | `0x5185b6` | 目標/目標記録表(20×114, 运行期填充) |
| S14 | `0x47f050` | 34B | 1176 | `0x51dc60` | 49国外交关系矩阵(严格上三角49×48/2, 续95/续100) |
| S15 | `0x47f0a0` | 32B | 25 | `0x5203c2` | S15 3段标志(25B, 续150) |
| S16 | `0x47f1b0` | 20B | 40 | `0x519680` | 文本ID表(20×2, MSGX 能力説明) |
| S17 | `0x47f210` | 16B | 133 | `0x517c73` | 3B前缀(0x517c70..72)+10×13B |

⇒ **0x516610 = S6（已定名，§3.20）；0x517c70 = S17 前缀区（§5.2）** —— 续165「待定名」(B) 已由 §5.2 闭合。18 子解码器计数与 §5.2 S0..S17 完全一致（§5.1「17 段」为续60 旧枚举，已过时；其流基址亦误用 `0x598`，权威应为 `0x58C`，见 §5.2）。

**④ 49B 记录读取器 stride 坐实（C4）**
- `0x47d890`=`read_record`：`lea ecx,[eax+eax*2]; shl ecx,4; lea edx,[ecx+eax+0x10]` = `idx*49+0x10`；`call [0x4fb0a8]`(lseek) + `push 0x31`(49) `call 0x4411b0`(read)。

**⑤ 🔧 纠正续164③ / 续160③**
- 续164③「谓词 0x47fb80 把 43B payload 当 3 段重叠子结构交给 0x47f350 解析」与续160③「0x47f350 在记录路径内解析 payload」**部分错**：0x47f350 确由谓词 0x47fb80 调用，但它解码的是**剧本 bulk 块**（实体/城/国/事件旗…序列化数据），谓词传来的 `edi`(记录指针)在 0x47f350 内未作为 3 视缓冲源——3 视缓冲(rec+6/19/32)的真正消费者是 `0x480000` 显示格式化器。续164 的「3 段重叠子结构」应改为「3 文本列」。

⚠️ **§5 流字节地图已有权威版（续166 闭合续165 仍未知①）**：本续「setter 调用点计数」量级小(累计~1400B)与旧 §5.1「累计 32KB+」不符的疑点，由 **§5.2（续99，Unicorn 实跑）** 解决——§5.2 给出 18 段真实流长度（S1=21830、S2=5200…累计≈39436B=流总长），与 §5.1 数值同源但 §5.2 经 emu 校准更可靠；§5.1「流偏移 0/22/21852/…」为续60 旧表（基址误用 0x598），**以 §5.2 为权威**，§5.1 标记过时。本续 C1/C3/C4 仍成立（18 子解码器计数、3 视仅 3 引用作字符串、stride idx*49+0x10）。

→ **仍未知 / 下一步（续166 已闭合①②，见下）**：③ 3 视文本列具体玩法（名称/描述/类别？须对照游戏）；④ 真正逐类型 payload schema 仍须走 `0x4e8625`/`0x4e89cd` 主循环（续159 证伪 type→handler 表后，改逐指令追头四字比较链）。① ② 已由 §5.2(续99, emu) 闭合：18 子解码器全部定名(S0..S17)、§5 流字节地图以 §5.2 为权威（§5.1 过时）、0x516610=S6/0x517c70=S17 不再是「待定名」。详见 `BREAKTHROUGHS.md` 续165 / 续166。

### 4.0.12 SNDATA 215 型逐字节「字面字段名」静态最大化闭包（⭐ 续229 · 2026-08-31）

🔑 **承接续221（215 型语义域分类）+ 续228（唯一真·余步=215 型字段枚举）。把 215 型×43B payload 的每个字节，从「domain 分类」推进到「具体名字 + 结构角色」这一静态可达的最大闭包。参考实现 `scripts/sndata_payload_fields_ref.py`（**ALL PASS**）+ 产物 `scripts/sndata_payload_fields.json`（215 型全字段地图）。

**① 字面字段名 = 域 + 解析名 + 结构角色**
- 每字节已分类为 5 种 domain（续221）：`entity_idx`(4423) / `city_idx`(2905) / `province_idx`(1856) / `zero_const`(38) / `bool`(23)。
- 用已破解名称表把 index 字节解析为具体名字（index 空间：实体 0..369 / 城 0..199 / 国 0..48，与续200/续221 一致）：
  - `entity_idx` → `scripts/entity_names_sc1.json`（370 实体槽，sc1）；
  - `city_idx` → `scripts/castle_names.json`（200 城）；
  - `province_idx` → `scripts/province_politics.json`（49 国）。
  - 例：type=0x00 `pos0`(entity_idx)→屋代景赖、`pos4`(city_idx)→黑川；type=0x13 `pos0`(city_idx)→滨松、`pos1`(province_idx)→尾张。
- 结构角色检测（按 domain 序列）：单域数组 / (entity,city) 配对 / (entity,province) 配对 / (entity,city,province) 三元组 / 布尔旗 / 混合；给每字节位置化标签 `e0/c0/p0…`。

**② 215 型形状分布**
- `mixed` **208 型**：entity/city/province 索引任意序组合（如 type=0x00 是三类索引混合列表，印证续221「武将-城-国 索引混合」）；
- `entity_array(43)` **6 型**：43 字节全为实体索引数组（如 type=0x3e）；
- `flags_only` **1 型**：type=0x4b，纯布尔开关记录（稀有的场景状态枚举，续221）。

**③ 真·字面玩法角色（主将/目标城/所属国…）—— 续230 架构证伪：游戏中本就不存在该 schema**
- 续163/164 已证类别簇 handler 0/44 不读记录缓冲；续229 实测：修好 IAT `ret N` 桩(续195 崩溃根因) + 4 路文件 I/O 桩(续194 读管线)后，emu boot 主循环 `0x4e8600` 单记录**不崩**但 payload 字节读/表写捕获 = **0**，实证「逐簇 handler 运行期仿真」方法受桩钳制。
- **续230 决定性结论**：对 `0x47f350` + 全部 18 子解码器(S0..S17) 各 0x600 字节窗口扫描，call `read_record`(0x47d890)=**0**、引用三视缓冲(0x522c..)=**0**、×49 记录 stride 乘法=**0** ⇒ **数据解码路径与「49B 记录 + 三文本视图」显示路径彻底不相交**。游戏对 payload 没有「按 type 的字段结构」——`0x47f350`(thiscall, ecx=scenario obj) 把文件当**扁平流**经 `0x47da10`(读字节)/`0x47da50`(读字) 顺序解出固定 section S0..S17 → 固定游戏表（实体/城/国…，续165 已映射），全程无 type 分派。故「逐 type 的 payload 字节→字段」映射在游戏里**本就不存在**，续229 静态闭包(domain+解析名+结构角色) 已是该 payload 的**静态可达最大理解**。

**④ 双路径模型（续230）**
- **Path A（显示）**：主循环按 idx 读 49B 记录、把 43B payload 当文本铺进 0x522c.. 三视图（续164/221/229，load 菜单展示），215 型 = 显示/资源类别。
- **Path B（数据）**：`0x47f350` 扁平流 bulk 解码 S0..S17 → 固定表；流原语 `0x47da10`=`al=byte[[obj+0x9a]]; inc [obj+0x9a]; add [obj+0x92],al`（每 0x2000 字节触发 `0x47d960` XOR 解密块）、`0x47d910`=`cmp [obj+0x8c],0; je 跳过存储`（skip 模式）、`0x4411b0`=`read(handle=[obj],buf,n)`。
- 流原语探针：`scripts/_probe_f350.py` / `scripts/_probe_stream.py` / `scripts/_probe_decode_path.py`（决定性 0/0/0 不相交校验）。

⇒ **交付物**：`scripts/sndata_payload_fields.json`（215 型全字段地图：domain + 解析名 + 结构角色 + 型形状）+ `scripts/sndata_payload_fields_ref.py`（自测 ALL PASS：域闭环一致 / 9184 索引字节解析零越界 / 结构自检）。`scripts/sndata_payload_capture.py` 作为 emu 脚手架保留（捕获=0，且续230 证明即便跑通也只会复现 S0..S17 固定表映射，不会产出按 type 的字段 schema）。

### 4.1 「49B 记录不是城池归属直接赋值表」（🚨 已双重确认）
- 对 43B payload 全偏移 + 全 word 偏移扫描：**无任何偏移值域被收紧到 0–91（城码）或 ≤699（武将id）**，所有字节值域 2–254。
- 推论：城码/武将id **不以裸小整数**存于 49B 记录；它实为「模板 + 稀疏覆盖」的剧本参数/旗标/事件块。
- 因此「谁拥哪城、谁效忠谁」的真实赋值表在 **SAVEDATA 的运行时状态区**（328KB，不加密）或 SNDATA 的解密流实体区。

---

## 5. 17 段全局状态序列化器（✅ 0x47dae0…0x47f1b0）· 续60 流地图闭合

`0x47f350` 在解密缓冲就绪后，依次调用 17 个子函数。包装器：`0x47d910`=1B、`0x47d930`=2B（`[obj+0x8c]==0` 时为 **加载**：流→内存）。

### 5.1 XOR 流字节地图（文件 `@0x598` 起，sc1 key=`0x0c` / sc2=`0x0a`）

| 流偏移 | 大小 | 序列化器 | 目标 / 语义 |
|--------|------|----------|-------------|
| 0 | 22 | `0x47dae0` | 全局 `0x5205f0..0x520604`（月/旗等） |
| 22 | 21830 (=59×370) | `0x47dce0` | 实体池 `0x519868` stride≈0x2f ×370 |
| 21852 | 5200 (=26×200) | `0x47e130` | 城/町对象 `0x51eb88` stride 0x1f ×200 |
| **27052** | **245 (=5×49)** | **`0x47e3a0`** | **国情表 `0x519548`（见 province_spec）** |
| 27297 | 539 (=11×49) | `0x47e440` | 国扩展 `0x5179bc` stride 0x0e ×49（首都指针+字段） |
| 27836 | 180 | `0x47e5a0` | ×6 实体 |
| 28016 | 46 | `0x47e770` | 固定块 |
| 28062 | 3200 | `0x47ea80` | ×200 实体 |
| 31262 | 360 | `0x47ebb0` | ×30 |
| 31622 | 80 | `0x47ecb0` | ×20 |
| 31702 | 120 | `0x47ed10` | ×30 |
| 31822 | 19 | `0x47ed70` | ×13 |
| 31841 | 160 | `0x47ee50` | ×20 |
| 32001+ | ~70 | 其余 | 尾段（计数器需逐函数精核） |

> ⚠️ **纠偏**：旧「17 段≈608B 无循环」**作废**——多数段是 **带循环的实体数组**；累计主段约 **32KB+**（流总长 39424）。  
> ⚠️ **纠偏**：明文「833×49 数据块 rec23–830」逐字节指纹 **不是**玩法主数据层——主导字节仍是填充键 `0x0c`/`0xf3`；真结构在本 XOR 流。

### 5.2 国情表字段（`0x47e3a0` → `0x519548`）

见 `GAME_DATA_SPEC.md §3.9.7` / `scripts/province_spec.json` / `scripts/province_ref.py`。

---

## 6. 引擎加载架构（✅ 已定位；🔴 槽步进 = **40960 × 8 槽**，续199 纠偏，旧标题「修正 40960→20480」已废）

| 函数 VA | 作用 |
|---------|------|
| `0x47d720` | **`OpenTR2(file_idx, mode)`**（🔴 续199 定名，原记 `LoadSNDATA(idx)`）：`cmp ax,2; jae` ⇒ `0`→`F:SNDATA1.TR2` / `1`→`F:SNDATA2.TR2` / **`≥2`→SAVEDATA**；`mode` 0=读 1=写 |
| `0x47d780` | `LoadSAVEDATA`：**栈上构造 `A:SAVEDATA.TR2` 并动态替换盘符**（`al=[0x526c78]+0x40` 覆写 `byte[esp]`），且 `and eax,4; or eax,edx` 把 `size_class` 或上 bit2 |
| `0x47d800` | **磁盘重试循环**：失败弹 `0x5095e8`「请在插入<启动盘或保存盘>。」→ `jmp` 回重试 |
| `0x47d860` | **`SeekSlot(idx)`：`offset = idx*40960 + 0x198`**（🔴 续199 纠偏，原记 20480，见 §12.1）；`[ecx+0x96]=off`；`fseek` |
| `0x47d890` | **`ReadSlotMeta49(idx,dst)`**：`offset = idx*49 + 0x10`，读 49B（🔴 续199：作用于 **SAVEDATA** 而非 SNDATA，见 §12.2）|
| `0x47d8d0` | **`WriteSlotMeta49(idx,src)`** —— 与 `0x47d890` 前 0x29 字节**字节级同构**，仅末尾 `0x4411d0`(write) 取代 `0x4411b0`(read) |
| `0x47f350` | **SNDATA 解码器(pass1)**：读头+700+700 → 建解密缓冲(0x47d960) → 调 18 个 SUB1 子加载器（**仅处理非记录的序列化数据**：实体池/城池/国/名表等，用 `0x47d910`/`0x47d930` 1B/2B setter 写 `0x519868`/`0x51eb88` 等，**不读 49B 记录**）|
| `0x47fb80` | **`LoadSaveSlot(slot)`**：`OpenTR2(2,0)` → `SeekSlot` → **`0x47f350` 两趟**（`word[esp+0x98]` 1→0 = two-pass flag）→ `0x47d850` 清理 |
| `0x47fc10` | **`SaveToSlot(slot)`**：`OpenTR2(2,1)` → `SeekSlot` → **`0x47f5c0`**（= `0x47f350` 写对偶编码器）→ Close → `mov [0x520620],1`（已存档标志）|
| `0x47fc60` | **`ReadSlotMeta(slot,&y,&m,&d)`**（🔴 续199 定名，原记「单条 49B 记录处理器」）：`push 0; push 2` ⇒ **SAVEDATA**；3 × `word` + 3 × `strcpy` → `0x522c88`/`0x522c60`/`0x522c70`（存档列表 UI 三显示缓冲）|
| `0x47fd10` | **`WriteSlotMeta(slot,y,m,d,name,country,place)`**：`push 1; push 2`；strcpy 目标 `lea [esp+0xa/0x17/0x24]` 各减 4（`push esi` 抬栈）= `buf+0x06/0x13/0x20`，与读方向**逐字段对称** |
| `0x47fa90` | **`LoadBSDATA()`**（🆕 续200 定名）：`push 0xa154`(=**41300**，与 `BSDATA1/2.TR2` 大小精确吻合) → 剧本二选一 `mov eax,0x5095d0/0x5095c0`(`F:BSDATA1/2.TR2`) → 分支键 `byte[0x520604]` 的 `test ah,4` → 目标常驻缓冲 **`0x524a20`**，错误串 `@0x50a2f0='BUSHOUbuf'` → 链 `OpenRes(0x4802e0)`→`Read(0x4eb5c0)`→`SetResident(0x4eb730)`。**一次整块读入，无解密循环**（BSDATA 明文）。⇒ 700×59B 武将主表，字段表见 `GAME_DATA_SPEC.md §1.2` |
| `0x4e8625` | **主循环①**：遍历记录，按类型串匹配触发场景初始化；判 0x5205fe（场景模式）分支 |
| `0x4e89cd` | **主循环②**：调 0x47fd10 + 0x47fc10 处理记录，按 0x5205fe 分支 |

> 🔴 **续199 纠偏（旧注已废）**：`0x47d860` 的步进是 **`idx*40960`**（原始字节 `@0x47d86b` = `8d0480 c1e00d 0598010000` ⇒ `lea eax,[eax+eax*4]`(×5) + `shl eax,0x0d`(**×8192**) + `add eax,0x198`；旧注把 `shl 0x0d` 误读为 `shl 0x0c`）。⇒ SAVEDATA = **8 槽 × 40960B**，不是 16 槽 × 20480B；数据侧独立佐证：头部 408B − 16B magic = 392B 元数据区，**392/8 = 49 整、392/16 = 24.5 不整**。用于 SNDATA 时 idx 实际仅 0（offset=0x198）。

---

## 7. SAVEDATA 完整场景状态（✅ 定位，待精细映射）

- 文件 328088B，Magic `TAIKOU2_SAVEFILE`，**不加密**。
- 结构：`0x00` SAVEFILE 头(到 0x197) → `0x198` 内嵌 `TAIKOU2_SCENARIO` 场景块（含 0x1ac 起 700B 武将登场标志，与 SNDATA 0x14 偏移一致）→ 之后为运行时状态区。
- 已确认数组（SAVEDATA 内偏移）：
  - **`0x1ac` = 700B 武将登场标志**（distinct=2，354/346；gen[13]=1 信长、gen[16]=1 藤吉郎、gen[27]=0 庆次，与 SNDATA 0x519288 一致）
  - `0x48c` = 92 word 城池状态区（前 7 个 0xffff=未占，余 0）—— 候选「城池激活/归属」数组，语义待定
- **「谁拥哪城 / 谁效忠谁」的最可能位置**：SAVEDATA 的运行时状态区（328KB 主体），以定长数组拼接（武将标志 700B + 城池数组 + 武将详细数组 + …）。

---

## 8. 待破解清单（诚实标注）

1. **城池归属 / 武将任职数组的精确偏移**（🎯 复刻最关键缺口）：需反汇编 `0x47d780`→`0x47d800` SAVEDATA 加载器，确定每个定长数组的偏移与大小；或逐字节映射 SAVEDATA 运行时状态区（优先 0x48c 起的 92 城区块 + 其后武将区块）。
2. **49B 记录的逐字段语义**：833 条记录的类型分类（事件/城池初始/武将登场条件）、43B payload 各字段含义。
   - 🟢 **类型系统已实锤（2026-08-31 续154）**：`type = id_word & 0xff`（高低字节对称=填充哨兵：sc1=`0x0c/0xf3/0x0f/0x00`、sc2=`0xa/0xf5/0x0f/0x00`）；两剧本各剔除填充后 **sc1=427 条/167 型、sc2=443 条/161 型**；首个被识别类型 `0x01`（13 条，payload 全 0x00/0x01）= **场景状态布尔开关位域**；payload 内 `0x0c`/`0xf3` 字节=字段默认值哨兵，破译须先剥离。记录处理器 `0x47fc60` 经 `0x4ebfe0`(strcpy) 把字段写入场景态缓冲 `0x522c88`/`0x522c60`/`0x522c70`。参考 `scripts/sndata_record_classify.py`（自校验通过）。
   - 🔶 **仍待**：① 其余 ~164 种类型 payload schema。正确入口是**记录遍历循环 `0x4e8625`(主循环①)/`0x4e89cd`(主循环②)**（二者经 `0x47fc60` 逐条扇出）。🔴 **续159 已证伪旧注「按记录类型分派 handler / 经函数指针表进入」**——全镜像扫描证明**不存在 type→handler 函数指针表**，类型是作为数据参与头字比较/索引（见 §4.0.4）。**原「emu 0x47f350 建映射」前提已证伪**——0x47f350 的 18 个 SUB1 子加载器只处理非记录数据。🟢 **续159 已解决**：② ~~`0x4ebfe0` 块拷贝 size 参数来源~~ → **前提不成立**（实为 strcpy，无 size 参数，见 §4.0.2）；③ `0x522cXX` 三缓冲逐字节 ↔ 记录类型对应（验证 §11 派生规则）。🆕 **续160（2026-08-31）主循环分派管线已坐实（代码级二次证伪 P0）**：`0x4e8604` 取记录索引(`0x480240`→`0x47fe00`,游标 `0x509684`, -1=结束)→`0x47fc60` 扇出→头三字全 0 判定→匹配器 `0x47b390`(模板 `0x50d834`)→谓词 `0x47fb80`(内部 `call 0x47f350`)→读全局 `0x5205fe` 做 **三路值开关(==0/==1/else)**→三簇 handler；handler 簇**不直读 0x522c88**，经 `0x4802e0`/`0x492800`/`0x4ec8c0` 读兄弟缓冲 `0x522ca0` 族写入游戏表（`0x524978`/`0x524918`/`0x524740`）。⇒ **完整 type→field schema 须追 payload 解析引擎 `0x4802e0`/`0x492800`/`0x4ec8c0` + 各 handler 表写入；master 注册间接分派器无静态 xref，宜用 emu 提取全 833 条映射**。详见 `BREAKTHROUGHS.md` 续158 / **续159** / **续160**（`scripts/sndata_fanout_ref.py` **38/38** + `scripts/sndata_dispatch_ref.py` **22/22**）。
3. **17 段序列化器 ↔ 0x5205xx 全局的字段映射表**：需逐个反汇编 0x47d910/0x47d930 的全局地址参数。
4. **两剧本差异区语义**：96.1% 差异中 40 个聚集区对应哪些子系统。

---

## 9. 验证状态

- ✅ 文件识别、大小、Magic、双剧本差异
- ✅ 顶层布局（头+2+2校验+700+700+加密流）
- ✅ **XOR 单字节加密流机制**（密钥=校验字节，0x2000 缓冲）— 核心突破
- ✅ 49B 记录扇出（`0x47fc60` 扇出 / `0x47ff50` 文本格式化 / `0x47ff68` 头全0→默认名助手）
- ✅ 700 武将登场标志（0x519288 / SAVEDATA 0x1ac）
- ✅ 17 段全局序列化器结构
- ✅ SAVEDATA 完整场景状态文件定位（328KB，内嵌场景块）
- ✅ 主循环 0x4e8604/0x4e8625 分派管线已坐实（续160）：索引游标 `0x509684`→`0x480240`/`0x47fe00`；扇出后走 **匹配器(`0x47b390`) + 谓词(`0x47fb80`→`0x47f350`) + 全局 `0x5205fe` 三路值开关**→三簇 handler；handler 经 `0x4802e0`/`0x492800`/`0x4ec8c0` 读 `0x522ca0` 缓冲族写入游戏表（代码级二次证伪 type→handler 函数指针表）
- ✅ **城池/武将归属机制已定位（2026-08-25；🔴 续199/续200 两次纠偏）**：文件中无平铺 92/700 赋值数组；归属由 **BSDATA 每武将的 `国 @0x30` + `城 @0x31` + `主君 @0x36`(2B) + `職位 = byte[0x39] & 7` 派生**（🔴 续200 纠偏：旧记 `home_city@49`+`status@57` 的十进制偏移虽恰等于 `0x31`/`0x39`，但 `status@57` **必须 `& 7`**，且 `@0x31` 是**当前所在城**而非「出身城」；完整字段表见 `GAME_DATA_SPEC.md §1.2`）。`0x47d860` 步长 = **40960 × 8 存档槽**（🔴 续199 纠偏，旧记 20480×16 已废）。
- ❌ 49B 记录字段语义、序列化器字段映射（仍待逐字节）

---

## 10. 导出产物

- `scripts/sndata_records.json`：sc1/sc2 各 833 条 49B 记录（含 payload hex），供增量映射。
- 提取/分析脚本：`_sndata_*.py` 系列；本轮新增 `_disasm_va.py`(VA反汇编)、`_scan_serializers2.py`(序列化器字段追踪)、`_decode_names.py`(EXE地名表)、`_derive_ownership.py`(归属雏形)、`name_table.json`。

## 11. SAVEDATA 容器 + 归属派生（2026-08-25 续破）

### 11.1 SAVEDATA 容器

> 🔴 **本小节原文已被续199 纠偏，正确版见 §12.1** —— 原记「408B 头 + **16 × 20480B** → 16 存档槽」有误。

- `SAVEDATA.TR2` = 328088B = **408B 头 + 8 × 40960B 槽体**（`0x198` 起每槽 40960）→ **8 存档槽**，每槽 = 完整场景运行时态；**不加密**（XOR 密钥=0）。
- 块0 @`0x198` = `"TAIKOU2_SCENARIO"`；@`0x1ac` = **700B 武将登场标志**(`0x519288`)；@`0x48c` = 92 word 区（实测全 0 = 旗标数组，**非归属表**）。

### 11.2 XOR 解密流（主解析器 `0x47f350`）
- 密钥 `obj[0x94] = header[0x12] ^ header[0x13]`（SNDATA 剧本1=0x0c / 2=0x0a；SAVEDATA=0）。
- `0x47d960`：分配 0x2000 滑动缓冲，逐字节 XOR 密钥，用尽再填；`obj[0x92]` = 累计校验和。

### 11.3 序列化器架构（32 函数 = 16 实体类型 × 读/写，`0x47dae0…0x47f1b0`）
- 主解析器依次调用读序列化器，从 XOR 流还原全局态与各实体数组。
- 已确认实体数组长度：**370 / 200 / 49 / 30 / 25 / 20 / 13 / 8 / 6 / 5** = 系统/查表型（技能·官职·町·据点），**不含 92 城 / 700 武将动态归属**。
- 49B 记录处理器 `0x47fc60` 写场景态 `0x522c88 / 0x522c60 / 0x522c70 / 0x522ca0`。

### 11.4 归属派生规则（谁拥哪城 / 谁效忠谁）

> 🔴 **续200 重大纠偏：「主君」不需要派生 —— 它是 BSDATA 的显式字段 `+0x36`(2B, `0xffff`=浪人)**。
> 本节原先「按 `home_city` 归组 → 组内 `status==7` 者 = 城主 → 该组成员主君 = 城主」的**三级推导链已可废弃**，仅在需要「城→城主」反查时保留。旧字段名与偏移一并纠正如下（权威字段表见 `GAME_DATA_SPEC.md §1.2`）：
>
> | 旧记                | 正确                                          | 说明                                    |
> | ----------------- | ------------------------------------------- | ------------------------------------- |
> | `home_city@49`    | **`城 @0x31`**（0–199 / `255`=浪人·无固定城）         | 十进制 49 == `0x31`，位置对；但语义是**当前所在城**非出身城 |
> | —                 | **`国 @0x30`**（0–48 / `255`=无）🆕              | 旧文档缺此字段，实测 <49:658 / ==255:32，无中间值    |
> | `status@57`       | **`職位 = byte[0x39] & 7`**                   | 🔴 **必须掩码**：原值 38/690 条 > 7（packer `0x49a7e0` 证实只占 3 bit） |
> | （靠归组推导）           | **`主君 @0x36`(2B)** 🆕                        | `0xffff`=浪人（实测 664 条），`<700`=主君武将 ID（26 条） |

文件中无平铺赋值数组，归属直接读 BSDATA 每武将字段：
- **`国 @0x30`** / **`城 @0x31`** / **`主君 @0x36`(2B)** / **`職位 = byte[0x39] & 7`**（`7=大名`(城主) / `4=家老` / `3=足轻头` / `1=足轻组头` / `0=无`）。
- 验证：织田信长#13 与柴田胜家#1·森可成#2·木下藤吉郎#16 同 `城=66`（清洲/尾张一带）；前田庆次#27=`255`（浪人/未登场）—— 与史实一致。
- 「城→城主」反查（仍需派生）：按 `城 @0x31` 归组，城主 = 组内 `職位==7` 者（否则最高職位）。
- EXE 地名表 `@0x506ca8` = 370 条（国名 0–48 / 城·町名 49–291 / 职种名 292–369），GBK 变长 null 结尾（省块 stride≈9 干净；精确 stride 待定）。

### 11.5 派生规则验证状态（2026-08-31 续157 · 已闭合）

- 🔴 **续200 纠偏**：本小节原据 `bsdata.json` 的 `fields` schema（`home_city@49`/`status@57`），该 schema 命名有误 —— 权威字段表已迁 `GAME_DATA_SPEC.md §1.2`（`scripts/bsdata_format_ref.py` **73/73 ALL PASS**）。偏移位置本身对（49==`0x31`、57==`0x39`），但 **職位须 `& 7`**、且 **主君是显式字段 `+0x36` 而非派生量**。
- ✅ **派生规则本体验证**（`scripts/sndata_derive_ref.py` 自校验全通过）：
  - 历史锚点：织田信长#13 `城=66` & `職位=7(大名)`；柴田#1/森#2/藤吉郎#16 同 `城=66`（尾张谱代）；前田庆次#27 `城=255`(浪人/未登场)。
  - 组内城主 = `職位==7` 者（无则最高職位）→ 信长组 18 人城主=信长#13，与史实一致。
- ✅ **三缓冲扇出映射**（`0x47fc60` 反汇编实锤，对应 §4 表）：`record[6:49]→0x522c88` / `record[0x13]→0x522c60` / `record[0x20]→0x522c70`（3 处 `push imm32` 立即数）。
- 🔴 **续200 二次纠偏「92 城」假设**：旧注说 `home_city` 索引**整个 370 条目名表**（49 国 + 243 城·町 + 78 职种）—— **值域数据不支持**。实测 `城 @0x31`（去 255）**min=0 / max=199 / distinct=115**，上限 199 恰好对上**城表 `0x51eb88` = 31B × 200 条**；而旧注引以为据的「不同归属地 = 115」正是这里的 distinct 数。⇒ `城 @0x31` 索引的是 **200 条城表**，不是 370 条名表。（`towns.json` 只有 92 条、§2 的「城名 = 名表槽 `88+c`, c∈0–91」仅覆盖 0..91 子集 —— **92 / 200 / 370 三套索引的对应关系仍是开放项**，勿混用。）
- 🆕 **续200：国与城半独立**（旧文档把两者混为一个「归属地」字段）：`国 @0x30` 去 255 后 **min=0 / max=44 / distinct=45**（≤48 合规）；两字段组合实测 **国+城均有效 392 条 / 国有效但城=255 271 条 / 国=255 但城有效 30 条** ⇒ 「属某国但无固定城」是大量存在的正常态（在野·流浪武士），不能用城=255 反推未登场。
- ⚠️ **名表 stride 修正**：定宽 **stride=9**，但仅对 ≤8B（4 汉字）名安全；城堡块（49–291）大量 5+ 汉字名(>9B)溢出污染后续槽，故 `name[66]` 等取到空/乱码。省块(0–48)49/49 干净。旧 `_derive_ownership.py` 用 stride 14 是双重错（stride 值错 + 硬编码 `F:/Games/...` Windows 路径）。已用 `ownership_derived.json`（本地 `bsdata.json`）重生成。
- ⚠️ **诚实标注**：`0x522c60/0x522c70/0x522c88` 在脱壳镜像中**静态全 0**（runtime-only），是「剧本脚本化初始态」通路，与本节「花名册派生基线」是**两条独立数据通路**，不可混为一谈；"派生规则 vs 运行态一致"须待 emu 实跑填充后才能对线。

### 11.6 仍待
1. 地名表溢出名（>8B）的精确解码（需定位名表偏移索引或换用城/国表 `0x51eb88`/`0x519548` 的名称字段）。
2. `0x522cXX` 运行态填充后与派生基线的逐字节一致比对（需 emu）。
3. 49B 记录 43B payload 逐字段（类型/事件语义，见 §8 待破①）。

---

## 12. SAVEDATA 容器全闭 + 49B 槽元数据（✅ 续199 · 2026-09-01）

> 参考实现 `scripts/savedata_container_ref.py`（**57/57 ALL PASS**）+ 规格 `scripts/savedata_container.json` + 探针 `scripts/_tr2_probe.py`。
> 本节含**两处对旧文档的纠偏**，与之冲突处一律以本节为准。

### 12.1 🔴 纠偏一：8 槽 × 40960B，不是 16 槽 × 20480B

旧 §6/§11.1 记 `offset = idx*20480 + 0x198`（`idx*5<<12`）→ 16 槽。**根因是把 `shl eax,0x0d` 误读为 `shl eax,0x0c`。**

原始字节 `@0x47d86b`：

```
8d0480        lea eax,[eax+eax*4]   ; ×5
c1e00d        shl eax, 0x0d         ; ★ 0x0d = 13 ⇒ ×8192（不是 0x0c=12 的 ×4096）
0598010000    add eax, 0x198        ; +408
898196000000  mov [ecx+0x96], eax   ; 保存 offset
ff15a8b04f00  call [0x4fb0a8]       ; fseek
```

⇒ **`slot_offset(idx) = idx × 40960 + 0x198`**

🔑 **决定性第二证据（数据侧，与反汇编相互独立）**：

| 假设 | 槽体整除 | 元数据区 392B 整除 | 判定 |
|---|---|---|---|
| **8 槽 × 40960** | 327680/40960 = **8.0** ✓ | 392/8 = **49.0** ✓ | ✅ 成立 |
| 16 槽 × 20480 | 327680/20480 = 16.0 ✓ | 392/16 = **24.5** ✗ | ❌ 被证伪 |

⚠️ **方法论**：`40960×8` 与 `20480×16` **都能整除 327680**，故**单靠文件大小整除性无法定槽数**，必须叠加 ① `shl` 立即数 ② 头部元数据区整除性 两条独立证据。

**容器最终布局**：

```
0x00000            16 B    magic "TAIKOU2_SAVEFILE"
0x00010           392 B    8 × 49B 槽元数据（存档列表条目，见 §12.3）
0x00198  8 × 40960 B      槽体（各为完整 TAIKOU2_SCENARIO 场景态）
                          总计 16 + 392 + 327680 = 328088 ✓
```

实测：slot0 已用（非零 97.2%，首 16B = `TAIKOU2_SCENARIO`），**slot1..7 全零**（未使用）。

### 12.2 🔴 纠偏二：那批「49B 记录」属 SAVEDATA，不是 SNDATA 的 833 条剧本记录

旧 §4 整节把 `0x47d890`/`0x47fc60` 当作 SNDATA 的「833 条 49B 剧本记录」处理器。**铁证推翻**：

```
0x47fc60  ReadSlotMeta:   6a00 6a02   push 0 ; push 2   → OpenTR2(2, 0)
0x47fd10  WriteSlotMeta:  6a01 6a02   push 1 ; push 2   → OpenTR2(2, 1)
0x47fb80  LoadSaveSlot:   6a00 6a02   push 0 ; push 2
0x47fc10  SaveToSlot:     6a01 6a02   push 1 ; push 2
                                             ↑ 四处全为硬编码 2
0x47d726  cmp ax, 2 ; jae 0x47d759   ⇒ ax>=2 走 LoadSAVEDATA
```

⇒ 四个站点**无一处传 0/1**（SNDATA1/2），全部指向 SAVEDATA。

**推论（须重做的部分）**：
- 旧 §4「49B 记录类型系统」（`type = id_word & 0xff`、sc1=427条/167型、sc2=443条/161型、`0x0c`/`0xf3` 填充哨兵）是**把 SNDATA 文件按 49B 强行切分产生的伪结构**。
- 佐证其薄弱性：`40840 / 49 = 833.47` **本就不整**，须额外凑一个 23B「尾区」才凑出「16 + 833×49 + 23 = 40856」。
- §2.1 当年已察觉「833 条不是从 0x10 明文排列」的矛盾 —— **本节给出答案**。
- ⇒ **SNDATA 加密流（`0x598` 起）内的实体记录 stride 须重新测定**，建议用 §12.6 的「整除 + 列对齐集中度」双判据。

### 12.3 🆕 49B 槽元数据 schema（读写双向对称实证）

| 偏移 | 长度 | 类型 | 字段 | UI 缓冲 | slot0 实测 |
|---|---|---|---|---|---|
| `+0x00` | 2 | `u16 LE` | 年 | → `word[arg2]` | **1560** |
| `+0x02` | 2 | `u16 LE` | 月 | → `word[arg3]` | **5** |
| `+0x04` | 2 | `u16 LE` | 日 | → `word[arg4]` | **20** |
| `+0x06` | 13 | GBK cstr | 主角名 | `strcpy → 0x522c88` | **木下藤吉郎** |
| `+0x13` | 13 | GBK cstr | 所在国 | `strcpy → 0x522c60` | **尾张国** |
| `+0x20` | 17 | GBK cstr | 所在地+身分 | `strcpy → 0x522c70` | **清洲城步兵头** |

**`6 + 13 + 13 + 17 = 49` 精确闭合**（`0x06+13=0x13`，`0x13+13=0x20`，`0x20+17=0x31`）。

🔑 **`0x522c88`/`0x522c60`/`0x522c70` = 存档列表 UI 的三个显示字符串缓冲** —— 这解释了它们为何在脱壳镜像中恒为 0：**runtime-only 的 UI 文本区**，而非 §11.5 旧注推测的「剧本脚本化初始态通路」。

**读写对称性**：写方向 `0x47fd10` 的 strcpy 目标 `lea [esp+0xa]` / `[esp+0x17]` / `[esp+0x24]`，各减 4（`push esi` 抬栈）= `buf+0x06` / `+0x13` / `+0x20`，与读方向**逐字段对称**。

★ **史实交叉验证**：slot0 解出 `1560年5月20日 / 木下藤吉郎 / 尾张国 / 清洲城步兵头`，与太阁2 剧本1「永禄3年(1560) 桶狭间之战、主角木下藤吉郎、仕织田信长、居尾张清洲」**完全一致** ⇒ 字段语义正确。

### 12.4 🆕 存档子系统四函数对称矩阵

| | 读 | 写 |
|---|---|---|
| **槽元数据（49B）** | `0x47fc60` ReadSlotMeta | `0x47fd10` WriteSlotMeta |
| **槽体（40960B）** | `0x47fb80` LoadSaveSlot → **`0x47f350` ×2 趟** | `0x47fc10` SaveToSlot → **`0x47f5c0`** |

- 🔑 **槽体沿用剧本格式**：`0x47fb80` 用的正是 SNDATA 主解码器 `0x47f350`，`word[esp+0x98]` 在两次 seek 之间由 **1 变 0**（two-pass flag，分工待定）；样本侧互证 —— slot0 槽体首 16B = `TAIKOU2_SCENARIO`。
- `0x47f5c0` = `0x47f350` 的**写对偶编码器**；存档成功后 `mov [0x520620], 1`。

### 12.5 🆕 存档子系统数据区 `@0x509590`

**stride 16 的 5 项 TR2 名串子表**（即续196 记的子表「存档剧本(`0x509590`, 5)」的完整内容）：

| VA | 名串 |
|---|---|
| `0x509590` | `A:SAVEDATA.TR2` |
| `0x5095a0` | `F:SNDATA1.TR2` |
| `0x5095b0` | `F:SNDATA2.TR2` |
| `0x5095c0` | `F:BSDATA1.TR2` |
| `0x5095d0` | `F:BSDATA2.TR2` |

紧邻 6 条中文 UI 错误串：`0x5095e8`「请在插入<启动盘或保存盘>。」/ `0x509610`「无法使用。」/ `0x509620`「保存的资料文件不正确。」/ `0x509638`「资料读取失败。」/ `0x509650`「保存资料文件制作失败」。

🔑 **`0x47d780` 动态替换盘符**：`al = [0x526c78] + 0x40` 覆写 `byte[esp]`（`A:` → `X:`），并 `and eax,4; or eax,edx` 给 `size_class` 或上 bit2 ⇒ **这就是续196 遗留敞口④「4 处寄存器派生站点」里 `0x47d741` / `0x47d821` 两处的运行期基址来源**（本条填掉一半）。

### 12.6 🆕 `.TR2` 是容器扩展名（4 类）+ stride 定夺方法论

| 类别 | 判据 | 文件 |
|---|---|---|
| `SAVEFILE` | magic `TAIKOU2_SAVEFILE` | SAVEDATA.TR2 (328088) |
| `SCENARIO` | magic `TAIKOU2_SCENARIO` | SNDATA1/2.TR2 (40856) |
| `GAIJI` | 544B 定长（§3.26） | GAIJI.TR2 |
| **`BSDATA`** | **无 magic**，`len % 59 == 0` | BSDATA1/2.TR2 (41300 = 59×700) |

⇒ 与续198 的「`.GRP` 三变体」构成同一模式：**复刻必须按加载站点分派解析器，不能按扩展名**。

**BSDATA stride 59 的位置学独立证据**（此前仅靠 `bsdata.json` 既有 schema）：BS1/BS2 逐字节差异 1646 处，按 `x mod s` 统计列分布 ——

| stride | 整除 | 占用列率 | 最热列占比 | 结论 |
|---|---|---|---|---|
| **59** | ✓ | **0.559** (33/59) | **0.341** | ✅ 真 stride |
| 41/43/47/49/53/58/60/61 | ✗ | 全 **1.000**（铺满） | 全 **< 0.05** | 无结构（随机） |
| 118 (=59×2) | ✓ | 0.424 | 0.171 | 显结构但**弱于 59** ⇒ 59 为基本周期 |
| 70 / 100 | ✓ | **1.000** | < 0.02 | ⚠️ 整除但无列对齐 ⇒ 假候选 |

🔑 **方法论（可复用）**：**整除性单独不足以定 stride**（70/100 也整除却无结构），必须叠加「差异位置按 `mod stride` 的列对齐集中度」；且需检查**倍数**（118）以确认取到的是基本周期而非其倍数。

### 12.7 ⚠️ 仍未知

1. **40960B 槽体的内部逐字段布局** —— 须走 `0x47f350` 的 18 个 SUB1 子加载器，或 emu 实跑后 dump `0x519868`/`0x51eb88` 等全局表反推。
2. **SNDATA 加密流内实体记录的真实 stride** —— 旧 49B 假设已废（§12.2），须用 §12.6 双判据重测。
3. two-pass flag（`word[esp+0x98]` 1→0）在两趟解码中的语义分工。
4. BSDATA 的 33 个变化列 ↔ `bsdata.json` 字段的逐列对应关系。
