# 太阁立志传2 — 合战（战斗）系统逆向规范

> 本文档记录合战系统的逆向成果。标记 ✅=EXE 反汇编实证；🔶=推断待验证；❓=未破。
> 关联：`GAME_DATA_SPEC.md §4.5`/`§4.5.1`、`SNDATA_SPEC.md`（战图 HJMAPDAT 三段结构）、`BREAKTHROUGHS.md`。
> **重要**：2026-08-26 已推翻旧版「兵种/阵形/计略 96B 记录位于 0x524978 池」的错误假设（见 §0）。
> **🎯 最新（2026-08-26 晚）**：**合战一回合兵力消耗结算公式已全破 → 见 §9**（主函数 `0x42d270`，可执行参考实现 `scripts/battle_formula_ref.py`）；同时 **§2.1 旧「比较矩阵」结论已纠偏**。

---

## 0. 重大纠偏（2026-08-26）

| 旧假设（❌ 已推翻） | 新实证（✅） |
|---|---|
| 兵种/阵形/计略 = 96B/条记录，存于 `0x524978` 运行时池（由 HKMAPNEW.LZW 载入） | `0x48cced` 的 96B 循环填充的是**地形对象**（源 `B:TERRAIN.LZW`，64×64B=4096B）。`0x524978` 池仅服务**战斗视觉地图**（HKMAPNEW.LZW 解压仅 1765B=26×68 战图块），**不是数值表**。 |
| 兵种数值在 EXE 静态段 | EXE 静态段 `0x517720/0x512e58/0x513a78/0x512f10` 全为 0；真实数据由 `C:HJMAPDAT.DAT` 在战斗初始化时载入 `0x512e58`。 |
| section A = 20×9 宏观网格（stride 20，整数） | section A = **9 个兵种/部队类别 × 20 个 4-bit 属性**，每属性值 0–15（实战多为 0/1/2），经访问器按 **stride 20** 读取；高 4 位在 38 战数据中恒为 0（访问器存在但返回 0）。 |

---

## 1. 总体架构（✅ 已实证）

合战数据真源 = **`C:HJMAPDAT.DAT`**（文件名串 `@0x5036f0` = `"C:HJMAPDAT.DAT"`，`@0x503700` = `"C:HKMAPNEW.LZW"`）。

- 战斗初始化函数 `@0x43a490`：打开 `0x5036f0`，按 `offset = variant × 1700` 读一条 **1700 字节记录**，三次 `call 0x4411b0` 拆分写入三个运行时缓冲：
  - `push 0xb4`(180) → `0x512e58`：**兵种/部队类别参数表（section A）**
  - `push 0x2f8`(760) → `0x512868`：**地形图（40×19）**
  - `push 0x2f8`(760) → `0x512b60`：**部署图（40×19）**
- 文件总大小 64600B = **38 战 × 1700B**（精确整除，无余量）。

---

## 2. section A 结构（✅ 已实证，本次核心突破）

`0x512e58` = 180B = **9 类 × 20 属性**。访问器：

```
函数 0x439050 (getLo):           函数 0x4390c0 (getHi):
  eax = arg2 (类别 a) & 0xff        eax = arg2 (类别 a) & 0xff
  ecx = arg1 (属性 c) & 0xff        ecx = arg1 (属性 c) & 0xff
  eax = a*5; off = a*20 + c         eax = a*5; off = a*20 + c
  al  = buf[off] & 0xF  (低4位)      al  = buf[off] >> 4   (高4位)
  ret                              ret
函数 0x439080 (setLo): 对 buf[off] 低4位做 XOR 写入（混淆）。
```

- **调用点**：`getLo` 29 处（`0x423a13`–`0x43c786` 横跨整个战斗/策略引擎）；`getHi` 11 处（`0x2a027`–`0x3c4e2`）；`setLo` 仅 2 处（`0x42de2b`, `0x37c69`，为每战填入/修改）。
- **调用约定实测**（`0x423a13`）：`arg1=ecx=unit_struct[0]`，`arg2=eax=unit_struct[2]`。即 `a` 与 `c` 来自某单位/实体结构体的两个字节字段 → section A 是「**按类别 a 查属性 c**」的索引表。
- **值域**：各属性为 0–15 的 4-bit 值；38 战跨战 nibble 直方图 = 0×11243 / 1×761 / 2×887 / 3×271 / 4×183 / 5×128 / 6×153 / 7×54（8–15 为 0），即实战观测到 **0–7**；`0x428b64` 处 `cmp al,2; ja` 表明至少该调用点将 >2 视为无效。个别战役（如 battle0）前 19 属性仅为 0/1，说明不同属性/战役取值不同。**高 4 位在全部 38 战数据中恒为 0**。
- **跨战变化**：section A 各格在 38 战间不一致 → 是**每战配置数据**，非全局固定兵种定义。

### 2.1 getLo 的真实用途（✅ 2026-08-26 修正）

> **🚨 纠偏（2026-08-26 晚）**：本节旧结论「section A = 两单位在同一属性轴上的强弱比较矩阵，交战结果由 `0x423a9d` 的 `cmp al,cl` 决定」**过度解读，已降级**。
> 反汇编 `0x423910` 整函数确认：它是**战前布阵/属性条显示例程**——`0x423a9d` 的比较结果只写入显示缓冲 `0x511358`（20×2 的仪表条数组），**不进入任何伤害计算路径**。
> **section A 已确证的真实战斗用途见 §9.3**：低 4 位 = **攻击除数表索引（单位类型码）**，被 `0x43a9c0` 用于伤害公式；高 4 位 = 双方除数 ±1 修饰。旧「比较轴矩阵」说仅适用于 UI 显示，不适用于数值结算。

以下保留原始反汇编片段（供追溯，语义按上文修正为「显示例程」）：

```
; 单位 esi / edi 各取同一属性索引的 getLo 值（a=struct[2]=类别, c=struct[0]=属性）
0x423a13  call getLo(esi)  -> al = buf[attr_esi + class_esi*20] & 0xF
0x423a28  call getLo(edi)  -> al = buf[attr_edi + class_edi*20] & 0xF
; 两值各经 LUT 转换（见 §2.2）
0x423a39  call 0x423fa0(result_esi)   ; -> LUT 0x503140
0x423a57  call 0x423f90(result_esi)   ; -> LUT 0x503138
0x423a68  call 0x423f90(result_edi)   ; -> LUT 0x503138
0x423a95  mov al, [esp+0x27]          ; A 方转换后属性值
0x423a99  mov cl, [esp+0x26]          ; B 方转换后属性值
0x423a9d  cmp al, cl                  ; ★ 两单位同一属性值比较
0x423a9f  jbe 0x423afe                ; A<=B 走一路，A>B 走另一路
0x423b??  ...  -> mov [0x511358 + i*2 + side], al   ; ★ 仅写入 20×2 显示缓冲
```

→ **修正后结论**：这是把 A/B 双方 20 条属性经 LUT 换算后写进 UI 仪表条（`0x511358`，20 项 × 2 阵营）的显示例程。属性轴的存在与「每战配置、稀疏 0/1/2」的形态依然成立，但**它不是交战结果的判定式**。真正吃 section A 的数值路径是 `0x43a9c0 → 攻击除数表`（§9.3）。

### 2.2 原始值 → 战斗有效值 转换 LUT（✅ 本次突破，静态可 dump）

原始 getLo 返回 0–7（4-bit），经两个 256 项查找表转换成**战斗有效值**：

```
函数 0x423f90:  al = [eax + 0x503138]   ; eax = 原始 getLo 值 (0..7)
函数 0x423fa0:  al = [eax + 0x503140]
```

两张表已完整 dump 入 `hjmapdat_units.json` 的 `luts` 字段。前 20 项（实际使用的 0..7 范围）实测：

| idx | 0x503138 (0x423f90) | 0x503140 (0x423fa0) |
|---|---|---|
| 0 | 0 | 0 |
| 1 | 1 | 0 |
| 2 | 2 | 1 |
| 3 | 3 | 0 |
| 4 | 6 | 0 |
| 5 | 2 | 2 |
| 6 | 0 | 0 |
| 7 | 0 | 0 |
| 8 | 0 | 2 |
| 9 | 0 | 2 |
| 10 | 1 | 5 |
| 11 | 0 | 5 |
| 12 | 0 | 8 |
| 13 | 2 | 8 |
| 14 | 0 | 11 |
| 15 | 0 | 11 |
| 16 | 2 | 14 |
| 17 | 2 | 14 |
| 18 | 5 | 17 |
| 19 | 5 | 17 |

- `0x503140` 从 idx 8 起为清晰等差数列：2,2,5,5,8,8,11,11,14,14,17,17…（每 2 级 +3）→ 典型的**等级/数值曲线**，说明原始 0–7 是某种"等级/强度档位"，经表映射成实际战斗量（伤害/移动力/适性修正等）。
- 复刻方可**直接把这两张表当黑盒 LUT 用**，无需先理解每档语义。

### 2.3 「9 类实体 × 20 属性」的语义（🔶 推断，待 emu 确认）

- 9 行（a=0..8）推测为 **9 个兵种/部队类别**（足軽/騎馬/鉄砲/弓/攻城/水軍/忍者/一向/… 候选，确切对应 TBD）。
- 20 属性（c=0..19）推测含 **16 种地形适性（0=不利/1=普通/2=有利）+ 4 项杂项**（对特定兵种克制、夜间、计略等）。但 0/1/2 量级与「每战变化」更支持「适性/flag 矩阵」而非基础攻击/防御数值（后者应更大）。
- **已排除**：非全局兵种基础数值表、非 96B 池、非 stride-20 整数网格。

### 2.4 反例（已证伪，勿重蹈）
- `0x52d211` 起**非数据表**，是十进制→ASCII 派发函数（`cmp al,N`/`mov bx,0xNNNN` 链，索引 1..34→"01".."34"）。此前「stride 8、34 条兵种定义表」是把机器码每 8 字节切片的伪像。
- `0x501e48` = 真实 16×48B 表，被军事/兵科对话框 `0x41bf20` 引用（已导出 `battle_units_spec.json`），**与合战数值无关**。

---

## 3. 地形图（✅ 已实证）

`0x512868` = 760B = 40×19。每字节 = **低 4 位地形类型(0–15) + 高 4 位修饰位**。
- 16 种地形全部使用，频率：0:38.9% 5:12.8% 4:9.8% 2:7.7% 3:6.6% 7:6.2% 1:4.6% 8:3.9% 6:2.4% 9:1.3% 10:1.1% 11:1.0% 13:1.8% 14:0.7% 12:0.7% 15:0.5%。
- 修饰位多为 0(60%)，偶见 1/2/3/4/6/7/8/15。
- 部署解密器 `@0x438fa0`/`@0x438fc0` 特判 ASCII 集 `{'/','1','7','9'}`(左军) 与 `{'+','-','3','5'}`(右军)，其余字符经 `0x512f10`(20B) 精灵表映射。

---

## 4. 部署图（✅ 已实证）

`0x512b60` = 760B = 40×19 ASCII 战术布局。每战 30–80 种不同非空字符（J/K/I/9/8/2/3/H/A/+/./t/0x81–0x9f…），含地形、部队标记、本阵、城门等。复用 `0x512b60` 基址另作 **stride 8 的单位实例数组**（`[ecx + eax*8 + 0x512b60]`，`0x439146`）。

---

## 5. 合战变体选择（✅ 已实证，公式完整）

函数 `0x43a460`：
```
tier = 0
for i in 0..9:
    if unit_byte <= TIER_THR[i]: tier = i; break   # TIER_THR @ 0x503740 (10 u8)
variant = rand() % TIER_RND[tier] + TIER_BASE[tier] # TIER_RND @ 0x503760, TIER_BASE @ 0x503750
offset  = variant * 1700                            # 从 HJMAPDAT.DAT 取第 variant 条
```
- `0x503740`=[13,17,21,25,29,33,36,40,70,80]；`0x503750`=[0,15,17,18,19,23,24,25,26,36]；`0x503760`=[15,2,1,1,4,1,1,1,10,2]。

---

## 6. 八方向偏移表（✅ 已实证）

`@0x503710`：步长 4 并行 word 表（取 +0/+2），含 `0xffff=-1`，为 8 方向 (dx,dy) 寻路/移动。

---

## 7. 地形属性表 / 部署精灵表（🔶 访问器已定位，内容运行时填充）

- 地形属性访问器 `0x43e820`：`index*5 + 0x513a78`，`index<0xf` → 地形属性表 `@0x513a78`，5B/条 ×16。静态映像全 0 → 运行时由战图文件填入。
- 部署精灵表 `@0x512f10`：20B（`0x43a580` 用 `push 0x14; push 0x512f10` 建立）。静态全 0。

---

## 8. 兵种/阵形/计略 名称（❓ 未定位）

- EXE 名表 `@0x506ca8`（370 条 GBK 变长 null）：0–48 国名 / 49–291 城·町名 / 292–369 城(294–301)+NPC 与官职(302–369:大商人/茶道大师/洋枪铸造/步兵头/隊長/侍大将/家老/大名…)。**不含兵种名**。
- 全 EXE 二进制搜不到「足軽/騎馬/鉄砲/弓/攻城/水軍/忍者」等 GBK 串（仅「弓」偶然命中）。
- **已证伪「名称在 MESSAGE*.LZW」**：解码全部 4 个 MESSAGE（MSGX+GBK，共 6211 条）后，**零条恰好 2 字的中文条目**；兵种/阵形/计略名不在 MSGX 简单字符串里。→ 名称或存于精灵 label 资源，或经代码表索引，待进一步定位（见 §10）。

---

## 9. 合战一回合兵力消耗结算公式（✅ 2026-08-26 核心突破 · 全静态反汇编实证）

> **可执行参考实现：`scripts/battle_formula_ref.py`**（自检通过，复刻方可直接照抄/移植）。
> 本节全部内容来自 `scripts/_unpacked_mem.bin` 手工反汇编 + 常量 dump + 魔数除法数值验证，**非猜测**。

### 9.1 函数地址对照表

| EXE 地址 | 作用 | 参考实现 |
|---|---|---|
| `0x42d270` | **一回合兵力消耗结算主函数**（就地回写兵力） | `battle_round()` |
| `0x42d5d0` | 阵营战力汇总 `army_strength(side)` | `army_strength()` |
| `0x42d5a0` | side1 战力（含 mode_m1 ÷8） | `side1_strength()` |
| `0x42d730` | side0 战力（含 80% / 同类 80%；**flags 位无关**） | `side0_strength()` |
| `0x43e550` / `0x43e520` | side0 / side1 存活单位计数 | `count_side()` |
| `0x43a9c0` | **攻击除数查表**（吃 section A 低4位） | `attack_divisor()` |
| `0x43cd10` | **兵力边际收益曲线** `troop_scale()` | `troop_scale()` |
| `0x439050` / `0x4390c0` | section A getLo / getHi | `sect_a_lo/hi()` |
| `0x43e200` / `0x43e260` / `0x43e220` | 单位 攻击力 / 防御力 / 装备档(0–3) | `Unit.stat_*` |
| `0x4ebcd0` / `0x4ebd10` | 饱和减法 `max(0,a-b)`（16/8 位） | `satsub()` |
| `0x4ebc50` | `muldiv(a,b,c)=a*b//c`（`c==0 → 0xffff`） | `muldiv()` |

### 9.2 单位槽内存布局（✅ 实证）

**15 个单位槽，基址 `0x513910`，stride 24 B**：

| 偏移 | 宽度 | 含义 |
|---|---|---|
| `+0x0c` | word | **兵力**（结算后由 `0x42d468` 写回） |
| `+0x11` | byte | 士气 |
| `+0x12` | byte | 士气损失（净士气 = `satsub(士气, 士气损失)`） |
| `+0x13` | byte | 状态：**低 2 位 = 兵种类别(0/1/2)**；**高 4 位 ≠ 0 = 已退场**（跳过结算） |
| `+0x15` | byte | **bit2 = 阵营**（0 / 1） |

关联属性对象：`+0x0b` 攻击力、`+0x0a` 防御力、装备档 0–3。若单位由 `[slot]` 间接解析，攻/防**各右移 1 位（减半）**。

### 9.3 攻击除数表（✅ 静态 dump，本次新解出的 section A 真实用途）

`0x43a9c0` 用 `getLo(col,row)` 的返回值 v（0–15）作**索引**去查 20 字节表，按战斗类型标志 `0x513548` 选窗口：

```
ATTACK_DIVISOR_TABLE @0x503770 = [10,12,15, 7, 7,15,100,100,
                                  10,10,12, 7, 7,10, 10,  8,
                                 100,100,12,12]
battle_type != 0 → 取 表[0 + v]     (窗口基址 0x503770)
battle_type == 0 → 取 表[8 + v]     (窗口基址 0x503778，同表 +8 视图)
                   且 v == 10 时：d += handle_stat // (50 或 100)
```

- 该除数**直接做伤害分母**（`base = 有效攻击 × 7 ÷ 除数`）→ **除数越小伤害越高**。
- `7` = 最强档（近战突击类），`8/10/12/15` 为常规档，**`100` ≈ 零伤害档**（🔶 推测为本阵/辅助/民夫等非战斗单位）。
- **高 4 位（getHi）用于双方对冲修饰**：`battle_type == 0` 时，`hiB > hiA → 对方除数 +1`（削弱），`hiB < hiA → -1`（增强）。因 38 战数据高 4 位实测恒 0，静态数据下此项**中性**。

### 9.4 兵力边际收益曲线 `troop_scale`（✅ 分段线性，处处连续）

```
t <= 100  : t
t <= 300  : t//2  + 50
t <= 500  : t//4  + 125
t <= 1000 : t//5  + 150
else      : 3t//20 + 200
```

采样：`50→50, 100→100, 300→200, 500→250, 1000→350, 3000→650`。
→ 兵力的战力贡献**强烈递减**：兵力 ×6（500→3000）战力只 ×2.6。这是太阁2「小队也能打」的数值根因。

### 9.5 单位战力（`0x42d5d0`，逐单位累加到阵营）

```
for u in units:                       # 15 槽
    if u.state & 0xF0: continue       # 已退场
    cat = u.state & 3
    if   cat == 1: v = atk + 15*equip_tier + 20
    elif cat == 2: v = (atk*2)//3            if mode_m2 else atk + 25*equip_tier + 10
    else         : v = atk
    m = satsub(u.morale, u.morale_loss)      # 净士气
    v = v * (100 + m//10) // 100             # ★ 士气加成：每 10 点净士气 = +1%
    v = max(v, 10)                           # 单位战力地板
    v = v * troop_scale(u.troops) // 23      # ★ 魔数 0xb21642c9/sar4 ≡ //23（已数值验证）
    total[u.side] += v
```

### 9.6 阵营战力修正

```
S1 = army_strength(1)
     if mode_m1(0x511bf8) and parity(0x513540 & 1):  S1 >>= 3      # 战力 ÷8
        # 0x42c150 返回 mode_m1(0x511bf8); 0x43cab0 返回 (parity&1)

S0 = army_strength(0)
     S0 = 4*S0//5                        # 恒定 ×80%（side0 天然吃亏 / 疑为「攻方」惩罚）
     if pA.kind == pB.kind: S0 = 4*S0//5 # 两将同类再 ×80%
     # ★ 实证纠正：0x42d730 全程【不读取】任何 flags 位。
     #   - 旧稿「if pB.flags & 0x10: S0 //= 2」为误读，二进制实跑 R2 不含此减半；
     #   - 0x42b8c0 的「battle_type==0 且 parity&1 且 flags&0x40」额外减半属于另一条
     #     battle 路径（读的是 flags&0x40，不是 0x10），不在 0x42d270 这一回合结算里。
```

### 9.7 主结算（`0x42d270`）

```
n0 = count_side(0);  n1 = count_side(1)

E1 = (S1 // n0) * 2          # ★ 除以「对方」单位数：攻击力摊薄到防守方每个单位
E0 = (S0 // n1) * 2

modB = attack_divisor(pB) (+1/-1 by getHi 对冲)
modA = attack_divisor(pA) (+1/-1 by getHi 对冲)
base_vs_side0 = E1 * 7 // modB      # 7 = (x<<3)-x
base_vs_side1 = E0 * 7 // modA

for u in active_units:
    base = base_vs_side0 if u.side == 0 else base_vs_side1
    dmg  = base // (u.dfn()//4 + 50) + 1        # ★ 防御力 ÷4 加 50 作分母；保底 1 兵
    if u.side == 1 and mode_m1: dmg = 0         # 0x42d446：side1 免伤（教学/演武模式？）
    u.troops = satsub(u.troops, dmg)            # 0x42d468 写回

loss_pct[s] = muldiv(casualties[s], 100, total_troops_before[s])
```

### 9.8 关键玩法推论（可直接用于复刻数值调优）

1. **每回合每单位最少掉 1 兵**（`+1` 保底）→ 战斗必然收敛，不会僵持。
2. **防御力是软上限**：分母 `def//4 + 50`，def=0 时 50、def=200 时 100 → 防御力从 0 堆到 200 只能把伤害减半，**边际收益极低**。
3. **士气才是乘数**：净士气 100 → 战力 ×1.10；士气归零 → 战力 ×1.00 且掉到地板 10。士气差在长回合里复利放大。
4. **兵力递减 + 攻击摊薄双重机制**：`E = S // 对方单位数 × 2`——**对方单位越多，单体承受伤害越低**（分摊），但总伤亡越高。故「多支小队」抗打击、「一支大军」吃集火。
5. **side0 恒吃 80%**（且同类再 80%）→ 阵营不对称是写死的，复刻时必须保留否则平衡崩。

### 9.9 尚未确定（🔶）

- `pA/pB` 两个指挥对象的 `+0x00`(col) / `+0x02`(row) 具体来源（哪个字段决定单位落在 section A 的哪一格）。
- 模式标志的玩法含义：`mode_m1(0x511bf8)`（side1 免伤）、`mode_m2(0x51352c)`（cat2 走 2/3）、`parity(0x513540)`、`battle_type(0x513548)`。
- `handle_stat`（`0x43cb50 → [0x513534]+0x0d`）的取值范围。
- 兵种类别 0/1/2 ↔ 中文名（足軽/騎馬/鉄砲？）映射仍缺（名称未在 EXE 静态段，见 §8）。

### 9.10 隔离仿真验证 ✅（Unicorn 2.1.4 实跑真实二进制 `0x42d270`）

> **验证脚本：`scripts/_emu_battle.py`**（Unicorn 跑 `scripts/_unpacked_mem.bin` 真实 `0x42d270`，插桩抓 R1/R2/n0/n1/base0/base1 与逐单位战力，与独立 Python `ref_battle` 逐字段比对）。

**方法**：构造干净可控的 15 单位槽 + 实体表 + 指挥结构（pA/pB），把全局标志（battle_type / mode_m1 / mode_m2 / parity / handle_stat）与 section A（180B）按场景写入脱壳映像，用 `UC_ARCH_X86 + UC_MODE_32` 从 `0x42d270` 仿真到 `0x42d4a7`，对未映射取指用 IAT 桩跳回、未映射数据取 0 放行。

**结果**：3 个场景（a：4+4 同兵种；b：7+7 混编 14 单位；c：2 side0 退场 + pA.flags=0x10 + 两将同类 kind=3）最终兵力 **逐单位 bit-for-bit 吻合**：

```
scenario a: PASS  bin=[472,472,472,472,379,379,379,379,0...]
                    ref=[472,472,472,472,379,379,379,379,0...]
scenario b: PASS  bin=[566,261,414,516,160,668,313,578,275,427,528,174,679,326,0]
                    ref=[566,261,414,516,160,668,313,578,275,427,528,174,679,326,0]
scenario c: PASS  bin=[500,500,444,444,391,391,391,391,0...]
                    ref=[500,500,444,444,391,391,391,391,0...]
  （c 情景：二进制 R1=3636 R2=1028 n0=2 n1=4 base0=3636 base1=514）
```

**本次验证纠正的误读**（已同步修正 §9.6 与 `scripts/battle_formula_ref.py`）：
1. `side0_strength(0x42d730)` **不读任何 flags 位** —— 旧稿「`pB.flags & 0x10 → ÷2`」「`battle_type==0 额外 ÷2`」均不实，二进制 R2 仅含 `×4/5` 与同类 `×4/5`。
2. 真实除法顺序是 `base = 14 * (R // n) // mod`（先 `idiv n` 取整，再 `×14`，再 `idiv mod`），**不是** `14*R // (n*mod)`；二者在 R 可被 n 整除时巧合相等，故 a/b 场景一度「假通过」，c 场景才暴露差异。
3. `0x42c150` = `mode_m1`(0x511bf8)、`0x43cab0` = `(parity & 1)`，已实证 side1 的 `>>3` 需二者同时非零。

→ 结论：**§9 公式目前为「静态反汇编 + 真实二进制隔离仿真双重实证」状态**，可直接用于复刻。

---

## 10. 待破清单（合战数值/玩法收尾）

| 项 | 状态 | 下一步 |
|---|---|---|
| **单位伤害/战斗结算 per-tick 公式** | **✅ 已破（§9）** | 主函数 `0x42d270`；参考实现 `scripts/battle_formula_ref.py`（自检通过） |
| section A 低4位用途 | ✅ 已破 = **攻击除数表索引**（`0x43a9c0` → `0x503770` 20B 表） | — |
| section A 高4位用途 | ✅ 已破 = 双方**除数 ±1 对冲修饰**（静态恒 0 → 中性） | — |
| section A：9 类实体 = 哪些兵种 | 🔶 推断为 9 兵种类别 | emu 实跑战斗，instrument `0x439050` 记录 a 取值集合与上下文，或定位 `pA/pB` 的 col/row 来源（§9.9） |
| section A：20 属性语义（地形适性？） | 🔶 部分否证：不是「交战判定比较轴」（那条是 UI 显示，§2.1 纠偏） | 关联 `0x43e820` 地形属性表；20 列中至少有若干列是「单位类型码」 |
| 模式标志玩法含义（mode_m1/m2/parity/battle_type） | ❓ | 4 个全局标志已定位地址（§9.9），需 emu 观察置位时机 |
| 地形属性数值(攻/防/移动) | 🔶 表位 0x513a78(5B×16) | 提取战图文件中的地形属性块 |
| 兵种/阵形/计略 中文名 | ❓ | 非 EXE 静态、非 MSGX；查精灵 label / 代码表；或 emu 时捕获渲染前的字符串指针 |
| MSGX 战斗词汇抽取 | ✅ 已完成 | 6211 条已解码存 `scripts/_probe/msgx/all_messages.txt`，无兵种名 |

---

*生成/大修：2026-08-26 ｜ 本次实证：推翻「96B 池」旧说，定位 HJMAPDAT.DAT section A = 9×20 nibble 访问器（`0x439050`/`0x4390c0`/`0x439080`），证实高4位恒0、每战变化、值0-2；证伪「0x52d211 兵种表」「兵种名在 MSGX」两说。39 战 × 1700B 三段结构、地形16类、八方向表、变体公式均保持实证。*

*增补（2026-08-26 晚）：**合战 per-tick 兵力消耗公式全破**（§9，主函数 `0x42d270`，参考实现 `scripts/battle_formula_ref.py` 自检通过）；解出 15 单位槽布局（`0x513910`, stride 24）、攻击除数表（`0x503770`）、兵力递减曲线（`0x43cd10`）、士气乘数、防御软上限、side0 恒 80% 不对称；**纠偏 §2.1**「section A = 交战判定比较矩阵」→ 实为 UI 仪表条显示（`0x511358`），真实数值用途是攻击除数索引。*

*增补（2026-08-26 深夜）：**§9 公式经 Unicorn 2.1.4 隔离仿真实跑真实二进制 `0x42d270` 双重验证**（§9.10），3 场景逐单位 bit-for-bit 吻合；据此纠正 §9.6 的两处误读（`side0_strength` 不读 flags 位、`base` 除法顺序为 `14*(R//n)//mod`），并已同步修正 `scripts/battle_formula_ref.py` 与 `scripts/_emu_battle.py`。*
