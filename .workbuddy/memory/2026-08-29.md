# 2026-08-29 工作日志

## 城表/城町记录 0x51eb88 字段布局（续70，结构级全破）
- 接手「城表字段」未破项。stride=31 **逐字节实锤**（`shl eax,5; sub eax,ecx; add eax,0x51eb88`，多处同形）。记录 31B×200（92城+108町）。
- C++ 对象模型：`+0x00` vtable(dword) / `+0x04` 链表 next(dword, @0x408e87 `mov eax,[eax+4]`) / `+0x08..+0x1e` 数据。
- 16 字段语义闭合（高置信，经济续33/34 + 任命续65 + 字段扫描）：城主+0x0a / 农商+0x0c / 次级+0x0d(城上下文=守城度) / 民心+0x0e / 生产率+0x0f / 军粮+0x10 / 米+0x12 / 资金+0x14 / 次级民情+0x1a / 城种+0x1b&7；+0x08/+0x16 为状态标记(中置信)；+0x0b/11/13/15/18/1c/1d/1e 低频未知。
- ⚠️ 方法论纠偏：ctor `0x4a3a4f` 经 helper 写 `+0x24..+0x2d`，属**更大运行时 C++ 实例**(经 `0x4a05a0` 链接)，**非 31B 记录**——既往「31B 全字段」粗估须剔除越界项。
- 参考 `castle_fields_ref.py`（16/16 PASS）。衍生工具 `_field_scan.py`/`_loader_find.py`/`_stride_find.py`。
- 待破：值语义需原版场景 dump（`F:/Games/Taikou2` 当前为空，无法实跑场景加载填充 0x51eb88）。
- 落盘：`GAME_DATA_SPEC.md` §3.17、`BREAKTHROUGHS.md` 续70、`MEMORY.md` 索引（已 trim 至 <3000）。

## 全新核心表 49 国「政治/关系表」0x5179b8（续71）
- 用户要求「先破大模块」。盘点：事件系统 §3.7 是唯一仍挂 4 个未打勾项的大模块 → 从 §3.7 的「事件条件判定器 `0x4e82c0`(opcode 13/14)」切入。
- 追 `0x4e82c0` 唯一 caller `0x4e7fd3` → 父函数 **`0x4e7f60`**；该族 `0x4e7110`–`0x4e84ff`（19 函数）实为**据点迁移/家臣团跟随**（`0x4ebe40(101-年龄)` 跟随概率、`0x4e8150` 落地、`0x4e81d0` 分支），非事件调度。
- 🆕 **全新表 `0x5179b8` = 49 国 × stride 14**，⚠️ **与国情表 `0x519548`(stride 5) 是两张表**（本表=人事·外交，国情表=地理·气候·产出）。241 处 xref、129 处 `lea [reg*2+tbl]`(=×14)、边界 `cmp al,0x31`。
- 字段：`+0x00`=该国武将链表头(next@elem+0x04) / **`+0x04`=国主武将号(24 处热字段，×47→`0x519868`)** / `+0x08`=关联国索引(该值再索引本表⇒国↔国关系图) / `+0x0c`未知 / `+0x0d`=flags bit4。
- 🔴 **方法论纠偏（影响所有后续字段扫描，务必复用 `scripts/_ins_index.py`）**：
  1. 指令索引在 `jmp` 处截断 → MSVC `lea;jmp <cont>` 三元续体整段丢失。改**队列式迭代**（jmp/jcc 目标亦作起点）：135,387→**272,567** 条，字段命中 1→37。
  2. `call` 后**未失效 caller-saved 寄存器**(eax/ecx/edx) → 返回值被误当表指针，产出**假字段**（`+0x02`/`+0x1d`，后者越界本可自证为伪）。修复后字段全落回 0x00..0x0d，与 stride 14 自洽。
- 参考 `province_politics_ref.py`（15/15 PASS）。落盘：`GAME_DATA_SPEC.md` §3.18（新增）、`BREAKTHROUGHS.md` 续71。
- 待破：值语义（无原版场景数据）；`+0x08` 是邻国还是上级；链表元素是武将还是城；**事件 opcode 全表**（现仅知 13/14）。

## 事件系统 §3.7 攻下两项（续72）
- 用户要求「先破完成度低的」。按盘点，事件系统 §3.7（35%）最低，挂 4 个未打勾项 → 挑两项下手。
- 🔴 **纠偏**：`0x4547f0` 实为**武将闲谈对话生成器**，非「个人遭遇事件处理器」。扫 `0x4547f0`–`0x454c00` 全部 `push imm`（0x300–0x700）恰好 8 处：`0x505/0x509/0x512/0x514/0x516/0x518/0x51a/0x51c`；**§3.7.5 旧记的 `0x403/0x405` 不存在**。文本＝米价情报/城况情报/兜售/主公评价四类，全闭合。
- 🆕 **MSGX 全局 id = 4 文件 × 2000 槽实锤**（M1=0/2=2000/3=4000/**4=6000**）。证据：`all_messages4.txt` 的 `MESSAGE4#0 (id=0x1770)`=6000。⚠️ 旧 `msgx_id_map.json` 的 sites 只覆盖 3091 条（本次 8 码全不在内，一度误判缺失）→ **新建 `scripts/msgx_all_texts.json`（6211 条全量）**，查文本一律走它。
- 🆕 **「口吻对」规律**（⚠️ **仅 `0x512..0x51d` 段**：偶数=直率口吻/奇数=谦让口吻，同一对话两种说法，对应性格字段）。✗ 不适用 0x505/0x509（其 ±1 邻居 0x504「喝吧」/0x508「军马」是不同话题）。
- 🚫 **结论性负结果**：`0x50dab0`(27×4B) 语义**静态不可破**——仅 1 处 xref(`0x4eb38f: push; call 0x47ad90`)，整块传给通用 UI 列表函数 `0x4f1d37`（读 `0x528c82` 样式表做渲染布局），**无任何代码按字段读取**。已从待破划掉。
- 参考 `event_dialog_ref.py`（21/21 PASS）。落盘：`GAME_DATA_SPEC.md` §3.7.5 重写 + §3.7.5b 新增 + §3.7.7 两项闭合；`BREAKTHROUGHS.md` 续72。
- §3.7 剩余：逐事件 handler 逻辑（虚表派发，最硬）、按年触发总调度、事件 opcode 全表。

## 🚨 全局纠偏 + 物品定义表（续73）
- 用户要求继续啃最低完成度（SNDATA 流尾 50%）。动手前复核前置条件 → **发现历次「原版数据为空」是错的**。
- 🚨 **原版数据路径纠偏**：`F:/Games/Taikou2` **不存在**；真身在 **`<工程>/Taikou2 Original/`（111 个文件）**：SNDATA1/2(40856)、SAVEDATA(328088)、BSDATA1/2(41300)、HJMAPDAT.DAT(64600=38×1700)、TOWNPOS/TOWNTBL、MESSAGE1-4.LZW、TAIK2W95.exe。**所有「待原版数据」项不再是阻塞**（城表/国政治表值语义、物品表、SAVEDATA 状态区全可实做）。
- 🆕 **SNDATA XOR 流内「物品定义表」189×19B**（文件 `0x82ab`＝流偏移 32019，此前 §5.1 只标「32001+ ~70 尾段」，实为 **7400B 未破**）。解密 key=`文件[0x12]^[0x13]`=0x0c/0x0a（真数据验证通过）。**间距恒19(188/188)实锤**；两场景逐字节相同=静态主数据；物品名**不在** EXE/SAVEDATA/BSDATA，仅在 SNDATA 流内（补上续35「物品池 EXE 侧工厂不存在」的空缺）。
- 字段：`名字GBK 4-12B+00 | 恒0 | **cat(0..26, 27类, 21空缺)** | **val(1..200)** | tier? | flag? | unk? | 恒0`。cat/val 高置信（名品排序验证：村正49>正宗46、备前福耳105>北野茄子85、大金块140>金块70）；**tier/flag/unk 未硬猜**。
- 流尾剩余 3791B：按 u16 读 1895 个含 **1173 个 0xffff**，按 ffff 分段 32 段（1×20/3×10/55×1/**617×1**），形如 `[26,32791,290]` → 疑商店库存/持有者索引，待破。
- 参考 `item_table_ref.py`（21/21 PASS）、数据 `item_table.json`、解密流 `_dec_SNDATA1/2.TR2.bin`。落盘 `GAME_DATA_SPEC.md` §3.19 新增 + §3.18.5 阻塞表述纠偏；`BREAKTHROUGHS.md` 续73。
- 未破：物品表序列化器（候选 `0x47eea0..0x47f110` 均无 189/19 循环，疑变长读或走 `0x47d890` 另路）。

## 国政治表数据层 + 49国国名（续74）
- 选最低完成度的**国政治表 60%**（此前卡「静态全0·值语义待dump」，续73 解锁数据后可直取）。
- 🆕 **49 国国名全表首次拿到**：0北陆奥 1陆奥 2出羽 3南陆奥 4上野 5下野 6常陆 7房总 8武藏 9相模伊豆 10甲斐 11信浓 12越后 13骏河 14远江 15三河 16尾张 17美浓 18伊势 19越中 20能登 21加贺 22越前若狭 23北近江 24南近江 25大和伊贺 26山城 27丹波丹后 28摄津 29河内和泉 30纪伊 31播磨但马 32因幡伯耆 33出云石见 34备前美作 35备中备后 36安艺 37周防长门 38阿波 39赞岐 40伊予 41土佐 42丰前 43筑前筑后 44肥前 45丰后 46肥后 47日向 48萨摩大隅。
- 🔴 **名表读法纠偏**：`0x506ca8` 是 **stride 9 字符串数组**，**不是 4 字节指针表**。§2.8 早记 stride=9，但实操惯性地按 `unpack('<I')` 读指针 → 49 国只解出 9 个名。改 stride9 切片后 **49/49 非空**。
- ✅ **国政治表数据层**：`0x47e440` 从 XOR 流 **27297** 读 **539B=49×11B** → 写 **`0x5179bc`**(＝`0x5179b8+4`) ⇒ 填 14B 记录的 **+0x04..+0x0e**（前4字节不由本流填）。**坐实 `+0x04`=武将编号**（41/49 随剧本变、4条 `0xffff`、可经 BSDATA 解析为名）。
- 字段静态/动态分类（两场景差分）：rec2/rec5/rec6-7/rec9 **静态**(49/49同)；**rec0-1 动态**(8/49同, 41条变)；rec4(38/49)、rec10(42/49) 混合。rec6-7 恒 0xffff=哨兵。
- ⚠️ **未坐实·不硬套**：49条记录顺序**不是**按国ID。按「记录i↔国i」套用得「甲斐(10)=芳贺高继、能登(20)=武田信玄」；而武田家臣连续集中在 19–27（国名却是越中/能登/加贺…），上杉家 28–33、德川家 34–43 ⇒ 疑按**势力/大名家/城主序列**排列。待反汇编 `0x47e440` 定写入索引。
- 参考 `province_names_ref.py`（16/16 PASS）、数据 `province_politics.json`。落盘 `GAME_DATA_SPEC.md` §3.18.6/7/8 新增；`BREAKTHROUGHS.md` 续74。
- 下一步最高价值：**(A)** 反汇编 `0x47e440` 闭合记录↔国映射；**(B)** 同法取城/町表（流 21852，5200B=200×26B→`0x51eb88` stride31）把城表 70% 推高。

## 事件系统 §3.7 时间编码 + 本能寺（续75）
- 选 §3.7（当时 ~55% 最低），攻「按年触发总调度」。
- 🆕 **时间全局编码**：`byte[0x5205f0]` = **从 1560 起的年偏移**（`add reg,0x618`=+1560），**不是绝对年份**（此前若当绝对年整体错位1560）。可表 1560..1815。`0x5205f1`/`0x5205f2` = 月/日，**顺序未定**（缺常量比对实证，勿断言）。
- 🚫 **结论性发现：不存在集中的「历史事件年表」**。`0x5205f0` 共 57 处 xref，构成年份阈值判定的**仅 9 处**（0x4042fc:1600 jne / 0x40c310:1566 jb / 0x440c62&0x4bb3e2:偏移3→1563 / 0x4a5370:1560 je（疑起始年）/ 0x4a4da4:0 / 另3处疑非年份）。⇒ 史实事件是**分散硬编码 cmp 判定**，§3.7「按年触发总调度」应改为「无总调度表，须逐个追 handler」。
- ✅ **本能寺之变全闭合**：主流程 `0x41aa70`（caller `0x4135e2`/`0x4179bc`）→ `0x41ab51` 显消息 `0x1ad6..0x1ad9` → 过场 `0x41c160` → 依 `word[0x520604]&0x3000==0x1000` 分支 → `0x4088d0(0x517c70,0xf)`。**台词 14 条全取到**（msg 6866..6879 = `0x1ad2..0x1adf`，从「报告！刚刚捉到一个人…」到「我们直奔二条城吧！！」）。
- 🔴 **自我纠错**：初判 `0x447780` 为「按年调度器」**错了** —— 内部是 `call 0x493500`(MSGX查找)→`call 0x47b8d0`(显示)，实为**消息显示封装**；`push 0x62e` 的 1582 是**消息ID**（msg#1582=「以这样的身体状况，不能用真刀比赛…」剑术道场）。**教训：见可疑立即数先查 MSGX 文本再下结论**。
- 参考 `event_year_ref.py`（14/14 PASS）。落盘 `GAME_DATA_SPEC.md` §3.7.7~3.7.11；`BREAKTHROUGHS.md` 续75。§3.7 现 ~70%，仅剩「逐 handler 虚表派发」最硬一项。
- 下一步可行路径：**用台词关键词（桶狭间/姊川/长篠/关原）在 msgx_all_texts.json 反查消息 id 区间 → 反查 push 这些 id 的代码点 → 定位 handler**（本能寺已验证可行）。

## SNDATA 流尾 3791B 结构（续76）
- 攻 SNDATA 剩余的表后 3791B（流 35610..39401，物品表 189×19B 之后）。
- ✅ **性质**：两场景逐 u16 比对 **仅 10/1895 处不同** ⇒ **静态定义数据**，非场景状态（否定续73「商店库存」推测）。
- 🔴 **纠偏：`0xffff` 是数据值不是分隔符**。两场景 ffff 计数**不同(1173 vs 1171)** ⇒ 是 65535=无效/空标记。**续73「按 ffff 切 32 段」方法作废**，勿再沿用。
- 🆕 **最长非 ffff 游程 = 617 个 u16**：默认值 **`0x0303` 占 504 次(81.7%)**，另 113 个特殊值聚成 **42 簇**（簇长 1×19/2×11/3×5/4×3/5×2/6×1/**29×1**）。形态两类：① **`(A<<8)|B` 位域对**（A 主要 0..8，B 主要 0..7/10/11）；② **独立大值**（`0x9800`/`0xA500`/`0xB700`/`0xFF00`，最大 0xFF00）。
- 尾端 **55 个 u16 全 0 且首为 255**（标志数组）；短区含 **`0x8000|n`** 标志值（n∈{11,12,21,22,23…}）。
- 🚫 **负结果**：自相关周期 2..39 各周期得分**全部 ≈0.817**（被 81.7% 默认值主导）⇒ **无定长记录周期**，稀疏/变长结构，静态推不出语义，须定位序列化器。
- 参考 `sndata_tail_ref.py`（15/15 PASS）。落盘 `GAME_DATA_SPEC.md` §3.19.5/6；`BREAKTHROUGHS.md` 续76。
- **方法论沉淀**：两场景差分是判定「静态定义 vs 场景状态」的最快手段（续74 国表、续76 流尾均靠它定性）；自相关若被默认值主导则无效，须先剔除默认值再算。
- 下一步性价比最高：**(B)** 城/町表值语义（流 21852，200×26B→`0x51eb88` stride31，有续70 结构定义可对照）。

## 城/町表值语义破出（续77）
- 攻城表 `0x51eb88`（70%）。流 **21852**，5200B=**200×26B**（92城+108町），序列化器 `0x47e130`。
- ✅ **`byte17`=所属国索引(0..48)**：按值分组城名地理**完全吻合**49国顺序——三户/八户→北陆奥0、弘前/米泽/岩出山→陆奥1、桑折/山形/横手→出羽2、鹤冈/二本松→南陆奥3、厩桥/沼田/箕轮→上野4、宇都宫→下野5、水户→常陆6、江户/河越→武藏8、小田原→相模伊豆9、踯躅崎→甲斐10、小诸/高远→信浓11、春日山→越后12、骏府→骏河13、滨松→远江14、冈崎→三河15、清洲→尾张16、稻叶山→美浓17、富山→越中19、金泽→加贺21、一乘谷→越前若狭22。
- ✅ **`byte19`=城主武将号**（199/200 可经 BSDATA 解析）：三户→前田利家、登米→浅野长政、鹤冈→藤堂高虎、二本松→织田信忠、须贺川→中野义时、馆林→织田信孝。
- ✅ **`byte14`=在城武将号**（**255=不在城**）：106/200 与 byte19 同、**95/200 为 255**；同国不同城可不同（上野4：须贺川=中野义时/厩桥=秋田实季/箕轮=森兰丸）⇒ 佐证 byte19 是城主非国主。
- 其余：byte12-13 恒 0xff 哨兵；`byte16`=第三武将编号(含织田信长13/木下藤吉郎16，语义待定)；byte22/23/24=0..250/0..200/0..100（byte24 最像百分比=民心）。
- ⚠️ **与续70 偏移对不齐**：续70 由 `0x49a990` 得「`word[城+0x0a]`=城主」，本流城主在 byte19（26=31−5，疑写 +0x05 起，但仍对不上）⇒ **映射未核对，不强统**，待反汇编 `0x47e130` 定写入偏移。
- 参考 `castle_town_ref.py`（16/16 PASS，含 8 座名城地理抽查）、数据 `castle_town.json`。落盘 `GAME_DATA_SPEC.md` §3.17.6；`BREAKTHROUGHS.md` 续77。城表 70%→~90%。
- 下一步：**(A)** 反汇编 `0x47e130` 统一流记录与续70 字段定义（最关键）；**(B)** 用 SAVEDATA（不加密，同结构）交叉验证。

## `0x47e130` 破译 + 城表完全闭合（续78 · 城表 70%→~95%）
- 反汇编 `0x47e130`：`esi` 起始 **`0x51eb8c`=`0x51eb88+4`**、`ebx=0xc8`(200)、`add esi,0x1f`(stride 31)。
- ✅ **25/26 之差终解**：初数 16 次读取得 25B，与数据 26B/条差 1 —— **漏数了 `0x47e1a4: call 0x47d910`（目标由 `lea edx,[esi+4]` 推入 = `+0x08`）**。补上后 **17 次 = 26B** ✓。宽度实证：`0x47d910`→`0x47da10` 读 1B；`0x47d930`→`0x47da50`（调两次 `0x47da10`）读 2B。
- ✅ **完整字段映射（流字节→记录偏移）**：`[0-1]→+0x00`武将号指针(查`0x519868`×47) / `[2]→+0x04`城索引指针(查`0x51eb88`×31) / `[3]→+0x08` / `[4]→+0x09` / `[5-6]→+0x0a` / `[7]→+0x0c`农商 / `[8]→+0x0d`次级 / `[9]→+0x0e`**民心**(0..200) / `[10]→+0x0f`生产率 / `[11-12]→+0x10`军粮(**剧本内恒 0xFF05 未设置**) / `[13-14]→+0x12`米 / `[15-16]→+0x14`资金 / `[17-18]→+0x16`**所属国** / `[19-20]→+0x18`**城主** / `[21]→+0x1a`次级民情 / `[22-23]→+0x1b`城种(&7) / `[24-25]→+0x1d`。
- 🔴 **纠偏续70**：续70 由 `0x49a990` 推「`word[城+0x0a]`=城主」**错了**。实测 `+0x0a` 191/200<370 但**仅19个不同武将名**(大量「林通胜」重复)；**`+0x18` 199/200<370、105个不同武将名**且逐个合理（三户→前田利家/登米→浅野长政/鹤冈→藤堂高虎）。⇒ **城主在 `+0x18`**。佐证：`+0x10`军粮剧本内恒 0xFF05 ⇒ 军粮/米/资金本就不由剧本初始化，续70 混淆了「运行时任命写入」与「剧本初始化写入」。
- `castle_town_ref.py` 的解析（流[17]=所属国、流[19]=城主）经修正映射**恰好正确**，复跑 16/16 无回归。落盘 `GAME_DATA_SPEC.md` §3.17.7 终版；`BREAKTHROUGHS.md` 续78·终解。
- 仍未知：`+0x08/+0x09/+0x0a/+0x1d` 语义；`0x49a990` 写的 `+0x0a` 究竟是什么。
- 🔑**方法论**：数「序列化器读了几个字节」时，**每条 `call 0x47d9xx` 前必看它 `lea/push` 的目标**——只数 call 条数会漏（本次就是漏了 `[esi+4]` 那次）。**字节数对不上数据步长时，优先怀疑漏数而不是数据错**（但要用数据反证，如本例 25B 会让地理分组崩坏）。

## 国政治表 0x5179b8 实测字段布局（续79 · 🔴 纠偏续71/续74）
- 用户「先破解完成度低的」。盘点后攻坚国政治表值语义（结构续71已破但值未实测）。
- 重新反汇编 LOAD `0x47e440` / SAVE `0x47e4e0` 逐指令：LOAD `mov esi,0x5179bc`(基+4)，`call 0x47d910`读1B→城idx，`shl eax,5;sub eax,ecx`(×31)，`add eax,0x51eb88`，`mov [esi-4],eax` ⇒ **`+0x00`=城/町指针**（非续71的「武将链表头」）。SAVE `@0x47e4f3` 反算 `(ptr-0x51eb88)/31`(magic `0x84210843`) 印证。
- 精确偏移（LOAD/SAVE 对称）：`+0x00`城指针 / `+0x04`=2B packed(`byte[+0x04]`flag 0/1 · `byte[+0x05]`国主武将号0..369) / `+0x06`=word(`(关连国idx<<8)|attr`,hi∈0..44<49) / `+0x08`=0x03常量 / `+0x09+0x0a`=0xff / `+0x0b..+0x0d`关系属性。
- 🔴 **纠偏续74**：`+0x04` 非整 word 武将编号、解码流**无 0xffff**（旧记 4×0xffff 系误读打包值）。续71「`@0x40eeef` `mov ax,[eax+4]`→×47→0x519868」实为取**高字节** gen_id。
- 解码真实流（XOR 流 27297，539B=49×11）：49 条中 44 有效（城idx 0..199），尾 5 条 `castle_idx=0xff` 哨兵。
- ⚠️ **开放矛盾**：`@0x4417ad`(`movzx ax,byte[esi+8]`→`cmp 0x31`→`lea ebx,[edx*2+0x5179b8]`)确将 `+0x08` 作国索引建表，但解码流 `+0x08` 恒 `0x03` ⇒ 疑 `+0x08` 运行时被覆盖，或 `@0x4417ad` 上下文与初始值不同。未断言续71「关连国」错，但流数据不支持逐省可变 +0x08。
- 落盘：`BREAKTHROUGHS.md` 续79（倒序新条目+顶部摘要）、`GAME_DATA_SPEC.md` §3.18.2/§3.18.3/§3.18.6 全改、`MEMORY.md` 索引三处更新。脚本：`_prov_disasm.py`/`_prov_decode.py`(+`prov_politics_decoded.json`)/`_prov_xref.py`/`_prov_chk4417.py`。
- 下一步：xref `+0x06` 高字节 ×14→`0x5179b8` 坐实关连国；Unicorn 跑一次场景加载确认 `+0x08` 是否被覆盖；取 BSDATA 把 44 条城idx/武将号还原为国名/武将名验地理。

## 物品定义表三字节语义破解（续80）
- 用户「先破解完成度低的」第二击：物品定义表 189×19B 的 `tier/flag/unk` 三字节（§3.19.2 标 🔶 待破）。
- 先确认**无 189/19 循环**（`_item_reader_scan.py` 查 `0xbd` 边界、`_item_stride_scan.py` 查 `imul 0x13` 均为 0 命中）→ 表仅存 SNDATA 流，运行时重排为 C++ 对象，故无 ×19 索引（解释为何静态找不到读取器）。
- 转值分析：`_item_3byte.py`/`_item_group.py` 按 `idx17`(原unk) 分组呈**压倒性语义聚类**——茄子茶入(199/215)、孙呉兵法(121/185)、平家三物语(177)、名刀流派(村正正宗124/菊一文字三日月100/长光一文字68/左文字92/太刀188)、绘画系列、书籍部类(137/145/153/161/169) ⇒ **`idx17`=系列/同类编号**。
- `idx16`=稀有度档位位域(0xff=传世孤品1/0x80=名物170/0x00=通用交易品18)；`idx15`=等级/稀有度评分(0..255，与val ρ≈0.56 非单调，疑历史名望序)。
- 位置 layout 不变（`name[0..12],cat[13],val[14],tier[15],flag[16],grp[17],0[18]`），仅语义由待破翻案。落盘：GAME_DATA_SPEC §3.19.2/§3.19.6、BREAKTHROUGHS 续80(顶链+详条)、MEMORY.md 物品表条。
- 仍待：idx15 公式、物品表↔物品池(stride12/10)绑定、流尾3791B。下一步建议用 idx17+idx16 重排 item_table.json 加 group/rarity 字段。

## 事件条件 evaluator 架构 + opcode 10/13/14 解码（续81 · 🔴 收回续79「+0x08 矛盾」）
- 续80 后按「先破完成度低的」继续攻事件系统 §3.7（最低完成度大模块）。切口：`0x4e82c0` 事件条件判定器。
- **dispatch 性质实锤为 C++ 虚表**：全镜像穷举——`0x4e82c0` 字面 0 命中（非静态函数指针/vtable）；`jmp/call [reg*4+imm]` 跳表 313 张（abs/rel-tbl/rel-jmp 三种解析）无一含 handler；函数指针 run 139 条无一含。⇒ vtable 不在静态镜像，运行时填充。全 opcode→handler 映射须 Unicorn dump vtable。
- **条件 evaluator 统一约定**（逐指令核对 `0x4e82c0`/`0x4e7e10`）：ctx=`call 0x49f6b0` 取；`[ctx+0]`=opcode(word)/`[ctx+8]`=arg(word)/`[ctx+0xc]`=flags(word)；命中→sub-eval(`0x4e83e0`/`0x49f610`)→`call 0x49b860` 触发→GUI `0x47b280`。
- **解码**：`0x4e82c0` op**13**=当前所在国==arg(`0x44e280`取国对象→`byte[+8]`=prov_idx,`lea ebx,[prov_idx*5+0x519548]`,`cmp bp,dx`)；op**14**=当前气候组==arg(`byte[prov_rt+1]`=climate_group∈{0,2,4}雪国,`cmp bp,ax`)；`0x4e7e10` op**10**=事件关联国==arg(`byte[param+0x24]`→politics entry→prov_idx,`test [ctx+0xc],2` 门控)。参考实现 `event_cond_ref.py` 自检 ALL PASS。
- 🔴 **收回续79 开放矛盾**：`@0x4417ad` 的 `lea ebx,[edx*2+0x5179b8]` 中 `edx=eax*7` ⇒ `ebx=0x5179b8+eax*14`——是按 **stride14 索引国政治表自身 `0x5179b8`**，非 stride5 的 `0x519548`。`+0x08`(流[5])实测 48/49 条=3(合法0..48 索引)=表内关联条目索引，**无运行时覆盖**；旧「矛盾」系目标表误读为 `0x519548` 所致，撤回。
- 枚举陷阱：以「`call 0x49f6b0`∧`call 0x49b860`」签名枚举出 24 候选，但多数(`0x443fe0`等)是**事件效果/应用器**(不读opcode/不比较arg)，`0x490c0`等是 `E8` 误指入 MZ/PE 的**假函数起点**。可靠 evaluator 仅 `0x4e82c0`/`0x4e7e10` 经手验。
- 已知 opcode 碎片：10/11/13/14/15/16/20/29/30/40（49/50/55/56/60 多为 `cmp al,0x31`=49 等 province-bound 误报）。opcode 10 与 13 同为「位于国X」但国源不同(10=事件关联国/13=玩家当前国)。
- 落盘：BREAKTHROUGHS 续81(顶链+详条)、GAME_DATA_SPEC §3.7.12、MEMORY.md 三处更新。脚本：`_evt_*.py` 系列 + `event_cond_ref.py`。
- 下一步：Unicorn 跑场景进入/事件求值，dump `0x49f6b0` ctx 对象+vtable → 解锁全 opcode→handler 映射与逐 opcode 语义。

## 事件派发 = 事件类型 id 双表（CONDITION/EFFECT）+ 枚举方法论三处误报根因修正（续82）
- 承接续81「先破完成度低的」继续攻事件系统 §3.7。切口：修正续81 的 opcode 枚举器，重新枚举 24 候选并逐条手验。
- **派发机制实锤**：事件按 `[ctx+0]` 的**事件类型 id**(word, 0..~60) 派发；每个 handler **自断言**自身 id——`mov reg,word ptr [ctx]` / `cmp reg,<id>` / `jne skip`，不匹配则跳过不触发。自断言证据逐条手验：`0x4e82c0` `cmp di,0xd`/`0xe` · `0x4e7e10` `cmp [edi],0xa` · `0x4b4b20` `cmp [edi],9` · `0x44ca90` `cmp [ebp],9`@`0x44cbf9` · `0x4b3ac0` `cmp [ecx],0xf`@`0x4b3bd5` · `0x4499f0`/`0x490c0` `mov ax,[ecx]`+`sub eax,0`/`dec eax`。
- 🔶 **修正续81 架构**：续81 称「每方法都是条件 evaluator，多数候选是误报效果器」不准确。实为**同一 id 空间上两张并行表**——CONDITION 表(读`[ctx+8]`=arg→算运行时值→比较→`call 0x49b860`)与 EFFECT 表(不比 arg，直接执行后果)。**决定性反证：id 9 同时有 condition `0x4b4b20` 与 effect `0x44ca90`**——单表无法解释一 id 两 handler。
- **新增 id→handler 映射 7 项**：condition `0x4e82c0`(13/14)·`0x4e7e10`(10)·`0x4b4b20`(9：`cx=[ctx+8]`→`0x49f430`→`cmp ax,[esp+0x2c]`)；effect `0x44ca90`(9：渲染消息 `0x1224..0x1227`+查全局`0x52063c`)·`0x4b3ac0`(15)·`0x4499f0`+`0x490c0`(0/1 近同构兄弟)。参考实现 `event_id_dispatch_ref.py` 自检 ALL PASS。
- 🔴 **纠偏续81**：`0x490c0` **不是**「`E8` 误指入 MZ/PE 数据的假函数起点」——它是真实 handler（自断言 id 0/1），与 `0x4499f0` 近乎逐条同构，差异仅在 fire 参数(push 0 vs 1)与尾部 getter(`0x4a0d50` vs `0x49b2b0`)。
- 🆕 **两套编号勿混淆**：底层事件类型 id(0/1/9/10/13/14/15…) ≠ §3.7 事件分类表 `0x50da40`(27 类：寺院/历史事件/本能寺大火，索引 2..28)。
- 🆕 **枚举方法论：三处系统性误报根因**（已固化 `scripts/_evt_enum3.py`，取代 `_evt_enum.py`/`_evt_enum2.py`）：① **寄存器复用未失效**——`0x4499f0` 中 `ax` 载 opcode 后被改作他用，其后 `cmp ax,0x1d` 被误报 opcode **29**，需按存活性失效；② **8 位子寄存器未归一**——`mov al,[edi+3]` 只写 `al`，按名字比无法失效 `ax`/`eax`，致 `and eax,0x1f`(位掩码)误报 **31**，需把 `al/ah/.../dil` 归一到 32 位父寄存器；③ **`dec`/`inc` 单操作数**——`len(ops)>=2` 前置条件整段漏掉 `dec eax`(判定 opcode **1**)。另：设置 opcode 寄存器的那条 `mov` 自身不得触发失效(否则刚设即失效，曾使 `0x4e82c0` 得出空集)。修正后 `0x4e82c0` 稳定 [13,14]、`0x4499f0`/`0x490c0` 得 [0,1]，与手验一致。
- 🔴 **顺带修正自身表述**：MEMORY 第 43 行(续73)已记「阻塞已解除·勿再写数据为空」——原版 111 文件在 `<工程>/Taikou2 Original/`(本次复核确认存在)。故续81「须 Unicorn 但受数据为空阻塞」**不成立**，Unicorn dump 可行。已在 BREAKTHROUGHS/GAME_DATA_SPEC/`event_id_dispatch_ref.py` 三处改正。
- 落盘：BREAKTHROUGHS 续82(顶链+详条四段)、GAME_DATA_SPEC §3.7.13、MEMORY.md 两处(已闭/仍待破)同步并清理重复条目。脚本：`_evt_enum3.py`(liveness修正版)·`_dumpfn.py`(带重同步的反汇编器)·`event_id_dispatch_ref.py`(自检ALL PASS)；反汇编 `scripts/_d_44*.txt`。
- 下一步：① 用 `_evt_enum3.py` 通读剩余 17 个候选(现 `opcodes=[]`)补全 id→handler；② 用 `<工程>/Taikou2 Original/` 实跑 Unicorn dump `0x516610` 的 vtable → 完整 id→槽映射；③ 各 CONDITION handler 抽出「arg 与哪个运行时值比较」落成谓词表。

## GAME_DATA_SPEC §4.3 文档同步升级（续88 · 📚→✅）
- 用户截图指出：BREAKTHROUGHS 已显示 §4.3「主命 12 项 + 报告 13 项 + 修行 11 项 + 授艺 + 任务 ID 空间 + 执行分发器」已破，但 GAME_DATA_SPEC §4.3 仍是 8 行 📚 设计层。任务：追平文档。
- **完成内容**：将 §4.3 从 8 行 📚 扩展为完整 ✅ 子章节：
  - **4.3.1 主命（12 项）** ✅：`0x504b28` 名表、`0x4607f0→0x46086c→0x496ba0` 执行链、`0x513fcc/0x513fd4` 运行时状态。
  - **4.3.2 报告（13 项 handler）** ✅：`0x504898` handler 表、`0x4603f0→0x460420→call [idx*4+0x504898]` 分发链、13 条 M3#0x1202–0x120e 文本、`0x513ff6/0x513ff8` 馬販子/米価开关、40% 概率门。
  - **4.3.3 修行/授艺系统** ✅：`0x45f710` 动作菜单、10 项修行动作 → 技能/五维增长、`0x4b5620` 资格判定、`min(31-日, 金钱/2)` 天数上限、`_emu_teach.py` 54/54 PASS。
  - **4.3.4 大名家臣会议菜单（21 项）** ✅：`0x50c7c0` 高压外交/友好外交入口、`0x4c2190` 菜单构建器、`0x525e30/0x525e50` dispatch 表。
  - **4.3.5 城内场所名池（15 项）** ✅：`0x508040` 茶室/铁匠铺/南蛮商馆/教堂/剑术道场/公卿府邸/画师工坊/寺院/神秘宅邸/医院/老家/民宅/武家宅邸/乡间寺院/皇宫。
  - **4.3.6 仍 📚/🔶**：外交具体 handler/关系值/AI 决策 🔶；探索/宴会/茶会/休息/出阵/商业/农业/治安/改修 📚。
- 同步更新：BREAKTHROUGHS 续88（顶链+详条四段）、MEMORY.md 仍待破新增 §4.3 条目、修复 MEMORY 行 40 与 41 的拼接错误。
- 参考实现/数据产物：`council_ref.py`(16/16)、`council_report_ref.py`(18/18)、`skills_spec.json`、`diplomacy_strings.py`。
- 下一步：外交 handler 侧（EMU dump `0x525e30`/`0x525e50`）或 §4.3 其他 📚 项。

## 外交系统 handler 级实锤 + 16 项工作类型执行跳表全破（续89）
- 用户要求「按建议继续破，先把大模块破了」→ 盘点确认**外交系统是唯一整块从未实证的玩法大模块**（续84 只钉字符串池，续85 断言 handler 静态不可见）。
- 🔴 **推翻续84/85 的关键误判**：`0x525e30`/`0x525e50` **不是**「静态全 0、运行时 C++ 填充、必须 emu」。填表代码就在同函数簇——`0x4c16a0` 填主会议 sub-handle（0/1/2/3），`0x4c20ed` 填工作类型码。**教训：断言「静态不可见」前，务必先做字节级 xref 找 WRITE 点，而不是只看表内容是否为 0。**
- ✅ **主会议派发跳表 `0x4c1668`（4 项，静态）**：`0x4c15ff` 取 `0x525e50[sel]` → `jmp [eax*4+0x4c1668]`。听取意见`0x4c1830` / 分派工作`0x4c1ef0` / 出兵`0x4c2590` / 结束会议→退出。名表 `0x50c7a0` **stride 16B**（🔴 纠偏续84 的「stride 14B · 21 项」——那是一次错位的连读）。
- ✅ **工作类型执行派发器 `0x4c55b0` = `jmp dword ptr [eax*4 + 0x50c950]`**，16 项全解码。**外交 handler 首次定位：高压外交 `0x4c41e0`、友好外交 `0x4c4320`**。其余 14 项 handler VA 一并到手（谋略 `0x4c4400` … 茶会 `0x4c5430`）。参数表 `0x50c990` 3B/项（高压/友好外交均 `(0,2,255)`）。
- ✅ **外交执行流程**：`0x4c4270` 选目标国（遍历 `0x5179b8` 国政治表 49 条，`cmp [esi+4],0x172` 判国主有效）→ `0x4c4300` 选使者（`0xffff`=取消）→ 武将实体 **`0x519868 + id*47`**（`lea ecx,[eax+eax*2];shl ecx,4;sub ecx,eax;add ecx,0x519868`，stride 47 再实锤）→ `0x49c2b0` 取名 → MSGX **`0x875`** 任命 → 存使者 `dword[0x525ea4]`。友好外交 `0x4c4320` 与之近同构（额外调 `0x49f640`）。
- 🆕 `0x4c2240` = 执行者确认菜单（任命 `0x50c8e0` / 其他武将 `0x50c8f0` / 取消 `0x50c900`，stride 16）。分派工作链：`0x4c2000` 选类型 → `0x525e30[sel]` 取码 → `0x4c55c0` 校验 → MSGX `0x85f` → `0x4c2240` 选执行者 → `0x4c1fcf`（`0x49c310` + MSGX `0x860`）。
- 产物：`scripts/diplomacy_ref.py`（**自检 ALL PASS**）、`scripts/_dip_xref.py`（dispatch 表字节级 xref 扫描器）、反汇编 `_d_4c15f0.txt`/`_d_4c2190.txt`/`_d_4c41e0.txt`/`_d_4c4320.txt`/`_d_4c55b0.txt`。
- 落盘：BREAKTHROUGHS 续89（顶链+详条四段）、GAME_DATA_SPEC §4.3.4 全替换（含 🔴 纠偏续84/85）、MEMORY.md 两处。
- 下一步：(A) 追 `dword[0x525ea4]` 使者下游消费函数 → 钉死抵达结算与关系值 ±；(B) 逐个反汇编其余 14 个工作 handler；(C) 核 `0x5179b8` 是否承载国关系值、解释 `0x5080cc` 无 xref。

## 仓库整理（文档/脚本去重）
- 新建 `README.md` 为破解入口；过时文档迁 `docs/archive/`；`SNDATA_SPEC.md` 升到根目录。
- 删除可再生垃圾（含 `_sk_thunk.asm` ~11.5GB）+ 探针 txt/insert 脚本；一次性 `_*.py` 迁 `scripts/_scratch/`。
- `scripts/` 现仅留 `*_ref.py` + 核心工具 + `_emu_*` + JSON 产物（~9MB）。
