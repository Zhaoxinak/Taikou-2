# 太阁立志传2 Godot 复刻 - 项目记忆（索引）

> 细节一律在仓库文档，本文件只留跨会话必需的边界/方法论/待破清单。
> **文档链**：`BREAKTHROUGHS.md`→`GAME_DATA_SPEC.md`→`BATTLE_SPEC.md`→`HANDOFF.md`→`REVERSE_ENGINEERING.md`→`HJMAPDAT_SPEC.md`/`NPK_SPEC.md`

## 边界
- Godot 4.7.1 代码自写；原版数据读`F:/Games/Taikou2`；仓库不打包素材。
- **2026-08-25 起**：停UI/像素/字体，只做数值+玩法→汇总进`GAME_DATA_SPEC.md`，美术用户自配。
- **用户硬性要求**：突破/推翻旧假设即插`BREAKTHROUGHS.md` dated条目(四段)+打勾，倒序(新在上)。

## 逆向方法论（必守）
- 映像 `_unpacked_mem.bin`(2MB,base 0x400000,OEP 0x4f44b0)。
- 全镜像仅80处栈帧序言→函数边界由「所有call rel32目标」推导；线性反汇编逐4KB+`va+=1`重同步。
- 工具：`_fdis.py`(只吃 call-target 函数头)、`_lindis.py`、`_string_pool_scan.py`、`_xref_reads.py`、`_emu_*.py`(Unicorn 2.1.4)。capstone 需 `from capstone.x86 import *`。
- 大量数值表**静态全0、运行时托管填充**(C++对象+虚表)→静态走不通就Unicorn实跑。
- **🔑 禁止猜表形状，必须穷举**：串池扫描挖出11张名表推翻「兵种名不在EXE」；数值表同法。
- **🔑 数据验证须读原始DAT**（勿用JSON转储）：字节经Unicode解码变`0xfffd`丢信息(续67 deploy即误判)。
- 教训：切片伪像会造假表；xref 只抓绝对立即数；已四次误判「唯一 xref 的静态表」语义 → 先确认引用函数真起点。
- **🔑 字段扫描必用 `scripts/_ins_index.py`**（续71 固化）。旧法两处**静默失真**：① 指令索引在 `jmp` 处截断→MSVC `lea;jmp <cont>` 三元续体整段丢失(索引135k→**272k**，命中1→37)；② `call` 后**未失效 eax/ecx/edx**→返回值被当表指针，产**假字段**(误得越界`+0x1d`)。另：立即数 xref 落指令中间，须按「包含」匹配(`ins_containing`)。

## 已破（速查，细节见文档）
格式 LS11/MSGX/GRP/IDX/SMODE/TOWNCHIP/KOS；BSDATA 700武将×59B、TOWNPOS 92城、SNDATA XOR流、SAVEDATA 16×20480B；名表 `0x506ca8` stride9×370；`HJMAPDAT.DAT` 38×1700B；per-tick伤害 `0x42d270`；天气/季节 §3.9；49国国情 §3.10/§3.13全闭；合戦布陣+計略 §3.10；静态名表11张 §3.11；地形系数 §3.12；技能/官位/授艺·经济·事件27型。
**阵形名不存在于 EXE**（2014 CJK 串零命中）⇒ `byte[p+4]` 仅内部编号，复刻可自命名。
**情报+武将实体表 `0x519868`(370×47B)+MSGX编号(`0x493500`)** §3.14；静态名表13张。**MSGX全局id↔文本映射已建**(`msgx_id_map.json`)——模块先查push的id。**单挑卡牌** §3.15；4个MESSAGE 6211条早已全解码。

**单挑大额闭合（续66,🚫）**：无「一击必杀」路径——伤害仅1处写入恒0..4；体力仅`dec`⇒台词9–24/>24不可达。`0x4684c0`跳表5分支=行动结果码。`duel2_ref.py` 38/38。
**🔴纠偏**：`0x466e40`返回**体力**非武力⇒背水一战(`rand()%(4-体力//20)`,<60生效)，非「武力越高加成越小」。
**大名/城主任命·继承全破（续65）**：城主任命`0x4d7c20`(门槛rank>4+礼法`byte[+0x11]&3`→`0x49a990`写`word[城+0x0a]`)；继承`0x4a42c0→0x4a3d70`(set_rank(7)+勲功满)。🔴纠偏：城主(8)是**城表派生态**(`word[城+0x0a]==武将号`)，不写職位字段(set_rank掩码`0xF8FF`仅0..7)。`promote3_ref.py` 42/42。
**section A（续67/68）**：**20列×9行空间网格**（`X=2*col`、`Y=2*row+((col&1)^1)`）。🚫EXE无地形名表⇒命名不可静态获得；✅值=8类战斗地形，攻城cls6/7不可通行/野战cls8/9不可通行⇒按battle_type选表。`sectA2_ref.py` 38/38。
**評定/任务分配彻底收口（续69）**：4尾巴全闭——0x521aa8=武将名表(0x47dce0拷贝stride7×370)；0x506c54=运行时指针(仅读)；0x441750=報告対象名formatter；主命执行入口0x4607f0(遍历0x513fd4,ID15..22)→0x46086c跳表→0x496ba0(状态引擎,711点)；0x45f0c0的bp∈{0,1}=米価/2=馬販子开关。`council_tails_ref.py` 29/29。
**城表/城町记录结构全破（续70）**：`0x51eb88` stride=31(×32−idx实锤)·200条(92城+108町)·31B；C++对象+0x00 vtable/+0x04链表next/+0x08..0x1e数据；16字段闭合(城主/农商/次级/民心/生产率/军粮/米/资金/次级民情/城种等)。⚠️ctor 0x4a3a4f的+0x24..+0x2d属更大实例(经0x4a05a0链接)非31B记录。`castle_fields_ref.py` 16/16。
**🆕49国「政治/关系表」`0x5179b8`(续71·🔴续79实测·🔴续81纠偏)**：stride14·49条·241xref；⚠️**与国情表`0x519548`(stride5)是两张表**(本表人事·外交/国情表地理·气候)。🔴续79 逐字节实测：`+0x00`=**城/町指针**/`+0x04`=2B packed(flag0/1·国主武将号0..369)/`+0x06`=word(`(关连国idx<<8)|attr`,hi<49)/`+0x08`=**0x03常量**/`+0x09+0x0a`=0xff/`+0x0b..+0x0d`关系属性；49条中44有效·尾5条城idx=0xff哨兵。🔴**续81 纠偏续79「+0x08 矛盾/运行时覆盖」**：`@0x4417ad` 的 `lea ebx,[edx*2+0x5179b8]`(`edx=eax*7`⇒`ebx=0x5179b8+eax*14`)是按 **stride14 索引本表自身**，**非** `0x519548`；`+0x08`=3 是表内关联条目索引(合法0..48)，旧「矛盾」系目标表误读，撤回。`province_politics_ref.py` 15/15。
**事件系统 §3.7 结构闭（续72）+ handler架构破（续81）+ 🆕派发=id双表（续82）**：🆕**续82 派发机制实锤**：事件按 `[ctx+0]` 的**事件类型 id**(0..~60) 派发，每个 handler **自断言**自身 id(`mov reg,[ctx]`/`cmp reg,<id>`/`jne skip`)；🔶**修正续81「每方法都是条件 evaluator」**——实为 **CONDITION/EFFECT 两张并行表**(同 id 空间)，CONDITION 读`[ctx+8]`=arg 比较运行时值后 `call 0x49b860`，EFFECT 不比 arg 直接执行后果；**同一 id 可兼有两者**(id 9：`0x4b4b20` condition + `0x44ca90` effect)。✅**id→handler 已破 7 项**：`0x4e82c0`(13/14 cond)·`0x4e7e10`(10 cond)·`0x4b4b20`(9 cond)·`0x44ca90`(9 eff)·`0x4b3ac0`(15 eff)·`0x4499f0`+`0x490c0`(0/1 eff 近同构兄弟)。🔴**续82 纠偏续81**：`0x490c0` **不是**「E8误指的假函数起点」，是真实 handler。🆕**两套编号勿混淆**：底层事件类型 id(0/1/9/10/13/14/15)≠ §3.7 事件分类表`0x50da40`(27类·索引2..28)。🆕**`_evt_enum3.py`(liveness修正版)**修三处系统性误报根因：寄存器复用未失效(误报29)·8位子寄存器未归一(误报31=`and eax,0x1f`掩码)·`dec`/`inc`单操作数被`len(ops)>=2`漏掉(漏opcode 1)。续72/续81 旧结论：🔴纠偏`0x4547f0`=**武将闲谈对话生成器**(8码`0x505/509/512/514/516/518/51a/51c`全闭)；🆕**MSGX全局id=4文件×2000槽**⇒`msgx_all_texts.json`全量6211条；🚫`0x50dab0`语义**静态不可破**。✅**续81 事件条件 evaluator 架构破**：每条件=C++虚表方法(ctx`[+0]`=opcode/`[+8]`=arg/`[+0xc]`=flags→命中`call 0x49b860`)；dispatch 经虚表(已穷举排除静态/动态索引表)；opcode **10/13/14** 解码：`0x4e82c0` op13=当前所在国==arg·op14=当前气候组==arg，`0x4e7e10` op10=事件关联国==arg；`event_cond_ref.py` 自检ALL PASS。🔴续81 另纠偏续79「+0x08 矛盾」：`@0x4417ad`按stride14索引`0x5179b8`自身(非`0x519548`)，`+0x08`=3合法表内索引，无运行时覆盖。

## 仍待破
- **section A**：命名/抽样意图/值中文名仍待运行时——结构+8类地形系数已闭(续67/68)。
- 单挑体力初值赋值点；`cmp word[0x514995],0x14`两处（疑换人/逃走）；AI选牌；虚表布局。
- `0x4ac690/0x4ac7f0`候选城策略；`byte[+0x11]`bit0/1技能；绝嗣`0x4a4410`；`byte[+0x2d]`bit3。
- 实体`+0x25/26/2c`语义；城表`0x51eb88`**结构已破(续70)·值语义待场景dump**；国情`+0`低4位/`+2`/`0x47e440`。
- **事件系统 §3.7（完成度最低的大模块·续72结构闭·续81 handler架构破·续82 派发=id双表）**：①✅**派发=事件类型 id 双表(续82)**——按`[ctx+0]` id 派发·handler 自断言 id；CONDITION 表(读`[ctx+8]`=arg 比较后fire)与 EFFECT 表(直接执行后果)并行，同 id 可兼有(id 9)；新增映射 `0x4b4b20`(9 cond)/`0x44ca90`(9 eff)/`0x4b3ac0`(15 eff)/`0x4499f0`+`0x490c0`(0/1 eff)；`event_id_dispatch_ref.py` 自检ALL PASS。②✅**opcode 10/13/14 解码(续81)**：`0x4e82c0` op13=当前所在国==arg·op14=当前气候组==arg；`0x4e7e10` op10=事件关联国==arg；`event_cond_ref.py` 自检ALL PASS。③🔶**完整 id→vtable 槽映射须 Unicorn dump**(🔴**不再阻塞**：原版111文件在`<工程>/Taikou2 Original/`)；剩余 17 个候选未读 opcode；id 2..8/11/12/16+ handler 未定位。④已闭：闲谈对话文本(续72)、`0x50dab0`静态不可破(续72)、本能寺台词(续75)、无总调度表(续75)。⑤🆕`_evt_enum3.py` 取代 `_evt_enum.py`/`_evt_enum2.py`(修三处误报根因)。- SNDATA流尾实体；商品基价`0x513ea8`；物品池种子；评价词8↔10绑定。
- 国政治表`0x5179b8`**结构+值语义已实测(续79)**：`+0x00`=城/町指针(非武将链表·链表元素疑城非武将)/`+0x04`=flag|国主武将号/`+0x06`hi=关连国idx(待xref确认)/`+0x08`=0x03常量(与`@0x4417ad`矛盾·待Unicorn核是否被运行时覆盖)。仍待：`+0x0b..+0x0d`语义、`+0x04` flag 0/1 精确含义、记录顺序(疑按势力排非按国ID)。
- 🚨**阻塞已解除（续73 纠偏，勿再写「数据为空」）**：`F:/Games/Taikou2`**路径不存在**；原版111个文件在**`<工程>/Taikou2 Original/`**(SNDATA1/2·SAVEDATA·BSDATA1/2·HJMAPDAT.DAT·MESSAGE1-4·TAIK2W95.exe)。⇒ 城表/国政治表值语义、物品表、SAVEDATA 状态区**全部可实做**。
- 🆕**物品定义表(SNDATA流 32019, 189×19B, 续73·✅续80语义已破)**：`名字GBK|0|cat(0..26,27类,21空缺)|val(1..200)|tier[15]|flag[16]|grp[17]|0`。🔴续80：`idx15`=**等级/稀有度评分**(0..255，名物0..20·孤品89/126/165/255)、`idx16`=**稀有度档位位域**(0xff=传世孤品1/0x80=名物170/0x00=通用18)、`idx17`=**系列/同类编号**(原记unk；同号=同流派:茄子茶入199/215·孙呉兵法121/185·平家三物语177·名刀流派124/100/68/92/188·绘画系列·书籍部类137/145/153/161/169)。**无189/19循环**(表仅存流,运行时重排C++对象)。`item_table_ref.py` 21/21。仍待：idx15公式·物品表↔物品池(stride12/10)绑定·流尾3791B。
- SNDATA流尾3791B；商品基价`0x513ea8`；评价词8↔10绑定。
- 🔴**名表读法（勿再错）**：`0x506ca8` 是 **stride 9 字符串数组，不是 4 字节指针表**（§2.8 早已记 stride=9，实操仍惯性按指针读 → 49国只解出9个名）。改 stride9 切片后 49/49 全非空。49国名全表见 `GAME_DATA_SPEC §3.18.8` / `province_names_ref.py`。
- **国政治表数据层(续74·🔴续79纠偏)**：`0x47e440` 从 XOR 流 **27297** 读 539B=49×11B → 写 `0x5179bc`(＝`0x5179b8+4`)。🔴续79 实测：流填 `+0x00`(城idx→指针)/`+0x04..+0x05`(flag|国主武将号)/`+0x06`(关连国idx|attr)/`+0x08..+0x0d`；**`+0x04`非整word武将编号、无0xffff**(旧记误读打包值)。49条顺序**不是**按国ID(武田家臣聚19–27/上杉28–33/德川34–43)⇒ 疑按势力/大名家排列，**勿硬套国名**。下一步：xref `+0x06`高字节×14、`@0x4417ad`运行时复核。
- **时间全局(续75)**：`byte[0x5205f0]`=**1560起的年偏移**(`add reg,0x618`=+1560)，**非绝对年**（当绝对年就整体错位1560）；`0x5205f1/f2`=月/日，**顺序未定**。🚫**无集中「历史事件年表」**——57处xref中仅9处年份判定(1600/1566/1563×2/1560×2)⇒ 史实事件是**分散硬编码cmp**，须逐个追handler。✅本能寺全闭合：`0x41aa70`→`0x41ab51`消息`0x1ad6..0x1ad9`→过场`0x41c160`，**台词14条全取到**(msg6866..6879)。🔴**教训：见可疑立即数先查MSGX文本再下结论**（`0x447780`实为消息显示封装，曾误判为按年调度器；`push 0x62e`的1582是消息ID非年份）。`event_year_ref.py` 14/14。§3.7现~70%，仅剩「逐handler虚表派发」。
- **可行路径（本能寺已验证）**：用台词关键词在 `msgx_all_texts.json` 反查消息id区间 → 反查 push 这些id的代码点 → 定位其余史实事件handler。
- **SNDATA流尾3791B(流35610..39401, 续76)**：✅**静态定义**（两场景仅10/1895不同，否定「商店库存」推测）。🔴**`0xffff`是数据值不是分隔符**（两场景计数1173vs1171不同）⇒ 续73「按ffff切32段」**作废**。最长非ffff游程**617个u16**：默认`0x0303`占504次(81.7%)+113特殊值聚**42簇**；形态=`(A<<8)|B`位域对(A 0..8, B 0..7/10/11)与独立大值(最大0xFF00)。尾端**55个u16**全0首255。含`0x8000|n`标志值。🚫自相关周期2..39全≈0.817⇒**无定长周期**，语义须定位序列化器。`sndata_tail_ref.py` 15/15。
- 🔑**方法论**：两场景差分=判定「静态定义 vs 场景状态」最快手段（续74国表、续76流尾均靠它）；自相关须先剔除默认值再算，否则被默认值主导而失效。
- **城/町表值语义(流21852, 200×26B→`0x51eb88`, 续77, 70%→~90%)**：序列化器`0x47e130`。✅**`byte17`=所属国**(0..48，地理分组全吻合：踯躅崎→甲斐10/春日山→越后12/骏府→骏河13/冈崎→三河15/清洲→尾张16/稻叶山→美浓17/金泽→加贺21)。✅**`byte19`=城主武将号**(199/200可解析：三户→前田利家/鹤冈→藤堂高虎/二本松→织田信忠)。✅**`byte14`=在城武将号(255=不在城)**(106条同城主, 95条255)。byte12-13恒0xff；`byte16`=第三武将编号(含织田信长13, 语义待定)。⚠️**与续70偏移对不齐**：续70由`0x49a990`得「word[城+0x0a]=城主」而本流在byte19(26=31−5疑写+0x05起但仍对不上)⇒**映射未核对勿强统**，待反汇编`0x47e130`。`castle_town_ref.py` 16/16。
- ✅**城表终解(续78, ~95%)**：反汇编`0x47e130`——`esi`起`0x51eb8c`(=`0x51eb88+4`)、200次、stride31。**17次读取=26B/条**（初数16次得25B是漏了`0x47e1a4→[esi+4]`=`+0x08`）。完整映射：`[0-1]→+0x00`武将指针 / `[2]→+0x04`城指针 / `[3]→+0x08` / `[4]→+0x09` / `[5-6]→+0x0a` / `[7]→+0x0c`农商 / `[8]→+0x0d`次级 / `[9]→+0x0e`**民心** / `[10]→+0x0f`生产率 / `[11-12]→+0x10`军粮(**剧本内恒0xFF05未设置**) / `[13-14]→+0x12`米 / `[15-16]→+0x14`资金 / **`[17-18]→+0x16`所属国** / **`[19-20]→+0x18`城主** / `[21]→+0x1a`次级民情 / `[22-23]→+0x1b`城种(&7) / `[24-25]→+0x1d`。🔴**纠偏续70：城主在`+0x18`不是`+0x0a`**（`+0x0a`仅19个不同名且大量重复「林通胜」；`+0x18`有105个不同武将）。
- 🔑**方法论**：统计序列化器读取字节数时，**每条`call 0x47d9xx`前都要看它`lea/push`的目标**，只数call条数会漏；**字节数对不上数据步长时先怀疑漏数**（用数据反证：25B会让所属国地理分组崩坏，26B则完美）。读取宽度：`0x47d910`→`0x47da10`=1B，`0x47d930`→`0x47da50`=2B。

## Godot 侧约定
预渲染画作 LINEAR+MIPMAPS；像素艺术 NEAREST+STRETCH_KEEP；mipmap 只在 Image 上生成；CJK 走系统字体（**所有 `*CHAR.LZW` 是角色精灵不是字体**）。
