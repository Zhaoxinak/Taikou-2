# 2026-08-26 工作日志

## 建立突破日志制度（用户硬性要求）
- 用户原话：「每次突破就记录起哦，因为我可能会用别的 AI 来继续破」。
- 新建 `F:/Games/Taikou 2/BREAKTHROUGHS.md`：跨 AI 接手的 chronological 增量日志。
  - 含「当前任务清单」（与 WorkBuddy #35–#42 同步，打勾制）。
  - 已 backfill 关键突破条目：2026-08-22/23 EXE脱壳+武将/城镇、LS11+KOS+图形、MSGX=GBK文本；2026-08-24 SNDATA结构化二进制+rmKOEI=卸载DLL+字体定位；2026-08-25 策略变更(停画面/字体/像素→转数值+玩法)+96B表定位(#35进行中)；2026-08-26 建日志制度。
- 修正 `HANDOFF.md`：
  - 顶部加 `BREAKTHROUGHS.md` 指针 + 2026-08-25 策略变更标注 + 最后更新日期改为 2026-08-26。
  - §0 重述状态：方向=数值+玩法；纠偏旧假设「字体=核心路径」「SNDATA字符串=核心阻塞」（均已降级）。
  - §4 标题加「已降级」标记 + §4.1 前加历史记录警示框。
  - §7 改为指向 BREAKTHROUGHS 任务清单，旧清单折叠为「历史仅供参考」。
- 项目 `MEMORY.md` 追加「跨 AI 接手工作流」长期规则。
- 当前数值/玩法任务（待破）：#35 96B合战数值表(in_progress) / #36 per-tick伤害 / #37 MSGX名称 / #38 地形数值 / #39 SNDATA字段 / #40 称号官位 / #41 事件系统 / #42 技能经济。

## 名称索引表 0x506ca8 全破译（#40 称号/官位→职种/特殊NPC名 已破）
- 纠正旧 decode：0x506ca8 实为 **stride 9 四块**（旧脚本按变长/null/stride14 全错位）。0–48国 / 49–87附加地名(flag前缀) / **88–291城·町(城id c→槽88+c, 已与towns.json验证)** / 292–369 职种·角色·特殊NPC(flag前缀)。
- 城名权威校正：`castle_names_exe.json`（EXE内部名，与社区 88/92 一致，仅 4 处异字）。旧 `towns.json` 用了社区后期名（岐阜→稻叶山、大阪→本愿寺、泷川→泷山、兴泽→兴津、二俁→二俣、长篠→长筱）待校正。
- 292–369 解出职种/师匠名：大名/宿老/队长/大将/步兵/枪铸造/大师/画匠/僧侣/南蛮商人/秘商人/国友(鉄砲)/山科言継/百地(忍者)/施药院/快川/近卫/伊达/阿尔梅(Almeida)/… →「修行师父/町角色」名称池。
- 交付：`name_table.json`(四块重生成)、`castle_names_exe.json`、`scripts/_decode_name_table_v2.py`/`_locate_castle_block.py`/`_dump_full_nametable.py`/`_decode_nametable_final.py`/`_build_castle_names_exe.py`。
- 文档：GAME_DATA_SPEC.md §3.3/§3.4 + §6 P1 更新；BREAKTHROUGHS.md 新条+#40打勾；HANDOFF.md §2.8；MEMORY.md 名表纠正。
- 仍未知：49–87块语义、292–369少数槽乱码精修、官位具体文本(勘解由左衛門尉)待MSGX、兵种(足轻/骑兵/铁炮)名仍不在静态段(待emu)。

## 本会话续破（2026-08-26 下午）
- **0x52d211 证伪（重大纠偏）**：反汇编证实它是十进制→ASCII 派发函数（cmp al,N / mov bx,0xNNNN 链，索引1..34→"01".."34"，结尾 call [ebp+0x20] thunk），并非数据表。此前「stride8 34条兵种定义表」是切分机器码每8字节的伪像。
- **0x501e48 确认**：16×48 真实表，全镜像唯一引用点 = 0x41bf20 军事/兵科对话框构建函数（循环16次读8字节/条进列表）。offset 24..31 恒为 [5,6,6,3,4,4,8,10]（8项基础属性模板）。已导出 `battle_units_spec.json`。
- **战斗模拟簇定位**：0x438c00–0x43a700；单位实例缓冲 0x512b60（字段偏移 0x28/0x29=0x512b88/89、0x512b61）；per-unit 4字节数组 0x512e58（战斗内只读，载入阶段初始化）；地形 0x512868、8方向表 0x503710、字符表 0x512f10、运行时池 0x524918/524978/524990。
- **关键结论**：EXE 静态段无干净兵种/阵形/计略数值表；这些数值运行时由 HKMAPNEW.LZW/HJMAPDAT.DAT 载入 0x524978 池（96B记录）与 0x512e58 数组填充。→ 兵种/阵形/计略 96B 记录在 0x524978 池，B:TERRAIN.LZW 是地形。
- 已更 BREAKTHROUGHS.md：任务清单打勾（#28/#35 两层纠偏、0x52d211 证伪、0x501e48 确认）+ 新增 dated 突破条目 + 新增待破项（unicorn dump 0x524978 池）。
- 新增脚本：_confirm_unit_tables.py / _disasm_regions.py / _trace_unit_init.py / _export_501e48.py。
- 下一步：unicorn 实跑战斗初始化 dump 0x524978 池 96B 切片，补全 battle_units_spec.json。

## 本会话续破（2026-08-26 晚）— 战斗数据真相 + 推翻 96B 池旧说
- **推翻旧假设「0x524978 池 96B 兵种记录」**：交叉引用证实 0x524978 池只服务战斗视觉地图簇(0x48bc00)；HKMAPNEW.LZW 解压仅 1765B(26×68 战图块)，非数值表。该「96B 兵种记录」假设作废。
- **定位真源 C:HJMAPDAT.DAT**：反汇编战斗初始化 `0x43a490`——打开 `0x5036f0`="C:HJMAPDAT.DAT"，按 `offset=variant×1700` 读记录，三次 `call 0x4411b0` 拆写入 `0x512e58`(180B兵种参数)/ `0x512868`(760B地形)/ `0x512b60`(760B部署)。
- **文件结构**：64600B = **38 战 × 1700B** = [180 兵种/阵形/计略参数(9×20,值0-7)] + [760 地形图(40×19, 低4位16种地形+高4位修饰位)] + [760 部署图(40×19, ASCII布阵字符)]。
- **参数表读取**：战斗 `0x439063` = `lea eax,[ut*5]; mov al,[ecx+ut*20+0x512e58]; and 0xf` → 9 兵种id(0-8) × 20 属性(0-7)。全38战nibble分布确认是 0-7 量级真实参数（1676 非零格）。
- **地形16种**：空/平地/荒地/草地/森林/河流/山地/桥/?8/?9/?A/?B/?C/城/阵/?F（?8-?F 中文定名待补）。
- 交付：`hjmapdat_battles.json`(38战全量提取) + `HJMAPDAT_SPEC.md`(结构规范) + `scripts/_extract_hjmapdat.py` + `scripts/_scan_all_battle_files.py`(证无明文96B表)。
- BREAKTHROUGHS.md：任务清单 #35 标已破(结构提取)，新增 2026-08-26 续条记录此推翻+定位。
- 待破(接手者)：section A 每兵种20字节字段语义(攻/防/HP/阵形/计略)；9兵种id↔足轻/骑兵/铁炮等对应；地形?8-?F定名；部署字符映射；MSGX兵种名对表。

## 本会话续破（2026-08-26 深夜）— section A 精确访问器 + 再证伪两处
- **纠正前文残留错误**（line 20 的「0x524978 池 96B 兵种记录」）：已被当晚「推翻 96B 池旧说」条目作废，本段确认——`0x524978` 池仅服务战斗视觉地图，兵种数值实际在 `HJMAPDAT.DAT` section A → `0x512e58`。
- **section A 访问器精确破解**：反汇编 `0x439050` getLo(a,c)=`buf[a*20+c]&0xF`、`0x4390c0` getHi(>>4，实战恒0)、`0x439080` setLo(XOR写入)。`getLo` 29 调用点、`getHi` 11、`setLo` 2（每战填/改）。调用约定实测(`0x423a13`)：`arg1=ecx=unit_struct[0]`、`arg2=eax=unit_struct[2]` → 按「实体类别 a × 属性 c」索引。确认 = **9 类(a=0..8) × 20 属性(c=0..19)**，4-bit 值 0-7（实战观测；`0x428b64` `cmp al,2; ja` 表明至少一处 >2 视为无效）；**高4位在全部38战恒0**；**跨战各格不恒定**（每战配置，非全局固定数值）。
- **证伪「0x52d211 = 34条兵种定义表」**（接续）：再确认是十进制→ASCII 派发函数，非数据表。
- **证伪「兵种名在 MESSAGE*.LZW(MSGX)」**：解码全部4个 MESSAGE（MSGX+GBK，共 **6211 条**）→ **零条恰好2字中文条目**；全 EXE 二进制亦搜不到「足軽/騎馬/鉄砲/弓/攻城/水軍/忍者」等 GBK 串（仅「弓」偶然命中）。兵种/阵形/计略名不在 MSGX、不在 EXE 静态段 → 待查**精灵 label 资源或代码表索引**，或 emu 时捕获渲染前字符串指针。
- **部署图再认知**：`0x512b60`=760B 是 40×19 ASCII 战术布局，每战 30-80 种不同非空字符（J/K/I/9/8/2/3/H/A/+/./t/0x81-0x9f…），含地形/部队标记/本阵/城门；复用基址另作 stride-8 单位实例数组(`[ecx+eax*8+0x512b60]`)。
- 交付：`scripts/_analyze_unit_table.py`(9×20结构+跨战方差+名表dump) / `scripts/_decode_msgx.py`+`scripts/_probe/msgx/all_messages.txt`(6211条全解码) / `scripts/_export_units.py`+`hjmapdat_units.json`(38战×9×20低4位矩阵) / 重写 `BATTLE_SPEC.md`(纠偏 96B池旧说、精确 section A) / `BREAKTHROUGHS.md` 追加 dated 条目 + 任务清单打勾(#42/#43/#44/#45)。
- **下一步建议**：emu 实跑战斗，instrument `0x439050` 记录 (a,c,返回值) 与 unit_struct 来源 + 关联 per-tick 公式(`0x438c00`-`0x43a700`)，一次性破解 9 类实体身份与 20 属性语义；并追溯 `0x512f10`(20B 精灵表) 定位兵种名。

## 本会话续破（2026-08-26 再深夜）— section A 比较语义 + 值转换 LUT
- **getLo 比较语义（✅ 反汇编实证）**：`0x423a13` 函数对两单位(esi/edi)各取 getLo 值（a=struct[2]=类别, c=struct[0]=属性），经 LUT 转换后于 `0x423a9d cmp al,cl` 比较两单位同一属性值，分两路设交战结果 → section A 实为**两单位在同一属性轴上的强弱比较矩阵**；`attr`(0..19)=比较轴，`class`(0..8)=参与比较的双方类别。
- **值转换 LUT 定位（✅ 静态可 dump）**：原始 getLo 0–7 经两张 256 项表转成战斗有效值——`0x423f90`→`0x503138`、`0x423fa0`→`0x503140`。`0x503140` 从 idx8 起为等差曲线 `2,2,5,5,8,8,11,11,14,14,17,17…`(每2级+3)，典型等级/数值曲线。两表完整 dump 入 `hjmapdat_units.json` 的 `luts` 字段，复刻可直接当黑盒用。
- **索引公式复核（纠偏自身误判）**：此前疑「JSON 把行列转置」——复核 `off=class*20+attr` 与 `off=attr+class*20` 同一数值（加法交换律），原 `hjmapdat_units.json` 布局本就正确，本次仅增补 LUT 未改布局。`_export_units_v2.py` 已重导出（含 luts）。
- **属性命名无法静态确定**：扫描 29 个 getLo 调用点，属性索引 c 全部来自结构体字段（struct[0]），无一常量立即数 → 20 属性轴玩法命名必须 emu 实跑。
- 交付/更新：`BATTLE_SPEC.md` 新增 §2.1(比较语义)/§2.2(LUT 表) + §9 待破清单更新；`BREAKTHROUGHS.md` 任务清单 #35 子项更新 + 新增 dated 条目（最新在前）；`hjmapdat_units.json`(含 luts)；脚本 `_trace_getlo_args.py`(29调用点参数溯源)、`_export_units_v2.py`。
- **下一步（待用户/接手 AI）**：搭建 Unicorn 战斗 emu 框架——把 `HJMAPDAT.DAT` battle0 的 1700B 按 `0x43a490` 逻辑灌入 `0x512e58/0x512868/0x512b60`，hook `0x439050` 记录 `(attr,class,返回值)`+调用方，跑若干 tick 一次性破 9 类身份/20 属性语义；并 hook 渲染前字符串指针定位兵种名。

## 本会话（2026-08-26 收尾）— SNDATA 记录分类实证 + 收口单一文档
- **用户策略再确认**：「不要弄界面/像素，把数据+玩法弄下来写成一个文档，美术后面自己配」。→ 目标 = 单一 `GAME_DATA_SPEC.md` 复刻文档，不再追视觉/调色板/精灵。
- **SNDATA 49B 记录分类（✅ 静态字节分析，免 emu）**：`scripts/_sndata_field_profile.py` 对 833 条逐字节统计 + 分类：
  - rec0 = 4B 场景 ID(`0x4bb8b7b7`/`0x1380b4be`) + flag；
  - rec1–22 = 45-bit 布尔开关(byte[6] 严格 0/1)；
  - rec23–830 = 数据块(每字节 distinct≈160、零值≈2%，**全字节均匀随机** → 非扁平枚举)；
  - 填充槽 = id_word `0x0c0c`/`0xf3f3`/`0x0f0f`，整条单值 `0x0c`/`0xf3`(剧本2 对应 `0x0a`/`0xf5`)。
  - **全部 49 字节在两剧本间均不同** → 动态场景态；跨场景 XOR 密钥 `obj[0x94]=hdr[0x12]^hdr[0x13]`。
- **收敛运行时项（避免再浪费精力）**：实 dump 确认 `0x513a78` 地形属性表(5B×16)、`0x512f10` 部署精灵表(20B)、`0x524978` 池 **静态全 0** → 由战斗初始化 `0x43a490` 从 `HJMAPDAT.DAT` 载入；复刻方直接读 `hjmapdat_battles.json` 即可。
- **常量表已 dump 直接可用**：八方向 `0x503710`、变体表 `0x503740/750/760`、LUT `0x503138/0x503140`(已入 `hjmapdat_units.json`)。
- **序列化器定位**：`scripts/_sndata_fieldmap2.py` 从主解析器 `0x47f350` 调用点定位 36 个序列化候选 @`0x47dae0`…，确认以 `lea edx,[base+idx+0x10]` 计算偏移读字段 → 静态字节统计法必然失败，数据块逐字段须 emu/逐函数反汇（记为 🔶 待续）。
- **收口文档**：`GAME_DATA_SPEC.md` 扩充——§0 总览表+已提取文件清单(含 3 个 battle JSON)、§4.5 合战数据产物(指向 JSON)、§5.1 SNDATA 记录分类(本条目)、§6 待办表更新(P0 合战/SNDATA 标记完成)。`BREAKTHROUGHS.md` 任务清单 #39 标已破(分类)、新增 dated 条目(最新在前)。
- **剩余真·待破(均须 emu)**：① SNDATA 数据块 rec23–830 逐字段(16 读序列化器反汇 / Unicorn 实跑 `0x47f350`)；② section A 9 类实体=哪些兵种+20 属性命名；③ per-tick 伤害公式；④ 兵种/阵形/计略中文名。



## 本会话（2026-08-26 晚）— 🎯 合战 per-tick 兵力消耗公式全破（#29/#36 完成）
- **定位方法（关键经验）**：此前一直在错的地方找（`0x438c00`–`0x43a700` 是视觉/布阵簇，算术密度极低）。改用「全镜像函数画像」：`scripts/_profile_all_funcs.py` 对 4212 个函数统计 muldiv/移位/比较 × 战斗上下文引用（单位槽 `0x513910`、section A 访问器），排名首位 `0x42d270`（6 muldiv + 7 slot15/getHi）→ 即真正的结算函数。
- **🚨 本镜像无标准栈帧序言**：全 2MB 仅 80 处 `push ebp; mov ebp,esp` → **函数边界必须用「所有 `call rel32` 目标」推导**（prologue 扫描必败）；线性反汇编需逐 4KB 块 + `va+=1` 重同步（否则 11836 条 vs 正确 304472 条）。工具 `scripts/_fdis.py`（带符号标注反汇编器，可复用）。
- **公式链**：`0x42d270`(结算·写回兵力) → `0x42d5d0`(战力汇总) → `0x42d730`/`0x42d5a0`(阵营修正) → `0x43a9c0`(攻击除数表 `0x503770`) / `0x43cd10`(兵力递减曲线) / `0x439050`·`0x4390c0`(section A) / `0x4ebc50`(muldiv)·`0x4ebcd0`·`0x4ebd10`(饱和减法)。
- **单位槽**：15 槽 @ `0x513910`，**stride 24B**：`+0x0c`w 兵力 / `+0x11` 士气 / `+0x12` 士气损失 / `+0x13` 状态(低2位=兵种类别0/1/2，高4位≠0=退场) / `+0x15` bit2=阵营；攻`+0x0b`/防`+0x0a`/装备档 0–3；间接解析时攻防各 >>1。
- **核心式**：`战力=(atk+装备加成)×(100+净士气//10)//100`（下限 10）`×troop_scale(兵力)//23`（魔数 `0xb21642c9`/`sar4` ≡ `//23`，已数值验证）；`troop_scale` 分段线性 `t≤100:t / ≤300:t//2+50 / ≤500:t//4+125 / ≤1000:t//5+150 / else 3t//20+200`（处处连续）；`E=本方战力//对方存活数×2`（攻击摊薄）；`base=E×7//攻击除数`；`dmg=base//(防御//4+50)+1`；`兵力=max(0,兵力-dmg)`（`0x42d468` 写回）；`伤亡率=muldiv(伤亡,100,回合前总兵力)`。
- **阵营不对称写死**：side0 恒 ×80%，两将同类再 ×80%，`pB.flags&0x10` ÷2，`battle_type==0 && 0x42b8c0` 再 ÷2；side1 在 `mode_m1(0x511bf8)` 下**免伤**且配合 parity 战力 >>3。
- **section A 真实数值用途解出**：低 4 位 = **攻击除数表索引**（`0x503770` 20B = `[10,12,15,7,7,15,100,100,10,10,12,7,7,10,10,8,100,100,12,12]`，按 `battle_type(0x513548)` 取 +0/+8 窗口；除数越小伤害越高，`100`≈零伤害档）；高 4 位 = 双方除数 ±1 对冲（38 战静态恒 0=中性）。
- **纠偏（推翻本日早条）**：§2.1 旧结论「section A = 两单位属性轴强弱比较矩阵，`0x423a9d` 的 `cmp al,cl` 决定交战结果」**证伪**——`0x423910` 全函数反汇编确认它是**战前属性条 UI 显示例程**，比较结果只写显示缓冲 `0x511358`(20×2)，不进任何伤害路径。
- **玩法推论（复刻平衡必读）**：① 每单位每回合至少掉 1 兵→战斗必然收敛；② 防御是软上限（分母 `def//4+50`，0→200 只减半，堆防收益极低）；③ 士气才是乘数（每 10 净士气 +1%，长回合复利）；④ 兵力递减+攻击摊薄→多支小队抗集火、单支大军吃亏（太阁2「小队可用」的数值根因）。
- **交付**：`scripts/battle_formula_ref.py`（可执行参考实现，自检通过：`casualties 112/144`、`loss_pct 5%/9%`）；文档 `BATTLE_SPEC.md §9`(完整 9.1–9.9)+`§2.1`(纠偏)+`§10`(待破表更新)、`GAME_DATA_SPEC.md §0/§4.5/§4.5.1/§6/§7`、`HANDOFF.md §2.9`+顶部清单、`BREAKTHROUGHS.md` 顶部新条目 + #29/#36 打勾 + 修正「末尾追加」为「倒序·新在上」。
- **仍未知**：(a) `pA/pB` 的 `col(+0x00)`/`row(+0x02)` 字段来源（哪个属性决定落在 section A 哪格）；(b) 4 个模式标志玩法含义(`0x511bf8`/`0x51352c`/`0x513540`/`0x513548`)；(c) 兵种类别 0/1/2↔中文名；(d) 地形是否影响本公式（`0x513a78` 静态全 0）。
- **下一步候选**：① Unicorn 实跑一场合战，hook `0x439050`/`0x43a9c0`/`0x42d270` 取 col/row 实际值与标志置位时机（一次性解 section A 语义 + 地形加成）；② 非战斗线：`castle_names_exe.json` 校正 `towns.json` 6 处城名 + 建技能→师父授艺表。

## 本会话（2026-08-26 深夜）— 🎯 合战公式 Unicorn 实跑验证 + 阵营修正误读纠正
- **建 Unicorn 隔离仿真框架** `scripts/_emu_battle.py`：跑真实脱壳映像 `scripts/_unpacked_mem.bin` 的 `0x42d270`，插桩抓 R1/R2/n0/n1/base0/base1 与逐单位战力(`0x42d6f5` 的 EDX=战力/ESI=单位指针)，与独立 Python `ref_battle` 逐字段比对。未映射取指用 IAT 桩跳回、未映射数据取 0 放行（脱壳映像缺页）。
- **3 场景 ALL PASS（逐单位 bit-for-bit）**：a(4+4)、b(7+7=14)、c(2 side0 退场 + `pA.flags=0x10` + 两将同类 kind=3)。c 情景二进制 `R1=3636 R2=1028 n0=2 n1=4 base0=3636 base1=514`。公式现为「反汇编 + 真二进制仿真」双实证。
- **🔴 纠正两处旧误读（已同步 docs 与 `battle_formula_ref.py`）**：
  1. `side0_strength(0x42d730)` **全程不读任何 flags 位**——旧「`pB.flags&0x10 → ÷2`」「`battle_type==0 额外 ÷2`」均不实（二进制 R2 仅含 `×4/5` 与同类 `×4/5`）。`0x42b8c0` 的 `flags&0x40` 额外减半属另一条 battle 路径，不在 `0x42d270`。
  2. 真实除法顺序 = `base = 14 * (R // n) // mod`（先 `idiv n` 取整→`×14`→`idiv mod`），**非** `14*R//(n*mod)`；R 整除 n 时二者巧合相等（a/b 一度假通过，c 才暴露）。
- **实证辅助**：`0x42c150 = mode_m1(0x511bf8)`、`0x43cab0 = (parity&1)`（side1 的 `>>3` 需二者同非零）；`0x43ca10 = battle_type(0x513548)`（仅 battle_type==0 做 hi±1 对冲）。
- **交付/文档**：`BATTLE_SPEC.md §9.6`(纠正)+`§9.10`(验证报告)、`GAME_DATA_SPEC.md §4.5.1`(验证印章)、`HANDOFF.md §2.9`(更新)、`BREAKTHROUGHS.md`(新增 dated 条 + #23 任务项修正)、`scripts/battle_formula_ref.py`(已订正)、`scripts/_emu_battle.py`(验证脚本)。临时 `_disasm_strength.py`/`_disasm_main.py` 已删。
- **仍未知**（同前）：col/row 来源、4 模式标志玩法含义、兵种类别↔中文名、地形加成。
- **下一步候选**：① 若继续，Unicorn 实跑一场真实合战解 section A 9×20 语义 + 地形加成；② 或转非战斗线（towns.json 6 处城名校正 / 技能→师父授艺表）。

## 本会话（2026-08-26 深夜 续）— 技能/官位/授艺系统逆向（#34/#42 技能·授艺部分完成）
- **静态表逐字节验证**（脱壳映像 `scripts/_unpacked_mem.bin`）：技能名 `0x507b58`(5B×10)=口才/马术/算术/剑术/忍术/兵法/洋枪/筑城/礼法/茶道（订正旧记 算用→算术、军学→兵法）；官位 `0x507b90`(7B×10)=从五位…正一位；修行菜单 `0x504800`=学习刀术/读书/学习口才/学习筑城/学习兵法/学习剑术/艺术鉴赏/宝物鉴赏/什么也不做/继续住宿（相邻「继续住宿吗？」、变体「读　　书」）。
- **授艺资格判定 `0x4b5600` 破解**：学习者/师父两结构体比 byte@0xf&3(技能和≥6)/byte@0xd(外交≥100)/byte@0xe(魅力≥100)，任一达阈值不可修；全过调 `0x47b900` 带码 0xae8/0xae9。解码串「的技能/外交/魅力」。
- **菜单构建 `0x45f600` + 选择器 `0x45f690` + 注册表 `0x517838`**：构建器按角色 word@8>>8&3 选类；选择器在注册表搜 byte@8&7(esi0→cat4/esi1→cat1/esi2→cat6,7/esi3→cat5,3)；注册表运行时填充(静态=0，同战斗表)。
- **交付**：`GAME_DATA_SPEC.md §3.5`(文档化)、`scripts/skills_spec.json`(机器可读)、`BREAKTHROUGHS.md`(新条目+#34/#42 技能部分打勾)。
- **仍未知**：武将技能位打包假设(BSDATA 27-29)未验证；注册表运行时内容(master→skill 路由)待 emu；授艺提升量公式；经济/物价公式(#34/#42 余下)。
