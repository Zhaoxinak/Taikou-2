# 2026-09-08 工作日志

## 状态核对：非图像是否全部破解完（用户问「你看下 git 描述」）

**结论：是。除图像豁免档外，非图像已 100% 收口。**

### 核对证据链
- **git**：HEAD=`38fad9e`「docs: 续248–253 非图像残留清零（评价词阈值/S13/城种/@28 等）」；工作区 `git status` 干净、`main` 与 `origin/main` 同步。前一提交 `a94187b`=续245/246「SNDATA id→class 语义层 100% 收口」。
- **HANDOFF.md §6**（权威定性）：「非图像结构/数据层：已清零（续253）。仅图像豁免档不动。」剩余仅 ① 可选 S13 存档局面快照（局面态，非漏破 LUT）② 图像豁免。
- **BREAKTHROUGHS 顶部=续253**：评价词档位阈值+真实绘制器静态全闭，纠偏旧「须 emu / 0x47ca70 / 0x4d0e10」误指；「仍未知：图像豁免档不动」「下一步：非图像逆向正式结案，转复刻」。
- **破解状态清单 §8（续231 文件级终审）**：142 原始文件 = 56 非图像全 PASS + 33 图像豁免 + 53 运行时资产 + **0 UNKNOWN**。

### 🔴 实测数字纠偏（重要，跨会话）
- **自测套件实测 = `180 PASS / 0 FAIL`（共 180 个 ref 文件）**，耗时约 12 分钟。
- HANDOFF §1 / MEMORY.md 记录的仍是 **166**（续243 时的快照），**已过时 14 个 ref**（续248–253 新增）。
- 套件计数按 **ref 文件数**，非断言数（断言总数几千）。后续引用请用 180+。

### 残留 ❓/🔶 标记定性（非真敞口）
三份 SPEC 仍扫到：GAME_DATA_SPEC 4 个 ❓/43 个 🔶、BATTLE_SPEC 1/6、SNDATA_SPEC 0/1。抽查确认为两类，**均非破解缺口**：
1. **图例定义行本身**（GAME_DATA_SPEC 第 10/12 行、BATTLE_SPEC 第 3 行——即「🔶=推断待验证」的说明文字）；
2. **历史条目未回清**（早期标 🔶，后续续篇已破但未回头改标记；如第 24 行「称号」原文自带 `✅ 称号（续232 闭）`）。
→ 属**文档卫生问题**，可单开一次清扫；不阻塞复刻。

### 环境备注（macOS 本机）
- `timeout` 命令在 macOS 不存在 → 跑套件直接用 `/Library/Frameworks/Python.framework/Versions/3.7/bin/python3 scripts/_run_all_selfchecks.py`（勿加 timeout，曾因此空跑）。
- HANDOFF §7 写的 Windows Python 路径 (`C:/Users/.../Python312`) 与 `F:\Games\Taikou 2` 是**另一台机器**的环境；本机为 `/Users/ts/Downloads/Taikou 2`，Python 3.7 可用。

### 下一步（待用户定）
- (A) 清扫残留 ❓/🔶 历史标记（纯文档活）；
- (B) 开工 Godot 复刻骨架（数据层吃已破表）。

---

## 执行 (A)：残留标记清扫（用户选定）

**改动 5 个文档**（`git diff --stat` = +47/-24），不动任何 ref 脚本。

### 1. 图例加「现状说明」（全局定性，一次性解决所有剩余标记的误读）
- `GAME_DATA_SPEC.md` 图例后 + `BATTLE_SPEC.md` 头部：声明「**现存 🔶/❓ 全部属 emu 运行期语义增强类，非结构敞口、不阻塞 Godot 复刻**」，并写明 180 ref 全 PASS / 56/56 文件终审。

### 2. 八处「已破未回清」改 ✅ + 注明闭合续篇
| 项 | 原标 | 闭合依据 |
|---|---|---|
| 伪兵造兵数 | 🔶 落地量待校准 | 续233+234（公式 `base[tier]+rand%mod`，`spawn_count_formula_ref.py` 9/9） |
| 谣言 | 🔶 落地效果待校准 | 续235（复跑确属已破未清） |
| 合战模式标志语义 | 🔶 | 续186+190（5 标志精确玩法语义） |
| 兵种中文名 | 🔶 仍缺 | 续211（`0x50bfe8`=步兵/骑兵/洋枪/城守备；阵形名结论性缺失） |
| section A 9 类实体 | 🔶 推断 | 续182+190（a∈[0,8]，emu 取值集合已闭合；与 4 个中文兵种名非 1:1） |
| section A 20 属性 | 🔶 部分否证 | 续250（=空间网格，无日文主名表，结论性负结果） |
| SNDATA ~164 类型 schema | 🔶 仍待 | 续230 **架构证伪**（不存在按 type 的字段结构）+ 续244/245/246 语义层收口 |
| 18 子解码器→S0..S17 | 🔶 待 emu | 续165/166（全映射，写入目标坐死） |
| S14 方向 / S13 全 FF | 🔶 剩余 | 续100（严格上三角 49×48/2）/ 续238+252 |

### 3. 真·emu 项**归类加注**（不伪标）
- `§3.20.3`（S6 `0x516610` 逐 bit 语义）：加归类说明——**结构几何已全破，仅逐 bit 命名待实跑**；S6 语义**静态不可得**是该结构固有性质（续138/139 已证：除复位清零外静态无非零写入、无 getter），**非破解遗漏**。
- `§4.3.6` 城内指令 / `§4.6` 事件系统：归为**玩法设计层（📚）**，菜单入口与派发链已闭合，剩余为可自定的设计参数。
- 牵制/修复/填埋：保留 🔶 + 加注「emu 增强·非阻塞」。

### 4. HANDOFF / MEMORY 数字同步 166 → 180
- 共 4 处（§1 状态、§3 接手流程、§8 收尾流程、§10 建议开场）+ MEMORY.md「当前状态」两处。
- 补 **macOS 环境坑**：无 `timeout` 命令（加了会空跑）、套件全量约 12 分钟、本机 Python 3.7 路径。

### 🔑 方法论（可复用）
- **判定优先级：BREAKTHROUGHS 条目标题 > SPEC 旧标记**——后者常滞后 10+ 续篇（本次 8 处全是这条）。
- **铁律：真·emu 项绝不可伪标 ✅**，只能归类加注「非结构敞口」。判定「已破 vs 待 emu」的试金石：看续篇是否已给出**公式/地址/名表**（已破），还是只重复「须 emu 采样」（真待）。
- **套件副作用**：跑完 `_run_all_selfchecks.py` 会再生 `scripts/bsdata_format.json` / `bsdata_tail_fields.json`（缩进 2→1 空格等），须 `git checkout --` 还原（HANDOFF §8 既定流程）。本次已还原。

### 结果
`SNDATA_SPEC.md` 🔶 清零（1→0）；`GAME_DATA_SPEC`/`BATTLE_SPEC` 剩余标记均已定性为「非阻塞」。
（注：❓/🔶 计数因新增说明文字内含符号而未大幅下降，**实质状态改动 = 8 处 🔶→✅**。）

---

## 文件/文档整理（用户「方便后续复刻」）

### 1. 🆕 新建 `docs/复刻导航.md`（核心增值，零破坏）
复刻专用入口，9 节：三句话总览 / **数据资产地图**（10 个核心 json 精确路径+规模）/ **原版文件格式表**（TR2 四类、KOS、LZW、HJMAPDAT 的读法）/ **关键几何速查**（10 张表基址+stride）/ **字段速查**（武将 59B + 实体 47B + S7 16B 全字段表）/ 已破玩法索引 / 图像豁免怎么办 / **避坑清单 7 条** / 复刻顺序 / 改动同步规则。
- 并在 `README.md` 顶部 + 权威链表挂入口。

### 2. 修 README 过时信息
- 「31 个 ref」→ **180**（并注明约 12 分钟、macOS 无 timeout）
- 「当前优先待破 4 项」→ **全部已闭**，逐条注明闭合续篇（续184/185、续212/220/227、续99、续253、续250、续241）
- 目录约定重写：`_probe/`（不存在）→ 修正为 `_scratch/`284项 / `_草稿/`22项 / `_decoded_kos/`权威 / `kos_wav/`旧副本 / `_decoded_grp/`
- 原版「111 文件」→ 149

### 3. 物理清理（用户逐项确认后执行）
| 操作 | 对象 | 说明 |
|---|---|---|
| `git rm` | `err.txt` | 0 字节空文件 |
| `rm` | `.DS_Store` | 未跟踪；`.gitignore` 第51行本已含该规则 |
| `git rm -r` | `scripts/kos_wav/`（40 文件） | 与 `_decoded_kos/` 39 个 wav **完全相同**，仅多 `_manifest.json` |
| `git rm` | 根目录 `sndata_s15_segc.json` | 与 `scripts/` 下同名文件**逐字节相同**(2176B)，是副本 |

**验证**：删除后跑 `kos_format_ref`(27/0)、`sfx_15_36_ref`(17)、`mci_av_subsystem_ref`(28/28) 全 PASS。唯一引用 `kos_wav` 的是 `_scratch/_decode_kos_wav.py`（草稿，非 ref，不影响套件）。

### 4. 探针脚本：用户选「保持不动」
429 个 `_*.py` 保留在 `scripts/`。**原因**：`_disasm_all`(8个ref依赖)/`_dis_helper`(5个)/`_ins_index`(1个) 被 ref import，Python 依赖同目录，移动会 ImportError。套件 `glob(BASE+"/*_ref.py")` 非递归，不受探针影响。

### 🔴 重要发现：套件会往**工程根目录**吐产物
`_run_all_selfchecks.py` 用**两种 cwd** 依次重试（ROOT 与 BASE，因为 ref 相对路径不统一）⇒ 以 ROOT 为 cwd 的 ref 会把产物写到**工程根**。
根目录那个 `sndata_s15_segc.json` 就是这么来的（mtime 16:00 = 套件运行时；`scripts/` 下同名是 9/1 原版）。
**对策**：已加 `.gitignore` 规则 `/sndata_s15_segc.json`（只忽略根目录，不误伤 `scripts/` 下的权威副本）。以后发现根目录冒出新 json，先怀疑这个机制。

### 整理后根目录（干净）
`BATTLE_SPEC / BREAKTHROUGHS / GAME_DATA_SPEC / HANDOFF / NPK_SPEC / README / SNDATA_SPEC / 破解状态清单.md` + `docs/ fonts/ scripts/ Taikou2 Original/ project.godot`

---

## docs/ 与根目录重复审查（用户问「是不是有重复」）

### 结论：**1 处真同名 + 1 处功能重叠**（其余为有意归档，不重复）
| 项 | 判定 |
|---|---|
| `docs/archive/HANDOFF.md`(22.5KB, 08-31) vs 根目录 `HANDOFF.md`(11.2KB, 09-08) | 🔴 **真同名**，内容不同（429 行差异）。归档版是 08-26 旧总览，根版是 09-08 新版。**有意归档**（archive/README 已说明），但同名易混淆 |
| `docs/archive/REPLICATION.md`(34KB, 08-24) vs `docs/复刻导航.md`(9KB, 新) | ⚠️ **功能重叠但定位不同**：前者 = **Godot 工程层**（环境配置/Demo 进度/引擎模块/死路警告，已归档停在「数据逆向~70%」）；后者 = **数据层**（资产地图/格式/字段速查）。**我建新文档时不知道它存在** |
| `docs/archive/README.md` | 归档目录说明，非重复 |
| 其余 `BATTLE_SPEC_OLD_wrong` / `HJMAPDAT_SPEC` / `ENGINE_SPEC` / `REVERSE_ENGINEERING` | 均带独立名或 `_OLD` 后缀，无冲突 |

### 🔴 由此修正了我新文档的一个**错误**
`docs/复刻导航.md` §1 原把 **`koei_codes.json`(149KB) 列为可用资产** —— 归档 `REPLICATION.md` §6 明确说它是**死路**（源于早期「光荣私有置换码」错误假设，真实文本是 GBK）。
**已核实：全 scripts/ 无任何 .py 引用它** → 坐实废弃。已从资产表移除，并加 ⚠️ 死路警告（含 `chip_palettes.json` 同理）。

### 吸收了归档 REPLICATION §6 的 5 条时间无关死路 → 新导航 §7 第 8–12 条
8. **搜武将全名搜不到**（姓与名间夹 `00 00 00` 三零字节，字节不连续）→ 分开搜或逐条 GBK 解码
9. **`TaikouParser.parse_bsdata()` 是错误占位桩**（假设 100B/条，实际 **59B**，未解字段）→ 别改它，直接用 `bsdata.json`
10. **KOS 无台词**（台词在 `MESSAGE*.LZW`）；旧 `kos_message_map.json` 系误读 PCM 已废
11. **`rmKOEI.bin` 是卸载程序 DLL 不是字体**（无 GDI/字体 API）；对话文本与武将名是明文 GBK 直解
12. **SNDATA 字段用 EXE 内部码**（GBK→字形索引→位图），需字体加载器 `0x424120`（archive/ENGINE_SPEC §7）

### 处理动作
- 在 `docs/复刻导航.md` 开头加 **「与 archive/REPLICATION.md 分工」对照表**（避免复刻者混淆两份"复刻"文档）
- 删 `docs/.DS_Store`（未跟踪，gitignore 已覆盖）

### 🔑 教训
**建新文档前先 `find docs -type f` 全查一遍** —— 本仓库有 `docs/archive/` 存放**已归档但仍有价值**的文档（尤其 §6 死路警告这类时间无关经验）。
根目录文档是权威，但归档文档里的**经验性/死路类**内容不随进度过期，值得回捞。

---

## docs/archive/ 整目录删除（续上「把过时的删掉」）

用户指示「把过时的、没用的删掉」+「刚卡住了继续」。之前因 429 限流中断的删除确认弹窗，本次直接执行。

### 判定依据（为什么安全删）
- `docs/archive/` 7 文件全受 git 跟踪（可恢复）；内容均被根目录文档取代。
- `REPLICATION.md` 描述的 Godot Demo **已灭绝**：全仓 `**/*.gd` = 0 文件；`project.godot` 的 `run/main_scene=res://scenes/Main.tscn` 与 `autoload GameState=res://scripts/GameState.gd` **均悬空**（scenes/ 不存在）。
- 归档里**时间无关、仍有效**的 5 条死路警告已在上一轮提炼进 `docs/复刻导航.md` §7 第 8–12 条；LS11 解码器实装在 `scripts/real_assets.py`（4 ref 覆盖），不依赖归档文档。

### 执行
- `git rm docs/archive/*`（7 文件）：BATTLE_SPEC_OLD_wrong / ENGINE_SPEC / HANDOFF / HJMAPDAT_SPEC / README / REPLICATION / REVERSE_ENGINEERING。
- 同步清理悬空引用：README.md（删 archive 行 + 修 kos_wav/scenes 过时描述）、HANDOFF.md、BREAKTHROUGHS.md、`docs/复刻导航.md`（开头分工框改"已删除"说明、§7 来源注释、§字体加载器去文件链接）。
- **`project.godot` 保留但去悬空项**：移除 `run/main_scene` 与 `autoload GameState`（指向不存在文件），保留工程名/窗口配置，作为复刻暂停期干净壳。

### 验证
- `git grep docs/archive` 在 `*.md` 项目文档中**零残留**（仅 BREAKTHROUGHS.md 注明"已删除"、`.workbuddy/memory/` 历史日志保留）。
- 全部改动未提交（与清理/整理两批一并待 commit；用户尚未批准 commit）。

---

## ⚠️ 用户收回「删 Godot 相关」指令（17:5x）

用户：「我以后还是需要用 godot 复刻的，那可以不删？」→ **Godot 工程本体保留，不删。**

### 已做
- 撤销前一轮误启动的「根目录哨兵迁移」：曾把 `scripts/*.py` 中 ~490 个 `project.godot` 根哨兵批量改成 `GAME_DATA_SPEC.md`（490 文件各改 1 行）。已**反向替换还原** `project.godot`。
- 反向替换时**误伤 3 个正当 `GAME_DATA_SPEC.md` 文档引用**（非哨兵）：`diplomacy_strings.py`(docstring)、`feint_ref.py`(注释)、`s15_table_ref.py`(`gspec = open("GAME_DATA_SPEC.md").read()` —— 改错会令该自测读错文件失败)。已 `git checkout` 这 3 个文件还原。
- 最终核验：`git diff --name-only scripts/` 仅 3 文件 → 还原后 **scripts/ 相对 HEAD 干净**（迁移零残留）。`project.godot`(干净壳, 无悬空 main_scene/autoload) + `fonts/`(NotoSansSC 17.7MB + .import 均受跟踪) 全部保留。
- 教训：批量语义替换（`replace` 全量）会误伤同名正当引用，**先 grep 确认唯一语义再改**；撤销类操作优先 `git checkout` 单文件而非盲反向。

### 未动
- `docs/archive/` 仍按上一轮「删过时」保持删除（用户仅收回"删 Godot 引擎"；REPLICATION.md 属已灭绝 demo 的旧写，仍删除）。若用户想要回，git 历史可 `git restore` 整目录。

### 结论
Godot 复刻工程完好无损，下次复刻可直接沿用 `project.godot` 作为工程根 + 脚本根哨兵。

## 📁 文档分类整理（18:xx，用户：「整理和整合优化，文档放文件夹下并分类」）
- 按推荐方案执行：根 `README.md` 保留作大门 + 加分类横幅；新增 `docs/INDEX.md` 总索引。
- 移动（git mv）：`GAME_DATA_SPEC/BATTLE_SPEC/SNDATA_SPEC/NPK_SPEC/破解状态清单`→`docs/specs/`；`BREAKTHROUGHS/HANDOFF` + `scripts/_x92.md`→`docs/re/`；`docs/复刻导航.md`→`docs/replication/`。
- 批量重写文档互链 **239 处**（按引用文件目录算相对路径，幂等）；`scripts/s15_table_ref.py:69` 的 `open("GAME_DATA_SPEC.md")` → `open(os.path.join(_ROOT,"docs/specs/GAME_DATA_SPEC.md"))`。
- 新增 `scripts/README.md` 分类地图（强调**脚本不移动**，否则崩 180 自测）。
- 修 1 个既有断链：`docs/replication/复刻导航.md` 的 `.../re/HANDOFF.md`→`../re/HANDOFF.md`。
- 验证：全仓真实 `.md` 链接 23 个全部可达（0 断链）；`太阁立志传说明.md` 磁盘缺失且无人引用，已从跟踪移除。
- 全部未提交（与前几批清理一并待 commit）。
