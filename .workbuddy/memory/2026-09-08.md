# 2026-09-08 工作日志

## 状态核对：非图像是否全部破解完（用户问「你看下 git 描述」）

**结论：是。除图像豁免档外，非图像已 100% 收口。**

### 核对证据链
- **git**：HEAD=`38fad9e`「docs: 续248–253 非图像残留清零（评价词阈值/S13/城种/@28 等）」；工作区 `git status` 干净、`main` 与 `origin/main` 同步。前一提交 `a94187b`=续245/246「SNDATA id→class 语义层 100% 收口」。
- **HANDOFF.md §6**（权威定性）：「非图像结构/数据层：已清零（续253）。仅图像豁免档不动。」剩余仅 ① 可选 S13 存档局面快照（局面态，非漏破 LUT）② 图像豁免。
- **BREAKTHROUGHS 顶部=续253**：评价词档位阈值+真实绘制器静态全闭，纠偏旧「须 emu / 0x47ca70 / 0x4d0e10」误指；「仍未知：图像豁免档不动」「下一步：非图像逆向正式结案，转复刻」。
- **破解状态清单 §8（续231 文件级终审）**：142 原始文件 = 56 非图像全 PASS + 33 图像豁免 + 53 运行时资产 + **0 UNKNOWN**。

### 🔴 实测数字纠偏（重要，跨会话）
- **自测套件实测 = `180 PASS / 0 FAIL`（共 180 个 ref 文件）**，耗时约 12 分钟。
- HANDOFF §1 / MEMORY.md 记录的仍是 **166**（续243 时的快照），**已过时 14 个 ref**（续248–253 新增）。
- 套件计数按 **ref 文件数**，非断言数（断言总数几千）。后续引用请用 180+。

### 残留 ❓/🔶 标记定性（非真敞口）
三份 SPEC 仍扫到：GAME_DATA_SPEC 4 个 ❓/43 个 🔶、BATTLE_SPEC 1/6、SNDATA_SPEC 0/1。抽查确认为两类，**均非破解缺口**：
1. **图例定义行本身**（GAME_DATA_SPEC 第 10/12 行、BATTLE_SPEC 第 3 行——即「🔶=推断待验证」的说明文字）；
2. **历史条目未回清**（早期标 🔶，后续续篇已破但未回头改标记；如第 24 行「称号」原文自带 `✅ 称号（续232 闭）`）。
→ 属**文档卫生问题**，可单开一次清扫；不阻塞复刻。

### 环境备注（macOS 本机）
- `timeout` 命令在 macOS 不存在 → 跑套件直接用 `/Library/Frameworks/Python.framework/Versions/3.7/bin/python3 scripts/_run_all_selfchecks.py`（勿加 timeout，曾因此空跑）。
- HANDOFF §7 写的 Windows Python 路径 (`C:/Users/.../Python312`) 与 `F:\Games\Taikou 2` 是**另一台机器**的环境；本机为 `/Users/ts/Downloads/Taikou 2`，Python 3.7 可用。

### 下一步（待用户定）
- (A) 清扫残留 ❓/🔶 历史标记（纯文档活）；
- (B) 开工 Godot 复刻骨架（数据层吃已破表）。

---

## M4 内政与修行 — 实现并验证通过

**用户从进度表选 M4**（HD-3 备选，仅"接入已就绪代码"不如 M4 有玩法闭环价值）。

### 落地内容（`src/core/game_state.gd` 扩展）
- `train_mode(mode, days, rand_vals=[])`：8 修行动作（§5.4 表）。
  - mode0 学习内政→口才(0)+1，概率 `rand%(16-sub)==0`；
  - mode1 学习外交→筑城(7)+1（概率门以 mode-0 门近似，待 0x45f710 精逆）；
  - mode2 学习魅力→兵法(5)+1；mode3→武力；mode4 读书→50% 内政否统御；mode5→魅力；mode6→外交；mode7 早退。
  - 技能提升(0/1/2) 时功勲 `+= (旧级+1)×500`，饱和封顶 `MERIT_CAP=60000`。
  - 花费：金 `2/日`、体力 `5/日`、上限 `min(31-日, 金/2)`；推进 `advance_days`。
- `can_train(mode, teacher_id=-1)`：资格判定（技能和≥6 / 外交和≥100 / 魅力和≥100 → 不可，仅当有师）。
- `issue_command(cmd_id)`：12 主命派发到主角居城资源覆盖层（gunryo/kome/shikin/nousang/seisan/f0d/f1a/f18）。
  - ⚠️ 各主命**精确资源增减量**尚未逐 handler(0x496ba0 链)逆向，现为符合原版经济常识的 documented 近似量；精确 delta 留待 handler 级补全。
- 新增覆盖层：`_force_override` / `_money_override` / `_castle_override`，运行期变更不回写 `data/*.json`（与原有 `_skill/_merit/_stamina/_loyalty` 同机制）。
- `get_status()` 现含 `money`/`day`；`_effective_merit()` 将 `0xFFFF`(65535) 哨兵归一化为 `60000` 上限。

### 验证
- 新建 `tools/_test_m4.gd`（无头，21 类断言）：8 动作 + 功勲公式 + 封顶 + 12 主命 delta + 原表不被污染 + 资格接口 —— **全 PASS**。
- 回归 `tools/_test_m3.gd`：M3 全 PASS（28ms），无退化。
- 运行：`Godot_v4.7.1/Godot_v4.7.1-stable_win64_console.exe --headless --script res://tools/_test_m4.gd`。
- ⚠️ 坑：`--check-only --script res://src/core/game_state.gd` 单独检查会因缺 autoload 上下文报 `Identifier not found: GameData`；必须以**完整工程** `--script` 跑验证（autoload 才载入）。

### 进度表
- `docs/replication/进度表.md` 阶段4 两行已勾 `[x]`。
- **未做**：git push 仍卡凭据（GCM 无凭据挂死，见 MEMORY 环境段）；本批改动尚未 commit/push，需用户发 PAT 或本机推。

---

## 执行 (A)：残留标记清扫（用户选定）

**改动 5 个文档**（`git diff --stat` = +47/-24），不动任何 ref 脚本。

### 1. 图例加「现状说明」（全局定性，一次性解决所有剩余标记的误读）
- `GAME_DATA_SPEC.md` 图例后 + `BATTLE_SPEC.md` 头部：声明「**现存 🔶/❓ 全部属 emu 运行期语义增强类，非结构敞口、不阻塞 Godot 复刻**」，并写明 180 ref 全 PASS / 56/56 文件终审。

### 2. 八处「已破未回清」改 ✅ + 注明闭合续篇
| 项 | 原标 | 闭合依据 |
|---|---|---|
| 伪兵造兵数 | 🔶 落地量待校准 | 续233+234（公式 `base[tier]+rand%mod`，`spawn_count_formula_ref.py` 9/9） |
| 谣言 | 🔶 落地效果待校准 | 续235（复跑确属已破未清） |
| 合战模式标志语义 | 🔶 | 续186+190（5 标志精确玩法语义） |
| 兵种中文名 | 🔶 仍缺 | 续211（`0x50bfe8`=步兵/骑兵/洋枪/城守备；阵形名结论性缺失） |
| section A 9 类实体 | 🔶 推断 | 续182+190（a∈[0,8]，emu 取值集合已闭合；与 4 个中文兵种名非 1:1） |
| section A 20 属性 | 🔶 部分否证 | 续250（=空间网格，无日文主名表，结论性负结果） |
| SNDATA ~164 类型 schema | 🔶 仍待 | 续230 **架构证伪**（不存在按 type 的字段结构）+ 续244/245/246 语义层收口 |
| 18 子解码器→S0..S17 | 🔶 待 emu | 续165/166（全映射，写入目标坐死） |
| S14 方向 / S13 全 FF | 🔶 剩余 | 续100（严格上三角 49×48/2）/ 续238+252 |

### 3. 真·emu 项**归类加注**（不伪标）
- `§3.20.3`（S6 `0x516610` 逐 bit 语义）：加归类说明——**结构几何已全破，仅逐 bit 命名待实跑**；S6 语义**静态不可得**是该结构固有性质（续138/139 已证：除复位清零外静态无非零写入、无 getter），**非破解遗漏**。
- `§4.3.6` 城内指令 / `§4.6` 事件系统：归为**玩法设计层（📚）**，菜单入口与派发链已闭合，剩余为可自定的设计参数。
- 牵制/修复/填埋：保留 🔶 + 加注「emu 增强·非阻塞」。

### 4. HANDOFF / MEMORY 数字同步 166 → 180
- 共 4 处（§1 状态、§3 接手流程、§8 收尾流程、§10 建议开场）+ MEMORY.md「当前状态」两处。
- 补 **macOS 环境坑**：无 `timeout` 命令（加了会空跑）、套件全量约 12 分钟、本机 Python 3.7 路径。

### 🔑 方法论（可复用）
- **判定优先级：BREAKTHROUGHS 条目标题 > SPEC 旧标记**——后者常滞后 10+ 续篇（本次 8 处全是这条）。
- **铁律：真·emu 项绝不可伪标 ✅**，只能归类加注「非结构敞口」。判定「已破 vs 待 emu」的试金石：看续篇是否已给出**公式/地址/名表**（已破），还是只重复「须 emu 采样」（真待）。
- **套件副作用**：跑完 `_run_all_selfchecks.py` 会再生 `scripts/bsdata_format.json` / `bsdata_tail_fields.json`（缩进 2→1 空格等），须 `git checkout --` 还原（HANDOFF §8 既定流程）。本次已还原。

### 结果
`SNDATA_SPEC.md` 🔶 清零（1→0）；`GAME_DATA_SPEC`/`BATTLE_SPEC` 剩余标记均已定性为「非阻塞」。
（注：❓/🔶 计数因新增说明文字内含符号而未大幅下降，**实质状态改动 = 8 处 🔶→✅**。）

---

## 文件/文档整理（用户「方便后续复刻」）

### 1. 🆕 新建 `docs/复刻导航.md`（核心增值，零破坏）
复刻专用入口，9 节：三句话总览 / **数据资产地图**（10 个核心 json 精确路径+规模）/ **原版文件格式表**（TR2 四类、KOS、LZW、HJMAPDAT 的读法）/ **关键几何速查**（10 张表基址+stride）/ **字段速查**（武将 59B + 实体 47B + S7 16B 全字段表）/ 已破玩法索引 / 图像豁免怎么办 / **避坑清单 7 条** / 复刻顺序 / 改动同步规则。
- 并在 `README.md` 顶部 + 权威链表挂入口。

### 2. 修 README 过时信息
- 「31 个 ref」→ **180**（并注明约 12 分钟、macOS 无 timeout）
- 「当前优先待破 4 项」→ **全部已闭**，逐条注明闭合续篇（续184/185、续212/220/227、续99、续253、续250、续241）
- 目录约定重写：`_probe/`（不存在）→ 修正为 `_scratch/`284项 / `_草稿/`22项 / `_decoded_kos/`权威 / `kos_wav/`旧副本 / `_decoded_grp/`
- 原版「111 文件」→ 149

### 3. 物理清理（用户逐项确认后执行）
| 操作 | 对象 | 说明 |
|---|---|---|
| `git rm` | `err.txt` | 0 字节空文件 |
| `rm` | `.DS_Store` | 未跟踪；`.gitignore` 第51行本已含该规则 |
| `git rm -r` | `scripts/kos_wav/`（40 文件） | 与 `_decoded_kos/` 39 个 wav **完全相同**，仅多 `_manifest.json` |
| `git rm` | 根目录 `sndata_s15_segc.json` | 与 `scripts/` 下同名文件**逐字节相同**(2176B)，是副本 |

**验证**：删除后跑 `kos_format_ref`(27/0)、`sfx_15_36_ref`(17)、`mci_av_subsystem_ref`(28/28) 全 PASS。唯一引用 `kos_wav` 的是 `_scratch/_decode_kos_wav.py`（草稿，非 ref，不影响套件）。

### 4. 探针脚本：用户选「保持不动」
429 个 `_*.py` 保留在 `scripts/`。**原因**：`_disasm_all`(8个ref依赖)/`_dis_helper`(5个)/`_ins_index`(1个) 被 ref import，Python 依赖同目录，移动会 ImportError。套件 `glob(BASE+"/*_ref.py")` 非递归，不受探针影响。

### 🔴 重要发现：套件会往**工程根目录**吐产物
`_run_all_selfchecks.py` 用**两种 cwd** 依次重试（ROOT 与 BASE，因为 ref 相对路径不统一）⇒ 以 ROOT 为 cwd 的 ref 会把产物写到**工程根**。
根目录那个 `sndata_s15_segc.json` 就是这么来的（mtime 16:00 = 套件运行时；`scripts/` 下同名是 9/1 原版）。
**对策**：已加 `.gitignore` 规则 `/sndata_s15_segc.json`（只忽略根目录，不误伤 `scripts/` 下的权威副本）。以后发现根目录冒出新 json，先怀疑这个机制。

### 整理后根目录（干净）
`BATTLE_SPEC / BREAKTHROUGHS / GAME_DATA_SPEC / HANDOFF / NPK_SPEC / README / SNDATA_SPEC / 破解状态清单.md` + `docs/ fonts/ scripts/ Taikou2 Original/ project.godot`

---

## docs/ 与根目录重复审查（用户问「是不是有重复」）

### 结论：**1 处真同名 + 1 处功能重叠**（其余为有意归档，不重复）
| 项 | 判定 |
|---|---|
| `docs/archive/HANDOFF.md`(22.5KB, 08-31) vs 根目录 `HANDOFF.md`(11.2KB, 09-08) | 🔴 **真同名**，内容不同（429 行差异）。归档版是 08-26 旧总览，根版是 09-08 新版。**有意归档**（archive/README 已说明），但同名易混淆 |
| `docs/archive/REPLICATION.md`(34KB, 08-24) vs `docs/复刻导航.md`(9KB, 新) | ⚠️ **功能重叠但定位不同**：前者 = **Godot 工程层**（环境配置/Demo 进度/引擎模块/死路警告，已归档停在「数据逆向~70%」）；后者 = **数据层**（资产地图/格式/字段速查）。**我建新文档时不知道它存在** |
| `docs/archive/README.md` | 归档目录说明，非重复 |
| 其余 `BATTLE_SPEC_OLD_wrong` / `HJMAPDAT_SPEC` / `ENGINE_SPEC` / `REVERSE_ENGINEERING` | 均带独立名或 `_OLD` 后缀，无冲突 |

### 🔴 由此修正了我新文档的一个**错误**
`docs/复刻导航.md` §1 原把 **`koei_codes.json`(149KB) 列为可用资产** —— 归档 `REPLICATION.md` §6 明确说它是**死路**（源于早期「光荣私有置换码」错误假设，真实文本是 GBK）。
**已核实：全 scripts/ 无任何 .py 引用它** → 坐实废弃。已从资产表移除，并加 ⚠️ 死路警告（含 `chip_palettes.json` 同理）。

### 吸收了归档 REPLICATION §6 的 5 条时间无关死路 → 新导航 §7 第 8–12 条
8. **搜武将全名搜不到**（姓与名间夹 `00 00 00` 三零字节，字节不连续）→ 分开搜或逐条 GBK 解码
9. **`TaikouParser.parse_bsdata()` 是错误占位桩**（假设 100B/条，实际 **59B**，未解字段）→ 别改它，直接用 `bsdata.json`
10. **KOS 无台词**（台词在 `MESSAGE*.LZW`）；旧 `kos_message_map.json` 系误读 PCM 已废
11. **`rmKOEI.bin` 是卸载程序 DLL 不是字体**（无 GDI/字体 API）；对话文本与武将名是明文 GBK 直解
12. **SNDATA 字段用 EXE 内部码**（GBK→字形索引→位图），需字体加载器 `0x424120`（archive/ENGINE_SPEC §7）

### 处理动作
- 在 `docs/复刻导航.md` 开头加 **「与 archive/REPLICATION.md 分工」对照表**（避免复刻者混淆两份"复刻"文档）
- 删 `docs/.DS_Store`（未跟踪，gitignore 已覆盖）

### 🔑 教训
**建新文档前先 `find docs -type f` 全查一遍** —— 本仓库有 `docs/archive/` 存放**已归档但仍有价值**的文档（尤其 §6 死路警告这类时间无关经验）。
根目录文档是权威，但归档文档里的**经验性/死路类**内容不随进度过期，值得回捞。

---

## docs/archive/ 整目录删除（续上「把过时的删掉」）

用户指示「把过时的、没用的删掉」+「刚卡住了继续」。之前因 429 限流中断的删除确认弹窗，本次直接执行。

### 判定依据（为什么安全删）
- `docs/archive/` 7 文件全受 git 跟踪（可恢复）；内容均被根目录文档取代。
- `REPLICATION.md` 描述的 Godot Demo **已灭绝**：全仓 `**/*.gd` = 0 文件；`project.godot` 的 `run/main_scene=res://scenes/Main.tscn` 与 `autoload GameState=res://scripts/GameState.gd` **均悬空**（scenes/ 不存在）。
- 归档里**时间无关、仍有效**的 5 条死路警告已在上一轮提炼进 `docs/复刻导航.md` §7 第 8–12 条；LS11 解码器实装在 `scripts/real_assets.py`（4 ref 覆盖），不依赖归档文档。

### 执行
- `git rm docs/archive/*`（7 文件）：BATTLE_SPEC_OLD_wrong / ENGINE_SPEC / HANDOFF / HJMAPDAT_SPEC / README / REPLICATION / REVERSE_ENGINEERING。
- 同步清理悬空引用：README.md（删 archive 行 + 修 kos_wav/scenes 过时描述）、HANDOFF.md、BREAKTHROUGHS.md、`docs/复刻导航.md`（开头分工框改"已删除"说明、§7 来源注释、§字体加载器去文件链接）。
- **`project.godot` 保留但去悬空项**：移除 `run/main_scene` 与 `autoload GameState`（指向不存在文件），保留工程名/窗口配置，作为复刻暂停期干净壳。

### 验证
- `git grep docs/archive` 在 `*.md` 项目文档中**零残留**（仅 BREAKTHROUGHS.md 注明"已删除"、`.workbuddy/memory/` 历史日志保留）。
- 全部改动未提交（与清理/整理两批一并待 commit；用户尚未批准 commit）。

---

## ⚠️ 用户收回「删 Godot 相关」指令（17:5x）

用户：「我以后还是需要用 godot 复刻的，那可以不删？」→ **Godot 工程本体保留，不删。**

### 已做
- 撤销前一轮误启动的「根目录哨兵迁移」：曾把 `scripts/*.py` 中 ~490 个 `project.godot` 根哨兵批量改成 `GAME_DATA_SPEC.md`（490 文件各改 1 行）。已**反向替换还原** `project.godot`。
- 反向替换时**误伤 3 个正当 `GAME_DATA_SPEC.md` 文档引用**（非哨兵）：`diplomacy_strings.py`(docstring)、`feint_ref.py`(注释)、`s15_table_ref.py`(`gspec = open("GAME_DATA_SPEC.md").read()` —— 改错会令该自测读错文件失败)。已 `git checkout` 这 3 个文件还原。
- 最终核验：`git diff --name-only scripts/` 仅 3 文件 → 还原后 **scripts/ 相对 HEAD 干净**（迁移零残留）。`project.godot`(干净壳, 无悬空 main_scene/autoload) + `fonts/`(NotoSansSC 17.7MB + .import 均受跟踪) 全部保留。
- 教训：批量语义替换（`replace` 全量）会误伤同名正当引用，**先 grep 确认唯一语义再改**；撤销类操作优先 `git checkout` 单文件而非盲反向。

### 未动
- `docs/archive/` 仍按上一轮「删过时」保持删除（用户仅收回"删 Godot 引擎"；REPLICATION.md 属已灭绝 demo 的旧写，仍删除）。若用户想要回，git 历史可 `git restore` 整目录。

### 结论
Godot 复刻工程完好无损，下次复刻可直接沿用 `project.godot` 作为工程根 + 脚本根哨兵。

## 📁 文档分类整理（18:xx，用户：「整理和整合优化，文档放文件夹下并分类」）
- 按推荐方案执行：根 `README.md` 保留作大门 + 加分类横幅；新增 `docs/INDEX.md` 总索引。
- 移动（git mv）：`GAME_DATA_SPEC/BATTLE_SPEC/SNDATA_SPEC/NPK_SPEC/破解状态清单`→`docs/specs/`；`BREAKTHROUGHS/HANDOFF` + `scripts/_x92.md`→`docs/re/`；`docs/复刻导航.md`→`docs/replication/`。
- 批量重写文档互链 **239 处**（按引用文件目录算相对路径，幂等）；`scripts/s15_table_ref.py:69` 的 `open("GAME_DATA_SPEC.md")` → `open(os.path.join(_ROOT,"docs/specs/GAME_DATA_SPEC.md"))`。
- 新增 `scripts/README.md` 分类地图（强调**脚本不移动**，否则崩 180 自测）。
- 修 1 个既有断链：`docs/replication/复刻导航.md` 的 `.../re/HANDOFF.md`→`../re/HANDOFF.md`。
- 验证：全仓真实 `.md` 链接 23 个全部可达（0 断链）；`太阁立志传说明.md` 磁盘缺失且无人引用，已从跟踪移除。
- 全部未提交（与前几批清理一并待 commit）。

## 🚀 Godot 复刻开工（21:xx，用户：「现在可以用godot复刻了游戏了，根据文档和文件这些，帮我详细写一下复刻文档」）

**前置**：先 `git pull --ff-only origin main`，fast-forward `38fad9e → a641f32`（66 文件 +701/−2715，即上面的文档分类整理 + 删 kos_wav）。

### 产出 1：`docs/replication/Godot复刻实施方案.md`（★ 施工图，约 900 行）
章节：目标与范围（做什么/不做什么）→ 技术栈与工程结构 → 数据层 → 核心数据模型(GDScript 骨架) → 八大玩法系统 → 表现层 → **M0–M7 里程碑** → 验证与回归 → 避坑清单(18 条) → 附录 A/B/C。
要点决策：
- **合战提前到 M2**——它是唯一「反汇编 + Unicorn 实跑二进制双验证」的闭环系统，做出来即原版手感，能最早验证整条数据链。
- 分层铁律：`src/core` + `src/battle` 不引用 Node，可无头压测。
- 数据管线：原版 → 导出器 → `data/*.json`，**不在 Godot 运行时解析 .TR2**。
- 内部分辨率建议改 640×400（现 1024×640 = 1.6× 非整数倍，会拉伸）——**仅作建议，未擅自改 `project.godot`**。

### 产出 2：`scripts/export_for_godot.py`（离线导出器，已跑通）
按续200 权威 59B 布局重解析 BSDATA → `data/{officers,castles,provinces,names,skills,text,gaiji,battles,consts}.json`。
内置自检（织田信长/藤吉郎样本 + 職位∈[0,7] + 忠诚∈[0,100] + 技能∈[0,3] + 占位槽=5 + 外字命中），**实测 PASS**，导出 2.0 MB。

### 🔴 关键发现：`scripts/bsdata.json` 的 `fields` 已被续200 证伪
字节级复核 `BSDATA1.TR2` 记录 0（林通胜）确认旧布局全错：
忠诚真在 `0x35`=74（旧记 56=47 实为状态字低字节）、`0x2c/0x2d`=88/88 体力（旧 `stamina@46=40` 实为 `0x2e` 体力消耗）、功勲 `0x32`=10000（旧记 50-51=trust）。
→ 已在实施方案 §3.2、`docs/INDEX.md`、根 `README.md`、`scripts/README.md` 四处加红字警告。

### 导出实测（可直接作 Godot 侧回归基准）
- 织田信长 #13：五维 **96/85/92/99/90**、職位=大名、功勲=65535、俸禄=250、主角槽 7 ✅
- 木下藤吉郎 #16：五维 **84/42/89/94/97**、職位=足轻组头、俸禄=1、主角槽 0 ✅
- **可选主角 6 人**：#1 柴田胜家 / #13 织田信长 / #16 木下藤吉郎 / #257 上杉谦信 / #364 明智光秀 / #423 本愿寺显如
- **大名 45 名**（職位=7）；信长 国=13(尾张) 城=66
- **外字解码正确**：#182 垪和氏续(国=6 北条家臣群)、#557 長宗我部元亲(国=35 土佐，大名)、#568 香宗我部亲泰、#581 長宗我部信亲、#583 長宗我部盛亲 —— 与 §1.6 史实锚点逐条吻合。
  注：#430 香川元景 含「香」但属正常 GBK 字，非外字。

### 顺带修正
- `scripts/README.md`：`_decoded_grp/` 数量 31 → **13**（实测）；新增「二之二、Godot 复刻导出器」一节。
- `docs/INDEX.md`：登记新文档 + 加「复刻开工顺序」提示 + `data/` 说明 + Godot 工程状态改为「恢复开工」。
- 根 `README.md`：加施工图链接 + 导出命令 + bsdata 警告；`project.godot` 描述更新。
- `.gitignore`：新增 `/data/`（导出产物可重生，含原版文本/数值不入库）。
- **清理 `MEMORY.md`**（已超限被截断）：12856 → ~7.5K 字符，重构为「边界与阶段 / 高频纠偏表 / 数据契约速查 / 方法论 / 当前状态 / Godot 约定 / 环境」七节，新增「🔴 高频纠偏」表（10 条易错直觉）。

### 待用户确认
1. 是否把 `project.godot` 分辨率从 1024×640 改成 1280×800（内部 640×400 的 2× 整数倍）？
2. 本批改动（新文档 + 导出器 + 4 处文档修正 + gitignore）是否 commit？（**push 前必定先经用户确认**）

## 🎨 画面定风格 = HD-2D（21:2x，用户：「我想用HD2D 重制，你看下怎么弄」）

**HD-2D 本质**：高分辨率像素 sprite + **3D 场景** + 现代后处理（Bloom/DOF/SSAO/ACES），
参考 `OCTOPATH TRAVELER` / `TRIANGLE STRATEGY` / `DQ3 HD-2D`。**不是把 16×16 放大。**

**对太阁2 的战略价值（本项目特有）**：
- ✅ **合战可做真 3D 战场** —— `HJMAPDAT` 的 **19 行 × 40 列**地形网格就是现成 3D 数据源（实测确认：rows=19 cols=40）
- ✅ **绕开「武将头像拿不到原图」死结** —— HD-2D 立绘本就是重画的，不依赖 FACE.LZW（该文件无解码器）
- ✅ 1080p/2K/4K 全段清晰 —— 3D 分辨率无关，sprite 按 256×256 规格

### 产出 1：`docs/replication/HD2D高清重制方案.md`（★ 画面方案）
章节：HD-2D 是什么（4 支柱/纠 4 个误解）→ 素材现状（可复用 vs 需重画）→ **Godot 渲染架构** → 素材规格（立绘 512×640/单位 256×256/地形 16 种）→ 多分辨率 → 分场景优先级 → HD-0..HD-6 里程碑 → 风险对策。
- 合战 **P0 最先做**：地形网格 + 合战公式都已 100% 破解，只差渲染层。
- sprite 规格定 **256×256**（甜点：1080p 缩小 0.7x 锐利，4K 放大仅 1.4x 且保留像素感＝HD-2D 美学）。

### 产出 2：Godot 渲染骨架（6 个 .gd，**全部通过 `--headless --check-only` 语法校验**）
| 文件 | 作用 |
|---|---|
| `src/core/DisplayAdapter.gd` | autoload：检测屏幕自动选 viewport（≥2160→4K / ≥1440→2K / else 1080p） |
| `src/core/Consts.gd` | 全局常量（哨兵值/钳制/规模/攻击除数表/名称表） |
| `src/core/Officer.gd` | 武将实体（语义映射 47B，`load_from_dict()` 从 JSON 填充） |
| `src/render/Hd2DEnvironment.gd` | HD-2D 后处理：Glow 0.55 + DOF + SSAO + ACES + 饱和度 1.15；含**天气预设**（晴/雨/雪/雾/夜）+ 4K 降级 |
| `src/render/UnitSprite.gd` | Billboard 像素 sprite：`TEXTURE_FILTER_NEAREST`（★ HD-2D 灵魂）+ 阵营色调 + 帧动画 |
| `src/battle/Terrain3DBuilder.gd` | **HJMAPDAT 19×40 → 3D Mesh**（顶面 + 侧壁 + 顶点色 + 11 种地形高度/配色） |

### 🔴 修复：`scripts/real_assets.py` 的 `DATA_ROOT` 硬编码错误路径
旧值 `"F:/Games/Taikou2"` **目录不存在** → 所有 `load_lzw()`/`decode_*()` 抛 `FileNotFoundError`（4 个解码器全废）。
改为按 `__file__` 推导：`os.path.dirname(_SCRIPT_DIR)/"Taikou2 Original"`。
影响面检查：`DATA_ROOT` 仅本文件内部使用，**无外部引用，修复无风险**。
✅ 修复后实测：4 个图块解码器全部可用（返回 PIL Image）：
| 解码器 | 输出 | 张数 |
|---|---|---|
| `decode_mapchip` | 256×88 | 88 张 16×16 地形 |
| `decode_townchip` | 272×336 | 357 张 16×16 城镇瓦片 |
| `decode_hbchar` | 256×384 | 384 张 16×16 合战精灵 |
| `decode_townchar` | 272×208 | 221 张 16×16 |
（样本已存 `scripts/_hd_probe/`，**该目录应加 gitignore 或删**）

### ⚠️ 素材真相（HD-2D 路线下的重新定性）
原版 16×16 tile **不直接超分使用** —— HD-2D 要"高清像素 art"而非"放大的模糊像素"，原图仅作**造型参考**。
- 需重画：武将立绘 **695 张**（512×640，ImageGen）、单位 sprite 16 张（256×256）、地形材质 16 种、背景板 13 个
- 拿不到原图：武将头像（FACE 无解码器）、合战地图 HJMAP（`ls11_decompress` 不适用，解压 1MB→1.6KB 异常）、店铺 SHOPCHAR/SHOP_BG

### 待用户确认（本轮新增）
3. **是否生成 HD-2D 样本图？**（ImageGen **约 5–10 积分/张**；建议 2 张：织田信长立绘 + HD-2D 合战场景）—— 需先确认额度
4. 立绘风格基线：和风写实 / HD-2D 像素 / 国风水墨 / 现代动漫？
5. 是否继续 HD-1（跑通 3D 地形，用简化材质先出画面）？

### ✅ 已生成 HD-2D 样本（用户确认，消耗额度）
风格基线 = **和风写实 HD-2D**（用户选定）。存 `assets/gfx/samples/`：
| 文件 | 尺寸 | 内容 |
|---|---|---|
| `sample_01_nobunaga_portrait.png` | 1024×1536（透明背景）| 织田信长半身立绘 |
| `sample_02_battle_scene.png` | 1536×1024 | HD-2D 合战场景（3D 地形 + 像素士兵 + 体积光）|
- 立绘提示词已验证有效（黑漆甲+金饰+三日月前立兜+暖琥珀色调+rim light），**可直接作为 695 张批量模板**
- ⚠️ `assets/gfx/samples/` 含原版派生素材，**建议 gitignore**（与 `assets/` 同策略）

### 本轮新增工程事实
- **Godot 语法校验命令**（很有用，记住）：
  `Godot_v4.7.1/Godot_v4.7.1-stable_win64_console.exe --headless --check-only --script <file.gd>`
  6 个 .gd 全部 OK。
- 🔴 **GDScript 两个坑（实测）**：
  1. 单文件检查模式下 `class_name` 未全局注册 → 跨文件常量引用改用 `const X = preload("res://...")`（Officer.gd 已改 `ConstsRef`）
  2. **`static func` 内不能引用自身 `class_name`**（报 `Identifier not found`）→ 改实例方法（`from_dict` → `load_from_dict`）
- `project.godot` 已改：1080p 默认 + `canvas_items` + `expand` + `fractional` + LINEAR/MIPMAPS + **autoload `DisplayAdapter`**

## 🐛 修复：`gen_portraits.py` 地理国名错乱（续上「请继续」）

**现象**：`scripts/gen_portraits.py` 初版用 `officer['province']`（BSDATA 0x30）直接当地理国名 → 织田信长显示「骏河」（错误）。

**根因（★ 实测推翻旧假设）**：`0x30` 是**氏族/出身国码**，**不是地理居城国**。横向对比：
| 武将 | 0x30 | 经城表推导的真实国 |
|---|---|---|
| 织田信长 | 13=骏河 ✗ | 尾张 ✓ |
| 上杉谦信 | 5=下野 ✗ | 越后 ✓ |
| 北条氏康 | 6=常陆 ✗ | 相模伊豆 ✓ |
| 武田信玄 | 10=甲斐 | 甲斐（巧合一致）|
| 本愿寺显如 | 16=尾张 ✗ | 摄津 ✓（石山本愿寺）|

**权威路径**：`officer.city → castle_table[city].province → 国名表[province].name`。
- 验证：城66清洲→province=16→尾张；城50春日山→12→越后；城40踯躅崎→10→甲斐；城35小田原→6→相模伊豆。

**改动**：`gen_portraits.py` 删 `pname(pid)`，改 `province_of_officer(o)` 读 `data/castles.json` 走城表推导；浪人(city=255/None)返回 ''。重跑 `--priority --limit 6` 全 6 名武将地理国正确，单测 #423 本愿寺显如(无居城城表命中摄津)不崩。

**⚠️ 需回写 MEMORY.md 数据契约**：原记「`0x30 国(255=无)`」应改为「`0x30`=氏族/出身国码（非地理居城国；地理国须 `city→castle.province` 推导）」。属推翻旧假设，但属字段语义细化、非结构突破，未单独立 BREAKTHROUGHS 条（城表.province 本就是文档记录的地理权威源）。

### 文档一致性收口结论（回答用户「复刻的文档都详细弄好了吗」）
- **是**。`docs/replication/` 三件套一致且 HD-2D 对齐：`Godot复刻实施方案.md`(施工图+HD-0..HD-6)、`HD2D高清重制方案.md`(画面方案)、`复刻导航.md`(入口)。
- 上一轮已消除「图像豁免/裸图」旧表述：删重复 `高清重制方案.md`、改 `Godot复刻实施方案.md` §2.2/§6.1/§6.4/§7+HD里程碑、`复刻导航.md` §6、`docs/INDEX.md`、根 `README.md` 的 `_decoded_grp` 计数 31→13。
- `gen_portraits.py` 是 HD-4 执行器（695 立绘提示词），bug 已修，待用户批额度后批量生成。

## 🗂 Godot 工程目录重构（22:1x，用户：「游戏目录要非常专业、清晰、有逻辑」）

**决策**：完整重构 + 保留 `scripts/`(Python RE，180 自测依赖扁平布局不可移)。

### 改动清单
- **字体** `fonts/` → `assets/fonts/`（git mv 受跟踪的 NotoSansSC + .import；mv 被忽略的 msyh/simhei）。
- **assets 子目录**：`audio/{sfx,bgm}` `textures` `sprites/{portraits,units,chips}` `environments` `samples`（各加 .gitkeep）；样本图 `assets/gfx/samples/` → `assets/samples/`。
- **新增 `src/core/game_data.gd`**（autoload 单例）：`_ready` 载入 data/*.json，提供 `get_officer/get_castle`；⚠️ 不可声明 `class_name GameData`（与 autoload 名冲突，实测报 "Class hides an autoload singleton"）。
- **新增 `scenes/`**：`main.tscn`(标题入口) + `screens/world_screen.tscn`(Control) + `screens/battle_screen.tscn`(Node3D+Camera3D+DirectionalLight3D)。工程现可启动（此前 scenes/ 空、无入口）。
- **`project.godot`**：`run/main_scene=res://scenes/main.tscn`；autoload 加 `GameData=res://src/core/game_data.gd`。
- **`.gitignore`**：加 `Godot/`(编辑器 app_userdata 日志) + `NVIDIA Corporation/`(驱动日志，运行时垃圾)；`fonts/→assets/fonts/`；`assets/gfx/→assets/samples/`。
- **`README.md`**：目录约定表重写（src/scenes/assets/scripts/tools 分离）+ 新增「Godot 工程结构（速览）」树 + 运行方式。

### 验证
- 7 个 .gd 全过 `--headless --check-only`（game_data.gd 初版因 `class_name` 冲突报错，去掉后 OK）。
- `git check-ignore` 确认 `Godot/` `NVIDIA Corporation/` `assets/samples/` 已忽略；`git status` 仅预期改动。

### 新约定（写回 MEMORY.md「Godot 复刻约定」）
四目录职责分离：src(游戏代码)/scenes(场景)/assets(资源)/scripts(RE工具,禁移)。autoload=DisplayAdapter+GameData。

## 📋 进度表（22:2x，用户：「做完一件就打勾，一步一步来」）

新增 `docs/replication/进度表.md`：勾选式清单，阶段 0–7，对齐 M0–M7 / HD-0–HD-6。
- 阶段 0 工程基建全 `[x]`（目录重构 + autoload + 入口场景 + GameData）。
- 阶段 1 M0 `[x]`、国名 bug 修复 `[x]`；M1 DataLoader 索引 / 模型补全 / 外字 `[ ]`。
- 阶段 2–7 多为 `[ ]`，HD-1/2/3 标注「代码已就绪待接入」、HD-4 标注「样本已确认待批量(需额度)」。
- 已登记进 `docs/INDEX.md` 复刻文档清单。

## ✅ M1 DataLoader 内存索引（续上「按照你的建议继续 复刻」）

**目标**：按 `Godot复刻实施方案.md` 的 M1，建运行时数据层 `src/data/data_loader.gd`（`RefCounted`，不引用 Node），把 `data/*.json` 载入并建 O(1) 内存索引，供各玩法系统按 id 查。

### 改动
- 🆕 `src/data/data_loader.gd`：`extends RefCounted`，`load_all()` 载入 9 个 json，建 `officers/castles/provinces/battles`(Dict by int id) + `skill_names/rank_names/force_names/province_names/castle_town_names/role_type_names/extra_place_names/gaiji_single/texts/consts`。查询 API：`get_officer/get_castle/get_province/get_battle/get_*_name/get_text/get_gaiji/get_const`。
- 重写 `src/core/game_data.gd`（autoload）：`_ready` 实例化 `DataLoaderRef.new()` 调 `.load_all()`，转发查询（保持无 `class_name` 避免与 autoload 名冲突）。
- 两文件均过 `--headless --check-only`。

### 🔴 关键坑（实测）：Godot `JSON.parse_string` 全数解析成 float
初跑集成测试计数正确（700/200/49/38/6211/10）但 `get_officer(13)` 返回空。调试见 `officers.keys()=[0.0,1.0,...]`、`rec["id"]=0.0`，`has(13)=false, has(13.0)=true`。根因：Godot JSON 把所有数字解析为 float，id 索引键变成 13.0，int 查询查不到。
**修复**：`_index_by_id` 用 `d[int(rec["id"])] = rec` 归一化键。重跑确认 `O13=织田信长 五维={lead:96,martial:85,domestic:92,diplomacy:99,charm:90} rank=大名`、`C66=清洲 地理国=尾张`、`P16=尾张`、`B0` 19×40 地形网格载入。临时测试文件 `tools/_test_dataloader.gd`、`tools/_dbg_keys.gd` 已删。

### 状态
**M1 完成**，进度表 `docs/replication/进度表.md` 已打勾。下一步按表推进 M2（`BattleSim` 纯逻辑移植）+ 接入 HD-1/HD-2（`Terrain3DBuilder`/`UnitSprite` 挂 `battle_screen.tscn`）。

## ✅ M2 合战沙盘 + HD-1/HD-2 + 占位图（22:4x 起，用户：「花积分的暂不做，弄占位图，继续复刻」）

**指令处理**：HD-4 立绘（烧 ImageGen 积分）暂缓；改用**程序生成占位图**（零积分）顶上，不阻塞复刻。

### 1. 占位图（零积分）
- 🆕 `scripts/gen_placeholders.py`：纯标准库（zlib+struct）写 RGBA PNG，**不依赖 PIL / 不调 ImageGen**。生成 `assets/sprites/{portraits,units,chips}/placeholder_*.png`（512×640 / 256×256 / 32×32，透明底 + 中性剪影）。
- 🆕 `src/render/asset_loader.gd`（`class_name AssetLoader`）：`load_unit_texture/load_portrait/load_chip(id)` → 真实图 `units/{id}.png` 缺失则回退占位图。**坑**：运行时 `load("res://x.png")` 需 `.import` 元数据，headless/未导入 PNG 改 `Image.load()+ImageTexture.create_from_image()`（`_load_tex` 已封装，先查 `.import` 再回退）。→ HD-4 不再阻塞任何渲染。

### 2. M2 `BattleSim` 纯逻辑移植（无 UI）
- 🆕 `src/battle/battle_sim.gd`（`class_name BattleSim extends RefCounted`）：把 `BATTLE_SPEC.md §9` + `scripts/battle_formula_ref.py` 的合战引擎**逐位移植**（troop_scale / 攻击除数表 / 战力汇总 / side0×80%(同类再×80%) / 一回合结算）。含 `BattleUnit`/`BattleCommander`/`BattleCtx` 内部类 + `simulate()` + `build_ctx_from_battle(battle_dict, seed)`（从 `battles.json` 的 deploy 图 + section A 构造确定性合成 ctx）。
- ⚠️ `battle_formula_ref.py` 场景 a 实测：side0=[472×4] / side1=[364×4] → GDScript 端 `scenario_a()` 断言**逐位一致**，证明移植无误。
- 真实「武将→战单位」映射未导出（属 M3+ 玩法层）；`build_ctx_from_battle` 用**确定性合成单位**（每方按 15 槽采样 cap），仅验证引擎收敛与不变量，数值非原版真实 roster（文档已注明）。

### 3. 无头压测（tools/_test_battlesim.gd）
- 38 战 × 100 场 = **3800/3800 收敛**（无死循环）、**0 负兵力单位**、**side0 总胜率 11.6%**（明显 <50%，验证 ×80% 不对称写死保留）。跑通约 21s。

### 4. HD-1/HD-2 接入 battle_screen.tscn
- `Terrain3DBuilder.cell_height()` 新增公开静态（复用高度表）。
- 🆕 `src/battle/battle_screen.gd`：挂到 `scenes/screens/battle_screen.tscn`（Node3D+Camera3D+DirectionalLight3D）。`_ready` 用 `Terrain3DBuilder.build_from_battle()` 建 3D 地形（HD-1），再按 deploy 图左右军在每格用 `UnitSprite.make(占位图, NEAREST billboard)` 摆单位（HD-2，占位图回退）。
- 修 `UnitSprite.gd`：`alpha_cut_mode` 在本版 Godot 非合法 setter → 删除（默认 alpha_cut 关闭即可）。
- 🆕 `tools/_test_battle_scene.gd`（extends SceneTree）实例化场景验证：地形 Mesh + **16 单位 sprite** 成功构建，无致命错误 → 首帧 HD-2D 合战可渲染。

### 状态
**M2 + HD-1 + HD-2 完成**，进度表已打勾（HD-4 标「占位图就位」）。待用户批 ImageGen 额度后批量替换立绘（按同名文件）。下一步按表推进 **M3 最小可玩**（选主角→看状态→执行主命推进 1 月）。

---

## ✅ M3 最小可玩（GameState + UI 全链路）

### 1. 逻辑层 `src/core/game_state.gd`（autoload `GameState`，无 class_name）
- `start_new_game(pid)`（校验 `is_selectable`）/ `get_status()`（合并覆盖层：五维/技能/職位/忠诚/体力/年月）/ `train_skill(idx)`（cap3、耗体 20、推月）/ `rest()`（回满、推月）/ `advance_month()`（月回 15、12月→次年1月）。
- **覆盖层设计**：officers.json 只读，运行期改动存 `_stamina/_skill/_loyalty/_merit_override` 字典，重开局清空即复位。
- 🔴 **必须 duplicate**：`_effective_skills()` 返回前 `.duplicate()`，否则原地改会污染 GameData 缓存的原表数组（缓存污染测试专门验证此点）。
- 均衡观察：耗20/月回15（净-5）且信长修行容量仅14次 → **修行本身耗不尽体力**，体力约束待 M4 引入其他主命后自然生效；low_stamina 守卫已用覆盖层注入确定性验证。

### 2. UI 三面（`src/ui/`，程序化构建）
- `title_screen.gd`（挂 main.tscn，「开始新游戏」钮）→ `protagonist_select.gd/.tscn`（按 id 排序列 6 名可选主角）→ `status_screen.gd/.tscn`（五维/技能/職位/忠诚/体力/年月 + 修行×10 钮 + 休养）。

### 3. 无头校验（两条测试全过）
- `tools/_test_m3.gd` **23 项**：开局校验/状态读取/修行封顶+耗体+推月/跨年滚动/重开局复位/**缓存不被污染**。
- `tools/_test_m3_ui.gd` **13 项全链路**：标题→主角选择（6 名、排序）→点选#13→状态→修行→休养，均经真实按钮 emit 驱动。
- ⚠️ `--script` 模式 `_initialize` 时 autoload **尚未进树** → 挂 `process_frame`（CONNECT_ONE_SHOT）再跑断言；场景切换用 `current_scene = 实例` 设好后 emit pressed 驱动 `change_scene_to_file`。

### 状态
**M3 完成**（进度表两行已打勾，Task #14 closed）。下一步按表：**HD-3**（Hd2DEnvironment 光照/天气接 battle_screen）或直接 **M4 内政与修行**（12 主命 + 修行 8 动作 + 功勲公式）。git 未提交（等用户确认）。
