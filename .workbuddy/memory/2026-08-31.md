# 2026-08-31 工作日志

- **S6（0x516610, 46B）玩家/事件上下文结构体：结构全破 + 核心字段语义已定（续137）**。
  - 方法：绝对地址 xref（`scripts/_s6_full_xref.py`）+ 方法表抽取（`scripts/_s6_vtable.py`），纯静态无需 emu。
  - 字段图：+0x14 玩家武将索引(getter 0x49f5d0→0x519868+idx*47)、+0x1e 所持金(48处,cmp 10/50000)、+0x16/18/1a 三项60000资源総計(setter 钳0xea60)、+0x22/+0x24 关联武将索引#1/#2(+0x2a 关联S5槽0x5197b0)、+0x26 4×4bit(各0..10)、+0x28 瞬态状态位域(140处,bit1/2/4)、+0x29/2a/2c 状态字节、+0x0e/0f/12/13 标志字节。
  - 踩坑：pkl 键是文件偏移(0x1000=VA 0x401000)非VA；共享方法库陷阱(0x49b960区非S6专有)。
  - 产物：`scripts/s6_struct_ref.py`(28/28)、`scripts/s6_struct.json`、`scripts/_s6_full_xref.txt`、`scripts/_s6_vtable.txt`。
  - 文档：`GAME_DATA_SPEC.md §3.20`(新增) + §5.2 S6 行改「已破」；`BREAKTHROUGHS.md` 续137(置顶)。
  - 仍未知：三项60000字段具体含义、+0x26/+0x20语义、+0x28各位域、+0x29/2a/2c逐bit。
- 清理超限 MEMORY.md（18.8KB→2.5KB），保留方法论+当前状态+边界。
- 待破清单现状：S6 已破；S5/S7 运行时全空静态难推进；事件id2(续136)已闭。

- **续138（纠错续137）：S6 三项60000「資源総計」假设被推翻**。
  - 全镜像扫描 `0x49b970/0x49b990/0x49b9b0`（钳60000 setter）的全部 call 站点（各2个）+ 寄存器感知 `ecx` 溯源（`_s6_ecx_trace.py`/`_s6_global_write.py`）：`0x487a00` 初始化循环里 `ecx=0x5197b0`（**S5 六人名单**），`0x4cf280` 是 stride-14 通用初始化器。**全镜像 0 处** `mov ecx,0x516610` 后调用这三 setter。
  - 绝对 xref：`+0x16/18/1a` 各仅 2 处且全是**读取**（存档 push `0x47e770` + 读 `0x47e8a0`），无写入。
  - 结论：续137 把「方法表 S6 偏移处有钳60000 setter」误判成「S6 有60000资源字段」，是共享方法库陷阱的二次踩坑。S6 `+0x16/18/1a` = **保留/未用**；真正的60000字段属 **S5**（`+0x16/18/1a` 钳60000、`+0x1c` XOR位域、`+0x14` idx，由 `0x487a00` 经 `ecx=0x5197b0` 写入）。
  - 产物：`scripts/s6_struct_ref.py`(28→**29/29**，新增「S6 +0x16/18/1a 无 ecx=0x516610 写入路径」断言)、`s6_struct.json`、`_s6_dump_60000.py`/`_s6_trace_60000.py`/`_s6_trace_60000b.py`/`_s6_ecx_trace.py`/`_s6_global_write.py`/`_cf280_callers.py`。
  - 文档：`GAME_DATA_SPEC.md §3.20.1/2/3` 三处改写（原「資源総計#1/2/3」标续138纠错）+ §5.2 S5 行补「S5 才是60000字段归属」；`BREAKTHROUGHS.md` 续138(置顶)。
  - 下一步：① 追 S5 `+0x16/18/1a` 写入链或 Unicorn 定名（石高/兵数/金？）；② 解 S6 `+0x28` 位域(bit1/2/4)；③ 转 S7 或经济基价 emu。

- **续139：共享方法库完整映射 + 全局复位例程破出 + 对续138 过度断言再纠错**。
  - 完整方法表 `0x49b960..0x49bda8`（**35 个方法**）全部抽取（`scripts/_s5_methods.py`→`_s5_methods.txt`）：+0x14(W)/+0x16,18,1a(W 钳0xea60)/+0x1c(XOR 低3位)/+0x1e,20,22,24(W)/+0x26 四nibble(getter+setter 各钳0xa)/+0x28(8个逐bit+bit10-12)/+0x29/+0x2a(3-bit+2-bit+bit5)/+0x2c。
  - **S6 与 S5 是同一「状态记录类」实例**（S6=46B 单体，S5=6×30B 数组），共用全部方法。
  - 🔴 **续138 过度断言被推翻**：续138 称「S6 +0x16/18/1a 静态无写入」——错。复位例程 `0x4cf240` 执行 `push 0x516610; call 0x4cf280`，而 `0x4cf280` 内 `mov esi,[esp+0xc]`（**取实参**）→`ecx=esi`→`call 0x49b970/990/9b0`，故 S6 三项 60000 字段**确实被复位清零**。续138 只扫字面 `mov ecx,0x516610`，漏掉「实参传递」间接。
  - 复位链：`0x4cf0d0`(上层，无静态调用方) → `0x4cf240`(复位 S6 + 循环 6 条 S5，每条先 `0x49b960(0xffff)` 置空 `+0x14`) → `0x4cf280`(清零 +0x16/18/1a、+0x1c 低3位、`[0]=0xffff`、word +0x2..+0xc=0、+0x0e 起 5 字节子结构)。
  - **方法论教训（已入 MEMORY.md）**：判「字段是否被写入」须沿 call 下探一层识别「helper 取实参作 this」模式，只扫字面 ecx 会漏。
  - 产物：`scripts/s6_struct_ref.py`(29→**32/32**)、`_s5_methods.py`/`_s5_methods.txt`、`_s5_field_scan.py`。
  - 文档：`GAME_DATA_SPEC.md §3.20.1/2/3` 再改写 + **新增 §3.20.4 完整方法表**、**§3.20.5 复位例程链与警示**；`BREAKTHROUGHS.md` 续139(置顶)。
  - 仍未知：三项60000具体含义（**静态到极限，须 Unicorn**）、+0x26/+0x20 语义、+0x28 各 bit、+0x29/2a/2c 逐 bit、`0x4cf0d0` 触发来源。

- **续140：文档矛盾清理 4 项 + 升進线突破（饱和加/减原语对、实体字段增量族、S6 +0x20 锚定、+0x2a=主君索引）**。
  - **文档矛盾清理 4 项**（其中 2 项文档内早已破解却未同步）：§3.8.5 地形攻防→续7/§3.12 已闭合；§3.10.7 阵形中文名→§3.11.3 已结论不存在；§3.11.6 `0x50bfd0`→§3.11 已破=兵种类别映射表；§3.16.12 `0x50bfb8`→§3.11.5 已破=金额阶梯表。
  - **饱和加/减原语对**：`0x4ebca0`=`sat_add(a,b,cap)=min(a+b,cap)`（56 调用）↔ `0x4ebcd0`=`sat_sub(a,b)=(a>b)?a-b:0`（153 调用）。原记「0x4ebca0 勲功增量」是误当专属函数。
  - **实体字段增量族 18 个包装器**（`_entity_inc_family.py`→`entity_inc_family.json`）：`byte[f]=sat_add(byte[f],delta,cap)`。`+0x26`cap60000=功勲、`+0x29`cap100=**忠诚增量**（原待破项闭合，且独立印证 @53→+0x29 钳100）、`+0x21`=体力回复(cap=体力上限+0x20)、`+0x23`cap100=野心。⚠️ 同族服务不同 struct（`+0x0e` 有 cap60000/200 两版）。
  - 🆕 **S6 `+0x20` = 俸禄/知行结算净额累加器**（cap60000）：`0x4a6970` 取玩家 `byte[+0x28]`，遍历 370 实体(stride 0x2f=47, base 0x519894=0x519868+0x2c)筛 `word[+0x2a]==玩家武将号` 的家臣，饱和减扣其 `byte[+0x28]`，净额经 `0x4a35e0` 累加进 S6+0x20。原「+0x20 语义 TBD」闭合。
  - 🆕 **`word[entity+0x2a]` = 主君（所属）武将索引**（0xffff=浪人），由该循环坐实；原记「word 状态字」过时。
  - 文档：`GAME_DATA_SPEC.md §3.16.12`(改) + **§3.16.14**(新增：原语对+增量族+S6+0x20 锚定++0x2a 升级) + §3.8.5/§3.10.7/§3.11.6/§3.20.3(改)；`BREAKTHROUGHS.md` 续140(置顶)。
  - 下一步：① `0x49ffc0`/`0x4a3260` 调用方定忠诚增量 delta 来源；② 大名(7)/城主(8) 任命·继承路径；③ 用「+0x2a=主君索引」重扫家臣逻辑。

- **续141：`0x49ffc0` 相性関係スコア破出 + 第5处文档矛盾（大名/城主早已破）**。
  - `0x49ffc0(target, lord)` **不是**忠诚增量 delta（与 `0x4a3260` 调用方无交集），而是**相性関係スコア**：`a=(word[t+8]>>11)&0xf`、`b=(word[l+8]>>11)&0xf`、`d=|a-b|&7`，跳表 `@0x4a0028` → **T=[4,2,3,1,4,2,0]**，d>6→2。返回 0..4。
  - ⚠️ **T 非单调**（d=4 反而回最大值 4）⇒ 手工调参表，复刻须照抄常量，勿用公式拟合。
  - 字段定位（−12 通则 + 数据侧互证）：entity `+0x08` = BSDATA `@20`(=`compat_a`)，`word[+8]`=`compat_a|compat_b<<8`，bits11-14 = `(compat_b>>3)&0xf`（`compat_b`=BSDATA `@21`「NPC 关系初值 B」）。setter `0x49a5a0`(ecx=base+8) `&0x87ff | val<<11`，`cmp ax,0xc` ⇒ 值域 **0..11**。
  - 消费：`0x443810` 武将池筛选，返回值阈值 **2** 配忠诚 40/95（bp==1: 忠诚<40 或 affinity<2；bp!=1: 忠诚>=95 或 affinity>2）。19+ 处调用方。
  - **第5处矛盾**：§3.16.11 早已 ✅ 大名/城主 任命·继承路径（续65，`promo3_ref.py` 42/42），§3.16.12 待破仍挂着 ⇒ 已同步。另发现 §3.16.11 编号重复（2025/2082 两处同号）。
  - 文档：`GAME_DATA_SPEC.md §3.16.12`(改) + **§3.16.14**(新增相性スコア) + §3.16.15(改，补脚本清单)；章节编号已修正为 13/14/15 顺序。`BREAKTHROUGHS.md` 续141(置顶)。
  - 下一步：① 解 `word[entity+8]` 剩余子字段（`compat_a` 8-bit、`compat_b` 低3-bit）；② 用「+0x2a=主君索引」重扫家臣逻辑；③ 增量族剩余 struct 归属。

- **续142：`word[entity+0x08]` 位域全解（bit15=茶会フラグ）+ 再踩多struct陷阱 + 修正 0x49a7e0 位段**。
  - 布局：`word[+0x08]` = BSDATA `@20`(compat_a) | `@21`(compat_b)<<8。bits11-14=(compat_b>>3)&0xf=相性(续141)。
  - 🆕 **bit15 = 茶会関連フラグ**：`0x443fe0` 链 = `0x49f5e0`(玩家) → `0x441cc0`(武将選択→esi=被招待者) → MSGX `0xc9e`/`0xc9f`「真希望举行茶会的日子赶快到来」/`0xca0`「那么，主持茶会之事就拜托你了！」/`0xca1`「明白了。很高兴接受您的邀请。」→ `and word[esi+8],0x7fff` 清 bit15。
  - **只有清除无从置位**：全镜像 `word[reg+8]`×`0x8000/0x7fff` 仅 2 处且都是 `and…,0x7fff` ⇒ bit15 由 BSDATA 初始化。
  - 🔴 **第4次多struct陷阱**：第二处 `0x44d7d0` 的 `word[esi+8]` 属 **S12 物品副池**（`sub ecx,0x517728` + 魔数 `0x2aaaaaab`+`sar 1` = **÷12**，idx 8..11），非武将实体。
  - bits0-7 = `compat_a`（已导出 bsdata.json `compat`，0..255/139 unique）；**bits8-10 仍未破**（无访问器）。
  - 🔧 修正：`0x49a7e0` 实为 **`word[+0x2c]` bits 8-10**（`and eax,0xf8ff`），旧记「bits11-13」位段有误。
  - 文档：`GAME_DATA_SPEC.md` **§3.16.15**(新增位域全解+茶会证据链+陷阱警示)；`BREAKTHROUGHS.md` 续142(置顶)。
  - 下一步：① 用「+0x2a=主君索引」重扫家臣逻辑；② 追 `compat_a`(bits0-7) 消费点；③ 增量族剩余 struct 归属。

## 续143 · `word[entity+0x2a]` 主君索引 —— 家臣逻辑全闭（承接续142 下一步 A）
- **字段落地**：`word[entity+0x2a]` = **主君（所属）武将索引**，哨兵 `0xFFFF` = 浪人。旧记「得力助手实体索引」作废（该名源自情报[10] MSG 的误读，已同步改读）。
- **唯一 setter `0x49a7d0(ptr,val)`**：`mov ax,[esp+4]; mov word[ecx+0x2a],ax; ret 4` —— **整字覆盖、无掩码**。
- **18 调用点 = 14 浪人化(`push 0xffff`) + 4 新主君**：
  - 浪人化：`0x40ff2c`/`0x41004b`(浪人生成 A/B，忠诚 100/80)、`0x440e19`(玩家改易→自身浪人，门控 `cmp word[esp+4],0x167`)、**`0x46a65b`/`0x4a393f`(主家灭亡→家臣全员浪人化)**、`0x4a5033`/`0x4a580e`(出奔·転属)、`0x4a3eab`/`0x4c3322`/`0x4cb9e0`(相性判定后离反)、`0x4e8089`/`0x4e8201`/`0x4e9855`/`0x4e9950`。
  - 新主君：`0x40c099`/`0x40ccef`、**`0x452b21`(登用)**、`0x4a5571`(継承·転封)。
- **登用 `0x452ab0` 由 MSGX 坐实**：`0x40c`「早就想在%s手下工作。」/`0x40d`「哦，太好了！那么赶快进城吧。」/`0x40e`「是！今后定为您效犬马之劳。」→ 复制 `byte[+0x24]` 親密度 → `0x49f5d0` 玩家号 → `0x49a7d0(esi, 玩家)` → `0x49a880(esi,1)`。
- 🔴 **第 5 次共享方法库陷阱**：`0x49ba30/60/70/aa0` 的 `+0x2a` 是**位域**（`&0xffe3`+`<<2` / `&0xfffc`+`min(v,3)`），**28 处调用 100% `ecx=0x516610`(S6)** ⇒ 属 S6，与实体 `+0x2a` 同名不同物。
- 🔴 **第 6 次多 struct 陷阱（方法论）**：100 处位移命中 → 14 处疑似写点 → 逐一溯源后 **10 处全误报**（S5 `0x5197bc`/`0x5197ba`、表 `0x510b54`/`0x525356`、栈局部、`add esi,2` 轮转、`add edx,4` 排序交换）。**结论：实体 `+0x2a` 唯一写入路径 = setter 0x49a7d0。**
- **表几何**：`0x51eb88` 城表 31B×200（`+0xa`=城主武将索引；stride31 三重证据）、`0x51e1f6` 物品池 10B×200（`+0x0`=主君式索引，主家灭亡时置 0xffff）；🆕 `0x5179b8` stride14、`0x521aa8`/`0x520660` stride7、`0x525356` stride12×20、`0x510b54`。⚠️ 前两张本体已见 §3.14.4，本轮只补语义。
- 🆕 **两档上界 370(0x172) vs 359(0x167)**，含义待考。
- 产物：`scripts/lord_index_ref.py`(**97/97**)、`_lord_idx_probe.py`、`_lord_idx_callers.py`；文档 `GAME_DATA_SPEC.md` §3.14.2 字段行改写 + **§3.14.8**(新增) + §3.14.4 情报[10] 改读；`BREAKTHROUGHS.md` 续143(置顶)。
- 下一步：① 定名 `0x51eb88`(31B×200) 字段布局（与 `0x4a5680` 転属直连）；② 追 `compat_a` 消费点（续142 遗留 B）；③ 清 370 vs 359。

## 续144 · 城表 0x51eb88 流映射 + 三项纠偏（承接续143 下一步①）
- ⚠️ **自我定位**：流映射与 `+0x0a` 证伪**均已在 §3.17.7**（续97 已证伪续70 的「`+0x0a`=城主」），本轮属**重复劳动 + 重犯旧错**。教训已入 MEMORY：**提出字段语义前先 grep 权威 spec**。
- **① 流映射独立复现**（loader `0x47e130`，`esi=0x51eb8c`=rec+4 ⇒ `[esi+k]` 偏移=k+4）：17 次读调用，序列 `W B B B W B B B B W W W W W B W W`=26B，与 §3.17.6/§3.17.7 完全吻合（独立互证）。城主=`+0x00`(W[0:2]→×47)、本城=`+0x04`(B[2]→×31)、**所属国=`+0x1d`(W[24:26])**。
- **② 🔴 `+0x0a` 非武将索引**（推翻续143 ⑥）：两剧本 400 条 **399 条 ≥370**，值域 0..64592、114 distinct。`0x46a4a0` 虽按武将索引读它，但 `jae` 恒真 ⇒ **死分支**。⇒ 「**代码形态 ≠ 运行期使用**」，定语义前先查数据分布。
- **③ 🔴 第 7 处文档矛盾**：§3.17.6 续99 #3「所属国=`+0x08`(stream[3])」**错误**（该字段 0..250/29dist 装不下 49 国），已批注撤销；真值 `+0x1d`(stream[24]，0..48/49dist)。§3.17.2 与 §3.17.7 正确。
- **④ 🆕 `byte[entity+0x24]` = 国索引 0..48，哨兵 255**（原「親密度」作废）：BSDATA `@48` 663/700<49、≥49 的 37 个**全为 255**、<49 覆盖 45/49；`0x46a4a0` 以 `cmp al,0x31` 为界 ×14 查 49 国表。`bsdata.json` 的 `intimacy` 应改名 `province`。
- **⑤ 🆕 `byte[entity+0x25]` = 在城索引 0..199，哨兵 255**（278/700=255 浪人，422<200）。
- **⑥ 🆕 `0x46a4a0` 语义**：按 `+0x0a` 反查城 → 该武将的国 `+0x24` → 49 国表 `word[+0x04]`=国主武将 → 其 `+0x25` 在城 → `0x49a990(城记录, 武将号)`；随后沿 `dword[城+0]`→`+4` 链表拷入 `0x51e9c0`。实质不触发。
- **⑦ 🆕 第 8 处文档矛盾**：49 国表 `0x5179b8` **`+0x04` 是 WORD = 国主武将号**（推翻 §3.18.2 的「`+0x04` flag + `+0x05` 国主号」拆分）。LOAD `0x47e440` 用 WORD reader 写 `[esi]`；`lea [ecx*2+0x5179b8]` 88 处中 14 处读 `word[reg+4]`、**11 处**带 `0x172`(370) 上界并转实体指针。
- 产物：`scripts/castle_stream_map_ref.py`(**55/55**)、`_castle_tbl_probe.py`；文档 `GAME_DATA_SPEC.md` §3.14.2(+0x24 改判)、§3.14.8、§3.17.2(+0x08/+0x0a)、§3.17.6(续99 #3 撤销批注)、**§3.17.8**(新增)、§3.18.2(+0x04/+0x05 改)；`BREAKTHROUGHS.md` 续144(置顶)。
- 下一步：① 用石高/規模候选反查 `+0x08`(0..250,29dist) 与 `+0x0a`(0..64592,114dist)；② 定 `0x49a990` 写入的城记录字段；③ 追 `compat_a` 消费点（续142 遗留 B）。

## 续145 · 城表 +0x08/+0x0a 定名尝试（承接续144 下一步 A）
- ⚠️ 结果：**石高/規模/武将号三假设全部证伪**，但拿到 3 项硬结论 + 1 处留档矛盾。
- **① 🔴 第 9 处文档矛盾**：`CASTLE_OFF` 真值 = **21845**（续97 正确）。打分（200 条 × 4 锚点）：21845 → 城主 199/200、`[19:21]==0xffff` 200/200、所属国 200/200、城種 200/200；21852（续99 主张）→ 1/1/15/72；21864（续99「旧坐标真值」）→ 0/0/200/199。§3.17.6 续99 #2 **判定反转，已批注撤销**。
- **② 🔴 城表 `+0x08`/`+0x0a` 运行期无写入**（第 7 次位移命中陷阱）：34 处疑似写点**无一**以城表指针为写目标。9 处呈严格成对 `mov word[reg+0xa],X` + `mov word[reg+8],X`（4 字节、同寄存器）= **S6 事件上下文 word 批量填充**。伪影样本 `0x4baff0@0x4bb23e`：`sub eax,0x51eb88`+÷31 魔数只用于「城指针→城索引」换算（`[0x525df8]`，空→哨兵 `0xc8`），随后 `mov word[esi+8],dx` 写的是 S6 ctx。
- **③ 🆕 `+0x0a` 是两个逻辑值打包**：低字节 0..100、**200/200 全为 5 的倍数**、15 distinct（90×47/80×39/70×32/60×18/100×18）；高字节 0..252、34 distinct（208×23/244×22/232×18）。作为武将号仅 1/200（sc1）、0/200（sc2）合法。
- **④ 🆕 `+0x08` 三种互不兼容消费**：当国索引（`cmp al,0x31` ×3：`0x40daa0`/`0x4432a9`/`0x44e440`）、当位域（`0x43596d` `shr eax,5; and eax,1` 取 bit5）、当阈值（`0x415e31` `cmp byte[eax+8],0x20`）。数据 0..250、≥190/200 为 5 的倍数、29 distinct。定为「多用途打包字节」。
- **⑤ ⚠️ 留档未解矛盾（不下结论）**：4+ 消费点把 `word[城+0x0a]` 当武将号 —— `0x40f7f3`/`0x41d0cc`（`call 0x49f5d0` 比玩家号）、`0x415224`/`0x4163c5`（比实参）、`0x419bf8`（`cmp ,0xffff` 哨兵）、`0x419cac`（比 0x172）、`0x46a506`。但数据 399/400 越界且②证无运行期改写 ⇒ **死分支群**，成因待考（第三剧本或 emu）。
- 产物：`scripts/castle_f08_f0a_ref.py`(**33/33**)、`_castle_f08_f0a_probe.py`；文档 `GAME_DATA_SPEC.md` §3.17.6(续99 #2 撤销批注)、§3.17.2(+0x08/+0x0a 改写)；`BREAKTHROUGHS.md` 续145(置顶)。
- 方法论新增：① 判定写点须精确到「写目标寄存器本身被赋表基址」，不能只看函数内含基址；② 成对 `[reg+0xa]`+`[reg+8]` 同值填充 = 结构体初始化指纹；③ 表基址争议用「N 条 × 多锚点」打分一次裁决。
- 下一步：① 验证「`+0x08` 低位=国」——比对 `+0x08<49` 的 30 条与其 `+0x1d` 所属国是否一致（静态一步可裁决）；② 复核续99 #1 的 `STREAM_BASE=0x58C`；③ 定 `0x49a990` 写入的城记录字段。

## 续146 · 「+0x08 低位=国」证伪 + 真实消费破出（承接续145 下一步 A）
- 🔴 **假设证伪（一步裁决）**：两剧本 `f08<49` 的 30/32 条，**无一**与自身 province 相等（0/30、0/32），全体 **0/200**。`cmp al,0x31` 只是上界检查，不等于「字段=国」。所属国仍唯一在 `+0x1d`。
- 🆕 **`byte[城+0x08]` 的真实消费 = 国情表索引**（`0x40da70` 全链路静态闭合）：`0x49f830(0xa)` 取对象 → `byte[+0x25]`(在城) → `×31+0x51eb88` 定位城 → `byte[城+8]` → `cmp al,0x31` → **`lea ecx,[eax+eax*4+0x519548]` = 国情表(stride 5) × 索引** → `call 0x49b400`。
- 🆕 **相关性画像（Pearson, scenario1）**：農商 `+0x0c` **0.550**（最强）> 地域 `+0x16` 0.486 > 軍糧 0.309 > 米 0.218 > 所属国 −0.175 > **資金 0.009（完全无关）**。⇒ 与农业/地域强相关、与资金无关的档位量。
- ⚠️ **系统性不匹配（留档，新增 §3.17.9）**：`+0x08`（上界 49 vs 数据 0..250，**170/200 越界**）与 `+0x0a`（上界 370 vs 数据 0..64592，**199/200 越界**）**同症**。已排除三解：运行期改写（34 处写点无一指向城表）、低位=国（0/200）、石高/規模（字节分解为两子域）。余两解：① EXE 与 SNDATA 布局/版本错位；② 遗留分支。**不下结论**，须第三剧本或 emu 打点。
- 产物：`scripts/castle_f08_f0a_ref.py` 由 33/33 升到 **46/46**（新增 13 条断言）；文档 `GAME_DATA_SPEC.md` §3.17.2(+0x08 行改写)、**§3.17.9**(新增留档)；`BREAKTHROUGHS.md` 续146(置顶)。
- 下一步：① 用 `f08` 与農商/地域的强相关反查 `0x519548` 国情表内容与 `0x49b400` 语义（静态一步可钉死）；② 复核续99 #1 的 `STREAM_BASE=0x58C`（打分法）；③ 定 `0x49a990` 写入的城记录字段。

## 续147 · 国名 getter `0x49b400` + 国名别名 getter `0x49b440`（承接续146 下一步 A）
- 🆕 **`0x49b400(国情表 ptr)` = 国名 getter**（全库此前零记录）：空指针 → 哨兵索引 `0x31`(49，恰好越过 49 国)；否则 `sub ecx,0x519548` → **÷5 魔数 `0x66666667`+`sar edx,1`** 得国 id → `lea eax,[edx+edx*8+0x506ca8]`（名称总表 **stride 9**，前 49 条=国名）。÷5 魔数独立坐实国情表 stride=5。
- 🆕 **`0x49b440(国情表 ptr)` = 国名别名 getter（築城/改称事件）**，四条**全部命中史实**：
  - 国 17 **美浓** → **岐阜** (`0x5076c8`)，条件 `0x49c390(tbl,4)` ∧ `0x49c3d0(tbl,4)` ∧ `word[0x520604]&0x400`；1567 信长稻叶山城改称。
  - 国 23 **北近江** → **长滨** (`0x5076da`)，条件 `0x49c390(tbl,0xf)`；1573 秀吉筑长滨城。
  - 国 24 **南近江** → **安土** (`0x5076d1`)，条件 (`0x49c390(tbl,8)` ∧ ¬`0x49c3d0(tbl,8)`) ∨ (`byte[tbl]==8` ∧ ¬`0x49c3d0(tbl,8)`)；1576 信长筑安土城。
  - 国 28 **摄津** → **大阪** (`0x5076e3`)，条件 `0x49c390(tbl,0x26)`；1583 秀吉筑大阪城。
  - `tbl = 0x5203c0` = 城表紧邻的下一张表（`0x51eb88 + 200*31`）。
- 🆕 **动态地名/称号串池** `0x507692` 起：饫肥/鹿儿岛/大口/加治木/伊集院/大隅高山/岐阜/安土/长滨/大阪/大商人/茶道大师（GBK null 结尾）。
- ⚠️ **工具坑（重要）**：`scripts/_insn_addrs.pkl` 在 **`0x49b417..0x49b43a` 有空洞**（缺 10 条指令，从 `0x49b416` 直跳 `0x49b440`）。基于该索引的断言会**静默假失败**（本轮一度 12 条全红）。⇒ **关键处必须用 `_lindis.py` 或 capstone 现场反汇编核对**。已写入 MEMORY.md 与 skill。
- 产物：`scripts/province_name_alias_ref.py`(**38/38**，全部改用 capstone 现场线性反汇编)；文档 `GAME_DATA_SPEC.md` **§3.9.8**(新增)；`BREAKTHROUGHS.md` 续147(置顶)。
- 下一步：① **攻 `0x5203c0` 表 + `0x49c390`/`0x49c3d0`** —— 决定四条历史改名的触发条件，玩法价值最高且静态可破；② 复核续99 #1 的 `STREAM_BASE=0x58C`；③ 定 `0x49a990` 写入的城记录字段。

## 续148 · S15 bitset 测试器对 + 纠正续147 改名条件（承接续147 下一步 A）
- 🆕 **`0x49c390(base,idx)` / `0x49c3d0(base,idx)` = bitset 测试器对**（此前全库零记录）：`esi=arg&0xff` → `mask=1<<(idx&7)`（`and eax,7`+`shl eax,cl`）→ `byte_off=idx>>3`（`shr esi,3`）→ `cl=byte[edi+byte_off+OFF]` → 返回 `mask & cl`（`ret 4`）。**唯一差别 = 段偏移 `+2` vs `+0xa`**。
- 🆕 **S15 三段标志数组结构闭合**（base `0x5203c0`，S15 基址 `0x5203c2`，续122 记 328 处引用）：
  - 段 A `@0x5203c2`(=base+2) 8B=64bit → **已築城/已改称**，由 `0x49c390` 测试
  - 段 B `@0x5203ca`(=base+0xa) 9B=72bit → **已破城/已喪失**，由 `0x49c3d0` 测试
  - 段 C `@0x5203d3` 8B=64bit → 测试器未发现
  - 校验 8+9+8=25=S15 全长，`0x5203c2+25=0x5203db` ✓；另 `byte[0x5203c0]`（S15 前 2 字节）被 `0x49b4f2` 与 8 比较。
- 🔧 **纠正续147 改名条件表（第 10 处文档矛盾，自伤）**：我把 `je`/`jne` 极性读反了。
  - 美浓→**岐阜** = **(A4 ∧ ¬B4) ∨ (word[0x520604]&0x400)**（续147 误记 A4∧B4）
  - 北近江→**长滨** = **A15**（仅段 A，无 ¬B）
  - 南近江→**安土** = **(A8 ∧ ¬B8) ∨ (byte[0x5203c0]==8 ∧ ¬B8)**
  - 摄津→**大阪** = **A38**（仅段 A，无 ¬B）
  - ⇒ 四条统一为「(已築城 ∧ 未喪失) ∨ 附加剧情条件」；**长滨/大阪无 ¬B ⇒ 不可破却**。
  - 教训：写「A ∧ B / A ∧ ¬B」条件表前须逐条确认 `je`/`jne` 目标与落到赋值的路径。
- 产物：`scripts/province_name_alias_ref.py` 由 38/38 升到 **63/63**（新增 25 条）；文档 `GAME_DATA_SPEC.md` §3.9.8(条件表修正 + 新增 ④ S15 bitset)；`BREAKTHROUGHS.md` 续148(置顶)。
- 下一步：① **穷举段 A/B/C 的 bit 语义** —— 扫全镜像 `call 0x49c390`/`call 0x49c3d0` 的 `push imm` 参数分布建表（直接给出「哪些城可築/可破」清单）；② 找段 C 测试器（搜 `byte[reg+0x13]` 形态，`0x5203d3 = 0x5203c0+0x13`）；③ 复核续99 #1 的 `STREAM_BASE=0x58C`。

## 续149 · S15 築城/劇本イベント旗幟塊 `0x5203c0` 结构全闭（承接续148 下一步 A+B）
- **入口**：续148 下一步(A) 穷举段 A/B/C bit 语义、(B) 找段 C 测试器 —— **两项全闭**，另推翻续148 三处布局主张。
- **① SAVE `0x47f0a0` / LOAD `0x47f110` 坐实权威布局 25B**：`+0`(1B) イベント進行 ID（值=bit 号, 0xff=無）→ `+1`(1B) 進捗（低5bit 段階 / 高3bit フェーズ）→ `+2..+9`(8B) 段A bitset → `+0xa..+0x11`(8B) 段B bitset → `+0x12`(1B) 実行済みマーカー → `+0x13..+0x18`(6B) 段C。
- **② 🔴 第 11 处文档矛盾（推翻续148）**：段B 是 **8B 非 9B**；`0x5203d2` 是**独立标量**非段B 末字节；段C 是 **6B byte 数组非 8B bitset**。末字节 `0x5203d8`（续148 记 `0x5203db` 错）。
- **③ 段C 测试器 = `0x49c410`**（`mov al, byte[ecx+eax+0x13]`，**无移位** ⇒ 读字节非读位）；配套写器 `0x49c500`。
- **④ 🆕 续148 遗漏的 setter 族（本轮关键）**：`0x49c460`=set_a、`0x49c4b0`=set_b、`0x49c420`=set_prog(低5bit,&0xe0)、`0x49c440`=set_hi3(<<5,&0x1f)、`0x49c520`=byte[+5]&7(=A24..A26)。**访问器类共 9 方法 `0x49c390..0x49c520`，调用点 27/23/29/55/21/31/37/25/4。** ⇒ **方法论第 16 条：给 bitset 定语义必须扫 setter，只扫 getter 拿不到「谁置 1」的证据。**
- **⑤ 14 bit 语义表**（已用 bit = {1..11,14,15,38}）：**bit8 安土城築城**(MSG 6787/6792/6802)、**bit14 将軍家断交**(MSG 6959/6974)、**bit15 今滨→長浜**(MSG 7025)、**bit2 将軍暗殺/追放**(MSG 6492/6498)、**bit9 本能寺→山崎合戦**(MSG 6881/6883/6894)、**bit10 光秀討伐**(MSG 6952/6810/6912)、bit4 岐阜、bit38 大阪（续147/148 已有）。**bit 1/3/5/6/7/11 缺 MSG 锚点，留档。**
- **⑥ 派发器**：`0x41a400`(bit 6→[身分門]→1→2→3→5→9→10)、`0x41a660`(bit 4→7→11)，形态 `if(A‖B) continue; if(handler()) return;`。bit6 在身分闸门**之前**。
- **⑦ `0x488030` = 主人公依存フラグ初期化**（`0x487f9a` 开局一次）：`id=0x49f8f0(0x49f5e0())`（÷47 魔数 `0xae4c415d`）；id==8 → B1,B2,B3,B7,B9,A4,A5；else → B5,B6,B7,A10,B10 (+ id!=0 时 B3)。
- **⑧ `0x5203d2` = 実行済みマーカー（布尔），证伪「城ID」说**：仅 3 处读，一律 `mov al,[0x5203d2]; test al,al` ⇒ 只判零/非零；写入值 30/60/61/110/200/250 为事件常量。按 88+c 查名称表得河越/挂川/高天神/高取，与 bit8=安土城 冲突。
- **⚠️ 工具坑（新增两处）**：(a) `_insn_addrs.pkl` 的 `_d[0]`/`_d[1]` 存**文件偏移**非 VA，直接按 VA `bisect` 会静默错归属（首跑得 fn=0x588bf3 越界）；(b) `find_calls` 写 `if dst in TARGETS` 会把调用点重复计入两张表（A/B 各报 50 = 27+23），必须 `dst == tgt`；(c) capstone 小立即数省 `0x`（`+2` 而非 `+0x2`），断言串须实跑确认。
- 产物：`scripts/s15_event_flags_ref.py`(**63/63**)、`_s15_bits_probe.py`、`_s15_writes_scan.py`、`_s15_setters_scan.py`、`_s15_ctx_scan.py`；文档 `GAME_DATA_SPEC.md` **§3.9.9**(新增) + §3.9.8 ④ 批注纠正；`BREAKTHROUGHS.md` 续149(置顶)。
- 下一步：① 攻 bit 1/3/5/6/7/11（handler `0x408c20`/`0x40d370`/`0x40ea20`/`0x40f850`/`0x411520`/`0x419150`）；② 定段C 6 字节（扫 `0x49c500` 25 调用点按索引归类）；③ 查 `0x49c520` 的 4 个消费者（A24/A25/A26 是 get_a 未覆盖的三个 bit）。

## 续150 · S15 14 bit 全部定名闭合（接续149 下一步①）
- 方法：`set_a/set_b` 调用点聚类（`_s15_bit_locate.py`）+ 派发器 bit→handler 提取（`_s15_dispatcher.py`）双法交叉。
- 6 个新定名：bit1 桶狭間(駿河 `cmp al,0xd`)、bit3 石山本願寺(下间赖照 `0xd9`)、bit5 足利義昭上洛(MSG6640/6642)、bit6 二条城築城(MSG6647/6674)、bit7 金崎撤退(MSG6722/6726/6741)、bit11 長篠(MSG6976/6977)；bit2/4/8/9/10/14/15/38 续前。
- 发现文本双轨：MSGX vs XOR 加密资源(`0x49fe40`)；bit1/3 走加密资源 ⇒ 纠正续149「5/6/7/11 缺 MSG 锚点」旧注（第12处矛盾）。
- 产物：`s15_event_bits_named.py`(**23/23**)、续150 入 BREAKTHROUGHS。

## 续151 · 段C 6字节 + segA 高位(bit A24..A28) 全闭（接续149/续150 下一步②③）
- 段C = 事件作業変数块：segC[0]=S13 目標記録索引(0..19, ×139+0x518588；S13 表身份坐实 §5.2.1)；segC[1]‖segC[2]=16-bit 打包(`(hi<<8)|lo`, 0x419ec0/0x419ef0)；segC[3]=表@0x513550 stride48 索引(0x43de30→`byte[rec+0x2d]`)；segC[4]=×1000→S13 初始化 0x4a1030；segC[5]=事件内计数/标志(桶狭間 0x408dc0 `cmp al,bl`)。
- 模型：段C 为「每事件工作内存」，仅 segC[0] 跨 handler 稳定；逐事件字段需 emu/第三剧本。
- segA 高位 byte +0x05 多用途：A24..A26=3-bit 计数器(get 0x49c520/set 0x49c540)、A27,A28=2-bit 计数器(get 0x49c530/set 0x49c560)、A29=标志(set 0x49c580)、A30,A31=个体标志；0x4c6d20 组合 5-bit 作索引（stride≈50 表身份待定）。
- 产物：`_s15_segc_scan.py`/`_s15_segc_read.py`/`_s15_a24_scan.py`/`s15_segc_ref.py`(**13/13**)；文档 §3.9.9 ⑦⑧ + ② 补全；续151 入 BREAKTHROUGHS。
- 仍未知：表@0x513550 身份、segC[4]/[5] 精确玩法、0x4c6d20 目标表身份。
- 下一步：(A) 解包 npk/lzw 回读 bit1/3 加密文本；(B) Unicorn 跑 handler 实体化 segC[1..5]；(C) 攻表@0x513550/0x4c6d20 身份；(D) 转 S7/经济基价 emu。

## 续152 · 段C[3] 表@0x513550 身份锁定 + 0x4c6d20 精修（接续151 下一步C）
- **@0x513550 = 合战单位池 (battle unit pool, stride 48)**：`scripts/_emu_tactic.py`(`UNIT_BASE=0x513550,UNIT_STRIDE=48`)+`battle_units_spec.json`(stride48)+`0x43de30(idx)=48·idx+0x513550`(117 调用者=全局通用记录访问器) 三重坐实。**纠正续151「疑为每国/每城日次状态数组」**。
- 单位结构(`_emu_tactic.py build()`)：`+0x05`=实体索引(word)、`+0x0a`=統率、`+0x10`=階位、`+0x2c/+0x2d`=16-bit 状态字(复用实体 `0x43dc40` setter 族,续125)→墨俣 handler `0x40a4f0` 读 `byte[unit+0x2d]` 判单位状态。
- 区分两张 stride48 表：`0x513550`=合战单位池(运行期填充)；`0x501e48`=16 条兵种/阵形目录(仅 `0x41bf20` 引用)，非一物。
- **0x4c6d20 精修**：arg=S15_base-0xe ⇒ `get_a24_26(arg+0xe)`/`get_a27_28(arg+0xe)` 实读 segA 第3字节；返回 `50*(word[S15+2]+4·(A24..A26+5·A27..A28))`(word[S15+2]=段A 低16bit=A0..A15；lea×5+lea×5+shl1=×50)。属 stride-50 battle 数组偏移助手(火计/戦術子系统)；容器 `0x4c69ed` 全镜像 0 直接调用者→间接/battle 路径，str50 数组精确身份仍待 emu。
- 产物：`s15_table_ref.py`(**15/15**)、`_s15_callers.py`(调用点扫描器)；文档 §3.9.9 ⑦ segC[3] 行 + ⑧ 0x4c6d20 注 + ② 头引用；续152 入 BREAKTHROUGHS。
- 仍未知：stride50 battle 数组身份(需火计 emu)、segC[4]→S13 rec0 `+0x64` 位域玩法(已定位 `0x4a1030`)、segC[5] 桶狭間计数语义、npk/lzw 解包回读 bit1/3 加密文本、S7/经济基价 emu。
- 下一步：(A) Unicorn 跑 `0x4c6d20`→str50 数组落点实体化；(B) 解包 npk/lzw；(C) 转 S7/经济基价 emu。

## 续153 · 内政/仕事実行 大模块全闭（接续「先破大模块」）
- 入口链：`0x4a9df0(entity)` 日次 → 按現在地 `+0x13` 分流(<0xC8 城表 stride31 / [0xC8,0xF8) 国情表 stride5 带 `add bl,0x38`/`add bl,0x31`) → `call 0x4a9f10` ディスパッチャ。
- 三层映射：code → idx=code-2 → slot=IDXMAP[0x4aa0c4](45) → handler(跳表 `0x4aa088` 15 项)；未登记 code 走 default `mov eax,1`。
- 12 主命 handler 全定位 + 主命名表指针 `0x504b28`(12) 坐实名。城表字段写入：开垦→`+0x0c` 农商(`0x4aa290`, `delta=div10(sat_sub(内政,30))+1`, cap=agri_capacity)、改建→`+0x0d` 守城(`0x4aa370`, `delta=div10(内政)+5*(tier2+1)`)、筑城→`+0x0d+=10`+`0x1c|=0x20`(`0x4ab530`+`0x49abe0`)、民心治安→`+0x1a`/`+0x0e`(`0x4aabd0`)、軍糧/米/資金 经包装器族 `0x4a33a0/0x4a33f0/0x4a3440`(cap 50000/30000/30000)。8 包装器 `0x4a32a0..0x4a3530` 全定位。
- 城種双跳表（索引约定不同！）：農商基档 `0x49f98c`(索引=城種)`CASTLE_TYPE_BASE={0:0,1:1,2:2,3:2,4:2,5:3,6:3,7:3}`；改修上限 `0x49fa24`(索引=城種-1)`CASTLE_TYPE_CAP={0:0,1:80,2:150,3:150,4:200,5:250,6:250,7:250}`。
- 工事：`word[+0x1d]` 低=改修(`0x49ac00`)/高=開発(`0x49ac30`)；`0x4ebc50=muldiv(a,b,c)`(c=0→0xffff)。
- ⚠️ 留档矛盾：scenario1 101/200 城 守城度 > 城種上限（接 §3.17.9）。
- 产物：`scripts/naisei_ref.py`(**66/66 PASS**)、`_naisei_scan.py`(call/handler 扫描)；文档 `GAME_DATA_SPEC.md` **§3.21**(八小节)；`BREAKTHROUGHS.md` 续153(置顶)。
- 待破：① S7 `0x516a28`(200×16B 每城运行时表) 身份——consumers `0x4a5b19` 同序遍历 城表+0x1b 与本表+0x0f，未找到写入点，剧本内全 0；② 軍糧/米/資金 handler 精确 delta(含随机项,需 emu)；③ 进贡/威吓/朝廷/谋略 关系变动量(需 emu)。
- 下一步：(A) 转 S7 `0x516a28` 扫 `0x4a5b19` 调用方找写入点 / 第三剧本 emu；(B) Unicorn 校准 軍糧/米/資金 delta；(C) 经济/物价大模块(§3.19.9 + 物品池 `0x51e1f6`) 作下一大模块。

## 续154 · SNDATA 49B 剧本记录「类型系统」奠基（17:13 起）
- **关键纠正**：上轮建议的「经济/物价大模块」经核查**早已闭合**——`economy_ref.py` 49/49、`item_pricing_ref.py` 21/21、`item_pool_spec.json` 全定；单挑(`duel_ref`18/18+`duel3`670/670+`duel_hp`17/17)、合战公式(`battle_formula_ref` emu 验证)、事件handler(`event_handlers_full`49/49)、外交(`diplomacy_ref` ALL PASS)、评定(`council_ref` PASS)、晋升(`promote3`42/42) 全部已破。§3.13 SECT_A 甚至已结构+语义双闭合(续22)。→ 项目进入「收尾」期，无干净未破大模块。
- **转攻真正的敞口**：SNDATA 833×49B 剧本记录的 **43B payload 语义**（复刻最关键缺口之一，§8 #2 / §11.5 #3）。
- **突破**：① `type = id_word & 0xff`（高低对称=填充哨兵：sc1=`0x0c/0xf3/0x0f/0x00`、sc2=`0xa/0xf5/0x0f/0x00`）；② 两剧本各剔除填充后 sc1=427条/167型、sc2=443条/161型，TOP：sc1 `0x3e`×15/`0x1`×13/`0x8`×12/`0xd`×11/`0x8c`×10；③ 首个识别类型 `0x01`（13条,rbc=0,payload 全0x00/0x01）=**场景状态布尔开关位域**(对应§5.1的22条开关)；④ payload 内 `0x0c/0xf3` 字节=字段默认值哨兵，破译须先剥离；⑤ 记录处理器 `0x47fc60` 经 `0x4ebfe0` 块拷贝写入场景态缓冲 `0x522c88/0x522c60/0x522c70`(§11 点名的归属/状态运行时区)。
- 产物：`scripts/sndata_record_classify.py`(自校验通过：对称id_word>300/有意义>400/剧本1含0x0c填充 + `dump_meaningful_samples` 打印TOP类型payload hex/字解析)；`BREAKTHROUGHS.md` 续154(置顶)；`SNDATA_SPEC.md` §8 #2 升为「类型系统已实锤」。
- 待破：① 其余~164种类型 payload schema(需 emu 加载器 0x47f350 建 type→processor→字段偏移映射)；② `0x4ebfe0` 块拷贝 size 参数来源；③ `0x522cXX` 三缓冲逐字节↔记录类型对应(验证§11派生规则)。
- 下一步：(A) emu `0x47f350` 主解析器插桩抓 type→processor 映射；(B) 精解 TOP5 类型(0x3e/0x01/0x08/0x0d/0x8c) payload 字节布局；(C) 交叉验证 0x522cXX 与 §11 home_city+status 派生。

## 17:35 · 用户要求"把文件分成2份：已破/未破"（整理而非继续破）
- 用户反馈"破解得很混乱"，要求按已破/未破二分。经澄清选定【出一份状态清单、不动文件】方案。
- 复盘结论：核心系统其实**早已全破**（经济/单挑/合战公式+地形/事件/内政/外交/评定/晋升/城表/武将/国情/SNDATA段+类型/技能/天气/时间/谣言/谍报/计略/主君势力/NPK）。真正未破收敛为少数硬骨头。
- 产物：`破解状态清单.md`（顶层）。结构：§0总览 / §1 已破解(按18模块+状态列✅🟡🔧) / §2 未破解Backlog(P0=49B记录43B payload语义、P1=合战emu残留+内政delta、P2=进贡等关系变动/S7运行时表/事件文本双轨/§11派生、P3=自测路径硬编码) / §3 工具草稿(645个`_*`非交付物) / §4 工作流建议。
- 关键提醒用户：原版游戏(`Taikou2 Original/`)与Godot工程(`project.godot`/`fonts/`)不计入二分；`scripts/` 645个`_`前缀为脚手架。
- 下一步建议用户：从 §2 P0 开破 49B payload（emu 加载器 0x47f350 建 type→processor→字段偏移映射）。

## 17:47–18:30 · 从简单到难开破（首战 S7 + P3 收尾）
- 用户指令"从简单到难的方向，开始破解吧"。建任务序列：P3(路径)→P2×3(S7/事件文本/§11)→P1(emu delta)→P0(49B)。
- **P3 热身**：写 `_fix_paths.py` 把 10 个 ref 脚本的 `open('scripts/...')` 改为基于 `__file__` 绝对路径（此前 promote3/terrain/s6_struct 因需在工程根运行而报 FileNotFoundError）。验证从工程根可跑通。
- **顺手修 capstone 枚举坑**（本环境 `CS_OP_MEM=128` vs x86 内存操作数 `.type=3`=`X86_OP_MEM`）：`entity_loyalty_thresholds_ref` 12/14→**14/14**；`entity_status2e_ref` 的 `tests` 集合从恒空→正确填 `[1,4,5]`（假绿真错，正是"混乱"根源之一）。全局仅 2 文件用 CS_OP_*，已全修。
- **首战 S7 `0x516a28`（P2 静态项，已破，续155）**：
  - 全映像 12 处字面值，11 处真实代码访问，统一模式 `cmp reg8,0xc8`(200)→`shl 4`(×16)→`add 0x516a28` → stride 16、与城表同序、200 条坐实。
  - "找不到写入点/剧本全0"根因：**运行时表**，写入全走参数化 setter `0x49bf50`/`0x49bf90`(ecx=&S7[城]) —— 典型"setter 按 ecx=base+N 参数化"陷阱。
  - 16B 字段布局：+0x04 dword 指针链(0x49f6b0 解引用) / +0x05 状态 / +0x07 写 / +0x08 word 标志字(bit15 切换 or 0x80/and 0xff7f) / +0x0a word 值(cmp 0x49f725) / +0x0b 写 / +0x0c byte 3-bit 标志(0x49b810 or/and 1/2/4) / +0x0d+0x0e 状态 / +0x0f byte 主标志(test 0x70=bit4/5/6 + >>4&7 子域,消费者 0x4a5b19)。
  - 产物：`scripts/s7_struct_ref.py` **9/9 PASS**；`GAME_DATA_SPEC.md` 新增 **§3.22**；`BREAKTHROUGHS.md` 续155 置顶；`破解状态清单.md` S7 从 P2 未破→已破。
  - 仍待破：各字段玩法语义（+0x04 链指向、+0x0f 三类别、+0x0c 三标志位）须 emu dump 填空。
- 下一步建议用户：继续 P2 静态项（事件文本 XOR 解码 / SNDATA §11 派生），或转 P1 emu 校准 delta，或接 P0 49B payload。

## 续156 — 事件文本「XOR 加密资源」轨道闭合（P2 静态·Task #15）
- 发现 HEXMES.LZW = 第 5 个 MSGX 文本容器（magic MSGX、指针表+6、GBK），283 条战斗/事件文本，从未并入 msgx_all_texts.json（旧索引仅 4 文件/6211 条）。
- 推翻旧注：0x49fe40=set_diplomacy、0x49fd80=rel_lookup，与文本无关；旧「XOR 加密资源/双轨制/无 MSGX 锚点」是误判（索引缺口假象）。
- MSGX 解析器 0x493500 全镜像唯一（仅 1 处 0x10624dd3 魔数 + 1 处 lea eax,[ecx*4+6]），只服务 slot0..3=MESSAGE1~4；HEXMES 走独立路径（句柄运行时填）。
- 交付：scripts/event_text_xor_ref.py（6/6 PASS）+ scripts/hexmes_texts.json（283 条）。
- 文档已更正：GAME_DATA_SPEC.md §3.9 两处误注、破解状态清单.md 事件文本项升「已破」、BREAKTHROUGHS.md 续156、MEMORY.md 索引误注修正。
- 仍未知：HEXMES 精确运行时访问函数（4-slot 外独立路径），留待 loader/emu 追踪。

## 续157 — SNDATA §11 归属派生规则验证闭合（P2 静态·Task #16）
- `scripts/sndata_derive_ref.py` 自校验全通过：home_city@49/status@57 字段实锤；历史锚点(信长#13 组18人→城主=信长/庆次#27 home_city=255未登场)；三缓冲扇出映射 `0x47fc60` 反汇编 3 处 push 立即数实锤(record[6:]→0x522c88 / [0x13]→0x522c60 / [0x20]→0x522c70)；三缓冲静态全0(runtime-only)。
- 修正两旧误：(a) §11「92 城」不成立——home_city 实际索引整个 370 条目名表(49国+243城·町+78职种)，花名册出现 115 个归属地；(b) 名表定宽 stride=9 仅省块(0-48)49/49 干净，城堡块>8B名溢出污染——旧 `_derive_ownership.py` stride14 双重错＋硬编码 Windows 路径不存在；已用本地 bsdata.json 重生成 ownership_derived.json。
- 文档同步：SNDATA_SPEC.md §11.5→§11.6(标记闭合+修正)、破解状态清单.md Task#16 升已破·续157、BREAKTHROUGHS.md 续157 置顶。
- 仍未知：名表溢出名(>8B)精确解码；0x522cXX 运行态 vs 派生基线逐字节比对(须 emu)；49B payload 字段(归 P0)。
