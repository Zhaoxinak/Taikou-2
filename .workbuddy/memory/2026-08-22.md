# 2026-08-22 工作记录

## Task #13 文本解码 - 重大修正与突破
- **修正字节序 bug**：原 `TaikouMessage.parse_bytes` / `TaikouTextDecoder.decode_bytes` 把 KOEI 码字当小端读（`data[o]|data[o+1]<<8`），但实际是**大端**（`CE E4`=0xCEE4=武）。导致种子永远不匹配。
- **发现 MSGX 是混合编码**：字节<0x80=ASCII；≥0x80=大端2字节码字；单个0x00终止；消息靠**指针表**界定（非00 00终止，长度可奇可偶）。写入 GDScript `TaikouMessage.gd` + `TaikouTextDecoder.gd`。
- **Godot 内验证通过**：`MSG[0]`=`武田[B5C4]...`，1735 条 walk-fail=0。
- **覆盖率**：4文件 90948 次出现 / 4078 去重码字，仅6种子(0.2%)。Top40 高频全是重复字节码（0xC3C3,0xA1A1…），疑为平假名/空格/控制码。
- **探查 rmKOEI.bin**（24KB MZ/PE DLL）：无线性 Koei→JIS/Unicode/SJIS 表；.data 稀疏(91.7%零)非简单反向表。全表在 .text 代码，需反汇编 → 留作下一步。
- 清理了 F:/Games/Taikou2 下的临时 _*.py 分析脚本。
- 重写 MEMORY.md 修正错误事实（FACE.LZW 大小、MSGX 条数、旧"必须提取字库"假设）。

## 待用户决策
- 是否让我反汇编 rmKOEI.bin 的 .text 提取 Koei→Unicode 映射函数（把表从0.2%补到100%的正道），或先并行推进 BSDATA/图形逆向。
