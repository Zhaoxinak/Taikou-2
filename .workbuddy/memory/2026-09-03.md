# 2026-09-03

## git 冲突解决 + push（双轨并集收口，main = 3c02e73）
- **背景**：interactive rebase 中途中断（本地 `3405ecd` 重放到 origin `6ab97c0` 上），`BREAKTHROUGHS.md`/`SNDATA_SPEC.md` 冲突。根因=并行 worktree 撞号：origin 轨含 续224-237（725fee7 续224-228 + 6ab97c0 收口），本地轨自共享祖先 续223 独立编号续224-227（六类顾问咨询消费链/0x462fd0 typekey map/leaf taint schema），内容互异。
- **解析**：
  - BREAKTHROUGHS.md：并集共存——HEAD 14 条在上、本地轨 4 条在下，中间插一行「并行轨并集注记」说明撞号；编号不重排（沿用惯例）。
  - SNDATA_SPEC.md：A 轨 §4.0.12(续229) 保留，本地轨 B 三节顺延重编号 4.0.12→4.0.13(续224)/4.0.13→4.0.14(续225)/4.0.14→4.0.15(续226)，修正体内交叉引用（权威表见 §4.0.13→§4.0.14、承接 §4.0.12→§4.0.13 等）。
- **结果**：`git rebase --continue` 完成 → `git commit --amend` 写描述性信息 → `git push origin main` 成功（6ab97c0..3c02e73，fast-forward，45 files/+3739）。本地与 origin 同步。
- **教训**：两轨文档 merge 若遇同节号不同内容，先查各轨节号分布再决定顺延哪侧；并行轨合并务必确认「撞号≠重复」，本仓惯例是并集共存不重排。

## 记忆索引同步（收尾）
- MEMORY.md 索引原停 2026-09-01/续203，已同步到合并后真实状态（顶部=续237、静态/结构层 100% 收口、残留敞口改列为 emu 运行期增强清单）；当日日志与索引一并入库推送。

## 续238：S13 矩阵段文件侧终审 + feint ref 路径修复（入库 22e5fd5 之后继续）
- **S13/0x518588 文件级坐实**（新 ref `scripts/sndata_s13_record_ref.py` 25/25 PASS）：
  - 0x47ef00 装载器逐指令：20 记录循环、pitch `add edi,0x8b`=139；每记录装载 blockA 25w@+0 → blockB 25w@+0x32 → blockC 5w@+0x64 → tail 4B@+0x6e..0x71（114B/记录，+0x72..+0x8a 25B 不装载）。
  - 🔴 §5.2 总表基址纠偏：0x5185b6 是 rec0 blockB 起点 0x5185ba 的近似（−4）；真基址 0x518588（续225 正确）。已校准 SNDATA_SPEC §4.0.11 与 GAME_DATA_SPEC §5 总表 S13 行。
  - **两剧本 S13 文件段 2280B 全 0xFF 占位** ⇒「运行期填充」升格文件级事实；S7 文件侧 3200B 全 0 互证续207。
  - 静态解密独立复算：解密区 `[0x58C..EOF)`、key=raw[0x12]^raw[0x13]，`sum(dec[0x58C:]) mod 0x10000==u16(raw,0x10)` 双剧本 b84b/8013 全中 → 不依赖 emu 即可精确切段。
- **修自测回归**：`scripts/feint_count_ref.py` 硬编码 macOS 绝对路径（/Users/ts/...）→ `__file__` 相对，修后 4/4 PASS。
- **文档**：BREAKTHROUGHS 续238 置顶（四段）；破解状态清单 §9 追加续238 已闭。
- **教训**：跑套件会确定性再生 `_gaiji_preview.png`/`sndata_wordarray_payload.json`（后者出现新 wordarray type 38 条目），非本次交付范围已还原——日后若见其 dirty 属再生漂移，可单独甄别提交。

## 续239：武将技能运行时读侧闭合（skill_runtime_consumers_ref 20/20）
- **读侧定性**：游戏无统一技能 getter/setter；各玩法消费点把 `level_k=(byte[ent+0x0f+k//4]>>((k&3)*2))&3` 内联。四处独立站字节签名钉死：口才(0)=`+0x0f &3`（0x4b5627/32 授艺资格判定 双实体加和 cmp6）、马术(1)=`+0x0f >>2&3`（0x43e23d 合战 kind==1 骑兵）、洋枪(6)=`+0x10 >>4&3`（0x43e250 kind==2）、茶道(9)=`+0x11 >>2&3`（0x4b3122/0x4b3160/0x4b318e 茶会主客，(lv+1)×host）。
- **基址佐证**（防「位移命中≠字段命中」）：`0x43e110`=unit→主将实体解析器（word[unit+0xa]→cmp ffff/0x172→×47→+0x519868，37B 签名）；授艺站同函数读 +0xd/+0xe 五维 sibling；茶会站实体槽=[0x525c44/4c/50/58] + 同函数 [0x525c40]+0x24 国索引<49。
- **写侧负向审计**：全镜像 `and/or/xor byte[reg+0x0f..0x11],imm`（2-bit 掩码族 {03,0c,30,c0,fc,f3,cf,3f}）**0 命中**；候选 0x4a3360..(城资金)/0x49a9e0(零初始化)/0x49bfxx(S7 旗标 setter)/0x4c0f7f..(数据区误反汇编) 排除 ⇒ 无共享 setter；技能写回只能走寄存器中介 RMW（§4.3.3 修行 mode0-2 是唯一已知玩法写入）。读点基线 +0xf/+0x10/+0x11=214/453/73（提取子集 146/175/57，4 锚点全含）。
- **修 T5b/T5d hex 窗口**（教训：反汇编窗口长度按指令边界 3/3/3 结构核对；T5b 9B=8a4811c0e90280e103、T5d 10B 裁掉越界 8844）。
- **文档/交付**：BREAKTHROUGHS 续239 置顶（闭合续237 仍未知① 读侧）；GAME_DATA_SPEC §3.5.6 加运行时读取表+§1.2 skills 行注；破解状态清单 §9 追加；套件 162/0 无回归（跑完还原 _gaiji_preview.png/sndata_wordarray_payload.json）；清理临时 _skillscan_tmp.py/_skillscan_out.txt。

## 续240：技能写侧闭合（skill_inc_writer_ref 83/83）
- **写器族坐死**：`helper_k @ 0x4a3040+k*0x20`（k=0..9=技能 id，32B 对齐）= cap-3 递增器：thiscall `ecx=实体+0x0f`，对 `byte[ecx+k>>2]` 的 `(k&3)*2` 位「读级→未满 3 则写回 +1」（与读侧公式同构反向）。A 段 10 条规范字节 + B 段 capstone 语义解码逐条断言（load→shr/and→mask→cmp/inc→store）。
- **驱动 0x45fca0**（唯一调用方 0x45f3eb，城内修行菜单）：mode≥3→0x45feb0 五维；mode0(学习内政)→口才 k0@0x45fdc8 / mode1(学习外交)→筑城 k7@0x45fd8d / mode2(学习魅力)→兵法 k5@0x45fd3d；各带概率门（rand%(0x10-x)==0 或 rand%600<三属性加和）。成功后公共尾：MSG 0x11fa→功勲 += (旧级+1)×500（0x45fe18 inc edi+lea×3+shl2=×500→call `0x4a3210` = 饱和加 `word[实体+0x26]` 上限 0xea60，+0x26=功勲 与 setter 0x49a770 互证）→MSG 名索引 {mode2→5,mode1→7,mode0→0}→`0x507b58+id*5`。
- **双胞胎 0x4de0e0**（授艺推进，`ebx=0x49f5e0()`=玩家对象）：mode2→k5@0x4de136 / mode1→k7@0x4de152 / mode0→k0@0x4de16c；递增后回读新级 `[+0xf]&3`@0x4de176 / `[+0x10]>>6`@0x4de15a / `[+0x10]>>2&3`@0x4de13e。
- **全镜像族调用点 19 处**可枚举（EXPECT 分组 k0:[0x45fdc8,0x4de171]…k9:[0x442e5d,0x442ed1,0x44304c]）；F 段逐站 ecx 审计 mem 型 11 处 disp 全 0xf、8 处寄存器/栈中转 ⇒ 技能位无其它写者。
- **修 v2 逻辑 bug**：C2 把「调用点地址(0x45fd3d/8d/c8)」拿去跟 sites 的 key(helper 地址)比——应拍平 values 后判断；E3/E4 窗口长度按指令边界核正（E3 10B 恰到 call 尾、E4 8B 到 xor dx,dx）。
- **文档/交付**：BREAKTHROUGHS 续240 置顶（消除续239「仍未知②」）；GAME_DATA_SPEC §3.5.6「写侧负向审计→正向坐实」段落 + §1.2 行注 + §4.3.3 mode0-2 行精确写器；破解状态清单 §9 追加；套件 163/0 无回归（跑完还原 _gaiji_preview.png/sndata_wordarray_payload.json）。

## 续240 推送收尾（蓝屏重启后）
- 蓝屏后本地代理端口从 13573 变 4759（`env | grep -i proxy` 现为 4759）；git config 里记录的 7890 已死。推送一律 `-c http.proxy=http://127.0.0.1:4759`（先 `curl -x ... -m 8 https://github.com` 探活）。
- 推送易挂起/SSL 抖动：可靠姿势 = 后台循环重试 + `-c http.lowSpeedLimit=1 -c http.lowSpeedTime=40 -c http.version=HTTP/1.1`（每次 40s 低带宽自动中止，第 1-2 轮通常成功）。本次 d510d50(续240) + 9caf9a3(chore) 均推送成功。

## 续241：技能写器族 13 消费站玩法归位 = 技能增长完整事件表落库（skill_writer_consumers_ref 82/82）
- **七技能完整事件表**（GAME_DATA_SPEC §3.5.6）：
  - k1 马术=马屋打工 `0x45d7a0`：体力门双方≥45 → yes/no（工钱 min((30−时)/4,5)×(同伴?2:1)）→ 主角站 0x45d9e3 级<3 且 rand(5×新级)==0；同伴站 0x45da41 另需 ebx==0 ⇒ **单名额**（每次至多升一人）；收尾推进 32−时。
  - k2 算术=商家学做生意 `0x458e20` **1 参 cdecl（学者=主角）**——此前「arg2 同伴」是栈槽复用误读（同伴由函数内 `0x49f610()` 自取）。门 `0x459000`：L0 需商人资本 N≥40 / L1≥70 / L2≥100，L==3 → msg570 拒绝；学费 `0x4590d0`：L0=90−N/2 / L1=600−3N / L2=5000−2N 贯，×10 文经 `0x44e350`；学者交费**必然 +1**；同伴仅当级≤学者级才 +1；时间 24−时+14h。
  - k3 剑术=道场试合 `0x448990`：**前置体力满（byte[+0x21]==byte[+0x20]）**；胜 0x640/惜败 0x642 且级<3 → +1，败 0x643 不加；`0x4a0d50(4,1)` 耗 4h；唯一站 0x448b56。
  - k4 忍术=忍里修业 leaf `0x451f90` **无条件 +1**；全镜像调用方恰 3 处：0x451f23/0x451f40（合格）+ 0x4521a7（再指导「你还得再费费劲」2729 仍 +1）。
  - k6 洋枪=铁炮锻冶打工 `0x447110` 自含循环（体力≥45 + msg628/629「一人一天200文」）→ `0x447230(主角,同伴)` **2 参非 4 参**；主角站 0x44736c / 同伴站 0x4473dd **各自独立判定**（可同回合双升——与马屋相反）；`0x45cc90()==1` jmp 回 0x44718d 连续打工。
  - k8 礼法=寺庙 30 日修行 `0x45ade0`：`cmp di,0x1e` 每迭代 `0x4a0d50(0x18,1)`=1 天；主角站 0x45aefd **必然 +1**；同伴站 0x45af25 = 资格(五维审查 0x45b100)→必然，否则 rand(10)==0 且级<3；**唯一不调 0x4704e0、无 0x507b80 名表引用的写站**（负向断言入 ref）。
  - k9 茶道=茶人品茶 `0x442a80(主角级)` → 茶具品质上限 0/6/13 → **品质=`byte[茶具项+0x08]>>3&0xf`（4-bit）** → `0x442d70(级,品质)`：主角站 0x442e5d = rand(10/25/30)<品质（级 0/1/2）；级==3 转调 `0x442f70`（`cmp si,3`@0x442d86）；同伴站 0x44304c = rand(600)<五维字节和（dword[+0xa]+byte[+0xe]）；低级同伴站 0x442ed1。
- **新结构事实**：实体 +0x20=最大体力 / +0x21=现在体力（0x4a3180 加 / 0x4a31b0 减互证）。
- **ref 结构**（82 断言）：A 13 站点 call helper_k 重解析（SITES 表 site→helper→host→玩法）；B 19 条防漂移字节窗口；C 28 项门条件结构断言（0x459000 门槛 40/70/100、学费 0x5a/0x258/0x1388、k3 体力满 + 试合 8 seq/msg、f1 双 0x2d 检查、茶具 4-bit 品质 + 上限 switch）；D 名表立即数审计 + f8 负向断言；E 全镜像 xref（leaf 调用方=={0x451f23,0x451f40,0x4521a7}）；F 消息链锚（含铁炮 msg 628/629=算术派生 `add ecx,0x272`/`add ebx,0x275`）。
- **坑**：B 区窗口长度手算差 2-6 字节逐一按指令边界修正（7 处）；capstone skipdata 下 `x.operands` 抛 CsError 须 try 包裹、`X86_OP_IMM` 从 `capstone.x86_const` import；BREAKTHROUGHS 前缀替换曾截断续240 条目开头（残行拼回修复，grep 验证 46 条完整）。
- **文档回填**：BREAKTHROUGHS 续241 置顶（四段）；GAME_DATA_SPEC §3.5.6 事件表 + §4.3.3 交叉引用；破解状态清单 §9 追加；MEMORY.md 同步续241 状态。
- **仍未知**（写入续241 条目）：f6 上层入口（菜单 emu 层）；`0x44e540`/`0x44e110` 精确语义；商人资本字段名。
- **收尾**：套件 164 PASS / 0 FAIL 无回归（再生文件 _gaiji_preview.png/sndata_wordarray_payload.json 已还原）；commit a09200a（6 files/+348）推送成功（代理 4759 第 1 轮即中）；_swc_ 临时探针/dump 按惯例暂留未入库。

## 续242：店铺主人格记录表闭合（静态）
- **模型**：`0x517850` 30×12B（..0x5179b8 恰顶国政治表）= S8 台词表/NPC 师父池同表；入店分发器 `0x44e710(shop_id)` 置 `[0x52063c]=0x517850+id*12`，id≥30→NULL、离店尾 0x44e7d4→NULL；**存储站全集恰 4 处** {0x44e753,0x44e75b,0x44e7d4,0x460890}（raw `3c065200` 768 引用）——「共享字节矛盾」消解。
- **设施身份（msg 锚）**：slot0 商家×5(481)/1 茶人(862)/2 锻冶×3(585)/3 南蛮×2(703)/4 教会×2(4629)/5 道场×3/6 公家×3(4923)/8 寺×3(4829)/9 忍里×2(2671)/10 医师×3(654)；slot11 死条目；#29=闇商人（slot[29]=0x0c→出店）。
- **🔴 纠偏**：`+0x07`=店主好感 0..100（非「商人资本」）——学做生意门 40/70/100与学费 90−N/2 = 好感折扣；读站 0x458e34；setter 三件套 0x49bfb0/0x4a3630(cap100)/0x44e560(floor1)+薄包装 0x44e540(21 方)。
- **闇商人**：0x460890 硬编码 0x5179ac；0x461da0 判定 word[S6+8]∈{0,1}；四 mode 阶段门 cmp byte[ecx+7],2 + 2 处 cap-3 写器（0x460a23/0x4614a8）。
- **0x44e110** = 通用持有物选择对话框（0x47b590→0x47b430，-1=取消哨兵，[0x517838][idx]→0x44d240→0x4b0ad0；9 调用方）——闭合续241 未知②。
- **交付**：`scripts/shop_npc_record_ref.py` **80/80 PASS**（A 分发器 13/B 生命周期 14/C 好感谱系 ~26/D 对话框 ~20/E 设施锚 ~13）；BREAKTHROUGHS 续242 + GAME_DATA_SPEC §3.25 + §3.5.6 k2 纠偏 + 破解状态清单 §9；MEMORY.md 口径修正（套件计数=ref 文件数，续242 后 165/0）。
- **调试坑**：① 跳表项本身是 e8 call 站址、目标是 handler（勿把 handler 当 call 目标）；② raw 扫描 off<2 边界；③ capstone imm 打十六进制（C8 f-string）；④ 窗口截断 ret 4（0x21→0x24）；⑤ msg push imm 站址须实测（E1 五锚全偏移）。
- **本机环境**：Windows 无 mac python3.7；用系统 Python 3.12（`C:/Users/Administrator/AppData/Local/Programs/Python/Python312/python.exe`，capstone 5.0.7）；managed venv 不存在。
