# 2026-09-03

## git 冲突解决 + push（双轨并集收口，main = 3c02e73）
- **背景**：interactive rebase 中途中断（本地 `3405ecd` 重放到 origin `6ab97c0` 上），`BREAKTHROUGHS.md`/`SNDATA_SPEC.md` 冲突。根因=并行 worktree 撞号：origin 轨含 续224-237（725fee7 续224-228 + 6ab97c0 收口），本地轨自共享祖先 续223 独立编号续224-227（六类顾问咨询消费链/0x462fd0 typekey map/leaf taint schema），内容互异。
- **解析**：
  - BREAKTHROUGHS.md：并集共存——HEAD 14 条在上、本地轨 4 条在下，中间插一行「并行轨并集注记」说明撞号；编号不重排（沿用惯例）。
  - SNDATA_SPEC.md：A 轨 §4.0.12(续229) 保留，本地轨 B 三节顺延重编号 4.0.12→4.0.13(续224)/4.0.13→4.0.14(续225)/4.0.14→4.0.15(续226)，修正体内交叉引用（权威表见 §4.0.13→§4.0.14、承接 §4.0.12→§4.0.13 等）。
- **结果**：`git rebase --continue` 完成 → `git commit --amend` 写描述性信息 → `git push origin main` 成功（6ab97c0..3c02e73，fast-forward，45 files/+3739）。本地与 origin 同步。
- **教训**：两轨文档 merge 若遇同节号不同内容，先查各轨节号分布再决定顺延哪侧；并行轨合并务必确认「撞号≠重复」，本仓惯例是并集共存不重排。

## 记忆索引同步（收尾）
- MEMORY.md 索引原停 2026-09-01/续203，已同步到合并后真实状态（顶部=续237、静态/结构层 100% 收口、残留敞口改列为 emu 运行期增强清单）；当日日志与索引一并入库推送。

## 续238：S13 矩阵段文件侧终审 + feint ref 路径修复（入库 22e5fd5 之后继续）
- **S13/0x518588 文件级坐实**（新 ref `scripts/sndata_s13_record_ref.py` 25/25 PASS）：
  - 0x47ef00 装载器逐指令：20 记录循环、pitch `add edi,0x8b`=139；每记录装载 blockA 25w@+0 → blockB 25w@+0x32 → blockC 5w@+0x64 → tail 4B@+0x6e..0x71（114B/记录，+0x72..+0x8a 25B 不装载）。
  - 🔴 §5.2 总表基址纠偏：0x5185b6 是 rec0 blockB 起点 0x5185ba 的近似（−4）；真基址 0x518588（续225 正确）。已校准 SNDATA_SPEC §4.0.11 与 GAME_DATA_SPEC §5 总表 S13 行。
  - **两剧本 S13 文件段 2280B 全 0xFF 占位** ⇒「运行期填充」升格文件级事实；S7 文件侧 3200B 全 0 互证续207。
  - 静态解密独立复算：解密区 `[0x58C..EOF)`、key=raw[0x12]^raw[0x13]，`sum(dec[0x58C:]) mod 0x10000==u16(raw,0x10)` 双剧本 b84b/8013 全中 → 不依赖 emu 即可精确切段。
- **修自测回归**：`scripts/feint_count_ref.py` 硬编码 macOS 绝对路径（/Users/ts/...）→ `__file__` 相对，修后 4/4 PASS。
- **文档**：BREAKTHROUGHS 续238 置顶（四段）；破解状态清单 §9 追加续238 已闭。
- **教训**：跑套件会确定性再生 `_gaiji_preview.png`/`sndata_wordarray_payload.json`（后者出现新 wordarray type 38 条目），非本次交付范围已还原——日后若见其 dirty 属再生漂移，可单独甄别提交。
