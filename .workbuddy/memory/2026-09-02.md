# 2026-09-02 工作日志

## 续213–续216：残余非图像资源格式全闭 + 「除图像外全部破解」结案

承接用户指令「除了图像破解外，都按文件来，把剩下未破解的都帮我破解了」。本会话闭合最后 3 个非图像资源格式，并正式结案。

### 交付脚本（均 ALL PASS）
- `scripts/anmseq_format_ref.py` **12/12** — ANMSEQ.LZW 动画脚本字节码：
  - 结构 `b"ANMX"` + 520×(u16 off,u16 len) 索引(@0x04..0x824) + 瓦片 bytecode(@0x824..EOF)；缓冲 `0x524960`。
  - consumer `0x496ba0`；opcode 表 `@0x496d20`(90B, `0x00..0x59`)；跳转表 `@0x496cf4` 11 handlers（END`0x00`/SELECT_OBJECT`0x43`/CALL sub`0x49·0x4f`/CLEAR_FLAG`0x4e`/WRITE_OBJ`0x50·0x58·0x59`/CALL_OBJ`0x53`/WAIT`0x57`）。
  - 🔴 纠正「0xff=终结符」→ 真 END=`0x00`，0xff=NOOP。
- `scripts/town_obj_format_ref.py` **14/14** — 城镇物件三件套（EXE 内嵌，非独立原版文件）：
  - HBOBJ.DAT：manifest `@0x5030d8`，loader `0x424020`(`push 0xa0`) ⇒ 32×160B(stride 0xa0)，类型字节钳 0..7。
  - TOWNTBL.DAT：manifest `@0x50c140`，loader `0x4ac9c0` ⇒ 1280 u16 单位阵；b30=24/b31=0 → grid 48×32。
  - TOWNPOS.DAT：缓冲 `@0x525af4` ⇒ 1225 u16(35×35 按城 id 索引)，0xffff sentinel；坐标与 `towns.json` 92 城对齐。
- `scripts/idx_format_ref.py` **23/23** — IDX 容器（HGRP.LZW/GRPDATA.LZW 解压后）：
  - `b"IDX"` + u8 flag(0x7e/0x8b) + u32 count(508/560) + count×i32 slot 三态表。
  - slot 正值=VRAM 偏移 / 0+0xffffffff=未用 / 负=跨引用 sentinel（**非文件偏移**，推翻初判「positive offset<decsize」）。
  - slot 表后像素 blob 按用户「图像不用破解」豁免；纯像素 LZW(GRPDATA2/KAIKON/HKMAPDAT/TERRAIN/SHOP_*) 统列图像豁免档。

### 自检基线
- `scripts/_run_all_selfchecks.py`：**140/140 ALL PASS**（由 129/0 升）。本批新增 3 个 `*_ref.py`；2 个 emu-boot 依赖探针 `_sndata_layer_resources_probe.py`/`_sndata_type_routing2_probe.py` 改名 `_probe` 前缀排除出自检 runner，合法闭合旧失败。

### 文档
- BREAKTHROUGHS.md 顶部插入 续213/214/215/216（续216 = 「除图像外全部破解」结案：盘点原版 149 文件 + EXE 内嵌非图像表，残余非图像全闭，仅图像类按用户明令豁免）。
- 破解状态清单.md：§0 模块数 21→24、关键结论补 3 格式、§1 加 3 模块行、§2 P1.5 改「非图像已全破」+ 加 3 子条 + 已破格式列表 + 建议顺序标 exempt。

### 残留敞口（非图像）
1. emu 运行期取样：S15 段C 16 处 runtime-var val 须钩 `0x49c500`（续193 emu_harness.py 已具备，仅待 boot 事件解释器）；segC[3] 战斗单位配对；未定名事件 bit 1/3/5/6/7/11 MSG 锚点。
2. 图像类（GRP/PK8/纯像素 LZW）按用户明令豁免不破。

### 环境提醒
- 跑反汇编/emu 用系统 Python `C:/Users/Administrator/AppData/Local/Programs/Python/Python312/python.exe`（有 capstone/unicorn）；managed 3.13.12 无此二库。
- 全镜像扫描须用 `scripts/_disasm_all.py` 的 `disasm_all()` 生成器（capstone ≥5 skipdata 静默截断坑，续205）。

## 续217：S15 段C 16 处 runtime-var val 源静态全定位（续216 下一步 (B) 收尾）

承接续212（25 处 set_c 写者全归因事件 handler，9 常量 + 16 变量 val 须 emu 取样）。本批把「变量 val 须 boot 事件解释器」坍缩为「源已知·具体值待钩 set_c 捕」。

### 交付脚本（54/54 ALL PASS）
- `scripts/s15_segc_runtimevar_ref.py` —— 4 组自测：
  - T1 set_c 字节级契约 emu 直验：`set_c(0x49c500)`=thiscall(ecx=`0x5203c0`)、`byte[ecx+0x13+idx]=val`、`idx&0xff`、`ret 8`；sanity `set_c(2,7)`→buf[0x13+2]=7。
  - T2 16 处 runtime-var val 源全部静态定位（栈感知后向 def-use 回溯）。
  - T3 语义类分布：`helper_ret=8`/`handler_arg=1`/`packed16=2`/`numeric=1`/`battle_unit=2`/`runtime_bool=1`/`?=1`（合计 16）。
  - T4 emu 孤立跑 owner 0x413d10 崩溃（0 次 set_c 写入）→ 具体值确须 boot。

### 关键技术点（可复用）
- 分类器：收集 call 前 push 取 (idx,val-push) + 跳过 mov + 止于 call/pop/add-esp/sub-esp；再后向找 val 寄存器最后定义 → 判 helper_ret/handler_arg/mem_read/numeric/packed16/battle_unit/runtime_bool/reg_pass/const。
- 🔑 推翻「须 boot 整解释器才能取变量 val」：结构层纯静态已全坐死，具体值只需钩 `set_c(0x49c500)` 抓 16 个 call_va 的运行期 (idx,val)。

### 文档
- BREAKTHROUGHS.md 顶部插 续217（四段：突破/证据/仍未知/下一步）。
- 破解状态清单.md §0 剩余敞口 ②、§2 P2 S15 段C 行（标签 `✅ 源已静态全定位·值待钩 set_c 捕·续217` + 待补改写）同步更新。
- 自检基线：续217 +1 (s15_segc_runtimevar_ref) → 141/141；续218 +1 (s15_segc_runtimevar_capture_ref) → 142/142；续219 +1 (s15_segc_entityctx_capture_ref) → **143/143**（待 `_run_all_selfchecks.py` 复跑）。

## 续219：S15 段C 16 处 runtime-var val 注入实体上下文 → 13/16 站产出具体 (idx,val)

承接续218（6 崩溃站缺实体指针）。本批注入真实实体上下文使 handler 跑通，抓到具体 (idx,val)。

### 交付脚本（7/7 ALL PASS）
- `scripts/s15_segc_entityctx_capture_ref.py` —— 对 16 owner × entity[0,100,369]：
  - T1 可达 13 / 崩溃 0 / 超时 3（续218 零态 7/6/3）；T2 续218 六崩溃站全可达并给值；T3 捕获 idx 全 ∈[0,5]。
- `scripts/s15_segc_entityctx_capture.json` —— 16 站 × 3 实体捕获表。

### 关键技术（可复用）
- **根因=缺实体指针**：映射 `0x0` 页为 `entity[entity_idx]`（@0x519868 stride 0x47B·370 条·静态已初始化）0x47B 拷贝，统一化解 `mov [null+0x25]` 崩溃。
- 辅以 `ecx=0x5203c0` + `ebx=实体` + `args[4]=实体` + 代码钩在 `0x4110e3/0x4110e8`(0x4110d0) 与 `0x413db7`(0x413d10→sub 0x413db0) 强制 edi/esi=实体。

### 捕获值（entity 0 基线）
- segC[0]: 0x4094c5=(0,0)/0x409795=(0,0)/0x413d4f·80·9c·65=(0,20)【仅 entity0 触发】
- segC[1]: 0x409814=(0,30)/0x412daf=(1,0)/0x419f09=(1,0)
- segC[2]: 0x409542=(0,30)/0x419f16=(1,0)
- segC[3]: 0x41112f=(3,1)/0x40a6ec=timeout
- segC[edx]: 0x41144a=(1,1)
- 3 超时站(须 boot)：0x409300(0x409250)/0x40c4f3(0x40c350)/0x40a6ec(0x40a4f0)

### 性质
- 捕获值基于 entity 0 + 零态他部；真实事件须具体实体+填充态，但 handler 逻辑已证可执行、(idx,val) schema 已实。
- `0x413d10` 四站仅 entity 0 触发 val=20 → 值条件依赖实体属性（在城/国/状态），非固定常量。

### 下一步（续219 指定）
- (A) 3 超时站构造事件参数/填充数组喂入；(B) 用真实事件实体(非 0) 复跑已可达 13 站标定 val 随实体映射；(C) 未定名事件 bit MSG 锚点（续149）。

## 续218：S15 段C 16 处 runtime-var val 孤立 emu 取样实证（0/16 可信值）

承接续217「具体值须 boot」。本批实证孤立跑 owner 取不到可信值，并定位精确阻塞点。

### 交付脚本（12/12 ALL PASS）
- `scripts/s15_segc_runtimevar_capture_ref.py` —— 钩 set_c(0x49c500)，16 个 owner 各用 zero / ecx=0x5203c0 两种预置跑，IAT 0x3000 ret 兜底：
  - T1 set_c 钩契约（合成 6 调）；T2 16 站分类：可达(零态产物) 7 / 崩溃 6 / 超时 3；T3 可达 idx 全 ∈[0,5] 有效。
- `scripts/s15_segc_runtimevar_capture.json` —— reached_zero_state / crashed / timeout 三类表。

### 关键实证结论（可复用）
- **0/16 取得可信运行期值**：7 个「可达」handler 跑到 set_c 但 idx/val 是零态产物（val 多=0、2 站=30），非真实事件值。
- **6 崩溃站全在 `mov al/bl, [reg+0x25]`**——读实体结构 +0x25（在城/城索引），实体指针零态 null。崩溃 VA：
  - owner `0x413d10`(站 0x413d4f/0x413d80/0x413d9c/0x413d65) @0x413db7（`esi=[esp+0xc]`=传入 `ebx`=实体指针）
  - owner `0x4110d0`(站 0x41112f) @0x4110eb（`esi=0x49f830` 返回实体指针·零态 0）
  - owner `0x4113a0`(站 0x41144a) @0x4113e8（`esi=[esp+0x14]` 传入实体指针）
  ⇒ 这 6 个 owner 须传入有效实体指针（事件上下文=某武将实体）。
- **3 超时站须 boot**：0x409300 / 0x40c4f3 / 0x40a6ec（零态下空循环/长路径）。

### 下一步（续218 指定）
- (A) 构造最小事件上下文（实体指针 + 事件参数）逐一喂 6 崩溃 owner → 捕 segC[3] 等真值（最直接）。
- (B) 否则 boot 事件解释器全局钩 set_c 抓全部 16。
- (C) 未定名事件 bit 1/3/5/6/7/11 的 MSG 锚点（续149 已列 handler 地址）。

## 续220：S15 段C「17 项 id×slot×val」残余非图像敞口全闭（45/45）

承接续219（13/16 产出）。本批收口最后 3 超时站 + 坐死 25B segC 结构 + 纠偏 owner 边界。

### 交付脚本（45/45 ALL PASS）
- `scripts/s15_segc_gated_capture_ref.py` —— T1 边界重算 / T2 两相 idiom 根因 / T3 三站捕获 / T4 val 公式 sweep / T5 门控+值源原语 / T6 25B 结构布局(10 检) / T7 16/16 逐站归属 / T8 0x413d10 模式选择器(4 分支) / T9 0x409814 ÷139(12/12) + 死代码确证。
- `scripts/s15_segc_gated_capture.json` —— owner_fix 5 处 / three_sites / primitives / struct_0x5203c0_25B / mode_selector / table_0x518588 / dead_code / runtime_var_merged 16 项。

### 三站收口（按站点捕获真值）
- `0x409300`=(0,29)【门控 `segC[0]!=0`，公式 `segC[0]=max(segC[0]-1,0)`】
- `0x40c4f3`=(1,0)【owner→0x40c4d0·bit2 将軍(足利)暗殺相位2，`segC[1]=0x40c520()`=目标城索引】
- `0x40a6ec`=(3,1)【owner→0x40a620·墨俣築城相位2，切片 0x40a6c9 仅读 `byte[0x513540]`，`segC[3]=max(byte[0x513540]>>1,1)`】

### 关键方法论（可复用 · 重要）
- **🔴 推翻 owner 归属启发式「max call 目标 ≤ va」**：EXE 用两相 handler 尾调用 idiom（`call 相位1; test eax; je skip; jmp 相位2`），相位2 只经 E9 jmp 到达。函数 owner 归因须**同时收 E8 call 与 E9 jmp 目标**；判别器用「ret + ≥2 连续 0x90 填充」边界。**「指令边界对齐」检查无鉴别力（25/25 全 True 却检不出此错）**——此坑务必记死。据此纠偏 5 处：0x40c4f3/0x40a6ec/0x40ad81/0x40a62b/0x40a79e。
- **逐站点归属**：钩 set_c 处读 `[esp]-5` 反推 call 站点，避免续219「首个 set_c 即停」串味（同 owner 多站被记成同一值）。
- **切片起点法**：全函数 emu 被无关 I/O 阻断时，改用 val 的 def-use 闭包起点（最小自足切片）替代整函数 entry。
- 25B segC 结构(0x5203c0) 摊平：+0x00 类型 / +0x01 打包位域 / +0x02..+0x09 组A 64bit / +0x0a..+0x11 组B 64bit / +0x12 未定名 / +0x13..+0x18 segC[0..5]。
- 0x413d10 模式选择器：`si=(word[0x520604]>>12)&3` → 4 分支（si=1 无写入）。

### 自检基线
- 续220 +1 (s15_segc_gated_capture_ref) → **144/144**（待 `_run_all_selfchecks.py` 复跑确证）。

### 下一步（续220 指定）
- (A) 转 `GAME_DATA_SPEC.md §3` 事件旗段汇总收口；(B) 可选：真实实体复跑 13 站标定 val 随实体映射；(C) 未定名事件 bit MSG 锚点；(D) 余硬骨头=emu-boot 全局（P0 全 833 idx→资源表、type=0x01 的 43 布尔字段名）。

## 续221：SNDATA payload「命名语义」深坑攻关（7/7）

承接续208（证伪 43 布尔=未解密字节伪像）/ 续206（boot 崩溃）。本批纯静态 + 名称表交叉引用解析 SNDATA payload 语义。

### 交付脚本（7/7 ALL PASS）
- `scripts/sndata_payload_semantics_ref.py` —— 聚合两剧本真实记录（非 empty），按 id_word&0xff 分类，逐字节按取值域判定语义域，索引字节交叉引用名称表解析名字。T1 名称表规模 / T2 type=0x01=索引数组(非布尔) / T2b 真实布尔开关记录存在 / T3 type=0x00 benum→实/城/国 / T4 type=0x00 索引解析出具体名 / T5 三类索引域 / T6 215 型全覆盖。
- `scripts/sndata_payload_semantics.json` —— correction_line68 / type1_index / type0_benum / 全 215 型 semantics。

### 🔴 关键纠偏（重要）
- 状态清单 §2 P0 line 68 旧「type=0x01 的 43 布尔」前提**错**：续208 已证伪其为续189 未解密字节统计伪像。真实解密 payload 复核 **type=0x01（id_word&0xff==1）实为索引数组（城/实体索引，值 0..245），非布尔**。
- 真实 43 字节场景状态布尔开关记录是具体【记录】而非整型：仅 **type=0x13 / 0x4b 各 1 条（极稀有）**，非 type=0x01。

### 真·可答问句已答
- line 68「type=0x00 的 benum 索引指向哪些实体/城/国」→ type=0x00 payload 字节按域解析为 城表索引(0..199)/实体索引(0..369)/国索引(0..48)，解析出具体名字（pos4=黑川城、pos0=屋代景赖、pos9=河田长亲）。

### 路线选择（可复用）
- 不走 emu 跑 consumer：续208/206 证 boot 主循环 `0x4e8625` 崩溃、dispatch 寄存器间接抽不出 type→handler、`sndata_payload_consumer.json` 的 `records_processed_no_crash=0`。改用**静态聚合真实记录 + 已破解名称表交叉引用**，直接回答「索引指向谁」。

### 仍未知
- ① 各索引字节精确玩法字段名（type=0x00「武将-城」配对语义）；② 43 布尔开关每字节对应具体场景状态 flag（目标数组须 boot，续206 阻塞）；③ ~164 型非索引 payload（word/枚举）含义。

### 自检基线
- 续221 +1 (sndata_payload_semantics_ref) → **145/145**（待 `_run_all_selfchecks.py` 复跑确证）。

### 续222 —— SNDATA 49B payload 消费模型全链路钉死（结构层闭合 续221 仍未知 ①②③）
- **生产者链**：`0x47d890`(read_record) 读 49B 进调用者局部缓冲 → `0x47fc60`(fan-out) 把 rec[6:]/[19:]/[32:] strcpy 到全局三视缓冲 0x522c88(43B)/0x522c60(30B)/0x522c70(17B)，3 头字写到 *out0/1/2。
- **显示消费者（唯一直接引用三视缓冲的函数）**：`0x47ff50` —— 把 payload 当 **null 结尾文本**：3 头字经 `0x4ebe60(…,0xa)` pad 宽10，三视列经 `strcpy(local,0x522c88)`+`strlen(0x4ebfc0)`+`sub ecx,0xe`(=14) pad 宽14 + 分隔串(0x509744/0x5038d0/0x509740/0x50973c) 拼「读档/选场景」菜单串。🔴 续164「仅 0x480000 引用」误记（真实函数起点=0x47ff50，0x48006b 在其体内）。
- **全镜像引用扫描**：直接引用三视缓冲的函数仅 `0x47fc60`+`0x47ff50`（`find_ref_funcs` 实锤）。
- **全 215 型 payload 值不落已知表**：`_sndata_wordtypes.py` 扫描 word/byte 值对 item(0..188)/skill(0..9)/prov(0..48) 覆盖 = 0%（word 值 9..65478 含 0xFF00 打包值）→ 佐证 type 专属打包参数/显示文本，非统一字段表（坐实续221 静态天花板）。
- **结构化消费者 = 主循环局部缓冲**：`0x4e8625`/`0x47fe00` 把 49B 读进自身局部缓冲（=0x47fc60 的 arg 所指，非全局 0x522c88），按 type 寄存器间接分派消费（续159/160 已证无 type→handler 指针表）→ 静态抽不出字段映射。
- **交付脚本（7/7 ALL PASS）**：`scripts/sndata_payload_consumer_ref.py`（T1 0x47fc60→read_record / T2 写三视 / T3 三视源=rec[6]/[19]/[32] / T4 0x47ff50→0x47fc60 / T5 0x47ff50 把 0x522c88 当文本 / T6 3头字pad10 / T7 仅 0x47fc60+0x47ff50 引用三视）。辅助：`scripts/_sndata_wordtypes.py`(215型表匹配扫描) / `scripts/_sndata_consumer_disasm.py`(0x47ff50 反汇编) / `scripts/_sndata_t0_deepdive.py`(type=0x00 混索引非干净配对) / `scripts/_sndata_deepdive.py`(记录语料)。
- **结论**：续221 仍未知 ①②③ 的**结构层已闭**；逐字节「玩法字段名」须 boot 主循环 `0x4e8625` + 钩局部记录缓冲 MEM_READ（续206 阻塞点 0x47fcad 须桩 I/O：I/O 重定向续194 + IAT 0x3000 ret 续209）。
- **自检基线**：续222 +1 (sndata_payload_consumer_ref) → 待 `_run_all_selfchecks.py` 复跑（预期 146/146，系统 python3.12 跑，因 capstone 仅该系统有）。

### 续223 —— SNDATA 49B payload「结构化消费」链路全钉死（🔴 推翻续222 boot 前提）
- **背景**：续222 把「per-type 字段名」归为须 boot 主循环 `0x4e8625` 钩局部缓冲。本批纯静态追到结构化消费的真实位置，证其不依赖 boot。
- **第三调用者 `0x488290` 排查**：反汇编(`_sndata_8290_disasm.py`)证其是 `0x4e8600` 双生子——同调 0x480240(取idx)→0x47fc60(read_record 落三视)→cmp 三头字==0 跳过→0x47fb80 谓词→按 `word[0x5205fe]` 派发；else 分支(MODE≥2)仅 `mov eax,1; ret`(只支持 MODE0/1)。非隐藏结构化消费者。
- **生产者 `0x47fc60`**：`_sndata_cluster_probe.py` 反汇编证其只写 3 局部头字 + strcpy 三视全局 0x522c88/0x522c60/0x522c70；**无全记录全局缓冲**。
- **`read_record 0x47d890`**：`_sndata_recordbuf_probe.py` 字节级坐实偏移=`idx*49+0x10`(stride49=0x31,头16B=0x10)；`_sndata_reader_callers.py` 证其唯一调用者=显示链 0x47fc60。全镜像另有 10 个 49B 流读取器同族，加载路径用 `0x47d580/0x47d680/0x47d6a0`。
- **加载期结构化链**：`_sndata_b9c10_probe.py` 证 `0x4b9c10`/`0x451860` 经 `0x4a09b0` 开子流、用 `0x47d580/0x47d680/0x47d6a0` 读 49B 流、每条 `call 0x462460`；`_sndata_462460_probe.py` 证 `0x462460` 把记录存 0x5256b8 经 `0x4eefa0` 入队 `0x526c58`(抽干条件 [esp+8]==0x7f7)；`0x49b140`=城/ref→名串解析器(÷31 魔数 0x84210843 + 0x5203c0 事件旗门控)。`0x526c50/0x526c58` 即 `0x47f350` push 的同一队列 → 结构化消费=延迟队列，type 分派在抽干端(静态可达)。
- **交付脚本（6/6 ALL PASS）**：`scripts/sndata_structured_consumer_ref.py`（T1 0x47d890 偏移 idx*49+0x10+读49B / T2 0x47fc60 写三视 / T3 0x488290 双生子 / T4 加载读取器被 0x4b9c10/0x451860 调用+0x47d890 唯一调用者=显示链 / T5 0x462460 队列入口 / T6 无字面 833 循环）。辅助：`_sndata_8290_disasm.py`/`_sndata_cluster_probe.py`/`_sndata_f350_probe.py`/`_sndata_find_section_decoder.py`/`_sndata_recordbuf_probe.py`/`_sndata_loadloop_probe.py`/`_sndata_reader_callers.py`/`_sndata_b9c10_probe.py`/`_sndata_462460_probe.py`。
- **结论**：续221 仍未知 ①②③ 的 per-type 字段名改走「队列抽干端 type 字节比较链」**纯静态**(不 boot)；下一步反汇编 pop `0x526c58`/`0x526c50` 的 consumer 建 `type→字段偏移` schema。
- **自检基线**：续223 +1 (sndata_structured_consumer_ref) → 预期 147/147（待 `_run_all_selfchecks.py` 复跑，系统 python3.12）。
