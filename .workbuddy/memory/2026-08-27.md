# 2026-08-27 工作日志

## 经济/物价子系统架构全破（#34/#42 经济·架构部分）
- 承接 08-26 的 §3.5 技能/授艺，本日主攻经济/物价（用户"继续破解"）。
- **买价 = 基价 × 1.5**：`0x445ff0`（`lea eax,[eax+eax*2]`×3 `sar eax,1`÷2）。
- 基价取链：买入处理 `0x446500`（置 `[0x52418e]=4` → `call 0x47bd10(0x513ea8,0x64,item,3,-1)`）→ `0x47bd10` 壳 → `0x489400` 结构体读取器。
- **价格结构体 `0x513ea8` 布局**：selector word `@+0x426`(0..5) 选字段：`≥5→+0x102`；`1..4→跳表 +0xba/+0xde`；字段经托管访问器 `0x4ee3f0`→`0x4ee340` 惰性取出。**静态全 0**（已 dump）→ 基价运行时托管填充，与战斗表同模式。
- 米市/批量买屏 `0x488810`：`[0x52418e]!=4` 时 `price=[0x52418c]×[0x52417a]÷3`（魔数 `0x66666667`）。
- 菜单构造 `0x459700`：按当前城 `byte@4`(城种)→索引0..10→菜单串`索引*5+0x504658`→`0x44df20`；`0x459798` 仅菜单构造跳表（非处理函数派发）。
- 月度波动区 `0x4e2d00`：消息码 `0xa3d/0xa3e/0xa3f/0xa40`，`/3` 魔数 + `0x7530=30000`，`0x49b140`(×10)/`0x4e0350` 参与。
- **关键方法学**：xref 工具只抓绝对 4 字节立即数引用；`0x446500`/`0x445ff0` 走**相对 call**，故 0 xref —— 处理函数派发需反汇编附近或搜相对编码，不能靠 xref。
- 交付：`scripts/economy_spec.json`（含 `price_struct_layout`）、`GAME_DATA_SPEC.md §3.6`（架构+§3.6.7 布局+§3.6.8 待破）、`BREAKTHROUGHS.md` 新条目。
- **仍待 Unicorn 实跑**：逐商品基价数值、月度波动算式、卖价系数（处理函数间接派发未定位）。
- 下一步候选：① Unicorn 实跑经济初始化/交易提取真实数值（推荐闭合经济）② 事件系统 #33/#41 ③ 地形数值 #36/#38。

## 经济深挖（续）：买价 Unicorn 实证 + 月度波动精确公式 + C++ 容器本质纠偏
- **买价公式 Unicorn 实跑验证（✅）**：`0x445ff0(base)=base×3//2`。Unicorn 2.1.4 加载脱壳映像、调 `0x445ff0` 喂 base=1..499(八例)与500/600/1000/2000/3000(五例)，eax 均精确=`base×3//2`；`x≥500` 时 `0x4ebc80(0x5a,x×1.5)` 纯净调用下不改结果。参考实现 `scripts/economy_price_probe.py`。
- **价格计算入口 `0x4e2ef0(price,mode=2/3)`**：设 `[0x52418e]=mode`→`call 0x47bd10(0x513ea8,10,price,1,-1)`；`0x513ea8` 为价格对象全局实例。
- **月度波动精确公式**：`新价=min(当前价×10/3,(30000−X)×10)`（魔数0x66666667=÷3；0x7530=30000）；分支二 `新价=min((6000−当前价)/3×10,(当前价−500)×10)`。价格上下限常数 `0x1f4`=500/`0x1766`=5990/`0x1770`=6000。月度事件码 `0xa3d–0xa52`。
- **🔶 本质纠偏**：`0x4ee3f0`/`0x4ee340` 反汇编 = **侵入式双向链表 unlink/free**（C++ 托管容器析构），非"字段读取器"；`0x489400` 实为**价格对象析构函数**。经济系统整体 = **C++ 面向对象运行时系统**（全局 `0x523d68/0x526c50/0x526c58`、vtable `0x4fcf10`、引用计数）。
- **SAVEDATA 取证验证+否定**：块0 @0x198=`TAIKOU2_SCENARIO`、195/700 武将登场、92-word 全0 → **明文不加密完整运行时态**，映射 `块内=VA−0x5178dc`。但 `0x513ea8` 及经济状态区在块0 映射 VA[0x5178dc,0x51c8dc) 之外 → 基价数值不在存档，只能 Unicorn 跑初始化/定位状态数组 VA。
- **结论**：经济架构+公式+常数+验证已齐，足以复刻框架（每城每商品维护当前价，月度按比例波动，买价=×1.5；初始基价可设默认）。建议转事件系统 #33/#41 或地形数值 #36/#38。

## 事件/场景类型系统架构实锤（#33/#41 结构完成）
- 用户"继续破解" → 主攻事件系统。
- **事件/场景类型学（27 类）**：名称指针表 `@0x50da38`，基址 `0x50da40` 索引 2..28（idx0/1占位）。覆盖 寺院/历史事件/木下藤吉郎·柴田胜家·明智光秀·新武将「主题」/京都·界镇·城内·自宅·茶会·会议·酒馆·剑术道场·教堂/行军·战斗·个人战斗/墨俣筑城·阿市的悲剧·金崎撤退战·本能寺大火/游戏开始·开始·结局。xref `0x50da40`@`0x4eafaa`。
- **查找函数 `0x4eaf70(scene_ctx)`**：读 `scene_ctx->0x72`(word 事件类型ID)→`0x50da40[id*4]`→名称→`0x4ede20` 显示（事件史屏）。
- **EventCtx 结构体**（构造 `0x4eae50`）：`+0x72`word事件类型ID、`+0x74..+0x84` 五 dword 参数。
- **事件列表配置表 `0x50dab0`**：27记录×4字节（恰 0x50dab0→0x50db1c=108B），与27类1:1；传 `0x47ad90`@`0x4eb38e`。4字节/类 = **能力位掩码字段**（值呈单比特 0x20/0x70/0xf8/0xb0/0xd0/0xd8）。bit 语义待追读表函数。
- **事件史屏 `0x4eaf00`** + 计数全局 `0x50db1c`（复位@`0x4eb09a`）。
- **个人遭遇处理器 `0x4547f0(g1,g2)`**：依 `byte[+0x21]`(年龄?比60/50)、`byte[+0x8]&0x80`、`word[+0x2c]` 决定事件，派发消息码 `0x403/0x405/0x516/0x518/0x51c/0x514/0x51a`。
- **硬编码史实事件示例 本能寺大火 `0x41c160`**：填充日期模板 `0x502638`、播信长自刃过场；经虚表间接派发（0 相对 call）。
- 交付：`scripts/event_system_spec.json`（类型学+EventCtx+配置表原始字节+处理器）、`GAME_DATA_SPEC.md §3.7`、`BREAKTHROUGHS.md` 新条目（#33/#41 打勾）。
- **仍待**：① 逐事件 handler 逻辑（虚表间接派发，需 Unicorn/虚表定位）② 配置表4字节 bit 语义 ③ 史实事件按年触发总调度（本能寺=1582 示例已得）④ 个人遭遇消息码对应 MESSAGE*.LZW 文本。
- 下一步候选：事件逐 handler 重型 emu / 地形攻防数值 #36/#38 / 战斗 section A 20 属性命名 #35 子项。

## 地形/战场要素系统实锤 + 🔶 #36/#38 旧假设纠偏
- 用户"继续破解" → 主攻地形数值 #36/#38。
- **🔶 推翻旧假设**：旧记「`0x513a78`(5B×16) 静态全0=地形攻防表，待 emu」**证伪**。`0x513a78` 实测是**战场设施标记实例表**（16槽×5B，运行时填）：类型byte[0](`0xff`=空)/等级byte[1](经 `0x4ebd10` rand设，谓词`0x43e7a0`返即战力值)/列byte[2]/行byte[3](section A 9×20网格)/有效位byte[4]。访问器 `0x43e820(id)`、查找`0x43e870`(xy)、扫描`0x43e8b0/0x43e8e0/0x43e920/0x43e950`。被26处战斗代码调用。
- **🎯 真·地形攻防修正矩阵 `0x5037b8`（静态，百分比20%–90%）**：读取点 `0x43d019 movzx cx,byte[eax+ecx+0x5037b8]`（函数 `0x43cfd0`，xref唯一@`0x43d01e`）。索引 `eax=(byte[0x5205f1]/3)*3`、`ecx`=输入(`cmp cx,3`⇒0..2)。**复刻方直接查此静态表即得地形/兵种攻防修正系数**——#36/#38 核心闭合。
- **设施名/格式串池 `0x503818`**（7B/条，`0x43e7b0(type)=0x503818+type*7`，14+调用）：已解码 本城/米仓/了望台/哨所/城门/这样；混合 `%-6s`/`%2d月`/`%2d岁` 与 UI 标签（`总大将/敌人/盟军…`）——通用"类型码→字符串"池，非数值表。
- **单位槽表 `0x513910`**（15×24B，运行时填）：访问器 `0x43e4a0(idx)=0x513910+idx*24`；单位地形类型 `byte[+0x10]`（战斗公式`0x42d270`读）。
- **方法学收获**：相对 call 搜索初版有「文件偏移 vs VA」bug（dest 与 target 比较维度错位，导致误报"0 xref→虚表派发"）。修正：target 取 `VA−0x400000`、调用点报 `0x400000+base`。此前 `0x446500/0x4e2d00/0x41c160` 等"0相对call"结论需重审（但架构结论仍成立）。
- 交付：`scripts/terrain_spec.json`、`GAME_DATA_SPEC.md §3.8`、`BREAKTHROUGHS.md` 新条目（#36/#38 打勾+纠偏）。
- **仍待**：① `0x5037b8` 矩阵精确二维语义（`byte[0x5205f1]`含义+`0x43cfd0`入参）② `0x513a78` 写入方 ③ 设施byte[1]随机分布。
- 下一步候选：① 追 `0x43cfd0` 入参钉死矩阵语义（收口#36/#38）② 势力/外交/内政数值 ③重型 Unicorn 提取 `0x513a78` 运行时设施布局。

## 天气/季节/地域气候系统全破 + 🔶 `0x5037b8` 二次纠偏（#43/#44 打勾，#36/#38 重开）
- 用户"帮我继续根据文档破解" → 主攻 `0x5037b8` 矩阵二维语义（原以为是地形攻防矩阵）。
- **🔶 二次纠偏（决定性）**：`0x5037b8` **不是**地形攻防修正矩阵，而是**天气「保持不变」概率表**。三条证据：① 用线性反汇编找到真函数起点 `0x43cfc0`（旧记 `0x43cfd0` 是中段；`_fdis.py` 喂中段地址会算出负长度静默返回垃圾）；② 索引第一维 = `(byte[0x5205f1]/*当前月*/ //3)&3` = 季节，第二维 = 当前天气(0..3)；③ 全镜像仅 1 处 xref `@0x43d01e`（就在天气函数内），伤害链 `0x42d270` 从不引用。⇒ #36/#38 重开，`0x513a78`/`0x5037b8` 两个候选皆已排除。
- **🎯 天气系统全逆向**：天气编码 `0=晴 1=曇 2=雨 3=雪`；`season=(month//3)&3`。
  - 四张概率表（值=百分比=保持原天气的概率，`rand()%100 < 值 ⇒ 不变`）：`T_SIMPLE=[90,80,40,80,60,70,90,20,50,80,60,90]`(12B @`0x503794`)、`T_SEASON=[0,0,0,70,60,50,80,20,60,60,50,70,0,0,0]`(15B @`0x5037b8`)、`T_WINTER_SNOWREG=[20,50,0]`、`T_WINTER_NORMAL=[90,40,20]`。
  - 转移函数 `0x43d060(threshold, mode)`：mode 0=非冬/简版(禁雪)、1=冬·非雪国、2=冬·雪国。
  - 两台推进器：`0x43cfc0` 简版（无地域，仅 `day_counter%4==0` 推进）、`0x43d0e0` 完整版（按国 id→气候组，雪国组=0/2/4）。
  - 战斗联动 `0x42d62e`：湿润标志 `0x51352c`≠0 且 `byte[unit+0x13]&3==2`(铁炮/洋枪) ⇒ 战力 ×2/3。
- **49 国运行时国情表 `0x519548`**（49×5B，153 处 `lea r,[id+id*4+0x519548]`，紧接 700B 武将登场标志 `0x519288`）：`byte[+1]`=气候组、`byte[+3]`=位域(`(v>>2)&0x1f`)；当前国 `word[0x524866]`；事件条件 opcode 13(指定国)/14(指定地域组) 判定器 `0x4e82c0`。静态映像全 0（运行时填充）。
- **验证**：`scripts/weather_ref.py` 4 表逐字节对映像自校验全 `[OK]`；20 年×每月4tick 蒙特卡洛涌现——雪国冬 雪84.2% / 非雪国冬 雪27.9% / 夏秋绝无雪 / 秋雨最多，与日本气候语义自洽。
- **方法学**：新建 `scripts/_lindis.py`（任意 VA 线性反汇编）规避 `_fdis.py` 只接受 call-target 函数头的缺陷 —— 历史上误判 `0x43cfd0` 为函数头即此坑。
- 交付：`scripts/weather_ref.py`、`scripts/weather_spec.json`、`scripts/_lindis.py`、`GAME_DATA_SPEC.md §3.9`(新章) + §3.8 纠偏注记、`BREAKTHROUGHS.md` 续4 条目（#43/#44 打勾，#36/#38 重开）。
- **仍待**：① 真实地形攻防表（线索 `byte[unit+0x10]`）② 49国表其余字段+填充函数→雪国名单 ③ 天气对行军/内政/收成的其他消费点 ④ `0x513534`/`0x513538` 语义。

---

## 深夜（续5）：合戦「初期布陣」+「計略」系统全破（#45/#46）

### 突破 1 — 初期布陣 `0x42c740`（调用方 `0x42c550`）
一次性摆放 15 个单位槽（`0x513910` stride 24），写 `byte[slot+0x05]`=x / `+0x06`=y（连带 `+0x07..+0x09`）。

**阵形相克矩阵 `0x5031d0`（4×4）**：`movzx cx, byte[edx+ecx*4+0x5031d0]`，ecx=byte[pA+4](攻方阵形)、edx=byte[pB+4](守方阵形) → 布阵变体 fi。
矩阵 = [1,3,0,2 / 2,1,3,0 / 0,2,1,3 / 3,0,2,1]，**闭式 `fi = [1,3,0,2][(B-A) mod 4]`（16/16 验证）= 循环拉丁方**
⇒ 结构性结论：原版 4 阵形是**环形克制**，相克只取决于阵形之差。

坐标/朝向表：
- 左军 `0x5031e0`(4×5×xy) / 朝向 `0x503208`=[1,3,2,0]
- 右军 `0x503210` / 朝向 `0x503238`=[3,1,0,2]
- 侧翼A `0x503240`=(38,8)(36,14)(14,14)(14,2)(36,2) / 朝向 `0x503258`=[3,3,0,2,3]
- 侧翼B `0x50324a`=(0,8)(2,2)(24,2)(24,14)(2,14) / 朝向 `0x503260`=[1,1,2,0,1]
- 6方向反向 `0x503268`=[3,4,5,0,1,2] ⇒ opp(i)=(i+3)%6

三条规律（逐字节 [OK]）：
1. 右军 = 左军 **180° 点对称**：x'=38-x, y'=16-y，**且单位序号置换 [0,2,1,4,3]**（左右翼互换）
2. 右军朝向 = 左军朝向 + 2 (mod 4)
3. 侧翼B = 侧翼A 点对称（无序号置换），朝向 +2
⇒ 复刻只存左军表；(38,16) 与 HJMAPDAT 战图 40×19 吻合，反证解码正确。朝向 0北 1东 2南 3西。

分支：`byte[slot0+0x15]&4` 置位→右军按变体/左军固定fi0朝向1/侧翼A；清零→左军按变体/右军固定读 `0x50321a`(fi1块)朝向3/侧翼B。`&0x10`→随机布阵 x=(rand()%10+5)*2, y=(rand()%7+1)*2，撞人(`0x423010`)重摇。

### 突破 2 — 計略系统
名表 `0x5032d8`(stride 7, GBK, 全角空格) + handler 指针表 `0x503328`，11 条全 [OK]：
鼓舞 0x435530 / 伏兵 0x435d20 / 伪兵 0x435b50 / 谣言 0x436190 / 火计 0x436710 / 开城 0x436a80 / 挑衅 0x436d20 / 落石 0x437300 / 牵制 0x437820 / 修复 0x437a60 / 填埋 0x437c90

同池合戦 UI 串：0x503270 逃之夭夭 / 0x50327c 全军覆没 / 0x503288 士气 / 0x503290 士兵 / 0x5032a8 士气∶ / 0x5032b0 (全军覆没) / 0x5032bc 受伤∶%4u人 / 0x5032c8 %-12s守备 / 0x503378 失败 / 0x503380 达成

「鼓　舞」公式（`0x435370` @0x435414–0x435486）：
```
r1 = rand() % (byte[ent+0x0a] // 2)     # ent=实体表 0x519868 47B 记录
r2 = rand() % 50
gain = max(0, (r1+r2)//4 - byte[corps+0x25]//10)
if byte[ent+0x24]==0x10 and not (byte[0x517aa5]&0x10): gain = gain*2+1
morale = min(morale+gain, 200)
```
蒙特卡洛：stat=100,f25=0 → 均值 11.83/最大 24；bonus → 24.77/最大 49。f25 每 10 点扣 1 收益，满 100 几乎废掉鼓舞（疑疲劳/已用次数）。

士気原语：`0x43db50` 读 byte[corps+0x23]；`0x43db60` 加(上限 **200**, `0x4ebcf0(cur,v,0xc8)`)；`0x43db80` 饱和减(`0x4ebd10`)；`0x43dba0` 加 `+0x25`(上限 100)。

### 纠偏 — 「計略中文名不在 EXE/不在 MSGX」推翻
計略名就在静态段 `0x5032d8`。教训：静态串池分散在多个小段（計略名与合戦 UI 串 0x503270–0x503380 同池），此前只扫了 0x506ca8/0x504800/0x507b58 几张大表就下全局结论。兵种/阵形名仍未找到。

### 纠偏 — #36/#38 第三候选 `byte[unit+0x10]` 亦证伪
`_unit_field_map.py` 全字段图谱：+0x10 只在 `0x42d270` 被读 7 次，**全镜像无经访问器 `0x43e4a0` 的写入点**；原以为写它的 `0x42c740` 写入簇实际写 `+0x05..+0x09`（布阵坐标/朝向）。⇒ +0x10 不是地形类型。三候选（0x513a78 / 0x5037b8 / +0x10）全灭。
其他字段定性：+0x13 读 11 次(兵种类别，低2位)、+0x15 读 9 次(标志: bit2 阵营/bit4 随机布阵)、+0x23 士気、+0x25 cap100、+0x24/+0x2c 标志。

### 本轮文件
- 新建 `scripts/deployment_tactics_ref.py`（布陣+計略参考实现，运行全 [OK]）
- 新建 `scripts/_unit_field_map.py`（单位结构体字段使用图谱）
- 新建 `scripts/_terrain_hunt.py`（TERRAIN_MAP/DEPLOY_MAP/UNIT_SLOTS 全 xref；坑：capstone 需 `from capstone.x86 import *` 否则 NameError: X86_OP_MEM）
- 修 `scripts/_fdis.py` 符号表：剔除两个已证伪标注（0x513a78→FACILITY_SLOTS、0x5037b8→WEATHER_KEEP_SEASON），新增 10 SYM + 10 DSYM
- 文档：`GAME_DATA_SPEC.md` §3.10 新增；`BREAKTHROUGHS.md` 续5 条目 + #45/#46 打勾

### 下一步
1. 逐个反汇编剩余 9 個計略 handler（优先 火计 0x436710、伏兵 0x435d20）
2. #36/#38 改路线：在 `0x42d270` 伤害链内逐指令追所有内存读，回溯每个读地址的填充函数
3. 兵种/阵形名：扫 `0x5030xx`–`0x5039xx` 小串池

---

## 深夜（续6）：EXE 静态段中文名表全集 11 张（#47）· 兵种名缺口闭合

### 方法论突破（最值钱的一条）
长期缺口「兵种/阵形/計略名不在 EXE 静态段」的**根因是扫描方法错**：过去只看 `0x506ca8`/`0x504800`/`0x507b58` 几张已知大表，从未做全段无先验扫描。
新建 `scripts/_string_pool_scan.py`：扫 0x500000–0x530000 全部 GBK 双字节串（**2014 条**）→ 按地址间隙 64B 分池 → 池内相邻串地址差取众数做**等距 stride 检测**（置信≥60% 且 3≤stride≤32 判定长表）→ 一次跑出 9 张疑似表，xref 定性后确认 11 张。
⇒ **固化教训**：本镜像静态串**分散在大量小池**（計略名与合戦 UI 串同池、兵种名夹在 byte 数组后），凭「表该长什么样」猜必漏。

### 兵种名（闭合 #31/#37）
```
0x43e140(unit): mov al, byte[unit+0x13]; and eax,3        ⇒ 兵种类别 cls
0x43e150(unit): call 0x43e140; lea eax,[eax+eax*4+0x50bfe8]  ⇒ 名指针 stride 5
0x43e170(unit): 显示名 = (byte[unit+0x15]&8 ? "盟军":"敌人") + 兵种名
0x4a0fa0: 另一条取名路径（cls 来自 0x4a0f80）
```
名表 `0x50bfe8` stride5：[0]步兵 [1]骑兵 [2]洋枪，[3]空；`0x50bff8` 独立串「城守备」（守城方）。
菜单串 `0x509a48` stride16×3：'  步  兵  '/'  骑  兵  '/'  洋枪队  '。
⇒ 同时给 `byte[unit+0x13]&3` 正名；cls=2(洋枪) 正是湿润天气 ×2/3 那一类。
兵种类别映射 `0x50bfd0`(24B, 访问器 `0x4a0b00`)：[0-3]=3 [4-7]=0 [8-15]=1 [16-19]=2 [20-23]=3（骑兵占 8 槽，图形变体最多）。

### 部队槽名 / 设施名（给已破结构补语义）
- `0x5037e0` stride7×5 = **总大将/第二军/第三军/第四军/第五军**。stride 由 xref 实证：`0x42fd32 shl ecx,3; sub ecx,ebx; add ecx,0x5037e0` = idx*7（另 `0x430e0d` 同型）⇒ **直接对上续5 布阵 5 槽**（slot0=总大将）。
- `0x503818` stride7×5 = **本城/米仓/了望台/哨所/城门** ⇒ 对上续3 设施实例表 `0x513a78`(5B×16)。
- `0x503808` stride8×2 = 敌人/盟军，显示选择位 `byte[unit+0x15]&8`（注意：阵营判定用 bit2，显示用 bit3）。
- 部队列名 A `0x5099d8`(5×4)=统御/武力/骑马/洋枪；B `0x509a78`(5×5)=统御/武力/兵法/马术/洋枪 → section A 20 属性命名首批线索（#35 子项）。

### 城池/国情面板 + 外交/经济附表
- 字段名 `0x50953c` stride8×10 = 士气/训练度/防御度/支持率/俸禄/军马/洋枪/士兵数/军粮/军资金（xref `0x47ccb6`，绘制循环 `0x47ca70` 行索引 bp）。
- 评价词 `0x50b6ba` stride9×24 = **8 组×3 档(低/中/高)**：缺乏·丰富·富强 / 低·普通·高 / 缺乏·足够·充裕 / 薄弱·普通·坚固 / 缺乏·充实·强大 / 缺少·丰富·无数 / 稚嫩·普通·精干 / 低·精神·勇敢。⇒ 城池概览**不显示数值**，量化成 3 档形容词。
- 说服手段 `0x502858` stride16×6 = 诱之以利/诱之以官禄/劝其报复/以亡国论劝说/一味劝说/**描述信长**（末项=剧本特化条目）。
- 金额阶梯 `0x50bfb8` word×10 = 1000,2000,3000,4000,6000,8000,10000,15000,20000,25000。
- 购买档 `0x50bfa0` 3×8B(dword id, word 金额, word 数量) = (4,5000,50)(5,10000,100)(6,30000,200) ⇒ 单价 100/100/**150**，大批量反而更贵。

### 负结果（结论性）：阵形名不在 EXE
2014 条 CJK 串中 鱼鳞/鹤翼/雁行/方圆/锋矢/长蛇/偃月/车悬/队形/布阵 **全 0 命中**；「阵」仅见剧情文 `0x50248c` 与 `0x50345c`「阵亡∶」。
⇒ **太阁2 合戦没有玩家可见阵形名**，`byte[p+4]`(0..3) 是内部布阵编号。复刻可自命名（fi=0 横一文字/fi=2 锋矢/fi=3 鹤翼）。这条把 #31/#37 的「阵形名待查」悬项关掉——不是没找到，是不存在。

### 本轮文件
- 新建 `scripts/_string_pool_scan.py`（全段串池扫描，`--tables`/`--grep`）
- 新建 `scripts/_battle_names_hunt.py`（变长串精确列举 + 绝对立即数 xref + 引用点反汇编；`-x VA...` 通用 probe）
- 新建 `scripts/_dmp.py`（快速 hexdump / 定长表列举 `-t VA stride n`）
- 新建 `scripts/static_name_tables.py` + `static_name_tables.json`（11 表自校验全 [OK] + 金额/购买档数值断言）
- 修 `scripts/_fdis.py`：新增 16 SYM（含 `0x43e140` unitTypeCls / `0x43e150` unitTypeName / `0x42c740` deployUnits / 士気原语）+ 22 DSYM（布陣/計略/名表全套）
- 文档：`GAME_DATA_SPEC.md` §3.11 新增；`BREAKTHROUGHS.md` 续6 条目 + #47 打勾 + #31/#37 改为已闭合

### 下一步
1. **#36/#38 新路线（复用本轮方法论）**：既然「猜静态表必漏」，改**穷举式**——枚举 `0x42d270` 伤害链内所有内存读地址，逐个查是否落在 `0x503000–0x504000` 小常量池，而非先假设表形状。
2. 反汇编剩余 9 個計略 handler（优先 火计 `0x436710`、伏兵 `0x435d20`）。
3. emu `0x47ca70` 绑定评价词 8 组 ↔ 10 字段。

## 续7（2026-08-27 深夜）：#36/#38 地形攻防 / 移动系数系统全破 + 文档落盘

### 突破
- **结论**：太阁2 **无独立地形攻防百分比矩阵**，地形影响 100% 走**攻击除数**（divisor 越大伤害越低，`dmg=E*7//divisor//(def//4+50)+1`），与移动消耗共用查表模板 `TAB[terrainClass]`（100=不可通行）。
- **7 张静态表（全在 EXE，逐字节自校验）**：
  - 攻击除数：`0x503770`(8B·攻城战) / `0x503778`(12B·野战) → 访问器 `0x43a9c0`（纠正旧记「按 battle_type 取 +0/+8 窗口」：实为按 terrain_cls 选表 + 设施格特例 + 高度±1 对冲）
  - 合战移动：`0x5036a0`(雪+1) / `0x5036a8`(12×2·雪+1) / `0x5036c0`(雪+2) / `0x5036c8`(12×2·雪+2) → 访问器 `0x438a60`/`0x438aa0`/`0x438af0`/`0x438b30`
  - 大地图移动：`0x503118`(9×2B·骑兵/其他) → `0x423070`；天气 getter `0x43cad0`(读 word[0x513530]，3=雪)；大地图地形类 `0x422f80`(byte[0x511358+cell])
- **方法论**：沿 `0x42d270` 伤害链穷举所有内存读地址（`_xref_reads.py`）→ 地形系数不在该链绝对寻址；对地形图 `0x512868` 全镜像反向索引（`_global_xref.py`）→ 锁定消费地形类的函数是移动/绘制簇（`0x438c60`/`0x439190`），带出整族表。
- **推翻旧注**：续3/续4/续6 写的「地形攻防未定位 / 同属运行时填充」证伪；`0x513a78`=设施标记实例表、`0x5037b8`=天气保持概率表（两候选证伪结论仍有效）。

### 文件
- 新建 `scripts/terrain_ref.py`（自校验全 [OK]：10 表逐字节 + 攻击除数/高度对冲/移动公式断言）
- 新建 `scripts/_xref_reads.py`（读地址穷举）、`scripts/_global_xref.py`（全镜像反向索引）、`scripts/_callers.py`（调用者查询）
- 文档落盘：`GAME_DATA_SPEC.md` §3.12（641–713 行）+ §3.8 三处旧注更正（327/354/768）；`BREAKTHROUGHS.md` 续7 条目（line 40）+ #36/#38 打勾 + 顶部更新。

### 仍待破
1. 合战移动 4 表「行选择」`corps_field2c` 第 2 位精确语义（行0/行1 对应哪种部队类别）。
2. 大地图 `0x51142a` 高度数组、`0x511358` 地形类数组填充来源（`0x422f80` 簇谁写入）。
3. 接续6 下一步：9 計略 handler（火计 `0x436710`/伏兵 `0x435d20`）、评价词 8↔10 绑定、section A 20 属性命名。

## 续8（2026-08-27 深夜）：#46 子项 計略效果数值模型突破（火计/伏兵 全反汇编）
- **通用架构确立**：11 計略 handler 表 `0x503328`(11×4B)，`cdecl` 两 corps 指针参数(`arg1=[esp+0x40]`作用方/`arg2=[esp+0x48]`目标方)；`0x43d630(corps)`=主将实体指针、`0x43d8a0`/`0x43d950`=对方主将实体、`0x43de30(range)`=随机敌方部队指针(range 5=5 槽)；MSGX 经 `0x47b180`(带表)/`0x47ba40`(实体,msgid,&arg)/`0x47b210`。
- **对抗 contest 核心机制**：每計略算 `atk`(RNG+属性,`/3200`) 与 `def`(目标实体属性滚动)，**仅 `atk < def` 才生效**否则 0 伤亡 → 即「计略有失败率」数值根因。
- **火计 `0x436710`→效果 `0x436840`**：`atk=(rand%40+80)*(byte[corps+0xa]+15*(byte[corps+0x10]&3))/3200`；`parity(0x43cab0)`→+30、`flag(0x43cb10)`→+20、`getHi(attacker)>getHi(target)(0x4390c0)`→+50；MSGX `0xe0`–`0xee`；前置 `[0x511dfc]==2` 走埋火结算(雪表`0x503548`/非雪`0x50354c`,`0xe0`，`[0x511e07]`→`0xe6`)。
- **伏兵 `0x435d20`(分发,[0x511df9]==2 走 `0x503540[index]` 消息即返)→`0x435d50`(真身)→`0x435ed0`(效果)**：`atk=(rand%80+50)*(byte[tgt+0xa]+25*((byte[tgt+0x10]>>2)&3))/3200`；`battle_type==0 && parity`→+20；位置修正表 `0x5176a8`(20×4B,待dump)；MSGX `0xd3`–`0xd8`。
- **仍未知**：`byte[corps+0xa/0x10]`/`byte[ent+0xa/0x10]` 字段语义与绝对量级(疑似攻/防或统率/兵法)→需 Unicorn 实跑校准；火计 `def` 后半段(`0x4368b1`起`rand%0x64`+实体属性)未展开；其余 8 計略(伪兵`0x435b50`/谣言`0x436190`/开城`0x436a80`/挑衅`0x436d20`/落石`0x437300`读图形标志调`0x47b350`/牵制`0x437820`/修复`0x437a60`/填埋`0x437c90`)效果数学待按同模板提取。
- **交付**：`GAME_DATA_SPEC.md` §3.10.7(火计/伏兵打勾)+§3.10.8(完整数值模型)；`BREAKTHROUGHS.md` 续8 条+顶部更新；反汇编证据全来自 `scripts/_fdis.py`。
- **下一步**：复用 `_emu_battle.py` Unicorn 框架 hook `0x436840`/`0x435ed0` 抓 `atk/def` 与字段实值；或流水线提其余 8 計略。

## 续10（2026-08-27 深夜）：#46 計略模型「除数 /3200」纠偏 + 挑衅/其余 8 計略结构
- **🔴 除数纠偏（关键，推翻续8）**：計略乘积收尾 `mov eax,0x51EB851F; imul; mul; shr edx,5` → `0x51EB851F` 是编译器 `n/100` 惯用 magic（`>>37 ≈ n/100`）。`shr 5` 属 `/100` 惯用式本身，**非额外 ÷32**。续8 写的 `/100/32=/3200` 是误读。真实除数 = **`/100`**（量级：atk 80–230 / def 160–350，多生效少落空，与「计略有失败率」吻合）。
- **🔴 火计系数 15→30**：`lea eax,[eax*3]→[eax*5]`(×15)→`lea ecx,[ecx+eax*2]`(×2) = ×30。伏兵系数 25（续8 正确）、挑衅系数 30（与火计同，b 取 `(byte>>2)&3`）。
- **挑衅 `0x436d20`→`0x436b80` 全反汇编**：`atk=(rand%40+80)*(a+30*b)/100`；`def=rand%50+byte[ent+0xa]+30*((byte[ent+0x10]>>2)&3)+50*((word[ent+8]>>10)&1)`，`byte[corps+0x2c]&0x80` 时 def×1.5；`atk<def` 生效并 `0x499810` 驱动敌军行动；另 `byte[actingCorps+0x2b]&0x3f==atk` 门控；`0x43de50(arg)` 专用选敌。
- **开城/落石复用**：开城直接 call `0x436840`(=火计燃烧)；落石成功后代调 `0x436b80`(=挑衅)。
- **伪兵 `0x435b50`→`0x435570`(走 `0x43dd50` 取坐标→`0x43a440` 造兵原语,非伤害)/谣言 `0x436190`→`0x435d60`→`0x435280`(→`0x43cf10` 通用落地,状态/士气类)/牵制 `0x437820`→`0x437910`/修复 `0x437a60`→`0x437ad0`/填埋 `0x437c90`→`0x437d50`**：handler+效果子程+MSGX 全部定位。伪兵=造兵、谣言=状态、牵制/修复/填埋=状态/地形类。
- **交付/文档**：`GAME_DATA_SPEC.md` §3.10.7(待破更新)+§3.10.8(除数/系数订正+挑衅+开城/落石/伪兵/谣言/牵制/修复/填埋)；`BREAKTHROUGHS.md` 续10 纠偏条(倒序置顶)+顶部更新；证据 `scripts/_d1.txt/_d2.txt/_d3.txt`(`_fdis.py` dump)。
- **仍未知**：4 字节字段语义(统率/武力/兵法 or 兵数派生)→需 Unicorn 抓实值；伪兵造兵量/谣言落地/牵制·修复·填埋数学→需实跑 `0x43a440`/`0x43cf10`/各效果子程。
- **下一步**：搭 Unicorn hook `0x436840`/`0x435ed0`/`0x436b80` 坐实字段语义与量级；或转非战斗线(towns.json 6 城名校正/技能→师父授艺表)。

## 续11（2026-08-27 深夜）：計略 contest 字段语义 + 绝对量级 Unicorn 双证
- 火计/伏兵/挑衅 Unicorn 实跑（`scripts/_emu_tactic.py`，全桩安全化 + hook 抓 atk/def）：`byte[corps/ent+0xa]`=**commander_stat**、除数=`/100`（magic `0x51EB851F`→>>37）、系数 火计30/伏兵25/挑衅30 + 火计 def +2 基差；atk/def 实测 (135,220)/(90,160)/(135,160) 逐位吻合。实体解析 `0x43d630`=`0x519868+47*idx` 实跑校验。
- 交付：§3.10.7 待破项打勾、§3.10.8 加「Unicorn 校准印章」实测表。

## 续12（2026-08-27 深夜）：其余 5 計略机制 + 静态数据表 dump
- 伏兵位置修正表 `0x5176a8`（32×4B）全零 → no-op（§3.10.8 可删）。
- 伪兵 `0x435570`：作用方 `(col,row)` 周围按 `0x503712`(造兵类型 {0,1,0xffff}) + `0x503710`([0,1,1,0,-1,-1,0,1]) 循环造 Dummy（≤6 落点）。
- 谣言 `0x43cf10`：4 次循环 `0x438c60`+`0x4a0c60(byte[0x5037b0+k])`；`0x5037b0`=[0x06,0x04,0x02,0x01]（4 个递减标志位）。
- 牵制 `0x437910` mag 公式（静态提取，续13 实跑坐实）。
- 修复 `0x437ad0`：成功门控 `roll=(rand%50)+value/5+byte[源+0xa]`，`roll>=120(0x78)` 成功；消息索引表 `0x503560`=[270,271,0,0,11031,10515,10000,8973,8203,7178,5899,4878,4114,3350,3098,2589]。
- 填埋 `0x437d50`=**封城战报**（非改图）：内联 GBK 格式串 `0x50360c`="%s    %6s封 对 %s"、`0x503620`="%4u年%2u月%2u日"、`0x503634`="      第%2u日"、`0x5035fc`="%12s  功勋！%5u" 等。
- 交付：`scripts/_extract_tactic_tables.py`+`tactic_tables.json`、`_verify_tables.py`、6 个 `_dis_*.txt`、`GAME_DATA_SPEC.md §3.10.9`。

## 续13（2026-08-27 深夜）：牵制 magnitude Unicorn 双证 + 两坑（方法论必记）
- **牵制 mag 实跑坐实**：`mag = ( troopDecayCurve/5  +  50*(byte[目标+0x10]>>6)  +  byte[源+0xa] ) / 10`。
  - `/5`=`0x66666667`+`sar edx,1`；`/10`=`0xcccccccd`+`shr edx,3`（**非** ×4/5，§3.10.9 曾误记，已纠偏）；`byte[+0x10]>>6`=目标 tier(0..3)×50；`byte[源+0xa]`=commander_stat。
  - 6 组受控用例全中（V,tier,S→mag）：(100,0,150)→17/(100,3,150)→32/(250,0,150)→20/(100,0,50)→7/(200,1,100)→19/(50,2,200)→31。脚本 `scripts/_emu_pin.py`。
- **🔴 坑1（`_fdis.py` 误反汇编）**：`_fdis.py` 把 `0x437922` 错反汇编成 `mov edi,eax`，真实是 `mov ecx,edi`（目标指针存于 `[esp+0x10]`，edi 始终=arg1）。→ **凡依赖 _fdis 的函数头，关键指令务必用 capstone 直接抠原始字节复核**。
- **🔴 坑2（Unicorn 翻译块缓存）**：同一 Uc 实例内反复 `mem_write` 改「代码段」不生效（旧翻译块缓存），导致 V 这类变参桩一直返回首值。→ **变参要走「数据内存」（如 `0x7f000000`），代码只 patch 一次、每次只写数据**，避开缓存。
- 仍待破：修复 roll 公式里 `value`(=byte[源+0x24]) 是否真参与（`value/5` 项疑似误植，需 emu 复核）；谣言 `0x4a0c60` 标志位语义；伪兵实际造兵数；section A 9×20 属性命名（#35 子项，最大未知）。

## 续14（2026-08-27 深夜）：修复 roll Unicorn 双证 + 谣言/伪兵落地定性澄清
- **修复 `0x437ad0` 成功门控 roll 实跑坐实**：`roll = rand%50 + byte[源corps+0x24]/10 + byte[源entity+0xa]`，`>=120` 成功。受控 6+ 组全中（斜率 dRoll/dVal≈0.1000 → 确为 /10），**证伪续12 所记 `value/5`（实为 /10，向零取整）**。脚本 `scripts/_emu_repair.py`（复用 `_emu_pin.py` 的 V 走数据内存防块缓存范式）。
- **谣言 `0x43cf10` / 伪兵 `0x43a440` 落地定性澄清（方向纠偏）**：二者均经 `0x438c60`（768B+ 部署函数）写 `0x512b60`(DEPLOY部署图)/`0x512868`(TERRAIN_MAP)/`0x512e58`(SECT_A)，属**部署·单位操作类**；`0x4a0c60` 实反汇编 = 6/4/2/1 递减**延时循环**（÷3 magic 算 `flag/3` 次调帧同步 `0x4f1ef8`/`0x4f2061`），**非状态标志位**。纠偏旧「降士气/倒戈/标志位」猜测（旧误读 `0x5037b0`=[6,4,2,1] 为状态标志）。
- **文档**：`GAME_DATA_SPEC.md §3.10.9` 修订（修复 `value/10` 双证章 + 谣言/伪兵「部署·单位操作」定性 + 删旧「标志位/降士气」误述 + 顶部导语/校准路线更新）；`BREAKTHROUGHS.md` 续14 条目（倒序置顶）+ 顶部更新。
- **下一步**：立 #48 专项逆 `0x438c60`（部署簇核心，含 `0x5133d0` 表语义 + 部署/地形字段精确写入量）彻底闭合谣言/伪兵落地数值；或转 section A 20 属性命名(#35 子项)/非战斗线(towns.json 6 城名校正·技能→师父授艺表)。

## 续15（2026-08-27 深夜）：#48 `0x438c60` 完整逆向 + 谣言落地 ±4 双证 + 续14 结论修正
- 用户"继续破解" → 主攻 #48：部署/单位编辑原语 `0x438c60`（768B+）完整逆向 + 谣言/伪兵落地数值闭合。
- **`0x438c60` 真相重构**：非「谣言专用部署函数」，而是**通用单格部署/单位编辑原语**。**15 处调用方**：6×计略 handler(`0x436xxx`–`0x437xxx` 部署写入簇) + 6×`0x43bxxx` + 3×谣言`0x43cfxx`（全局 rel32 反查实得）。
- **谣言落地数值 Unicorn 双证（`scripts/_emu_rumor_land.py`，4 用例 ALL PASS）**：写块 `0x438cd6–0x438d4f` 恒向 DEPLOY 图 `0x512b60`、单位缓冲 `0x512b88/0x512b89`、TERRAIN_MAP `0x512868`、SECT_A `0x512e58` 写入；写入量**恒 ±4**（无缩放）；四步门控：① 坐标偏移∈有效 ② parity(`0x43cab0`)==0 ③ `getLo(sectionA,a=B4,c=B3)==11` ④ 军队类 token 门控（`arg5==0` 查左军类 `{'/','1','7','9'}` / `arg5!=0` 查右军类 `{'+','-','3','5'}`）。
  - index 公式 `index_of(B3,B4)=((B3<<1)+40*((B3&1)^1)+80*B4)&0xffff`；左军字符 arg5==0 写 `−4`（`'9'→0x35` 右邻同 −4）；右军字符 arg5!=0 写 `+4`（`'+'→0x2f`）。
  - 谣言 4 轮主循环 + 换位 + 末次 = **9 次部署格编辑全 arg5==0→左军 −4→单位换边(token 左↔右翻转)+实力 −4**，即**兵变/倒戈**。
- **🔶 修正续14「非倒戈」笼统结论**：续14 称谣言「非降士气/倒戈」，本会话据实修正——确为**倒戈/换边**效果（通过改写部署 token 类别实现，非改士气数值条），方向与旧「降士气/倒戈」猜测一致、与续14 措辞矛盾。
- **🔶 `0x5133d0` 纠偏**：dump 全 256B 为零，实为 `0x438c60` 构造战报串用的**零初始化报告缓冲**，非数据表（任务表述与前序误称「表」）。
- **4 错误修复**（`_emu_rumor_land.py`）：UC_ERR_MAP（删重复 mem_map，DEPLOY/缓冲在 BASE 映像内）/UC_ERR_INSN_INVALID（EDIT_END 0x438d52→0x438d51 并 `reg_write EBX=UBUF_A+esi`）/+4 期望误写（`'+'`+4=`0x2f` 非 `0x2d`）/UC_X86_REG_EBX 漏导入。
- 交付：`scripts/_emu_rumor_land.py`、`GAME_DATA_SPEC.md §3.10.9`（顶部导语+谣言段重写+校准状态行+#46 任务清单 共 4 处 Edit）、`BREAKTHROUGHS.md`（顶部更新+`[x] #48`+倒序续15 四段条目）。
- **下一步 (A)**：转 **section A 9×20 属性命名（#35 子项，最大未知）**——emu 跑 `0x439050`/`0x4390c0` 访问器，逐 (a,c,返回值) 与调用上下文定名。(B) 或转非战斗线（towns.json 6 城名校正 / 技能→师父授艺表）。(C) 或补 填埋 `0x437d50` 改图精确量。

## 续16（2026-08-27 深夜）：#49 section A `0x512e58` 结构全破
- 用户"继续破解" → 主攻 #49（续15 下一步 A）：section A 9 类×20 属性命名。
- **索引公式纠偏**：旧记 `getLo(a,c)=buf[a*20+c]` 写反；真实 `getLo(a,b)@0x439050 = SECT_A[a + 20*b] & 0xF`、`getHi(a,b)@0x4390c0 = >>4`（`a`=`[esp+4]`=列 0..19、`b`=`[esp+8]`=行 0..8）。`setLo(a,b,v)@0x439080` 置低4位=`v&0xF`（异或序列保高4位）。
- **维度锁 20 列 × 9 行 = 180B**（0x512e58–0x512f0c）：边界检查包装器 `0x438b80` 查表前 `cmp cl,0x14`(a<20)+`cmp al,9`(b<9) 实证；SECT_A 仅 180B 故 b 不可 >8。
- **每格双 nibble 语义**：低 nibble = 地形/移动 class（0..15），移动子系统 `0x438a60/0x438aa0/0x438af0/0x438b30` 直接索引合战移动表 `0x5036a0/0x5036a8/0x5036c0/0x5036c8`（含 `0x64`=不可通行）；高 nibble = 火计战斗修正档（`0x436840` 内 `getHi(att[0],att[2])` vs `getHi(tgt[2],tgt[3])` 比较，攻>守则 +0x32=+50）。
- **调用面全枚举 42 点**：getLo×29 / getHi×11 / setLo×2（仅 2 写：0x42de00 单位初始化 `setLo(esi[2],esi[3],0)`、0x437ad0 修復）。全局 xref 仅 4 处绝对引用 0x512e58 且全在访问器内 ⇒ 表只经 3 访问器读写。
- **🔴 纠偏 `0x439130`/`0x439150`**：读的是 **DEPLOY 图 `0x512b60`**（`byte[col+40*row]`，stride 40），**非 SECT_A**。
- **索引 (col,row) 系派生而非常数**：由单位结构体字段(offset 0/2/3) 经 `0x43a440(x)=word[x*4+0x503712]`（伪兵造兵类型表）、`0x43a420`/`0x43a410` 派生（典型 `6*unit[0]` 线性组合）；无调用点常数 ⇒ 命名须 emu 索引派生函数。
- 交付：`scripts/_sectA_xref.py`/`_sectA_args.py`/`_decode_names.py`/`_dis_args.py` + 多份 `_dis_*0x*.txt`；`GAME_DATA_SPEC.md §3.13` 新增专节；`BREAKTHROUGHS.md` 续16 条目 + 任务清单 #35 子项两行修订 + 顶部更新；`_fdis.py` SYM/DSYM 校准（索引公式 + 新增 4 符号注释）。
- **仍待破（命名）**：9 类/20 属性人类可读名 = 逆向 `0x43a440`→`0x503712` 派生（已知兵种喂入回收 (col,row)）；SECT_A 运行时值表（静态全 0，须 Unicorn 跑部署链）。下一步 (A) 立 #49 子专项 emu 派生函数命名 / (B) 非战斗线 / (C) 填埋 0x437d50 精确量。

## 续17（2026-08-27 深夜）：#49 子专项「命名须 emu 喂兵种回收 (col,row)」证伪 · 真实机制厘清
- 用户"继续破解"+摘要续接（续16 下一步 A）→ 立 #49 子专项：emu 跑 `0x43a440`/`0x43a420`/`0x43a410` 索引派生函数命名 9 行/20 列。
- **🔴 核心假设证伪**：续16 设想「命名须 Unicorn 喂兵种回收 (col,row)」——**不通且方向错**，真实机制四证：
  - **① 值直取 `HJMAPDAT.DAT` §A**：加载器 `0x43a490` 开 `0x5036f0`="C:HJMAPDAT.DAT"→`call 0x4802e0` 读→3×`call 0x4411b0` 写 `0x512e58`(180B)/`0x512868`(760B)/`0x512b60`(760B)。值已在 `hjmapdat_battles.json`，**无需 emu 取数**。
  - **② 索引全调用方派生**：对 getLo×29+getHi×11 全部调用点向后切片 EAX(col)/ECX(row)，**0 个常量索引站点**，col/row 全 `arg:4`/`arg:8` 调用方传入 ⇒ 不存在「已知兵种→(col,row)」集中函数（`scripts/_trace_const_idx.py`）。
  - **③ 三助手纯表查非计算**：`0x43a410(x)=byte[x+0x512f28]`（读**并行运行缓冲** `0x512f28`，备战 `0x43a080` `memset 0xff` 180B，**非主名表**）、`0x43a420(x)=word[x*4+0x503710]`(DIR8 静态)、`0x43a440(x)=word[x*4+0x503712]`(SPAWN_TYPE 静态)。emu 实测 `0x43a410` 静态返0/填0xAB返0xAB（证读0x512f28），`0x43a420/440` 表查正确（`scripts/_emu_deriv.py`）。
  - **④ 高 nibble 38 战恒 0 → 火计 +50 路径死**：`getHi` 恒0 → 火计 `0x436840` `cmp cl,al; jbe` 恒跳 → +0x32(=+50) 永不触发（发布数据死代码）。
- **🔴 §A 值随剧本调参**：38×9×20 低 nibble 各类均值~1.0、跨战 std~1.5 → 非固定兵种属性，**不能靠值聚类命名**（`scripts/_analyze_sectA_naming.py`）。
- **🔬 9 类 ≠ 3 显示类**：`0x50bfe8`=步兵/骑兵/洋枪(仅3大类)；SECT_A 9 行是更细内部单位分层。纯串扫描命中 3/4/7 项名表：`0x50bfe8`(3)、`0x5099d8`=统御.武力.骑马.洋枪(4维)、`0x5033e0`=马术.筑城.忍术.洋枪.兵法.统御.武力(7技能)（`scripts/_search_soldier_names.py`）——**无 20 项主名表**。
- **交付**：`scripts/_emu_deriv.py`/`_trace_const_idx.py`/`_analyze_sectA_naming.py`/`_search_soldier_names.py`/`_dump_deriv_tbls.py`/`_dis_loader.out`(`0x43a490`)/`_dis_mvm.out`(`0x43a080`)/`_dump_deriv.out`；`BREAKTHROUGHS.md` 续17 倒序四段条目 + 任务清单 #35 子项 `[ ]`→`[x]` 改写方法论 + 顶部日期→续17；`GAME_DATA_SPEC.md §3.13.5/§3.13.6` 据纠偏重写（值源=HJMAPDAT §A 直取、索引调用方派生、三助手纯表查、高 nibble 死、命名走调用链+名表）。
- **命名方法论已定（弃用 emu 喂兵种法）**：走「调用链追踪（哪单位字段→(col,row)）+ 静态名表交叉」。下一步可转 (B) 非战斗线（towns.json 6 城名校正 / 技能→师父授艺表）或 (C) 补 填埋 `0x437d50` 改图精确量。

## 续18（2026-08-27 深夜）：#46 子项 — 填埋/封城战术「改图」假设证伪 + 真实机制全破
- 用户"继续破解" → 选 (C) 填埋 `0x437d50` 精确量。推翻续15/续17「改图精确量」旧称。
- **战术 handler 表 `0x503328` 槽位定位**：`[8]=0x437820`(牵制)/`[9]=0x437a60`(修复)/`[10]=0x437c90`(**填埋**)——全经表间接分发（.text 无 E8 直调点）。⇒ 填埋 handler = `0x437c90`。
- **handler `0x437c90` 机制**：① `byte[0x511e02]==2` → 发消息 `0x503560[byte[0x511e0d]]`，无状态改变；② 否则目标 `esi=[0x513528]`（`0x43ca40`）；③ 条件 `0x43c6f0(0x5133d0,esi)`（`[+0xa2]` memset 0xb4→`0x43a440`/`0x43a420` 派生(col,row)→边界检查 col<0x14/row<9→返回格状态）；④ `ax>=2` 分配 `0x493500(3,7,0x110)`→`0x435100`（返回非0则跳过），`ax<2`(或分配失败) 释放 `0x511d68`/`0x5119e8` 后**施加效果 `0x437ad0`**（全局 `0x513470`/`0x513471`）。
- **效果 `0x437ad0`（填埋真实效果，已全反汇编）**：目标 `field[0x25]+=6`(cap100,`0x43dba0`)；置标志 `0xe`(14,`0x4997c0(0xe)`)；`roll=rand%50+byte[目标+0x24]/10+commander_stat[+0xa]`（`0x66666667` magic=`÷10`向零，坐实续14 的 `/10`，证伪续12 `/5`）；`roll>=120(0x78)` 成功播 `0x115`，否则 `0x114`+`0x499810` 敌军行动。
- **封城战报 `0x437d50`（纯报告，经包装 `0x437d30` 调用）精确量坐实**：模板 `0x50360c`="%s    %6s封 对 %s"、`0x503620`="%4u年%2u月%2u日"、`0x503634`="      第%2u日"、`0x5029b8`="城"、`0x503644`="家  "、`0x5035fc`="%12s  功勋！%5u"、`0x503630`="封"；**第N日=(`0x43ca60()`>>1)+1**、**功勋=`0x43d710(目标城)&0xffff`**、**日期=`0x5205f0`(年+0x618)/`0x5205f1`(月=CUR_MONTH)/`0x5205f2`(日)**。
- 🔴 **纠偏续12/续14 战术↔效果映射**：填埋 handler `0x437c90` **直接调 `0x437ad0`** ⇒ `0x437ad0` 是**填埋效果**（续14 实跑的「修复」实为它）；修复 handler `0x437a60` 实际调 **`0x437910`**（其内 `0x4997c0(0xd=13)`+`troopDecayCurve(0x43cd10)`+magic，形似牵制 magnitude 公式）。⇒ 修复/牵制/填埋 三者↔效果函数归属乱，续12 的"修复=0x437ad0"误归因，需续18 下一步 emu `0x437910` 重审。
- **交付**：`scripts/_fdis.py`（反汇编 `0x437c90/0x437a60/0x437820/0x437ad0/0x437910/0x43ca40/0x43c6f0/0x435100/0x437d50/0x437d30`）+ 表槽 dump；`BREAKTHROUGHS.md` 续18 倒序四段 + 顶部更新；`GAME_DATA_SPEC.md` §3.10.x「修复/填埋」两节重归因（修复→0x437910 待解、填埋效果=0x437ad0+报告0x437d50）。
- **仍未知**：`0x437910`(修复效果) 真公式 / `0x437d50` 触发路径(.text 无 E8 直调→疑战役结算屏间接调) / `0x435100`+`0x493500` 分配语义。下一步 (A) emu `0x437910` 坐实修复效果并闭合「5 计略精确量级」最后缺口 / (B) 非战斗线 / (C) 回 SECT_A 命名。
