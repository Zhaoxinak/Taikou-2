# 2026-08-28 工作日志

## 续60 — #44 49国表字段闭合 + SNDATA XOR 流地图

### 突破
- 填充器 `0x47e3a0` @流+27052；字段 +0/+1/+2/+3..+4 全钉；雪国 36 名单。
- XOR 流主段地图（370/200/49/49ext…）；明文 rec23–830 非主层。
- `province_spec.json` / `province_ref.py` 自检 PASS。

### 下一步
- `0x47e440` 11B 扩展；或 200 城町字段；或 `0x513ea8`。

---

## 续35 — 物品池种子 EXE 工厂否定

### 突破
- **静态否定**：无 category/level 空槽工厂；仅 vtable init + 扫描/绑 owner。
- `_analyze_item_seed_negative.py` **PASS**；`setFlags`×45 从不写池 `+5`。
- 换月 hub **无** `0x513ea8`（店面改价另线）。

### 仍未知
- MSGX/另存档如何灌 +5/+8；店面月度写点。

### 下一步
- `0x513ea8` 写路径；或开发 UI；或 MSGX dump。

---

## 续34 — 生产率月增 + 农商等级软顶/开发

### 突破
- **`0x4a5b50`**：`delta=max(1,stat/5−(2×tile+5))`；非领有−15。
- **软顶 `0x49f9b0`**：`max(1,城种基档×[+9]×4/3)`。
- **开发 `0x4aa290`**：`grow=(内政−30)/10+1`；朝软顶 `4a32a0` 加算。
- emu **33/33 PASS**。

### 下一步
- 池种子；或店面月度价；或开发 UI 链。

---

## 续33 — 月度城产公式（生产/饥荒/米→军粮）

### 突破
- 字段：`+0xc`农商 / `+0xe`民心 / `+0xf`生产率 / `+0x10`军粮 / `+0x12`米 / `+0x14`金 / `+0x1a`次级民情。
- **`0x4a5c80`**：`prod=(c×f)/2`；米+prod/4；金+prod；维持 `军粮/20`；饥荒降民心+扣军粮+金清零。
- **`0x4a5d80`**：1 米→10 军粮；民心按地形混合。
- emu `_emu_town_month.py` **20/20 PASS**。

### 下一步
- `0x4a5b50`；或池种子；或店面月度价。

---

## 续32 — 「商业指令」证伪 + 调度筑城天数 + 月度城产 hub

### 突破
- **证伪**：`0x504658`「商业」= 设施名表；`0x459456` ≠ 城商业值加成。
- **调度筑城**：`days=(special?12:14)−player[+6]`；写入 `player[+0xa]`；城种特殊=`[+0x1b]&7∈{2,3,6}`。
- **月度**：`0x4a0d50`→`0x4a4c60`→`0x4a5aa0` 城产月结（字段 setter 已钉）；店面基价月度仍开。
- emu **21/21 PASS**。

### 下一步
- 闭合 `0x4a5c80`/`0x4a5d80` 公式并对面板名；或池种子；或 `0x513ea8` 改价。

---

## 续31 — 卖出/购入宝物计价

### 突破
- **卖价** `0x458000`：`base=getValue/10`；普通随 prog 降，南蛮随 prog 升；成交×10 金。
- **买价** `0x44f4e0`：`base + base×(100−prog/2)/100`。
- 米市 gold 除数纠偏为 **/10**。emu **16/16 PASS**。

### 下一步
- 真月度改价 / 池种子 / 商业指令。

---

## 续30 — 茶具卖价 0.9 + 米市投机（纠偏月度波动）

### 突破
- 茶具：**卖价 = 买价 × 0.9**；出价随 `player[+7]` 降。
- **`0x4e2ce0` = 米市买卖 UI**（非月度改价）；买/卖数量上限公式闭合。
- emu `_emu_economy.py` **17/17 PASS**。

### 下一步
- 真月度物价 / 一般卖宝物 / 池种子。

---

## 续29 — 授艺 mode≥3 五维属性增长

### 突破
- **`0x45feb0`/`0x4600d0`**：剑术→武力、读书→内政/统御、艺术→魅力、宝物→外交；`+0xa..+0xe` 封顶 100。
- 概率：`base∈{16,20}-sub`；有/无师父、tier 匹配三档公式已闭合。
- emu **54/54 PASS**。授艺增长线闭合。

### 下一步
- 经济基价 / 池种子。

---

## 续28 — 授艺天数/花费/级数 + 茶具售价纠偏

### 突破
- **天数**：玩家自选；`max = min(31-日, 金/2)`；每天扣 **2 金**（`0x45f1e0`/`0x45f2b0`）。
- **增长**：`0x45fca0`；mode0/2 概率 `1/(16-sub)`；mode1 `attr_sum/600`；成功威名 `+lv×500`。
- **10 技能 2bit** @ `NPC[+0xf..+0x11]` 坐实。
- **纠偏**：`0x4422b0`=卖茶具价公式；`0x51662e`=金钱非日历；资格 fn=`0x4b5620`。
- emu `_emu_teach.py` **36/36 PASS**。

### 仍未知
- mode≥3 完整概率；内政菜单名 vs 口才位名对应。

### 下一步
- `0x45feb0` 全分支；或经济基价 / 池种子。

---

## 续27 — 物品池生命周期纠偏（无空槽工厂）

### 突破
- **证伪** first-fit 空槽工厂：FFFF 比较全是 skip，无 fill。
- **`0x4a3960(owner)`** 批量释放；`0x4a4115` owner 重映射。emu **23/23 PASS**。
- 生命周期 = 预置/转让/激活，非分配。

### 仍未知
- category/level 首次种子写入路径。

### 下一步
- 跟新局种子；或授艺公式 / 经济基价。

---

## 续26 — SECT_A↔deploy 空间映射 + 物品槽绑定

### 突破
- **空间公式**：`off=(col<<1)+40*((col&1)^1)+80*row`；38 场 **99.5%** 命中非空部署格。
- **物品槽**：空闲 `+6==0xFFFF`；`NPC[+0xb]`=池索引；不在存档窗 `[0x5178dc,0x51c8dc)`。

### 仍未知
- 空槽分配+写 category/level 的完整工厂。

### 下一步
- 跟首次 store 到 `0x51e1f0[slot]`；或叠图可视化。

---

## 续25 — 0x51e1f0 物品/技能对象池模型

### 突破
- **对象池**：200×10B，vtable `0x4fc0e0`（init `0x47a390`）；辅池 20×12B。
- **字段**：+5 level、+6 owner_key、+8 flags；类别名 酒/书籍/道具/财宝/武器/南蛮物/美术品/茶具。
- **getValue** 按类公式；`_emu_item_pool.py` **19/19 PASS**。
- MSGX `0x370`=#880「那好，给我吧。」=给物对话。

### 仍未知
- 空槽首次创建工厂；存档序列化。

### 下一步
- 跟对象创建路径 / 存档块；SECT_A↔deploy 空间对齐。

---

## 续24 — 0x43daf0 锁定计时 + 技能填流 MSGX 链 + deploy 翼带

### 突破
- **corps+0x19**：per-slot uint16 行动锁定；`0x43daf0`/`0x43db20` cap `0xea60`。`_emu_corps_timer.py` 10/10 PASS。
- **技能流**：`0x442010` 只读；填流 = MSGX `0x1362`(进町) / `0x370`(授艺) → `0x44e020`/`0x442010` 重建。
- **deploy↔tier**：翼带共现 ~89–94%，非 1:1。`scripts/deploy_tier_map.json`。

### 仍未知
- MSGX 字节码写字段细节；进町运行时 dump。

### 下一步
- Unicorn 进町 dump `0x51e1f0`；SECT_A 与 deploy 图空间对齐。

---

## 续23 — (D) 修复 0x4ebcd0 + (E) 技能扫描流格式

### 突破
- **0x4ebcd0(a,b)**：`a/b` = 设施槽 `byte[1]` 修复后/前等级；成功=等级上升。`_emu_repair_facility.py` 10/10 PASS。
- **0x443d80 扫描流**：200×10B @0x51e1f8 格式闭合；`_emu_skill_registry.py` ALL PASS。

### 仍未知
- 谁填充 0x51e1f8 真实内容（0x442010 链）。

### 下一步
- 跟填流调用链 / SECT_A tier↔deploy。

---

## 续22 — SECT_A 命名 (C)：`sectA_spec.json` 首版

### 突破
- **实体绑定闭合**：大将/部队 `byte[0]=col`、`byte[2]=row`；单位槽 `+5=map_x`、`+7=map_y`。
- **29 getLo 调用点分类**：move(4) / atk_div(1) / threshold(8) / spawn 派生等。
- **命名首版**：9 行=内部部署 tier；20 列=调参槽（无 EXE 主名表）；格值=`terrain_class_idx` 0..7。
- 交付：`scripts/sectA_spec.json`、`scripts/_build_sectA_spec.py`、`scripts/hjmapdat_units.json`。

### 仍未知
- tier ↔ deploy ASCII token 1:1；`0x512f28` 运行时填充规则。

### 下一步
- (D) 0x4ebcd0 a,b；(E) Unicorn 进町 dump `0x517850`。

---

## 续21 — 非战斗线 (B)：towns.json 城名校正 + 技能→师父授艺表

### 突破
- **towns.json 6 处校正**：泷川→泷山、兴泽→兴津、二俁→二俣、长篠→长筱、岐阜→稻叶山、大阪→本愿寺（`scripts/_patch_towns_exe_names.py`）。
- **skill_master_table.json 首版**：10 技能 MSGX 路由 + 33 师父名池 + 注册表架构（`0x517850` 12B / `0x51e1f8` 200×10B / `0x443d80`）。
- 工具：`scripts/_build_skill_master_table.py`。

### 仍未知
- 逐城 NPC spawn → 技能类映射（运行时池静态全 0）。
- 授艺天数/级数增长公式。

### 下一步
- (C) SECT_A 命名；(D) 0x4ebcd0 a,b；(E) Unicorn 进町 dump 运行时池。

---

## 续20 — 牵制 `0x437590` 全破（5 计略闭合）

（见 BREAKTHROUGHS.md 续20）
