# 2026-09-01 工作日志

## 本轮突破（承接续185，继续「把剩下的全部破解」）

### 续186 — P1 合战模式标志玩法含义（✅ 已破）
- 5 个全局战斗标志 setter/getter 几何 + 值语义 Unicorn **20/20** 验证（`scripts/battle_mode_flags_ref.py`）。
- setter：`mode_m1`@0x42c140(0x511bf8,dword) / `mode_m2_a`@0x43cb20(0x51352c) / `mode_m2_b`@0x43cfc0(**xor [0x51352c],1** 翻转 bit0，前置 rand100+2×0x43cad0 条件) / `parity_a`@0x43ca70(**min(arg,X)**,X=0x43ca30) / `parity_b`@0x43ca90(**cur+1 钳顶**) / `battle_type`@0x43ca20(0x513548,byte) / `handle_stat`@0x43cb70(0x513534,dword)。
- getter：`mode_m1`@0x42c151 / `mode_m2`@0x43cb11 / `parity`@0x43cab1（读址=setter 写址，round-trip 闭合）。
- 置位时机：battle_type **0=攻城戦(@0x434000 init) / 3=野戦(@0x422000 init)** 推断强；mode_m1/mode_m2/parity 在 0x433000(合战 init) 等置布尔/小计数。
- 关键结论：这 5 地址是「固定基址战斗状态 struct」的字段——绝对直读点极少（getter 调用点=0），消费侧经 base 寄存器+偏移。**清单原记 4 标志，补全第 5 个 handle_stat。**

### 续187 — P1 兵种/阵形/计略中文名（部分破）
- `scripts/battle_names_ref.py` 负向自测 PASS：EXE + 111 原始资源文件 cp932 扫描，9 阵形/兵种/计略 **具体名称 0 命中** → 坐实「非 EXE 静态、非 MSGX」。
- 附 TK2 常见名称**参考表**（外部领域知识，非逆向产物，须运行时确认）。索引→名称绑定须 emu 捕获战斗渲染路径字符串指针 / 精灵 label。

### 续188 — P0 49B payload 关键纠偏（✅ 已破表征）
- **纠正续183「word 数组 {15,38,75,166,214}」标签**：5 类型实为**字节粒度数组 + 偶发 0x0c/0xf3 填充**；16-bit LE 解释出的「word」(3084=0x0c0c、62451=0xf3xx) 全是填充字节配对假象。
- `scripts/sndata_payload_fields_ref.py` **2/2 PASS** + `sndata_payload_fields.json`：仅 `type=75` 是真·位域/字数组(0x0000/0x0001/0x0101)；`type=15`(34条) 满长 43B 字节数组(值多 0..15 小计数、偶有≤253)；`type=38/166/214` 越稀疏。
- 方法坑：`field_probe`「填充率<0.49⇒word数组」误判；正确判据=字节粒度 + 是否 16-bit 对齐有意义值。

## 残留敞口（本轮未闭合）
- **P0 49B payload 逐字段语义**：真实字节位的**字段名**仍须 emu 驱动 `0x492e20`/`0x492ed0` 簇按 type 抓 payload 消费点（续188 下一步 A）。
- **#19 兵种中文名**：索引→名称绑定须运行时捕获（续187 下一步）。
- 注：MEMORY.md 因超长被截断注入，需后续 consolidation（本轨续143 与并行轨撞号，接手须先 `grep 续N` 取 max+1）。

## 交付物（本轮新增 *_ref.py）
- `scripts/battle_mode_flags_ref.py` (20/20)
- `scripts/battle_names_ref.py` (负向 PASS)
- `scripts/sndata_payload_fields_ref.py` (2/2) + `sndata_payload_fields.json`
- （`sndata_wordarray_payload_ref.py` 保留为 count 自测，其 word 解释已被 续188 纠偏，json 已移入 `_草稿/`）
- 探索草稿 20 个文件已移入 `scripts/_草稿/`。

### 续189 — P0 49B payload 全量字段指纹 + 三栏文本/二进制分类（✅ 已破结构层）
- `scripts/sndata_payload_classify_ref.py` **4/4 PASS** + `sndata_payload_classify.json`：833 条/171 型全量分类。
- 推翻「164 类二进制无结构黑箱」笼统印象 → 建立 171 型 per-byte 字段假设表（const/small_int/nibble/byte_enum/wide）。
- 关键发现：① `type=0x01`(n=13) 全部 43 字节 ∈{0,1} → **43 字节独立布尔开关数组**（坐实续174「场景状态布尔开关」最细粒度）；② `type=0x00/0x95/0x62` = 索引+标志混合（benum 位=实体/城/国索引 0..~250，sint/nibble=小枚举，偶发 const=0x0c 默认哨兵）；③ 3 文本栏(rec+6/19/32) 经严格 cp932 解码（拒 PUA/mojibake/半角片假名巧合）**全量 0 可读日文** → payload 二进制/自定义编码非显示标签（印证续174/176）；④ TOP 型 0x0c(249,fill0.54)/0xf3(111,fill0.64) 填充占位、0x0f(34,fill0.12) 小计数数组。
- 敞口收窄：P0 从「黑箱」→「171 个有结构指纹的待命名字段表」；剩「字段名/玩法含义」须 emu（`0x47fc60` 逐记录 hook 追 type=0x01 的43布尔消费者 + 对照游戏 dump type=0x00/0x95/0x62 的 benum 索引落点）。
- BREAKTHROUGHS.md 已插续189（倒序顶部）；破解状态清单.md §2 P0 已加 🟢 续189 并收窄缺口；MEMORY.md 已 consolidation 至续189 前沿。

### 续190 — #89 合战 5 全局模式标志 精确玩法语义（✅ 已破）
- `scripts/battle_mode_flags_semantics_ref.py` **9/9 PASS** + 草稿 `_modeflag_callsites.py`(21 call-site 上下文)/`_modeflag_consumers.py`(已移 `_草稿/`)。
- 用 7 setter 的 **21 个 call-site 战斗相位上下文**钉死 5 标志含义：`mode_m1`(0x511bf8)=交战进行主标志(0/1，配置·合战init)；`mode_m2`(0x51352c)=合战子模式(0/1/计算值；攻城戦内 bit0 概率翻转=攻城戦变体)；`parity`(0x513540)=阶段/回合奇偶计数(合战init 置初值、攻城戦每阶段+1 钳顶)；`battle_type`(0x513548)=0攻城戦/3野戦；`handle_stat`(0x513534)=攻城戦状态字(攻城戦init 自[esp+0x3c]写)。
- 消费侧机制坐实：3 getter 调用点=0 + 5 全局绝对引用仅限 setter/getter 包装器 ⇒ 消费经 base 寄存器+偏移访问 struct（基址簇 0x51352c：+0=mode_m2/+0x08=handle_stat/+0x14=parity/+0x1c=battle_type；0x511bf8 mode_m1 独立）。
- emu 偏移扫描(0x513400/0x5133d0 基址 +0x12c..+0x178 字段偏移) 在 0x420000..0x4f0000 区 0 命中 ⇒ consumer 基址非简单立即数（深坑，须 emu 钩 MEM_READ 抓读者）。#89 语义层已闭合，仅剩 consumer 门控细节深坑。
- BREAKTHROUGHS.md 已插续190（倒序，续189 下）；破解状态清单.md §2 P1 模式标志条目已加 续190 并标注「含精确语义」；MEMORY.md 当前状态已加 续190 + #89 改「已闭合」。

### 续191 — S7 `0x516a28` `+0x0f` 主标志字节位域语义（✅ 已破）
- `scripts/sndata_s7_flags_ref.py` **8/8 PASS** + 草稿 `s7_ecx_trace.py`/`s7_sibling_callsites.py`/`s7_setter_family_probe.py`/`s7_off08_probe.py`(保留于 scripts/)。
- 两参数化 setter 字节签名实锤：`0x49bf50`(setter A)=写 `+0x0f` 低4位 bits0-3，`new=(old&~0xf)|(arg&0xf)`；`0x49bf90`(setter B)=写 `+0x0f` 高3位 bits4-6=0x70，`new=(old&0x8f)|((arg&7)<<4)` —— 与续155「+0x0f 主标志(test 0x70)」一致。
- E8 rel32 call-site 计数：setter A=4(0x4511e5 push0 / 0x4702bd ebx / 0x4b4a7b push0xc(SNDATA默认哨兵) / 0x4bb4b0 push0)；setter B=9(0x4511ee push7→0x70 / 0x46e642 edi / 0x4702c6 push7 / 0x4a5b41 reg / 0x4b4da6 ebx / 0x4b4e4c esi / 0x4b9c92 push7 / 0x4bb4b9 push0 / 0x4bc386 push0)。
- **关键：ecx 溯源坐实 S7 每城条目专属**（破共享库陷阱）：13 call-site 中 9 个回溯到 `0x516a28`(S7 基址)；典型链 `sub ecx,0x51eb88`(减城表基)→`imul`(×16 得城索引)→`add ...,0x516a28` ⇒ ecx=S7 条目，并揭示「城堡→S7 条目」映射约定(城指针-0x51eb88 得索、×16+0x516a28 得条目)。
- 语义推断：`+0x0f`=城主/据点主标志位域（低4位=状态枚举含默认哨兵 0xc；高3位=激活掩码 7→0x70 全开，续155 `test 0x70` 正查此域）。
- **共享库陷阱规避（续155 规则）**：同簇 `0x49bfba`(+0x08)/`0x49bfca`(+0x0a)/`0x49bfda`(+0x0b) 经反汇编确认紧凑 setter 族(含 +0x08 状态机：`and [ecx+8],0xff7f` 清 bit15 + 低3位跳表 0x49c1cc 派发)，但**三者 E8 call-site=0** ⇒ 符合续155「共享/通用 setter 库」特征，不纳入 S7 专属判定；`+0x08/+0x0a/+0x0b` 语义待独立 xref 确认。
- **全局态势**：静态已到极限——剩余敞口(P0 49B 字段命名/#19 兵种中文名/#89 consumer门控/S15段C 6B/S7其余字段) 全为 emu 深坑。建议下一阶段转入 emu 环境搭建（钩 MEM_READ 抓消费者 + 整局/事件抓 setter 入参分布）。
- BREAKTHROUGHS.md 已插续191（倒序顶部）；破解状态清单.md §2 S7 行已加 续191 标注「+0x0f 位域语义已破」+ §1 文件列表已加 `sndata_s7_flags_ref.py`。

### 续192 — S7 `+0x08` 状态机归属判定（✅ 已破：共享库非 S7）
- `scripts/sndata_s7_statemachine_ref.py` **4/4 PASS** + 探索稿 `s7_off08_statemachine_probe.py`/`s7_off08_callers_raw.py`。
- 状态机形态：派发器 `0x49c064` 载 `word[+0x08]` 取低3位(0..7) 选 handler[0..7]（跳表 `0x49c1cc` 8 项）；7/8 handler 读 `byte[+0x05]` 经 `shr 3; and 0xf` 取高半字节再 ×5/×10/×20/+5 算查表索引 ⇒ **通用「类型→资源查找」派发器**，键在 +0x05、状态选在 +0x08。
- **归属判定（续155 陷阱确证）**：E8 rel32 + raw 4-byte 字面「双重扫描」均为 0 调用方 —— 派发器 `0x49c064`=0/0、`+0x08` setter `0x49bfba`=0/0、`+0x0a`/`+0x0b` setter 同 0/0、跳表 `0x49c1cc` 仅 1 raw(@0x49c085，位于派发器体内) ⇒ 完全孤立=共享/通用库，**非 S7 专属**。
- 与续191 对照（方法论坐实）：同区段 `0x49bf50..0x49c1cc` **混居**「S7 专属 +0x0f setter(4/9 E8+回溯 S7 基址)」与「共享库 +0x08 状态机簇(0 调用方)」；用「E8 + raw 双重 0 调用方」判据干净区分。
- 结论：S7 已验证字段**仅 `+0x0f`**；`+0x08/+0x0a/+0x0b` 不属 S7 专属；其余 `+0x04`/`+0x0c` 须 emu 钩 `0x516a28` 抓读者/写者确认。BREAKTHROUGHS.md 已插续192（倒序顶部）；破解状态清单.md §1 文件列表+§2 S7 行已加 续192。

### 续193 — emu 仿真骨架落地（`emu_harness.py`）+ S15 段C 运行期再验证
- `emu_harness.py`（Unicorn 2.1.4 统一入口：加载映像@0x400000、stdcall `call(va,args,regs)`、STOP 页终止、可选 MEM_READ/WRITE hook、叶子函数自测）—— 续192 后「静态已到极限」的转折基础设施。**自测 ALL PASS**：sat_sub(0x4ebcd0) 4/4 + set_c(0x49c500) byte[+0x13+idx] 6/6。踩坑：Unicorn 2.x 寄存器常量在 `unicorn.x86_const` 为 `UC_X86_REG_*`（非 `UC_REG_*`），且用 `mu.reg_read/write(UC_X86_REG_ESP)` 非 `.reg_esp`。
- `sndata_s15_segc_ref.py` **5/5 PASS** + `s15_segc_probe.py`：段C 布局 emu 确认 `byte[ecx+0x13+idx]`(idx 0..5 ⇒ +0x13..+0x18)；getter round-trip OK；25 set_c call-site（E8 扫描）中 idx/val 多为运行时变量(edx/eax/esp) ⇒ 段C 以变量索引读写（可变状态槽）。
- **重要校正**：段C 逐槽语义**已于 续151 定名**（`scripts/s15_segc_ref.py`：segC[0]=S13目标索引/segC[1]‖[2]=16-bit打包事件参数/segC[3]=表@0x513550索引/segC[4]=×1000喂S13初始化/segC[5]=事件内计数·桶狭間 handler 0x408dc0）。续193 仅 emu 再验证 + 补 usage-pattern，**非新命名**——避免重复记功。
- BREAKTHROUGHS.md 已插续193（倒序顶部，明确标注「段C 非新命名、emu 再验证」）；破解状态清单.md §1 子表行已加 `emu_harness.py`+`sndata_s15_segc_ref.py` 并标注 续193 emu 骨架落地。
- 下一步：用 emu_harness 攻关事件解释器 boot，钩 `0x47fc60`/`0x4e8604`/`0x49c500` 抓消费者，转 P0 49B 命名/#19/#89 等剩余 emu 深坑。

### 续195 — 资源 / 音效子系统全闭（90 名串 + 39 音效 ID→文件，emu 39/39）
- **90 个资源名串全量枚举**（此前从未完整枚举）：正则 `[A-F]:[A-Z0-9_]{1,12}\.[A-Z0-9]{2,3}` + 终止 NUL 校验扫全镜像 → 90 distinct / 16 组阵列，每个名串全镜像**仅出现 1 次**。可直接驱动 Godot 重制资源导入表。
- **音效子系统几何**：`@0x50ba40` = 40 项音效名指针表（ID 0..38→文件名）；`0x4997c0` = `play_sfx(id)`，硬编码上限 `cmp si,0x27`(39) 与指针表项数吻合（表长独立第二证据）。
- **门控链四层（Unicorn 端到端实证）**：`cmp si,0x27` 越界 → `ecx=0x5256c8`(thiscall 音效对象) → `cmp word[0x5256c8],id` 已载入数 → `cmp word[0x50b9b0],0x10` 就绪 → `mov eax,[eax*4+0x50ba40]` 查表 → `call 0x4015f0(name,0,0)`；全局开关 `byte[0x520604]` bit1。
- **emu 端到端 39/39**：真跑 play_sfx 全 39 ID，验证底层 `0x4015f0` 实参恒为 `(SFX_NAMES[id],0,0)` ⇒ 映射由运行期坐实；门控负例 E1–E5 5/5。
- **调用约定坑（同链三约定并存）**：`0x499780/0x499770` = thiscall（ecx=this、无栈参、普通 ret，桩 esp+=4）；`0x4015f0` = cdecl 3 参（调用方 add esp,0xc，桩 esp+=4 读 [esp+4..]）；play_sfx 1 栈参调用方 add esp,4。桩写错任一即栈崩。
- **🆕 中文版 BGM 方案替换（避坑）**：EXE 引用但原版目录缺失 4 项，3 项是 TMP/SWP 运行时临时档（正常），**唯一实质缺失 = `B:MMLDATA.LZW`（BGM/MML 序列）**——原版目录以 `MP3/`(34 首)+`mp3.dll` 取代 ⇒ 中文版 BGM 改 MP3 方案，EXE 内 MMLDATA 名串成死代码。**复刻 BGM 走 `MP3/`，别找 MMLDATA**。另有 20 项目录有但 EXE 未引用 = AVI 过场（外壳播放器调）+ 备档（如 HKMAPDAT.LZW，EXE 实际用 HKMAPNEW.LZW）。
- 音效 ID 玩法语义 30/39：扫 play_sfx 全镜像 75 调用点，66 处可静态定 ID；锚点 ID0=点击/ID1=取消/ID11=火计/ID31=技能提升/ID33=忍者。
- ⚠️ **工具坑（可复用）**：抽 `call` 实参**不能**从单一固定起点反汇编（x86 变长指令必错位，实测 0 命中）。正解 = 枚举回溯 `back=1..span`，只接受「指令流中存在 `address==call_va`」的起点（指令边界对齐），取 back 最大者。见 `sfx_subsystem_ref.py arg_at()` / `_res_manifest_probe.py pushes_before()`。
- 产物：`scripts/sfx_subsystem_ref.py`(ALL PASS) `scripts/resource_manifest_ref.py`(ALL PASS) `scripts/resource_manifest.json` `scripts/_res_manifest_probe.py` `scripts/_sfx_probe.py`；`GAME_DATA_SPEC.md §3.23` 全节；BREAKTHROUGHS 续195。
- 下一步：(A) 资源加载簇全图——扫全部 44 个 `call 0x4802e0` 站点抽 arg0(资源表基址)+arg1(尺寸)，建「簇 handler→资源表→文件名」完整映射；(B) 破 GRP/PK8/TR2/KOS 四种未破资源格式；(C) emu 补剩余 9 个音效 ID 语义。

### 续196 — 资源加载簇全图闭合（44 站点全解码 + 152 次 emu 端到端）
- **全图闭合**：续161 只钉 4 簇 → 现 **29 个簇 / 40 个可解析站点 / 4 处寄存器派生**（`0x43379d`/`0x47d741`/`0x47d821`/`0x47fab9`，与续163 完全吻合）。续161 的 4 个锚点全部交叉验证通过。
- **函数边界新法**：用「最大 call/jmp 目标 ≤ va」定函数入口 —— 比 prologue 模式稳健，**本 EXE 大量 FPO 函数无 `push ebp; mov ebp,esp`**（旧法实测全部返回 0）。配合续195 的指令边界对齐回溯法抽实参。
- **主资源表 `@0x506ad0` = 19 项 stride 16**：GRPDATA/MESSAGE1..4/MAPCHIP/MAPCHAR/SHOP_BG/OBJ/MSK/ANMSEQ/NPKDATA.IDX/PK8DATA/TOWNCHIP/TOWNCHAR/HEXMES/KOSENGRP/FACE/EXTFACE.PK8；**索引19 @0x506c00 已非资源名 ⇒ 表长止于 18**（独立第二证据）。另 8 张子表：0x5030d8(4)/0x5036f0(2)/0x509590(5)/0x50a1f8(2)/0x50a3c0(1)/0x50b650(2)/0x50c140(3)/0x50ca08(1)。
- **管线逐指令钉死**：`0x4802e0(this,base,size_class)` = `memmove(0x522ca0,base,0x20)`(32B=2个表条目，容31字符长名) → `movsx ecx,word[esp+0x18]`(符号扩展16位) → `0x4ec8c0`；`0x4ec8c0` = **`add eax,2` 剥 `X:` 盘符** → strcpy → **`and al,0xfb` 清 bit2 修饰位** → `cmp eax,3;ja` → 4路跳表 `0x4ec948`(0/1/2/0x1000，>3走默认0x1000) → `call [0x4fb07c](裸名,this+4,size)` **stdcall3参 ret 0xc** → `this[0]=handle` → 失败(-1)返0/成功返1。`0x492800` 坐实 = cdecl3参 memmove 转发 `0x4f40b0`。
- **两个关键玩法结论**：① 触发回调条件 = `(size_class & 0xfb) <= 3`；44 站点中 **38 处 =4（→尺寸0）+1 处 =0，无一处走 1/2/0x1000** ⇒ 实际只用「尺寸 0」，bit2 是「已注册/需加载」修饰位。② **`A:`–`F:` 盘符在加载前被剥掉，只是 EXE 内部逻辑盘符代号、非真实路径** ⇒ Godot 复刻**按去前缀裸名查原版目录**，别照抄 `B:MAPCHIP.LZW`。
- **emu 端到端 152/152**：19 主资源 × 8 种会触发回调的 size_class，把 `[0x4fb07c]` 重定向到自写桩(`mov eax,<h>; ret 0xc`)，逐项验证裸名/arg2==this+4/size/this[0]/返1；负例 D1–D4（句柄-1→返0、`sc=8` 完全不回调）全过。
- ⚠️ **emu 工具坑两条（新增）**：(a) **读 emu 内存 ≠ 读静态镜像** —— 栈/局部缓冲(`lea eax,[esp+8]`→`0x5feeec`)在静态镜像里是垃圾，校验回调实参字符串**必须 `mu.mem_read()`**，用静态 `rd()` 读到空串（实测踩中）。(b) **Unicorn 有 TB 缓存** —— `mem_write` 改桩代码**不一定生效**(改句柄 0x42→-1 仍返旧值)，切换桩行为**必须新建 Uc 实例**。另：反汇编窗口 0x2c 正好截掉 `0x48030a` 的 `ret 8`，需 0x30。
- 产物：`scripts/resource_cluster_ref.py`(**31/31 ALL PASS**) `scripts/_res_cluster_scan.py` `scripts/resource_cluster_map.json`；`GAME_DATA_SPEC.md §3.24` 全节；BREAKTHROUGHS 续196。
- 下一步：(A) 破资源格式 —— 优先 `KOS`(39 音效，样本多/文件小) 与 `TR2`(剧本·存档容器，关乎 SNDATA §4)；(B) emu 补 4 处寄存器派生站点 + 9 个音效 ID 语义；(C) 回攻 P0 49B payload。

### 续197 — 资源格式开破：KOS 音频容器 + GAIJI.TR2 外字表（均 25/25，纯静态+本地样本，未用 emu）
- **KOS = 「1 字节 XOR 密钥 + 标准 WAV」**（39/39）：`raw[0]`=key（全 39 个均为 `0xAE`），`raw[1..]` 逐字节 XOR key 即标准 RIFF/WAVE，恒 PCM 8bit/单声道/22050Hz，仅 fmt(16)+data 两 chunk，data 长奇数补 1B pad（CLICK/NIGERU/OOATARI）。
- 🔑 **解码器 `0x499380` = `xor16(buf,key16,len)`（cdecl 3 参）**：`xor word [eax],dx` + `add eax,2`（**16 位步进**），计数 `ceil(len/2)`。密钥**来自文件第 0 字节**：`0x4993a0` 用 `0x4ec9a0` 读首字节 → `0x4994ea mov dh,bl` + `0x4994f4 or ebx,edx` 扩展成 `key16=key|key<<8`。**这解释了全镜像扫 `xor …,0xae` 立即数零命中**（曾两轮扩大扫描含 80 /0..7 ae、34 ae、35 ae、81/83 /6 ae、80 30..37 ae 全部编码形式，均 0 命中）。
- **分派（`0x4993a0`）**：`0x499360(name,'.')` = strchr → `.KOB`→`A:MIDI.TMP`；`.KOS`→`A:WAVE.TMP`；其它扩展名不解码直接用。🆕 **`.KOB` 是第二个音频扩展名（BGM/MIDI）**，但原版目录与 90 个资源名串中**均无 .KOB**（扩展名分布 LZW33/DAT4/TR2 6/GRP3/KOS39/IDX1/PK8 1/TMP2/SWP1）⇒ 中文版 BGM 走 MP3/（续195），.KOB 成死路径。
- **统计指纹佐证**：解码后 1021298B 均值 126.5（≈8bit PCM 静音中心 128）、覆盖 0..255、平均相邻差 4.90 vs 未解码 26.33（平滑 5.4×）。
- **GAIJI.TR2 = 16 × 34B = 544B**：每条 = `u16 LE` 源码 + 32B 的 16×16 1bpp 位图（行主序 MSB 先行）；**前 15 条实字形（源码 0x7670+i），第 16 条终止记录（0x7721 + 32B 全 0）**。
- **外字三函数**：`0x48c070` LoadGaiji（esi=0xa140 / edi=0x10，循环 read(2)→read(0x20)→0x4f1ae6）；`0x4f1ae6` InstallGaiji（`cmp eax,0xa140` / `cmp eax,0xa14f` **硬编码上下界=表长独立第二证据**，`(code-0xa140)*32+0x529e38`，memmove 32B）；`0x4f1a06` GetGlyph（命中走外字缓冲，否则回退常规字体 `0x4f1a42`）。缓冲 **@0x529e38 stride 32 共 512B**，静态全 0。`0x4f4750`=memmove（含重叠判断）。
- 🆕 **GBK 码位劫持**：游戏文本 GBK/CP936（EXE @0x50bd28 →「硬盘已满。」），**0xA140..0xA14F 在 GBK 属未分配区**，被劫持作外字区。文件里的 2 字节源码**读入但安装时未使用**（安装用循环计数器 0xa140+i）。
- ❌ **证伪**：源码 0x7670+i 恰落在 Unicode U+7670..U+767E（癰癱癲…癸発登發白百）很诱人，但与系统 CJK 字体 15×15 **判别性交叉匹配自命中 0/15**（低于随机期望，IoU 无优势）⇒ Unicode 假设证伪，语义仍未知（疑 DOS/PC-98 遗留码）。
- ⚠️ **方法论局限**：连通块数/孤立像素数对**转置与镜像不变** ⇒ 只能判字节交换(243)/位反转(213)劣于行主序(213)，**行主序 vs 列主序（转置）无法由数据单方面区分**。另与 `_font_bulk0.bmp`（128 格）交叉匹配最高 IoU 仅 0.46 且 best/2nd 并列 ⇒ 外字本就不在标准字库。
- 产物：`kos_format_ref.py`(25/25) `_decoded_kos/*.wav`(39 个可播放) `kos_sfx_manifest.json`(ID→文件→时长，合计 46.31s，与续195 音效表对齐)；`gaiji_format_ref.py`(25/25) `_gaiji_preview.png` `gaiji_spec.json`；`GAME_DATA_SPEC.md §3.25 / §3.26`；BREAKTHROUGHS 续197。
- 下一步：(A) 破 GRP（5 个：ACERTWP/END/KOEILOGO/PRESS/SMODE）；(B) 破 PK8（EXTFACE.PK8）；(C) 定 SNDATA/SAVEFILE 头 +0x10 的 4 字节字段（查 0x47d720 LoadSNDATA）；(D) 用 39 WAV + 音效 ID 语义驱动 Godot 音频。

### 续198 — GRP 图像格式三变体全破（12/12，纯静态+样本+PNG 目视验证，未用 emu）
- 🔑 **`.GRP` 不是单一格式**，同扩展名下三种互不兼容布局，**必须按加载站点分派解码器**：
  - **格式 A · Raw**（ACERTWP/KOEILOGO/PRESS，加载 `0x48ae89`/`0x48c95a`）：`6B 头(u8 type=4 + u8 pad + u16 W=640 + u16 H=400)` + 128000B **NPK 风格 planar-nibble 4bpp（无 RLE）**，2B→4px，位序 `b1.bit7→px.bit3, b1.bit3→px.bit2, b2.bit7→px.bit1, b2.bit3→px.bit0`，解 4px 后 b1/b2 左移 1 位。128000×2=256000=640×400 ✓。**调色板不在文件内 → 在 EXE 数据段紧邻资源名串**：`@0x50a188`(ACERTWP) / `@0x50a2c0`(KOEILOGO+PRESS)，16 项×3 nibble RGB，nibble→8bit 用 **×17**；下发函数 `0x47ad90`=SetPalette16。⚠️ `@0x50a1bd` 是 Press 提示字符串**不是调色板**，勿误取。
  - **格式 B · Planar**（SMODE.GRP，加载 `0x4eaec7`）：`[132512B 像素][1024B RGBQUAD 调色板][1B 0xff 终止符][剩余 4512B 子图标]`。⚠️ **与格式 A 布局完全不同** —— 这里是**标准 VGA 4-plane bit-level 交织**，每 4 字节 = `[P3,P2,P1,P0]`，32000B/plane×4=256000 bit=640×400。加载器 read 序列 `read(0,0xfa00)+read(6,0xfa00)+read(12,0x15a0)` 为独立第二证据 ⇒ 前 128KB = **两个 640×200 半屏**。调色板**在文件尾**，不取 EXE。
  - **格式 C · END.GRP 容器**（加载 `0x407cc3`，名串 `@:END.GRP`）：`u32 BE n0=0x1a0` + `104×u32 BE 偏移表` + **103 个 NPK016 条目**（§3.1，含**逐条内嵌** 32B LE-u16 RGB444 调色板 + 0x30 起 RLE）+ ASCII `END` 尾标。🔴 **偏移表是大端 u32**（疑 KOEI 68000/MD 系移植遗留），小端解析必得荒谬偏移 —— 本变体最大坑。原生尺寸 top5：`192×128 ×46`(结局立绘主规格)/`128×360 ×5`/`48×312 ×5`/`640×200 ×4`/`112×364 ×3`，显示统一 640×400。
- **自测 12/12**：T1 三文件头一致(type=4,640×400,body=128000)；T2 解包像素数 256000/256000 ×3；T3 END.GRP **103/103 RLE 恰好填满**+END 尾标；T4 SMODE 640×400+1024B 调色板+0xff；T5 两组 EXE 调色板值域 [0..255]。
- **目视验证全过**：KOEI logo 完美还原、第三波认证画面清晰、SMODE 选择模式 CD 音响界面+日文菜单完美、103 帧结局图全部正确 ⇒ 格式模型正确（PNG 目视是本轮最强验证手段，优于纯统计指纹）。
- ❌ **PK8 未破**（EXTFACE.PK8 唯一样本）。逐一实测排除：NPK RLE（**恒出 2240px 且与 W 无关** ⇒ 非该 RLE）、packed 4bpp/8bpp（噪声）、planar 4-plane 含 LSB 变体（尺寸不整）、rle_type1/2（2424px 不整）、灰度 8bpp 直渲（噪声）。WebSearch 查 KOEI PK8 无格式记载。⇒ 需 **emu 追踪读后处理链**。
- ⚠️ **踩坑记**：`struct.unpack("<BxHH")` 只解 3 字段（`x` 是填充不产值），头是 4 字段须用 `<BBHH`；内联 python heredoc 多次 SyntaxError ⇒ **复杂脚本一律写独立 .py 文件再跑**，别塞 heredoc。
- 产物：`scripts/grp_format_ref.py`(**12/12 ALL PASS**) `scripts/_decoded_grp/*.png`(含 ACERTWP/KOEILOGO/PRESS/SMODE + END 多帧) `scripts/_decoded_grp/grp_spec.json` `scripts/_grp_probe.py` `scripts/_grp_xref.py`；`GAME_DATA_SPEC.md §3.27` 全节；BREAKTHROUGHS 续198。
- **Godot 复刻要点**：按加载站点分派而非扩展名；格式 A 调色板须从 EXE 提；END.GRP 偏移表按大端读；建议**离线批量转 PNG**，运行期直接加载，别在 Godot 里重写 RLE。
- 下一步：(A) emu 追踪 PK8 读后处理（hook `0x4411b0` 返回后→`0x492d70` close 之间全部 MEM_READ/WRITE）；(B) 破 **TR2**（SNDATA/SAVEFILE 容器，6 文件最大 328KB）；(C) 定 SNDATA 头 `+0x10` 4 字节字段（查 `0x47d720` LoadSNDATA）；(D) 用 39 WAV + 音效 ID 语义驱动 Godot 音频。

### 续199 — SAVEDATA.TR2 容器全闭 + 49B 槽元数据定名 + **推翻旧文档两处定性**（57/57，纯静态+样本，未用 emu）
- 🔴 **纠偏一：SAVEDATA = 8 槽 × 40960B，不是 16 槽 × 20480B**。旧 SNDATA_SPEC §6/§11.1 记 `idx*20480+0x198`（`idx*5<<12`）→16 槽，**根因是把 `shl eax,0x0d` 误读成 `0x0c`**。原始字节 `@0x47d86b = 8d0480 c1e00d 0598010000` ⇒ `lea(×5)+shl 0x0d(×8192)+add 0x198` ⇒ **stride = 5<<13 = 40960**。🔑 **决定性第二证据（数据侧独立）**：头 408B−16B magic = **392B，392/8=49 整、392/16=24.5 不整** ⇒ 16 槽被数据证伪。⚠️ **方法论**：`40960×8` 与 `20480×16` **都整除 327680**，**单靠文件大小整除性无法定槽数**，必须叠加 shl 立即数 + 头部元数据整除性双证。
- 🔴 **纠偏二：那批「49B 记录」属 SAVEDATA（8 条存档列表条目），不是 SNDATA 的 833 条剧本记录**。铁证 = `0x47fc60`(读)/`0x47fd10`(写)/`0x47fb80`/`0x47fc10` **开头全部硬编码 `push 2`** → `0x47d720` 的 `cmp ax,2; jae` 对 `ax>=2` 走 LoadSAVEDATA（0→SNDATA1/1→SNDATA2），**四站点无一处传 0/1**。⇒ 旧 §4 整节「49B 记录类型系统（sc1=427条167型/sc2=443条161型/0x0c·0xf3 哨兵）」是**把 SNDATA 按 49B 强行切分的伪结构**；佐证：`40840/49 = 833.47` 本就不整，须凑 23B 尾区才「整除」。§2.1 当年察觉的「833 条不是从 0x10 明文排列」矛盾**由此得解**。⇒ **SNDATA 加密流实体记录 stride 须重测**。
- 🆕 **49B 槽元数据 schema（读写双向对称）**：`+0x00 u16 年` / `+0x02 u16 月` / `+0x04 u16 日` / `+0x06 char[13] 主角名` / `+0x13 char[13] 所在国` / `+0x20 char[17] 所在地+身分`，**6+13+13+17=49 精确闭合**。三字符串经 `0x4ebfe0`(strcpy) 扇出到 **`0x522c88`/`0x522c60`/`0x522c70` = 存档列表 UI 三显示缓冲** ⇒ **这解释了它们在静态镜像恒为 0（runtime-only UI 文本区）**，而非 §11.5 旧注推测的「剧本脚本化初始态通路」。写方向 strcpy 目标 `lea [esp+0xa/0x17/0x24]` 各减 4（push esi 抬栈）= `buf+0x06/0x13/0x20`，逐字段对称。
- ★ **史实交叉验证（语义正确性强证据）**：slot0 解出 **1560年5月20日 / 木下藤吉郎 / 尾张国 / 清洲城步兵头**，与太阁2 剧本1「永禄3年(1560) 桶狭间、主角木下藤吉郎、仕织田信长、居尾张清洲」完全一致；slot1..7 全零。
- 🆕 **四函数对称矩阵**：槽元数据 `0x47fc60` 读 / `0x47fd10` 写；槽体 `0x47fb80` LoadSaveSlot（`0x47f350` **两趟**，`word[esp+0x98]` 1→0 two-pass flag）/ `0x47fc10` SaveToSlot（`0x47f5c0` = `0x47f350` 写对偶 → `mov [0x520620],1` 已存档标志）。**槽体沿用剧本格式**：slot0 槽体首 16B = `TAIKOU2_SCENARIO`。
- 🆕 **`0x47d720` 定名 `OpenTR2(file_idx, mode)`**（mode 0=读 1=写）；`0x47d780` **栈上构造名串并动态替换盘符**（`al=[0x526c78]+0x40` 覆写 `byte[esp]`，`A:`→`X:`）+ `and eax,4; or eax,edx` 给 size_class 或上 bit2 ⇒ **填掉续196 敞口④「4 处寄存器派生站点」中 `0x47d741`/`0x47d821` 两处**；`0x47d800` = 磁盘重试循环（弹 `0x5095e8`「请在插入<启动盘或保存盘>。」）。
- 🆕 **`@0x509590` = stride16 × 5 项 TR2 名串子表**（A:SAVEDATA / F:SNDATA1 / F:SNDATA2 / F:BSDATA1 / F:BSDATA2）+ 紧邻 6 条中文 UI 错误串（0x509610/0x509620/0x509638/0x509650）⇒ `0x509590..0x509660` = 完整存档子系统数据区。
- 🆕 **`.TR2` 是容器扩展名，4 类**：SAVEFILE / SCENARIO / GAIJI(544B) / **BSDATA(41300B，无 magic)** ⇒ 与续198「.GRP 三变体」同一模式，**复刻必须按加载站点分派解析器，不能按扩展名**。
- 🆕 **BSDATA stride 59 位置学独立证据 + 可复用方法论**：BS1/BS2 差异 1646 处按 `x mod s` 统计 —— **59：占用列率 0.559(33/59)、最热列占比 0.341**；对照 8 个非整除 stride 占用率**全 1.000**（铺满=无结构）、最热列**全 <0.05**；倍数 118 显结构但热点 0.171 **弱于 59** ⇒ 59 为基本周期；**整除但非真 stride 的 70/100 占用率仍 1.000** ⇒ 🔑 **整除性单独不足以定 stride，必须叠加「差异位置 mod stride 的列对齐集中度」，且要查倍数确认取到基本周期**。
- 产物：`scripts/savedata_container_ref.py`(**57/57 ALL PASS**) `scripts/savedata_container.json` `scripts/_tr2_probe.py`；`SNDATA_SPEC.md` **新增 §12 全节 + §6 函数表纠偏 + §11.1 纠偏**；BREAKTHROUGHS 续199。
- 下一步：(A) **重定 SNDATA 加密流实体记录 stride**（旧 49B 已废，用「整除+列对齐集中度」双判据，样本=两剧本解密流 diff）；(B) emu 追踪 PK8 读后处理链；(C) emu dump `0x47f350` 解码后全局表反推 40960B 槽体布局；(D) 39 WAV + 音效 ID 驱动 Godot 音频。

### 续200 — BSDATA1/2.TR2 = **700×59B 明文武将主表**全闭 + 字段表纠偏 + 推翻续181②（73/73 ALL PASS）
- 🆕 **容器**：`BSDATA1/2.TR2` 各 **41300 B = 700 × 59 B**，**无 magic、无加密、明文 GBK**（与 SNDATA 的加密流截然不同）⇒ 700 条武将主记录**可脱离 EXE 直接读**，是目前最干净的数据源。
- 🆕 **加载器 `LoadBSDATA @ 0x47fa90` 代码级绑定**：`push 0xa154`(=41300，与文件大小逐字节吻合) → 剧本二选一 `mov eax,0x5095d0/0x5095c0`（对上续199 `@0x509590` stride16 名串子表的第 4/5 项 `F:BSDATA1`/`F:BSDATA2`）→ 分支键 `byte[0x520604]` 的 `test ah,4` → 目标缓冲 `0x524a20`，错误串 `@0x50a2f0='BUSHOUbuf'` → 链 `OpenRes(0x4802e0)`→`Read(0x4eb5c0)`→`SetResident(0x4eb730)`。
- 🆕 **schema == 解码器 `0x47f7b0`**（emu 游标反算：49 次读、恰好消费 59 B、**无空洞**）⇒ 字段表不是统计猜测而是**代码派生**。三个落点：姓表 `0x521aa8`(7B/条) / 名表 `0x520660`(7B/条) / 实体池 `0x519868`(stride 47B)。
- 🆕 **字段表（十六进制偏移，已写入 GAME_DATA_SPEC §1.2）**：`0x00-0x06` 姓(GBK 7B) / `0x07-0x0d` 名(7B) / `0x10` 武将ID / `0x14` 相性 / `0x16-0x1a` 五维 / `0x1b-0x1d` 技能 / **`0x27` 生年−1490**(r=0.9875 对史实) / `0x30` 国 / `0x31` 城 / `0x32-0x33` 功勲 / `0x35` 忠诚 / `0x36-0x37` 主君 / `0x38-0x39` 16-bit 状态字（**職位 = `byte[0x39] & 7`**，由 packer `0x49a7e0` 的 `and eax,0xf8ff; or eax,edx` 反推，38/690 条原值 >7 佐证需掩码）/ `0x3a` 高 4 位 = 主角槽+武将档枚举。
- 🔴 **纠偏旧 §1.2 五处**（详见 GAME_DATA_SPEC 纠偏表）：其中最关键 —— **主君是显式字段 `+0x36`**，旧文档靠「home_city 归组推导主君」的整条派生链**可废弃**；**`城 @0x31` 值域 min0/max199/distinct115 ⇒ 索引的是 200 条城表**（非旧说 370 名表）；`国 @0x30` min0/max44/distinct45；**国有效而城=255 有 271 条 = 在野正常态**（两字段半独立，不能互推）。
- 🆕 **`+0x3a>>4` 枚举完备**：6 主角槽 + 441/212/36（档 4/5/6）= **695 == REAL_ALL**，取值域恰 `{0,1,2,4,5,6,7,8}`，两剧本逐值同分布。
- 🔴 **推翻续181②「关系记录指针」**：`0x49c2b0`(姓)/`0x49c310`(名) 是**姓名 getter**，四段 ID 空间（边界 `0x3e8/0x7d0/0xbb8` = 1000/2000/3000），调用点 434+371 = **805 处**。
- 🆕 **外字闭环**：GAIJI.TR2 = 16×34B，位序 = **行主序 2B/行 MSB 先行**（闭合续197⑨）；外字实为 **48×16 连排位图切成 3 个 16px 格**（右 4 列是跨格连排伪影，非解码错误）；已认出身份 長宗我部 / 香宗我部 / 垪。
- ⚠️ **自测踩坑（6 项 FAIL 全是断言口径写严，实测数据反而更干净）**：① 分布类断言的样本集必须分清 **REAL_ALL(695，仅排占位槽) / REAL(690，GBK 可解) / GAIJI_REC(5)** 三集合，混用即错；② 「姓表引用后 N 字节内必有名表引用」**位移可为负**（`0x4cef19` 处名表在前，−172）⇒ 判据取 `abs(delta)`；③ SNDATA S1 姓名有效数 352/354 < 预期，坏名**全集中在尾部空槽**(354..369)，中部仅 2 条外字 ⇒ 判据改「有效≥350 且坏名全在尾部」；④ `+0x10` ID 含 **`0xffff` 空槽哨兵**，唯一性只对 live 判。**方法论**：先看数据分布再定断言阈值，别把「预期」写成硬边界。
- 产物：`scripts/bsdata_format_ref.py`(**73/73 ALL PASS**) `scripts/bsdata_format.json` `scripts/bsdata_names.json`(700×两剧本姓名)；`GAME_DATA_SPEC.md §1.1–§1.7 重写 + 三处旧说纠偏标注`；`SNDATA_SPEC.md §6 函数表补 LoadBSDATA + §11.4/§11.5 纠偏`；`破解状态清单.md §1/§0/§P1.5`；BREAKTHROUGHS 续200。
- ⚠️ **作废旧脚本**：`bsdata_fields_ref.py` / `bsdata_spec.json` / `bsdata.json` 字段命名有误（已被 `bsdata_format_ref.py` 取代）；`bsdata_lifespan_ref.py` 前提已证伪。
- 仍未知：`+0x0e`/`+0x12` 两处 2B 只进解码器局部未入实体；`+0x28` 语义弱；`+0x1e..0x21`/`+0x22`/`+0x2b..0x2f` 玩法语义未定；`+0x3a` 档 4/5/6 具体含义；15 外字槽中 10 个未被用到；生年 ±7 离群（林通胜/北条氏邦）；**92/200/370 三套索引对应关系**。
- 下一步：(A) **用 `bsdata_names.json` + 字段表驱动 Godot 武将数据层**（700 条明文已可脱离 EXE）；(B) emu 钩实体池 `0x519868` MEM_READ 抓 `+0x1c`/`+0x3a` 消费者；(C) 续199 (B) emu 追 PK8 读后处理链破 `EXTFACE.PK8`；(D) emu dump `0x47f350` 解码后全局表反推 40960B 槽体布局；(E) 39 WAV + 音效 ID 驱动 Godot 音频。

## 续201 · S1=BSDATA 模板 + 同城武将双链表（17:5x 收尾）
- 收尾续201 自测 `entity_record_unify_ref.py` 由 61/64 → **64/64 ALL PASS**（修 3 处断言口径：A6 改「字节覆盖区间」判据、A8 修抽取器 `lea["edi"]` 误记绝对常量导致 `push edi` 归位失败、C2 改真实字节断言 `mov dx,[ecx];and edx,0x87ff;mov [ecx],dx` 坐实相性居实体+0x08 字 bit11-14）。
- 核心结论：① 加密流起点 `0x58C`（非文档 0x598）；② 59B 流↔47B 实体映射（S1 解码器 0x47dce0 游标=59、BSDATA 0x47f7b0 的 59*idx 互证）；③ BSDATA=S1 模板（按武将ID 对齐 52/59 全等）；④ `entity+0x04`=同城武将链 next（2B 槽索引×47，S2 城表 word@0x00 命中链头 46/46·44/44，链头=大名）；⑤ 城结构+0x04=城链 next ⇒ 双层链表；⑥ 明文段A==1=登场标志（数==S1 有效槽 354/356）；⑦ 相性 setter `0x49a5a0` 字节级定死。
- 落盘：`BREAKTHROUGHS.md` 续201 条目 + `scripts/entity_record_unify.json`。续199(A)「SNDATA stride 待重测」关闭（=59）。
- 用户指令：把剩余非图像敞口全部破解（P0 payload 命名 / 兵种中文名 / S7 写入路径 / 头+0x10 4B / 合战§10）。已建 Task #4-#8。

### 续202 · SNDATA/SAVEDATA 头 +0x10 的 4 字节字段语义全闭（emu 端到端坐死）
- `scripts/sndata_header_ref.py` 修通 emu 后 **ALL PASS**：静态三断言 [A] 0x47f350 密钥=byte[0x12]^byte[0x13] / [B] 0x47da10 累加器 `add word [esi+0x92],cx` / [C] 0x47f350 校验 `cmp [esi+0x92],[esi+0x90]` + emu 实跑 `0x47f350` 在 `0x47f4da` 捕获校验点。
- 🆕 **头布局修正（旧 `SNDATA_SPEC §2` 两处错）**：`file[0x10..0x11]`=**16-bit LE 校验和**（解密后流字节累加和 mod 0x10000，写入 `obj+0x90`，`0x47f4da` 与 `obj+0x92` 累加器比对）；`file[0x12..0x13]`=**XOR 密钥种子**（key=byte[0x12]^byte[0x13]，写入 `obj+0x94`，喂 `0x47d960` 整段 XOR 解密）。
- 🆕 **机制闭环**：`0x47d960` 流缓冲填充时整段 `loop xor` 解密 → `0x47da10` 每读 1 字节 `add word[esi+0x92],cx` 累加密字节 → `0x47f4da cmp [esi+0x92],[esi+0x90]` 校验（不等则 `call 0x47f5b0` 报错）。旧注「0x10=剧本签名 / 0x12=填充哨兵」**两处错**。
- 🆕 **emu 实证**：SNDATA1 `emp_csum=0xb84b == file[0x10..0x11]=0xb84b`（match? True）、SNDATA2 `emp_csum=0x8013 == 0x8013`（match? True）；key sc1 `0xbb^0xb7=0x0c`、sc2 `0x80^0xbe=0x0a`。SAVEDATA 经静态 magic=TAIKOU2_SAVEFILE + key=0x06^0x05=0x05 交叉确认（`0x47f350` 是 SCENARIO 专用 loader，SAVEDATA 专属 loader 不同，故 emu 在 SAVEDATA 上 csum 不匹配属预期）。
- 🔧 **emu 基础设施沉淀（可复用，后续 Task #5/#7）**：① hook 句柄须保留变量防 GC（`on_code` 否则不触发）；② 栈映射须覆盖 esp（本工程 esp=`0x600000+0x58000`，栈区须 map 0x60000）；③ stdcall 读回调 `[0x4fb0a0]` 入口 `[esp+4]=handle/[esp+8]=dst/[esp+0xc]=count`、桩须 `esp+=16`（ret 0xc）；④ 校验点后崩溃用 `0x47f4da` 捕获判定、`emu_start` 异常 `except: pass` 吞掉。⚠️ **包依赖坑**：managed venv 未装 capstone/unicorn，跑脚本须用系统 python3.7（`/Library/Frameworks/Python.framework/Versions/3.7/bin/python3`，capstone 5.0.1 + unicorn 2.1.4）。
- 落盘：`BREAKTHROUGHS.md` 续202（倒序顶部） + `SNDATA_SPEC.md §2` 头布局修正 + `破解状态清单.md` P1.5「仅剩头+0x10 4B」改「已闭」+ 本日志。**Task #6 关闭**。
- 仍未知：① 校验和精确字节范围（emu 捕获 `pos=0x9f98` 仅是校验发生时刻的流位置）；② SAVEDATA 专属 loader 未实跑校验点；③ 下一步转 **Task #5（P0 49B payload 命名）**——emu 钩 `0x47fc60` 出口抓 type→字节消费点。

- **续203（2026-09-01 晚）BSDATA 尾部未定字段全定名（闭合续200 残留小敞口，Task #10 关闭）**：`scripts/bsdata_tail_fields_ref.py` **19/19 ALL PASS**。① `+0x0e`/`+0x12` 全 700 = 0xFFFF → 解码器 `0x47f7b0` 局部哨兵，无语义；② `+0x1e..0x21` 全常量占位初值（+0x1e≡0xFF/+0x20≡0/+0x21≡0、+0x1f 多数 0xFF），城属性由 getter 族 `0x49af00`/`0x49af50`/`0x49afa0`（镜像 42/42/43 调用点）按 `城[+0x31]` 运行时派生 → entity+0x12..0x15，**BSDATA 不存储城属性**；③ `+0x3a>>4` 档 4/5/6 = 441/212/36 条，五维和 295.4/298.7/313.6 单调 ⇒ **武将强度档**；④ `+0x28`(492/695 为 32 倍数·疑兵规模基数)/`+0x29`/`+0x2a`(539/695=0xFF·可选保留)/`+0x2b`(0..62 小枚举)/`+0x2c`(57..100·疑隐藏能力) 特征化（runtime 待坐死，诚实标 🔶）。落盘：`BREAKTHROUGHS.md` 续203 + `GAME_DATA_SPEC.md §1.2`（6 行更新）+ `破解状态清单.md` BSDATA 行补 续203 + `MEMORY.md` 当前状态(续202→续203)/残留敞口收敛。
- 下一步（用户「把剩下的破了」= 全部非图像敞口）：**续204** S7 `+0x04`/`+0x0c` 写入路径 emu（钩 `0x516a28`，复用 sndata_header_ref 基础设施）；**续205** P0 49B payload 全 833 条 type→字节消费映射（UC_HOOK_MEM_READ 追踪 0x522c88/0x522c60/0x522c70，复用续194 I/O 回调管线）；**续206** 兵种/阵形/计略 中文名静态再探（查 HJMAPDAT.DAT/HKMAPNEW.LZW/0x501e48 军事对话框表）。图像类 GRP/PK8 按用户规则豁免。
