## [2026-08-29（续92）] ✅ 单挑「AI 选牌逻辑」核心破（0x469310 一击必杀决策 + 行动代码派发架构）

### 突破内容

- **AI 一击必杀决策全反汇编（🎯 决定性）** `0x469310`：攻击者强度指标 `strength = char[0xb] + ((char[0xf]>>6)*10)`；阈值 `thr = tier*10 + enemyHP(0x514833) + (char[8]&0x20 ? 0 : 10)`（`char[8]` bit5 置位时再封顶 `0x46`=70）。**发动条件**：`char[0x21] >= 0x41` **且** `strength > thr` → 置 `this+0x18=1`（一击必杀标志）并经 `0x4663f0` 执行；否则不发动（`this+0xc` 维持默认 0=普通攻击）。与玩家「自动升级必杀」走同一 `0x469310` 升级路径。
- **行动代码架构钉死**：`this+0xc` = 战斗行动码（0=普通/1=瞄准/2=快刀/3=击中要害/4=一击必杀/5=换人…）。玩家经菜单 `0x469070`（5 项，回调 `0x469180`）→ 跳表 `0x46916c` → executor（`0x468af0`/`0x46a680`/`0x468cd0`/`0x468f00`/`0x4663f0`）→ **通用执行例程 `0x4663f0`**（全部 5 个 executor 与 `0x469310` 均 `push 2;push 1;push 0x5147f8; call 0x4663f0`）。
- **AI 侧 `this+0xc` 不显式写入** → 默认 0（普通攻击）；仅一击必杀条件满足时经 `0x469310` 升级为 `action=4 / flag=1`。战斗行动派发 `0x4684c0[this+0xc]`：0→`0x468457`、1→`0x468489`、2→`0x468495`、3→`0x4684a0`、4→`0x4684a9`。
- **`0x466470` = 行动代码检查**：`action ∈ {3,4,5}` → 1，否则 0（击中要害/一击必杀/换人）。**`0x468860` = AI 回合**（置 `this+0x10=0` → `0x4687b0` 伤害计算 → `0x468640` 应用，读 `this+0xc`）。
- **🔴 纠偏续91 旧假设**：`0x4687b0` 并非 AI 决策，而是**伤害计算**（读 `this+0xc` 仅用于 武力 bonus 门控：`action∈{3,4}` 时跳过）；`0x46baa0`/`0x46ba20`/`0x46bbb0`/`0x46ab20`（被 `0x468860` 调用）**全是动画/演出控制器方法**，不写 `this+0xc`。

### 证据文件

- `scripts/duel3_ref.py`（**21/21 自校验 PASS**：`check_action_code`/`compute_threshold`/`ai_decide_ikill`/`dispatch_handler`/`ai_select_action` 边界全覆盖）
- `scripts/duel_spec.json`（新增 `ai_selection` 段；`duel_state.this+0xc/this+0x18` 重写；`still_unknown` 刷新）
- `scripts/_ai7.txt`..`_ai20.txt`（`0x468860`/`0x469310`/`0x469070`/`0x466470`/`0x4663f0`/5 个 executor 反汇编实证）

### 仍未知

- 体力上限与初始值（`0x514995`/`0x514835` 初始赋值=寄存器间接写入，静态映像无绝对 store，需 emu）。
- `0x504d40`「手札数」双重语义（`[0x514978]&3` 索引，值+1）。
- **AI 非必杀档触发规则**：本次仅确认「普通攻击(默认) + 一击必杀(`0x469310`)」两档；快刀/击中要害/威吓/挑衅/换人 等是否由 AI 主动触发、其触发条件待查（玩家经菜单 / `0x469480` 等 handler 触发）。
- 跳表 `0x4684c0` 调度器静态无 `jmp/call [reg*4+0x4684c0]` 直接引用（疑为按序调用，表仅作地址表）。

### 下一步

- 收 **体力公式**（需 emu 跑初始化 dump 寄存器间接写入点）或 **領地表 entry[+4] 运行期语义**（需 emu）；或回头补 **AI 非必杀档触发规则**（追 `0x469480`/`0x4694aa`/`0x46950c`/`0x469547` 这 4 个 `this+0xc` writer 的调用方——它们无 `e8`/绝对表引用，疑经 `call reg`/`jmp [mem]` 抵达）。

---
