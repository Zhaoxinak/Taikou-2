# 太阁立志传2 · Godot 复刻进度表

> **用法**：做完一件就把 `- [ ]` 改成 `- [x]`。本表与 `Godot复刻实施方案.md`（M0–M7）和 `HD2D高清重制方案.md`（HD-0–HD-6）对齐。  
> 数据层（非图像）已 100% 破解，复刻只需「搬数据 + 写玩法 + HD-2D 表现」，不阻塞。  
> 最后更新：2026-09-10（**立绘批量生成续跑：105 → 205/695**，tier4 两波合计 100 张完成（tier4 157/647），id 60–168；本机 Mac 环境 `pip install Pillow 12.3.0` 打通 finalize 落地，流程与 SOP 一致）

---

## 阶段 0 · 工程基建 ✅（已完成）

- [x] Godot 工程根 + `project.godot`（1920×1080、`canvas_items`、`autoload DisplayAdapter`）
- [x] `src/` 目录分层：`core/` `data/` `world/` `battle/` `ui/`
- [x] `scenes/main.tscn` 标题入口 + `scenes/screens/` 占位（world/battle）
- [x] `assets/` 资源分层：`fonts/` `audio/{sfx,bgm}` `sprites/{portraits,units,chips}` `environments/` `samples/`
- [x] 目录重构（字体 `fonts/→assets/fonts/`；`.gitignore` 加 `Godot/` + `NVIDIA Corporation/` 运行时垃圾）
- [x] `GameData` autoload：`_ready` 载入 `data/*.json`，提供 `get_officer/get_castle`

---

## 阶段 1 · 数据层（M0 ✅ / M1）

- [x] **M0** 导出器 `scripts/export_for_godot.py` 跑通 → `data/*.json`（officers/castles/provinces/skills/battles/names/gaiji/text/consts）
- [x] **M1** `DataLoader` 内存索引：武将/城/国/技能/文本 按 id 查，无解析错误（float 键已用 `int(rec["id"])` 归一化；真实 Godot 跑通 `get_officer(13)=织田信长`）
- [x] 数据模型补全：`Castle` / `Province` / `Skill` / `Item` / `Battle` 强类型模型（镜像 `Officer.gd`，`src/core/*.gd` + `GameData` 五个 typed getter）；`Item` 数据由 `scripts/item_table.json`(189) 经 27→8 类目归并新生成 `data/items.json`（`_test_models.gd` 全过）
- [x] 国名 / 外字（GAIJI）渲染层：長宗我部 / 香宗我部 / 垪 三姓正确显示（`src/core/gaiji.gd` 解码 + 字形替换；`UiLabel`/`UiButton` 经 `Gaiji.draw_string_subst` 绘制，无外字时纯原生零回归；位图集 `assets/fonts/gaiji_atlas.png` + 生成器 `scripts/gen_gaiji_assets.py` + `_test_gaiji.gd`）
- [x] 修复 `gen_portraits.py` 国名推导（`0x30` 是氏族码，地理国走 `city→castle.province`，非 0x30）

---

## 阶段 2 · 合战沙盘 ⭐（M2 + HD-1 / HD-2）

- [x] **M2** `BattleSim` 纯逻辑移植（无 UI）— `src/battle/battle_sim.gd`，§9 公式逐位对齐 Python ref
- [x] 跑 38 张战图各 100 场，结果收敛（3800/3800 收敛、0 负兵力、side0 胜率 11.6% 验证 ×80% 不对称）
- [x] **HD-1** 3D 地形接入场景（`Terrain3DBuilder` 挂 `battle_screen.tscn`）— HJMAPDAT 19×40
- [x] **HD-2** 单位 sprite 系统接入（`UnitSprite` billboard + NEAREST，占位图回退）
- [x] **合战生产入口 `src/battle/battle_flow.gd`（闭合「合战能看不能打」缺口）**：
  - 🔴 **实测纠正**：`battle_sim.gd` **早就有** `build_ctx_from_battle`（真实战图 → ctx：sect_a ← `unit_table[*].raw` 9×20、单位 ← `deploy` 图非中性格）与 `simulate`（跑到一方归零，返回 `{winner, rounds}`）—— 测试里 38 图×100 场 = **3800 场全收敛**。真正缺的是**生产侧没人调用它**（`src/` 下无 `BattleCtx` 构建点，`battle_screen.gd` 仅静态展示）。本文件即那层薄入口。
  - **API**：`build(battle, seed, wet)`（建 ctx 并按降水设 `mode_m2`）+ `run(battle, seed, wet, max_rounds)`（跑完整场，附加 `wet`/`gun_units`/双方面剩余兵力）。自含不依赖 `GameData`，战图与 wet 旗由调用方注入。
  - **天气 gun 惩罚端到端打通**：`weather` wet 旗 → `ctx.mode_m2` ⇒ `cat==2` 洋枪 ×2/3（0x42d62e）。`tools/_test_battle_flow.gd` **12/12 实跑通过**——战图 0 降水后总战力 **15064 → 12862**（5 个洋枪被惩罚）、战斗由 35 回合延至 **42 回合**（战力低拖更久），抽样 8 张战图全收敛。
  - ⚠️ **诚实未接**：单位 `stat_atk/equip_tier/morale` 仍由 `build_ctx_from_battle` 依部署图派生（原版取运行时 obj `0x43e200` 等，静态无表 → 属既有占位，非本层引入）。~~UI 层待接~~ → **已在下一项闭合**（`battle_screen.gd` 调本入口 + 战果面板）。

---

## 阶段 3 · 最小可玩（M3 + HD-3）

- [x] **M3** 选主角 → 看状态（五维 / 技能 / 職位 / 忠诚）— `GameState` autoload + 选主角/状态画面，`_test_m3.gd` 23 项全过
- [x] **M3** 执行 1 条主命 → 推进 1 月（体力回升逻辑）— 修行（cap3/耗20/月回15）+ 休养；`_test_m3_ui.gd` 全链路 23 项（标题→选主角→状态→主命→事件流弹窗）全过
- [x] **HD-3** 光照 + 天气联动（晴/雨/雪/雾/夜）：`src/render/Hd2DEnvironment.gd` 已**接线进战斗场景** —— `battle_screen.gd` 新增 `@export var weather`（默认「晴」）+ `_setup_environment()`（建 `WorldEnvironment`、HIGH 档 Glow/ACES/SSAO（DOF 本版已移除），4K 经 `DisplayAdapter.is_4k()` 自动 `downgrade_for_4k`）+ `_apply_weather_now()`（经 `apply_weather` 联动 env 雾/底色 + `DirectionalLight3D` 能量/色温）+ `_spawn_weather_fx()`（雨/雪 `GPUParticles3D`）+ 运行时 `set_weather(kind)` 可切换；新增 `src/render/WeatherFX.gd`（雨丝/雪片材质·面片 + `needs_particles` 分支，CPU 安全可测）+ `tools/_test_hd3.gd`（42 项：三档环境/5 天气预设/雾开关/太阳能量/4K 降级/粒子参数/集成路径 / **实跑 42/42 通过**）。**实跑中暴露并修复两处 Godot 4.7.1 API 不兼容**：① 测试用旧 `env.tonemapper`→已改 `env.tonemap_mode`（生产 `Hd2DEnvironment.gd` 本就正确）；② 生产代码 `Hd2DEnvironment.gd` 引用已删除的 `dof_*` 属性（赋值直接 SCRIPT ERROR）→ 删除 DOF 块，`build_environment`/`downgrade_for_4k` 仅保留 Glow/SSAO/ACES/色彩微调（HD-2D 招牌已齐）。
- [x] **天气/季节判定逻辑（数据层，`weather_ref.py` 1:1 移植）**：新增 `src/core/weather.gd`（`class_name WeatherState`，RefCounted 自含、随机由调用方注入）—— 复刻 `0x43cad0..43d0e0`：天气 0晴/1曇/2雨/3雪 + 湿润旗 wet（`word[0x513530]` / `dword[0x51352c]`）、`season_of`（`(月//3)&3`，冬{12,1,2}）、`bucket` 3 档、`tick_simple`（0x43cfc0 简版）/`tick_region`（0x43d0e0 完整版，含雪国气候组 {0,2,4} 冬季分支）、概率表 `T_SIMPLE/T_SEASON/T_WINTER_SNOWREG/T_WINTER_NORMAL`（与映像自校验一致，weather==3 alias 到下一行首列为原版行为）、战斗联动 `gun_strength_penalty`（0x42d62e：降水且兵种 kind==2 ⇒ 战力 ×2/3）。`GameState` 接线：`weather` 实例（开局重置）+ `advance_month()` 每月 7 tick 简版推进 + `get_weather_state()`（含 `preset` 映射「曇→晴」供 HD-3 表现层）。`tools/_test_weather.gd` 51 项（17 显式场景 + 24 月长跑轨迹，期望值 Python 等价镜像推导）。⚠️ **雪国地域气候走 `tick_region`，需国表气候字节（0x519548+国*5+1）** —— 🔴 **实测纠正：该地址是运行时 BSS 缓冲区（脱壳映像 49 条全 `00`），静态导不出来**，原「尚未导出、静态替换即可」的设想不成立；须 Unicorn 实跑初始化代码后 dump 才能拿到。暂用简版 `tick_simple`。
- [x] **洋枪降水战力惩罚（0x42d62e）两处实现一致性校验闭合**：`tools/_test_gun_penalty.gd` **11/11 实跑通过**。
  - 🔴 **实测纠正（原标注「battle_sim 无兵种 kind 概念，gun 惩罚待补类别后接线」不准确）**：`battle_sim.gd` **早已有**兵种类别（`BattleUnit.category() = state & 3`）**且已实现**惩罚分支——`army_strength` 中 `cat==2 且 ctx.mode_m2 ⇒ idiv(atk*2,3)`（而 `mode_m2` 正是 weather 的 wet 旗 `0x51352c`）。真正缺的**不是概念而是接线与测试覆盖**。
  - 校验内容：cat2+降水 → 869（= ×2/3）、干燥 → 1347、非洋枪（cat0/1/3）**不受** `mode_m2` 影响、`weather.gun_strength_penalty` 四例、以及**跨模块一致**（weather 惩罚结果经同一 `idiv(v*100,23)` 缩放 == battle_sim 降水流，869==869）。
  - [x] **合战 UI 接线（battle_screen.gd 调 battle_flow，「能看」→「真能打」）**：
  - `@export auto_simulate/battle_seed/max_rounds/resimulate_on_weather_change` + `signal battle_finished(result)` + `var result`；`_ready` 跑完自动推演，`set_weather()` 因降水影响战力而自动重算。
  - `wet_flag()`：雨/雪 ⇒ 1（weather.gd `0x43d223/0x43d233`），进 `ctx.mode_m2` ⇒ cat==2 洋枪 ×2/3。
  - 战果面板：`CanvasLayer("BattleResult")` + `UiPanel`（标题「合戦結果」）+ `UiLabel`（居中，降水时附「※ 降水中：洋枪隊戦力 ×2/3（0x42d62e）」）。
  - `tools/_test_battle_screen_sim.gd` **13/13 实跑通过**：战图 0 晴 37 回合（左残 1738）/ 雨 52 回合（左残 2091），7 支洋枪队被削 ⇒ 战局真实改变；同 seed 同 wet 可复现；结果面板已挂。
  - ⚠️ 诚实保留：战果为**一次性整场结算**（跑完即出结果），尚无逐回合演出/手动指挥；`battle_screen.tscn` 目前无上游跳转入口（仅独立可开）。
- ⚠️ ~~`battle_sim` 从未被生产代码调用~~ **已闭合**（见上 `battle_flow.gd` + 本条 UI 接线）。

---

## 月循环纯逻辑（time_rollover / 评定 寄合）

- [x] **游戏日历 / 时钟 time_rollover 复刻**（`src/core/calendar.gd`，复刻 `scripts/time_rollover_ref.py` 0x4a0d50 进位链 + 0x49a120..0x49a1a0 setter 夹紧）：所有月份 **30 天**、每年 12 月、无闰年；日进位阈值 31（日上限 30）、月进位阈值 13（月上限 12）、年上限 1815（单字节宽度）。提供 `advance`/`roll_days`/`roll_months`/`year_of` 静态函数。`GameState` 接入：`advance_days/advance_month` 改委托 `CalendarRef`，并把原 `MONTH_DAYS=31` **修正为 30**（此前与原版不符，是 bug）。`tools/_test_calendar.gd` 25 项 + `scripts/_test_calendar_mirror.py` **25/25 实跑全过**（Python 等价镜像验证；本批次实跑 30/30 通过）。
- [x] **评定 / 寄合（council）纯逻辑核心**（`src/core/council.gd`，复刻 `council_ref.py` 16/16 + `council_report_ref.py` 18/18 + `council_tails_ref.py` 29/29）：12 主命常量 + 13 报告 handler→MSGX 映射 + `count_asked`(0x460500) + `rand_avail`(0x460530) + **报告选取状态机 `report_index`(0x460420)**（di/count/asked/flag6/flag8/slots/rng 全参数化）+ `target_name` 四段路由(0x49c2b0，ID17「商　人」/ID22 築城業者特例，resolver 注入) + `menu_items`(0x460320) + `dispatch`(0x4603f0)。`tools/_test_council.gd` **41 项** + `scripts/_test_council_mirror.py` 实跑全过（Godot 实跑 41/41）。
  - ✅ **NPC 名表已导出（闭合原诚实缺口）**：`scripts/export_npc_names.py` 从脱壳映像导出 `data/npc_names.json`（stride 7 内联 C 串，GBK/gb18030 解码）——**special 96 条 id 1000..1339**（历史人物/商人/南蛮传教士/剑豪 + 关系状态串 绝交/战/友/邸/家）+ **generic 94 条 id 3000..3278**（职业/信件/宝物/官位/建筑/关系：旅店主/农民/忍者/使者/同盟/坏）。过滤三层：① 7B 单元须含 `0x00` 终止符；② `is_name`（禁 Latin 字母/PUA(U+E000~U+F8FF)/`\ufffd`/控制符，须含 ≥1 CJK/假名/全角字符）；③ 孤立滤除（±5 窗口 ≥3 邻居）——排除名表之后其它数据结构（调色板/查找表/指针区）恰好解码成的伪名（挢/䎬/踟/浇B/E/>H）。
  - ✅ **解析接线**：`DataLoader.load_npc_names` + `GameData.get_npc_name(id)`（1000..1999 查 special、3000+ 查 generic、2000..2999 返回 `""`）+ `GameState.council_name_resolver`（`_council_resolve`：id<1000 武将 surname+given / 1000..1999 special / 2000..2999 `""` / 3000+ generic，注入 `council.gd.target_name`）。`tools/_test_npc_names.gd` **16/16 实跑全过**（导出产物纯净度 + GameData 端到端解析）。
  - ⚠️ **剩余诚实未接**：対象ID 段 **2000..2999 为运行时指针**（`dword[0x506c54]`）无静态名表 → `target_name` 回退 `"NPC{id}"`；名表外 id（如 1756/3400）同样回退占位。

## 事件解释器（MSGX 事件派发 / 条件判定 / 每 tick 状态机）

- [x] **事件系统纯逻辑核心 event_sys**（`src/core/event_sys.gd`，复刻 `event_handlers_full_ref.py` 18 id→49 handler + `event_predicates_ref.py` 49 handler 元数据 + `event_cond_ref.py` 条件求值 + `event_dispatch_tables_ref.py` 状态机 + `event_system_spec.json` taxonomy/EventListConfig）：
  - **EventCtx**：派发上下文（id/arg/flags/resultA/resultB）+ 显示上下文（taxonomy type_id + param0..4），结构 1:1。
  - **taxonomy 27 类**（寺院/历史事件/本能寺大火/结局…）+ **EventListConfig 27×4B**（每类 BGM/scene/params）。
  - **派发注册表**：`HANDLERS`（18 id → 49 handler，含 id29 纠正、0x490c0 笔误剔除、0x31/0x3f 边界排除）+ `PREDICATES`（49 handler → kind/ids/tables/gate/semantics/conf 全量）+ 查询辅助 `handlers_for/all_ids/predicates_for/kind_of/tables_of`。
  - **条件求值（纯函数可测）**：`eval_4e82c0`(op13/14：当前所在国==arg / 当前气候组==arg) + `eval_4e7e10`(op10：事件关联国==arg，flags bit2 门控) + `would_fire(ctx, rt)` 高层封装（rt 注入 current_province/current_climate/assoc_province）。
  - **每 tick 状态机 0x44d950**（ids 3/15）：`tick_dispatch(state, global_mode, arg_flags)` 经转码表 `TC_44DA00`=[0,4,1,4,4,4,4,2,4,3] + 跳表 `JT_44D9EC`=[DISPATCH_ID,STATE7,TERMINAL,EXIT]，门控 global_mode∈{2,3}/arg_flags&0x8000/state>9 均 → EXIT；有效状态恰 {0,2,7,9}；`state0_dispatch(id)` 二级分派 id3→0x44da10 / id15→0x44da90。
  - `tools/_test_event_sys.gd` 70+ 项 + `scripts/_test_event_sys_mirror.py` **ALL PASS 实跑全过**（Python 等价镜像验证；本批次实跑 47/47 通过；Godot `--check-only` 编译零错误）。
- [x] **事件解释器效果执行层 event_effects**（`src/core/event_effects.gd`，复刻 `event_id_dispatch_ref.py` 真实运行期 vtable **7 id = 0,1,9,10,13,14,15** + `event_handlers_full_ref.py` DECODED_CONDITIONS 的 MSGX id 序列）：
  - **真实 vtable 收敛**：原 `event_sys.gd` 的 49 handler 含菜单/UI/对话 applier（永不测 `[ctx+0]`，非事件调度成员），**真正事件调度成员仅 7 id**；`EventEffects` 据此只实现这 7 个。
  - **效果主体执行**：`dispatch_event(id, ctx, rt)` 按各 handler 的 MSGX id 序列经 `GameData.get_text` 发射叙事 —— id0/1→0x7d；id9→0x1224..0x1227(4 段)；id10→0x845/0x846(fire 经 sub-eval 0x49f610)；id15→0x44/0x53/0x2b/0x21/0x24/0x2f(七路跳表公共尾 6 段)；并写 `outcome` 占位记录（gift_or_event/local_event/province_event/scenario_trigger/audience_or_multi）。
  - **月度接线**：`GameState.poll_events()` 对条件门控事件(10/13/14)用 `EventSys.eval_4e82c0`(op13/14)/`eval_4e7e10`(op10) 按运行期注入状态(`evt_province/evt_climate/evt_assoc`，`set_event_location` 更新)评估，`advance_month()` 每月驱动 → 触发叙事写入 `event_log`（UI 事件面板源）；`force_event(id)` 显式驱动 effect 型事件(0/1/9/15)（原版 0x4d0ca0 月度 applier 调度谓词已证伪为 vtable 成员，本复刻未建模其自动触发 → 仅暴露显式入口）。
  - `tools/_test_event_effects.gd` **23/23 实跑全过**（force_event 5 类发射 + poll_events 自动触发 3 类 + advance_month 驱动 + 未定位/未开局不触发 + id13/14 narrative_pending 诚实标注）。
  - **状态画面事件流弹窗（event_log 渲染出口，闭环效果执行层 UI）**：`status_screen.gd` 新增 `_show_events_popup()`/`_close_events_popup()`——`_refresh()` 末检查 `GameState.pending_event_lines(_event_read_idx)`，非空则模态弹出 `UiPanel` 遮罩（和风面板 + 可滚动 `UiLabel` 叙事 + 「知道了」关闭钮）；`_event_read_idx` 推进到队尾避免重复弹；Esc 在模态中先关弹窗不回标题；`_release_skill_focus` 模态期间清技能钮焦点防误触。`_test_m3_ui.gd` 新增 6 断言（id15 叙事弹出 / id13 narrative_pending 占位行 / 关闭复位 / 不重复弹）闭环验证。
  - ⚠️ **诚实未接（效果层新项，同类缺口）**：① id0/1 概率分支(msg 0x8f/0x7f)+FIRE 后状态写入 依赖 0x49f430 存储值+RNG；② id13/14 触发后「历史事件叙事文本」未逆 → `narrative_pending=true` 仅记录触发；③ id9 条件 0x4b4b20（调 0x49f430(arg)==存储值）未建模全局 → 由 force_event/调度层决定能否触发；④ 完整 C++ vtable 仅 18 id 静态自断言（NPC 名表已导出，见上）。
  - ⚠️ **诚实未接（事件系统新项，同类缺口）**：
    1. **完整 C++ vtable 仅 18 id 经静态自断言反推**（0..0x3f 其余槽位需 Unicorn 运行时 dump，ref 已证无静态表）。
    2. ~~**NPC 名表**（id 1000..1999/3000+）同 council，待导出 → 显示名占位。~~ ✅ **已闭合**：`data/npc_names.json`（special 1000..1339 / generic 3000..3278）经 `GameData.get_npc_name` 解析。

## 经济 / 物价（economy · 与月循环耦合，填补 SHOP 经济模拟占位）

- [x] **经济 / 物价纯逻辑核心 economy**（`src/core/economy.gd`，1:1 复刻 `scripts/economy_ref.py` 单一入口模块，源端 `_selftest` **49/49 PASS** + 映像自校验一致 + `_emu_town_month.py`/`_emu_economy.py` 叶函数实跑验证）：
  - **算术基元**：`_imul_sar`（imul; sar 魔数还原：0x66666667 / 0x51eb851f / 0xae147ae1）+ `div10`(÷10)/`div20`(÷20)/`div200`(÷200)/`div_94_12`(÷94.12) + `muldiv`(a\*b/c，c==0→0xFFFF) + `sub_sat`(max0)/`add_cap`(min cap)。
  - **城镇字段**：`town_type_base`（0x49f960：(城种&7)→{0:0,1:1,2..4:2,5..6:3}）+ `TOWN_FIELDS` 31B 布局文档。
  - **月度城产 0x4a5aa0**：`rice_capacity`(min(+0xc×300,50000)) + `productivity_delta`(0x4a5b50) + `productivity_month`(未领有且非本藩 −15) + `month_produce_full`(0x4a5c80：prod=商×农/2、缺金军粮税 + 饥荒治安衰减) + `rice_to_food`(0x4a5d80 米→军粮 1:10 且民情/治安加权混合) + `commerce_rating`(0x49f9b0) + `develop_commerce_gain`(0x4aa290)。
  - **调度筑城 0x4593e0**：`castle_materials_days`（城种∈{2,3,6} 12 天、否则 14 天 − 已过天数）。
  - **商店买价 0x445ff0**：`shop_buy_price`（×1.5）。
  - **茶具 0x442270**：`tea_offer`(val×(100−prog≫1)/100) + `tea_buy_price`/`tea_sell_price`(×0.9)。
  - **宝物 0x44f4e0/0x458000**：`treasure_value`（6 段 CAT/SUB 钳制）+ `treasure_buy_quote` + `treasure_sell_quote`（普通类 ÷94.12 / 南蛮 cat=5 ÷200）+ `treasure_sell_gold`(×10)。
  - **米市 0x4e2d80/0x4e2f20**：`rice_buy_qty`/`rice_sell_qty`(price>500 且 gold/10≤5990) + `rice_tier`(<5便宜/5..11普通/≥12昂贵)。
  - `tools/_test_economy.gd` 49 项（`extends SceneTree` + `process_frame` 触发）+ `scripts/_test_economy_mirror.py` **49/49 实跑全过**（Python 等价镜像验证；本批次实跑 49/49 通过；Godot `--check-only` 编译零错误）。
  - ⚠️ **诚实未接（沿用 ref 标注，非本模块引入）**：① `0x513ea8` 店面逐商品月度改价（已证伪 EXE 月结链含此项）；② `0x51e1f0` 物品池 category/level 首次种子来源（已证伪 EXE 内空槽工厂）。本模块只搬运「可由静态数据算出的定价/生产公式」，不碰运行时对象池。与 SHOP 商业核心（`shop.gd` / `shop_flows.gd`）的关系：本模块提供其交易 handler 缺的**真实经济数值**（此前 `GameState` 对交易/外交主命维持经济模拟占位），后续接线即可替换占位。

---

## 大地图最小可玩空壳（方案 B · 主循环打通）

> 用户卡在状态画面无法继续 → 按建议先做最小可玩大地图空壳，打通「状态画面 → 大地图 → 移动 → 进城」主循环。纯逻辑 + 美术占位，城下町设施/主命/剧情待后续。

- [x] **坐标数据**：`scripts/gen_castle_map.py` 生成 `data/castle_map.json`。✅ **已升级为真实日本地图坐标**：源 = `scripts/towns.json`（前序已破 92 城原版 `map_x/map_y ∈ 0..47×0..36`，TOWNPOS.DAT + `castle_names_exe.json` 2026-08-28 EXE 权威名）；`map_w=48, map_h=36` 直接取原版网格（`project()` 自动等比 fit 屏幕）。
  - 200 城构成：**92 城真坐标** + **60 城同国中心派生**（国中心 + id 哈希 ±2 cell，确定性）+ **48 城全图兜底**（14/49 国无已知城 → 全图中心 ±6 cell 哈希）。
  - ⚠️ **诚实未接**：fallback 的 48 城**不代表真实地理**，仅保证不与已知城重叠 / 同国聚集 / 重跑稳定；待 Unicorn 跑 TOWNPOS 消费者 `0x4acbb0`/`0x4acc30` 完整 dump 剩余 108 城可闭合。旧版「按国 7 列网格 + 每格 5 列子网格」聚类（map 1820×1120）已废弃——其产物城名严重重叠、不像日本地图。
- [x] **数据接入**：`DataLoader.load_castle_map()` 读 `castle_map.json` → `castle_map{id:Vector2}`+`map_w`/`map_h`；`GameData` 加 `get_castle_pos`/`get_castle_positions`/`get_map_size`/`has_castle_map`。
- [x] **纯逻辑 `src/core/world_map.gd`**（RefCounted 无 class_name）：`project`/`unproject`（view 矩形 fit+center 投影）/ `clamp_pos` / `nearest` / `step`（移动 clamp，STEP=36）/ `within_enter`。可无头 + Python 镜像。
- [x] **GameState 大地图状态**：`player_map_pos`/`current_castle` 字段 + `enter_world`/`enter_castle`/`leave_to_world`/`move_player`/`nearest_castle`；`start_new_game` 置初始位置=城0坐标。
- [x] **状态画面入口**：`status_screen.gd` 加「外出（大地图）」按钮 + `G` 键 → `enter_world()` 切 `world_screen.tscn`。
- [x] **大地图场景 `src/ui/world_screen.gd` + `world_screen.tscn`**：方向键/WASD 移动主角（投影绘制 200 城点 + 主角 marker）、靠近城（距离≤90）高亮并在底部提示、Enter 进城显示城下町占位 overlay（城名/国/城主/兵米金/农商石高）、Esc 关闭 overlay 回大地图/回状态画面。
- [x] **测试**：`tools/_test_world.gd`（SceneTree + process_frame 触发）+ `scripts/_test_world_mirror.py` **12/12 实跑全过**（Python 等价镜像）；Godot `--check-only` 编译零错误。
- ⚠️ **诚实未接（本模块）**：① 城坐标为聚类近似（见上）；② 美术为极简几何占位，HD-2D 升级待 HD-6；③ 城下町通用设施（商店/宿屋/道場/医館）与主命执行已由 `castle_town.gd` / `command_screen.gd` 实现（非占位）；30 店铺人格专属交互与剧情推进仍待实现。

## 阶段 4 · 内政与修行（M4）

- [x] 12 主命 + 修行 8 动作（`GameState.train_mode` / `issue_command`，`_test_m4.gd` 全过）
- [x] **12 主命精确 delta 精逆（续254）**：开垦/改建/筑城（主命4/5/6）落 `game_state._command_deltas` —— 按 実行者内政力 + 城規模/城種 推导 + 字段硬上限钳制，与 naisei_ref 逐字节对齐（`_test_cmd_delta.gd` 28/28）；贩卖/购买军粮 handler 实测只算 仕事成果値、真实转移走纳结算（§3.21.9），复刻层对交易/外交主命维持经济模拟占位
- [x] 技能增长 cap 3 生效；功勲 `+= (旧级+1)×500` 封顶 60000（0xFFFF 哨兵归一化 60000）
- [x] **主命执行 UI（`src/ui/command_screen.gd` + `scenes/screens/command_screen.tscn`，填补「主命执行 仅占位 overlay」缺口）**：12 主命真正对玩家开放 —— 内政/军备类(0..6) 直接执行并显示**精确 delta**（M4 精逆 `_command_deltas`）+ 推进 1 月；外交类(7..11) 先开**目标国选择器**（49 国，标注当前外交関係/主从関係，自国按钮禁用）；谋略(11) 因原版无静态关系模型（`work_not_modeled`）**置灰不做假**；失败原因直接透传 `issue_command.reason`（not_started/bad_cmd/no_castle/bad_target/dispatch_blocked/work_not_modeled）且**不消耗月份**；状态画面新增「执行主命」入口。`tools/_test_command_ui.gd` **19/19**。⚠️ 诚实未接（本模块）：城下町**通用设施**（商店/宿屋/道場/医館）已由 `castle_town.gd` 实现（非占位）；但 30 个**店铺人格专属交互**（画师/医师/教会/南蛮商馆等，`shop_flows.gd` 已收口）尚未接入城下町 UI；剧情推进仍为占位 overlay

---

## 阶段 5 · 職位晋升（M5 ✅）

- [x] 勲功达阈值可晋升（`GameState.try_promote`：new_rank=cur+1 须 < 主君職位 且 勲功>=阈值；同城规避；`_test_m5.gd` 全过）
- [x] 大名 / 城主任命（`can_appoint_castle_lord` 门槛 rank>4+礼法!=0 / `appoint_castle_lord` 写城表派生状态；`inherit_daimyo` 继承人→大名+勲功拉满、嫡系→宿老+忠诚100）
- [x] 職位 ladder 名表换为正版（0x50d850 二进制自校验）：浪人/步兵头/队长/侍大将/部将/家老/宿老/大名/城主（修正旧 `足轻组头` 等误植）

---

## 阶段 6 · 表现层 HD-2D（M6 + HD-4 / HD-5）

- [x] **HD-4** 占位图就位（零积分）：`scripts/gen_placeholders.py` 生成 portrait/unit/chip 占位 PNG + `AssetLoader` 回退加载；真实 695 张立绘待批 ImageGen 额度后按同名替换
- [x] **HD-5 自绘 UI 基座**（`src/ui/`）：`UiTheme.gd`（设计空间 1920×1080 + 和风调色板 + CJK 字体）/ `UiButton.gd`（自绘 normal/hover/pressed/disabled 四态）/ `UiPanel.gd`（自绘底+金边+标题栏）/ `UiLabel.gd`（自绘多行文本）
- [x] **HD-5 全画面改造完成**：标题 / 主角选择 / 状态画面三者均已改自绘控件；全工程 **0 处 `add_theme_font_size_override`、0 个原生 `Button`/`Label`**；多分辨率由 `canvas_items` 自动缩放（1080p 1.0x / 2K 1.333x / 4K 2.0x），`_test_ui_hd5.gd` **42/42**
- [x] **音效 39 个接入**：`scripts/gen_sfx_assets.py`（`sfx_subsystem.json` 续195 权威表 + `_decoded_kos/` → `assets/audio/sfx/` 39 wav，全部 8-bit PCM@22050Hz 可载入）+ `src/audio/Sfx.gd`（id→名称/语义/类别/路径 常量表，含死资源 id15/36 留档）+ `src/audio/AudioManager.gd`（autoload：`play_sfx(id)` 对齐原版 `play_sfx(0x4997c0)` 数字 id、8 路并发池、惰性载入缓存、静音/音量、BGM 占位）；`UiButton` 点击联动 CLICK(0)，`_test_audio.gd` **21/21**
- [x] **MSGX 文本渲染接 UI（首批）**：状态画面新增帮助栏（`_help` UiLabel）——悬停 10 技能钮显示原版 MSGX 说明文（`GameData.get_text` + 技能槽→msgx id 映射 `7/8/9/12/15/13/14/16/10/11`），移出还原提示；`_test_m3_ui.gd` +3 断言（MSGX 帮助栏，17/17）。**事件流弹窗接入**：`_test_m3_ui.gd` 再 +6 断言（id15 叙事事件弹出 / id15 ≥1 行 MSGX / 含「知道了」关闭钮 / 关闭复位 / id13 narrative_pending 诚实占位行 / 已读不重复弹）→ 全链路 **23/23**；`_test_ui_hd5.gd` 43/43。对话/事件文本渲染留待 M7。
- [x] **BGM 34 首接入（原版 CD 音轨 / 中文版 MP3 方案，续255）**：`scripts/gen_bgm_assets.py`（最小 MP3 帧头解析器 → `src/audio/Bgm.gd` 常量表 COUNT=34/PATHS/DUR/SLOT_OFFSET + `scripts/bgm_tracks.json`，总长 **39.0 分钟**）+ `AudioManager` 逐分支复刻 `CdPlay(0x401310)` 语义（`play_bgm_track` 轨0/越界⇒停止、同轨重播跳过、`get_bgm_track` 对齐 `byte[0x501294]`、`play_bgm_slot` = 槽位+2）+ `title_screen` 起播轨 1；`tools/_test_bgm.gd` **24/24**（含 Python↔Godot 时长交叉校验）。⚠️ 未逆：「场景→BGM 槽位」调度表（播放函数 0 直接调用者，走虚表，需 emu 钩 CdPlay caller）；轨 1 = 标题曲属推定
- [x] **素材规格落地**：新增 `src/render/AssetSpec.gd` 作单一真源（立绘 **512×640** / 单位 sprite **256×256** / chip 32×32 / **地形材质 16 种**，`size_for`+`validate`+`describe`）。**地形材质补齐至 16 种并修掉真实显示 bug**：`Terrain3DBuilder` 旧表只覆盖 11 种，`?8/?9/?A/?B/?C` 五种共 **4234 格**落回 DEFAULT 灰+高度0；现按邻接统计推定补值（?8 邻接河流 53%→河原、?9 砂州、?A 湿地、?B 葦原、?C 城濠），**格子覆盖率 28880/28880 = 100%**（Python 校验）；新增 `TERRAIN_ORDER`(16)/`covers()`/`terrain_material()`/`build_all_materials()`。`gen_placeholders.py` 增出 16 张地形 chip(`terrain_00..15.png`)+`terrain_index.json`，`AssetLoader.load_terrain_chip(i)` 接入；`tools/_test_terrain16.gd` 校验（规格/16 种/全格覆盖/材质去重/占位图尺寸）。**实跑验证**：`tools/_test_terrain16.gd` 18/18 通过（Godot 零 Parse Error，地形覆盖 Python 独立验证 100%）

---

## 阶段 7 · 打磨（M7 + HD-6）

- [x] 单挑（duel）— `src/core/duel.gd` 复刻 `DuelSim`（RefCounted，无 class_name）：跳表 0x4684c0 派发 / 常规三段式伤害 0x4687b0→0x4698d0→0x466340（4×7 基表 0x505020 + 上限表 0x504d40）/ 大额公式 0x467c80·0x468000 / 暴击 0x467a70 / 瞬杀阈值 / AI 一击必杀决策 0x469310（仅 {普通攻击0,一击必杀4}，续93 实锤）/ auto_battle 收尾 + 确定性 `DuelRng`（seed + 脚本化队列）；`GameState.run_duel` 接线；`tools/_test_duel.gd` 全过（对齐 duel2_ref/duel3_ref/duel_ref 自校验）
- [x] 店铺 / 商业（核心逻辑）— `src/core/shop.gd` 复刻（RefCounted，无 class_name）：30 条店铺人格记录（`data/shops.json`，由 `msgx_text_tables.json` S8 导出，12B 布局 name_key/shop_msg/job/province/rank/favor/facility/progress/flags + talk/shop 文本）+ `JOB_NAMES`(12，续247 B1) + `ID_SLOT_TABLE`(30B，续242 A5) + 入店分发器 `enter_shop`（<30 有效 / ≥30 空，几何 0x44e710·0x44e7d4）+ favor 关系值饱和运算（sat_add cap100 @0x4a3630 / sat_sub floor1 @0x44e560 / set 钳 0..100 @0x49bfb0）+ 买药 ÷50（`medicine_doses`=gold//50，续247 C5 魔数 0x51eb851f+sar4；`medicine_cost`=服数×50；门槛 gold≥50 @0x445647）+ slot→设施名映射；`GameState.shop` / `GameState.enter_shop` 接线 + `tools/_test_shop.gd` 全过（33 断言）。设施内交互流程（续243）**已收口** — `src/core/shop_flows.gd`（纯逻辑、随机值由调用方注入以保证可测）：**画师 slot7**（袄绘依頼好感门 30 / 80 贯 / 天数=20+rand(41) / 鉴定 5 贯）、**医师 slot10**（就诊亲密度 +10、免费治疗「好感100 必免 else rand(100)<好感-10」、诊金 `gap×(100-親密度)×身分/100` 取 10 倍数保底 10、穷人流计数 cap3、药罐上限 10）、**教会 slot4**（义工门 70 / +5·+10 分歧 / 好感钳 69 / `+0x0b` bit1·bit0 首回各 +5 / 魅力 cap100、情报费 `5-捐额/20` 且捐 100 免费、`×10` 支付）、**南蛮商馆 slot3**（陌生人门 favor==0→msg738、问候 693/694 门 50、洋枪价 `15-好感/30`、好感100→特殊路径 `0x4508d0`），以及通用 setter/getter（`0x49bfc0/0x49bfd0/0x49bfe0/0x49bae0`）。依据 `scripts/shop_facility_flows_ref.py`（续243 字节锚点校验，**实跑 41/41 PASS**）+ `tools/_test_shop_flows.gd` 46 项（**Python 等价实现全过**；Godot 零 Parse Error）。⚠️ 剩余：闇商人事件流 `0x460890` 与品茶/铁炮打工/试合/忍里修业/30 日修行/学做生意等**其余设施**尚无逆向证据，留待后续。
+ **AI 主动外交决策（续104）已收口** — `src/core/ai_diplomacy.gd` 复刻原版 `0x4a84e0`（自含不依赖 GameData，数据由 ctx 注入）：**国力 `0x49faf0` 逐指令还原**（实机 capstone 反汇编 + 魔数数值验证）—— 城贡献 `(seisan×nousang)/25 + 100`（`0x51eb851f`+sar3=/25）、城種`&8`→+500、`0x49ace0` 判定假→减半、`castle_sum` 逐步 `min(acc+contrib,60000)`（`0x4ebca0`=min(a+b,c)）；国力 = 自国城力 + Σ従属国(主从=3 且 `word[+4]<370`)`×2/3`（`0x4ebc50`=(a*b)/c），再 `×(byte[0x0c]&0xf + 20)/20`（`0x66666667`+sar3=/20），`byte[0x0d]&3==3` → 翻倍，全程 cap 60000。决策：LOOP1 在「主从==2 且 lord<370」中挑**国力最大**者 → 解除主从 + 外交 `min(rel+2,7)` + MSGX `0xd1f`；LOOP2 对邻国（主从==1）同样处理 + MSGX `0xd1e`；`skip`/`ai_active` 双开关（对应 `dword[0x52060c]` / `0x4a0d10`）。`tools/_test_ai_diplomacy.gd` 23 项（**实跑验证**：`tools/_test_ai_diplomacy.gd` 25/25 通过）。⚠️ 未决：`0x49ace0`（城贡献减半判定）函数体较大未完全还原，已抽象为 `full_value` 入参并在注释标注。
- [x] 外交（diplomacy）— `src/core/diplomacy.gd` 复刻（RefCounted，无 class_name，自含不依赖 GameData）：国関係マトリクス 1176B（49×48/2，每字节 bit0-2=外交関係 8 级 / bit3-4=主从関係 4 级）+ `tri_index`=i*49-i*(i+1)/2+(j-i-1)（**实测 0..1175 全覆盖唯一**；🔴 已证伪 `diplomacy_spec.json` 里那句 asm 改写「0x51dc5f+(j+48i-i(i-1)/2)」，其最大值 1222 溢出矩阵）+ `rel_offset`（i>j 自动交换，非法 -1）+ get/set_diplomacy（异或掩码只改 bit0-2 @0x49fe40）+ get/set_master_vassal（掩码 0xE7 @0x49ff10，**有向 2↔3 镜像** `mirror(v,i,j)=(v∈{2,3} 且 i>j)?5-v:v`）+ `DIPL_NAMES`(8 盟友…交战)/`MV_NAMES`(4 空白/同盟/从属/支配)/颜色表 + `can_dispatch`(0x4c4270，按**主从**过滤：高压拒{2,3}/友好仅 0/情报无条件) + `init_relations`(0x4c2e4e) + `worsen_on_conquest`(0x4c7734，跳过 #24、主从非 0、已交战) + `friendly_success`/`pressure_success`(0x4b5bcb/0x4b6095) + `diff`/`min_trunc`(0x4ebcd0/0x4ebcf0) + `mission_level`(min((g[0x10]&3)+g[0x0d]/20+1,7)，**除数 20 非 10**)/`intel_merit`(城数*(lv+5)+100)/`mission_merit`；`GameState.diplomacy` 接线 + `tools/_test_diplomacy.gd` 全过（60 断言）。~~UI 层留待后续~~ → **外交类主命已接线（见下条）**；外交关系一览画面仍未做。
- [x] **外交类主命真实接线（主命 7..11 脱离经济占位）**：`game_state.issue_command` 经 `CMD_TO_WORK {7→9进贡,8→10威吓,9→11朝廷,10→12情报,11→13谋略}`（0x4c5699 写 ent+0x16，diplomacy2_ref §4）走 `diplomacy.gd`：
  - `can_dispatch`(0x4c4270) 目标国筛选（按主从；被拒 ⇒ ok=false + 不耗时/不扣钱/不推月）；`friendly_success`/`pressure_success` 关系写入；**有向镜像约定**：威吓成功后 mv(目标→我)=3 支配、我→目标 镜像=2（同 `_test_diplomacy [8]`）。
  - 0x4b9250 使者归还功勋：进贡 600 / 威吓 1000 / 朝廷 800·1000(得官位) / 情报 = 城数*(mission_level+5)+100（`province_city_count` 复刻 0x4d9e50 链表计数）；功勋经 `_add_merit` 封顶 60000。
  - ⚠️ **诚实建模边界**：① 成败无随机分支（0x47b5f0 恒成；0x4b94ac/4b94f4 直接「完成/成功」消息）→ 进贡/威吓按必定完成建模，不发明 RNG；② `work_type` 语义未逆 → opts 覆写（默认 0）；③ `g[0x10]/g[0x0d]` 语义未逆（BREAKTHROUGHS 续1105/1235 挂账）→ 情报默认 0/0、opts 覆写；④ 朝廷不改关系矩阵（RE 无写入点）；⑤ 谋略（拉拢武将）无静态模型 → `work_not_modeled` 占位不耗时。
  - `tools/_test_diplomacy_command.gd` **31/31 实跑通过**（进贡→同盟+亲密+600+耗1月 / 同盟再进贡被拒不耗时 / 威吓→支配镜像+1000 / 从属拒高压 / 情报功勋两档 / 朝廷 800/1000 不改关系 / 谋略占位 / 非法目标三种）。
- [x] 事件链（S15 事件进度位集，25B / 14 bits 命名）— `src/core/event_flags.gd` 复刻 25B 旗塊（段A/段B bitset + 段C byte数组 + 14 命名历史事件 + 主人公依存初始化 0x488030 + 派发链 poll + 序列化）+ `GameState.event_flags` 接线 + `tools/_test_s15.gd`（45/45 全过）；MSGX `%s` 模板事件文本渲染 **已完成**（`src/core/event_text.gd` 复刻 10 个有 MSGX 锚点 bit 的叙事文本渲染 + `%s`→主角名填充 + `GameState.event_text` 接线 `render_event_text`/`render_resolved_events` + `tools/_test_event_text.gd` 全过；bit 1/3/4/38 为 const/ref 锚点，无 MESSAGE1~4 文本，留待事件解释器接入 HEXMES 第5容器）
- [x] **HD-6 视觉回归 + 4K 性能基准（完成）**：
  - **无头可测部分 `tools/_test_hd6.gd` 21/21**：三档档位/缩放比（1080p 1.000x / 2K 1.333x / 4K 2.000x）+ `DisplayAdapter._pick_target_resolution` 选档 + 四主要画面控件零溢出 1920×1080 + 场景构建成本基线（0.7–1.9 ms/次）+ 字号层级（最小 24 → 4K 实际 48 px）。
  - **真实 GPU 基准 + 像素级视觉回归 `tools/_bench_hd6_gpu.gd`（GUI 实跑，45/45）**：SubViewport 与物理窗口解耦（2K 屏也能测真 4K）+ 关 vsync + `RenderingServer.viewport_{set_measure_render_time,get_measured_render_time_gpu/cpu}` 实测；**4K 最重画面 = 合战 1.069 ms GPU（≈935 FPS 等效），全部画面远低于 60FPS 预算 16.67 ms**（世界地图 4K 0.061 / 状态 0.069 / 标题 0.063 ms）；15 张三档截图落 `_hd6_out/`（gitignore），与 `baseline.json` sha256 比对 + 降采样 128×72 差异像素率 ≤2% 判回归，**当前全部 0.00% 差异**；基准前自动 `start_new_game(13)` 保证画面带真实数据。⚠️ 截图/基线为**本机 GPU 特定**，换机需重建基线（首跑自动）。
  - 状态画面截图目检：织田信长/五维/技能等级（3/3 封顶灰化）/帮助栏渲染全部正确，CJK 字体正常。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            

- [x] **外交关系一览画面（diplomacy_screen.gd，闭合进度表 line 156「外交关系一览画面仍未做」）**：以自国（`GameState.protagonist_province`）为中心，列出 49 国与自国的**外交関係（8 级 → DIPL_NAMES + DIPL_COLOR）**与**主从関係（4 级 → MV_NAMES + MV_COLOR）**，按等级色板着色的只读参考屏；含自国高亮、图例（8 外交色 + 4 主从色）、Esc/返回关闭。`command_screen` 底部新增「外交関係一览」入口钮（与「返回状态画面」并排，经 `DiplomacyScreen.new()` + `callback_back` 注入关闭回调，不改动 status_screen 按钮数）。全部走 `GameState.diplomacy` 已验证纯逻辑（diplomacy.gd 60 断言），本屏只做布局/着色，不修改矩阵。`tools/_test_diplomacy_screen.gd` **14/14**（49 行 / 自国正确 / 关系值合法 0..7·0..3 / 色板 / 图例 / callback 接线 / command_screen 入口与挂载-关闭）。⚠️ 诚实未接：① 主从过滤提示 `can_dispatch(0x4c4270)` 不在此屏展示（属可交互校验层，由 command_screen 目标国选择器按主从过滤）；② 关系为静态矩阵快照，不随月自动演变（原版月结链未在复刻层建模）。

---

## 待确认 / 阻塞 ⏳

- ⏳ 695 立绘批量生成：**205/695 已完成**（tier1 6 ✅ / tier3 42 ✅ / tier4 157，2026-09-10 续跑 100 张，id 60–168）；剩余 490 张 tier4 需 ImageGen 额度（约 5–10 积分/张）；**占位图已就位，复刻不阻塞**
- ✅ 本批改动已 commit + push（2026-09-10，`e57333e` → `origin/main`，30 文件 +2304/−20）：BGM / HD-6 / 主命执行UI / 外交关系一览 里程碑
