# NPKDATA.IDX 像素格式规范（✅ 已完全破解，2026-08-24）

> 配套解码器：`scripts/_decode_npk.py`（输出 `scripts/_probe/npk_decoded/entryNN_WxH_4bpp.png`）
> 逆向方法参考：kaodata (tzengyuxio) 项目的 `unpack_npk`（三国志英杰传 DOS 版 4bpp RLE），
> 但**太阁2 的 NPK 是 4bpp + 16 色 LE-16bit 调色板**变体，不能直接套用英杰传代码。

---

## 1. 容器结构

`NPKDATA.IDX` 是一个**单一归档文件**，内含 23 个图像条目。每个条目以固定 6 字节魔术
`"NPK016"`（0x4e 50 4b 30 31 36）为起点顺序拼接，**没有**独立的目录/索引表——靠扫
`NPK016` 魔术定位每个条目（下一魔术地址 = 上一条目结束）。

### 1.1 条目头（偏移相对各条目的 `NPK016` 起点 `p`）

| 偏移 | 长度 | 类型 | 含义 | 实测 |
|------|------|------|------|------|
| 0x00 | 6    | ASCII | 魔术 `"NPK016"` | 固定 |
| 0x06 | 1    | u8    | **type** | 全部 = `4`（→ 4bpp） |
| 0x08 | 2    | u16 LE | 显示宽（屏幕目标） | 全部 = `640` |
| 0x0a | 2    | u16 LE | 显示高 | 全部 = `400` |
| 0x0c | 2    | u16 LE | **原生宽**（图像真实像素宽） | 96 / 160 / 168 / 200 / 400 |
| 0x0e | 2    | u16 LE | **原生高** | 64 / 120 / 256 / 352 |
| 0x10 | 32   | 16×u16 LE | **16 色调色板**（RGB444） | 见 §2 |
| 0x30 | —    | 4bpp 控制位流 | **像素数据**（RLE 压缩） | 见 §3 |

> ⚠️ **关键纠正（推翻此前 8bpp 假设）**：
> 早期把部分大条目（200×352 / 400×256）误判为 8bpp，是因为"解码器填满了缓冲区"。
> 那是用 256 色调色板（实为像素数据误读）做垃圾调色板 + 回退拷贝产生的**假阳性**。
> **决定性证据**：全部 23 个文件的字节数都远小于 8bpp 所需（`0x310 + W*H`），
> 且 `0x10` 处都是同一个 **16 色** 调色板（不是 256 色）。→ **全部 23 条都是 4bpp**。

### 1.2 各条目尺寸表（证明全是 4bpp）

| e | 原生 W×H | 文件 size | raw8(最小) | raw4(最小) | 结论 |
|---|----------|-----------|-----------|-----------|------|
| 0–5 | 200×352 | 18.5–23k | 71,184 | 35,248 | 4bpp（size<raw4 因 RLE 压缩） |
| 6,7,10–12 | 400×256 | 20.8–36.2k | 103,184 | 51,248 | 4bpp |
| 8 | 160×120 | 7,857 | 19,984 | 9,648 | 4bpp |
| 9 | 168×120 | 7,514 | 20,944 | 10,128 | 4bpp |
| 13–22 | 96×64 | 1,365–1,405 | 6,928 | 3,120 | 4bpp（小肖像） |

判定规则：若 `size >= 0x310 + W*H` → 可能 8bpp；否则（所有条目）只能是 4bpp。
本档案**无任何条目满足 8bpp 字节下限**。

---

## 2. 调色板（16 色，LE-16bit RGB444）

- 位于 `p+0x10`，共 16 个颜色，每个 **2 字节 little-endian uint16**。
- 解码：`v = LE16(p+0x10+2*k)`；`r=(v>>8)&0xf`，`g=(v>>4)&0xf`，`b=v&0xf`；
  最终 RGB = `(r*17, g*17, b*17)`（4 位扩展到 8 位）。
- ⚠️ **字节序陷阱**：必须用 **little-endian**。若误用 big-endian，调色板会错乱
  （出现重复色、错误色相）——这是早期 PNG 颜色怪异的根因。
- 全部 23 条共用同一组 16 色（典型 VGA 风格 16 色）：
  `黑、蓝、绿、青、红、品红、黄、白、灰、深蓝、深绿、深青、深红、深品红、深黄、浅灰`。
  即前 8 色是亮 VGA 基色（0x0000/0x000f/0x00f0/0x00ff/0x0f00/0x0f0f/0x0ff0/0x0fff），
  后 8 色是对应的暗版本（0x7777/0x000a/0x00a0/0x00aa/0xaa00/0xaa0a/0xaaaa/0xaaaa 等）。

---

## 3. 像素解码（kaodata 风格 4bpp 控制位流）

像素从 `p+0x30` 开始，是 **4 bit-per-pixel** 数据，用一段**控制位流**做 RLE/回退解压。
输出 `W*H` 个 0–15 的索引，再经 §2 调色板映射成 RGB。

### 3.1 算法（伪码）

```
n = W * H
out = bytearray()
bitflag = 0          # 16-bit 控制位累加器
i = 0                # 源字节指针
while i < len(src) and len(out) < n:
    if (bitflag & 0xFF00) == 0:        # 控制位耗尽 → 取下一个控制字节（LSB-first）
        bitflag = 0xFF00 | src[i];  i += 1
    if bitflag & 1:                     # --- 回退（back-reference）---
        b = src[i];  i += 1
        run_size   = (b & 0x1F) + 1
        run_offset = ((b & 0x60) >> 5) + 1
        if b & 0x80:  run_offset *= W   # 跨行偏移（上一行同列）
        else:         run_offset *= 4    # 同行近邻（4 像素 = 2 字节）
        for _ in range(run_size * 4):    # 每次回退复制 4 个像素（= 2 字节）
            if len(out) >= n: break
            s = len(out) - run_offset
            out.append(out[s] if s >= 0 else 0)
    else:                               # --- 字面量（2 字节 → 4 个 4-bit 像素）---
        b1 = src[i];  b2 = src[i+1];  i += 2
        for _ in range(4):
            if len(out) >= n: break
            d  = ((b1 & 0x80) >> 4) | ((b1 & 0x08) >> 1)
            d |= ((b2 & 0x80) >> 6) | ((b2 & 0x08) >> 3)
            out.append(d & 0x0f)
            b1 = (b1 << 1) & 0xFF       # 逐像素左移取高位
            b2 = (b2 << 1) & 0xFF
    bitflag >>= 1
```

### 3.2 要点

- **控制位流是 LSB-first**：`bitflag` 初始为 `0xFF00 | byte`，每次 `>>= 1`，
  取最低位 `bitflag & 1` 决定本"单元"是回退还是字面量。
- **回退单元**复制 `run_size*4` 个像素（即 2 字节），`run_offset` 按 `b&0x80` 决定
  是"整行跨距（`*W`）"还是"同行近邻（`*4`）"。这与 kaodata 英杰传版一致。
- **字面量单元**用 2 字节解成 4 个像素，位打包方式见上式（`b1/b2` 的 bit7 与 bit3
  交错映射到 4-bit 像素的 bit3/bit2/bit1/bit0）。
- 全部 23 条该算法**恰好填满 `W*H`**（无截断、无溢出）→ 判定为正确解码。

### 3.3 验证指标（解码器打印）

| 条目类 | iso(孤立像素率) | 熵(比特) | 色数 | 判定 |
|--------|----------------|----------|------|------|
| 大图 200×352 / 400×256 | 0.28–0.49 | 2.5–3.4 | 12–16 | 真实图像 ✅ |
| 小肖像 96×64 | 0.21–0.23 | 1.9–2.1 | 11–13 | 真实肖像 ✅ |

（iso 越低越平滑、熵在 1.5–5 之间、色数≤16 且接近满调色板 = 正常 4bpp 图像）

---

## 4. Godot 复刻接入要点

1. 运行时从用户目录 `F:/Games/Taikou2/NPKDATA.IDX` 读取（不入库）。
2. 扫描 `NPK016` 定位 23 条；按 §1.1 读原生宽高。
3. 调色板按 **LE u16 RGB444**（×17 扩展）建 `Color[]`。
4. 像素按 §3.1 解成 `PackedByteArray`（0–15 索引），再用 `create_from_data` /
   `Image.create_from_data(W,H,false,Image.FORMAT_INDEXED? …)` 或手动 `putdata`。
   - 注意 Godot 内建议先解成索引 `Image`（或 RGBA），避免依赖 INDEXED 格式限制。
5. **渲染过滤器**：NPK 是 KOEI 预渲染画作/UI → 用 `LINEAR_WITH_MIPMAPS` +
   `generate_mipmaps()`；不要用 `NEAREST`（会出彩虹噪点）。
6. 显示尺寸用头里的 640×400（display W/H）做 blit 目标，原生 W/H 做源纹理。

---

## 5. 附：解码脚本用法

```bash
python scripts/_decode_npk.py
# → 打印 23 条验证表 + ASCII 预览
# → 导出 scripts/_probe/npk_decoded/entryNN_WxH_4bpp.png（正确 LE 调色板）
```

依赖：`Pillow`（已装 12.3.0）。
