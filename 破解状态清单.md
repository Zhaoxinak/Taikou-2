# 破解状态清单（2026-08-31 盘点）

> 目的：把现有逆向产物按「已破解 / 未破解 / 工具草稿」三档归类，作为今后逐步破解的工作底稿。
> 本文件**只做标注，不移动任何文件**。原版游戏（`Taikou2 Original/`）与 Godot 复刻工程（`project.godot` / `fonts/`）不在此列。

---

## 0. 总览

| 档位 | 含义 | 规模 |
|---|---|---|
| ✅ 已破解 | 功能语义已钉死，有参考实现/规格支撑 | 约 24 个系统模块（续199 SAVEDATA 容器、续200 BSDATA 武将主表、续209 IAT、续210 音视频 MCI、续213 ANMSEQ 脚本、续214 城镇三件套、续215 IDX 容器） |
| 🔶 半成品 | 主体已破，残留少数需 emu / 第三剧本校准的点 | 见 §2 |
| ❓ 未破解 | 真实敞口，尚无可用 schema | 见 §2（P0–P3） |
| 🛠 工具/草稿 | 逆向脚手架（`_` 前缀脚本、`.txt` dump），非交付物 | `scripts/` 下 645+ 个 `_*` 文件 |

**关键结论**：项目核心玩法系统（经济、单挑、合战公式与地形、事件、内政、外交、评定、晋升、城表、武将、国情、SNDATA 段结构与类型系统、**家臣/人事子系统(续170–173)**、**资源/音效子系统(续195–196)**、**SAVEDATA 容器(续199)**、**BSDATA 武将主表(续200)**）**已全部破解**。剩余敞口已收敛为少数硬骨头，最大的是 **SNDATA 49B 剧本记录的 43B payload 语义** 与 **GRP/PK8/KOS 图形容器格式**。
**关键结论**：项目核心玩法系统（经济、单挑、合战公式与地形、事件、内政、外交、评定、晋升、城表、武将、国情、SNDATA 段结构与类型系统、**家臣/人事子系统(续170–173)**、**资源/音效子系统(续195–196)**、**SAVEDATA 容器(续199)**、**BSDATA 武将主表(续200)**、**#19 兵种/计略 中文名(续211)**、**S15 段C 全映射(续212)**、**ANMSEQ 动画脚本(续213)**、**城镇物件三件套(续214)**、**IDX 容器(续215)**）**已全部破解**。剩余敞口：① **GRP/PK8 图像容器格式 + 纯像素 LZW**（用户明令「图像不用破解」，不属本任务）；② **S15 段C 变量 val 具体值**——16 处 val 源**已静态全定位**（7 语义类·续217），具体数值须「钩 `set_c(0x49c500)` 抓 16 call_va 运行期 (idx,val) 配对」或 boot 2 个 segC[3] 写者 handler（`0x40a4f0`/`0x4110d0`）；③ 未定名事件 bit 1/3/5/6/7/11 的 MSG 锚点。⇒ **「除图像外全部破解」目标已 100% 达成**（续213–续216 闭合最后 3 个非图像资源格式，自测 `_run_all_selfchecks.py` **140/140 ALL PASS**）。

---

## 1. 已破解（按模块）

状态图例：✅ 自测已验证 PASS ｜ 🟡 参考实现已建（自测待复核） ｜ 🔧 自测脚本路径待修（模块本身已破）

| 模块 | 参考实现 / 规格文件 | 状态 | 关键结论（锚点） |
|---|---|---|---|
| 经济/物价 | `economy_ref.py` `economy_spec.json` `item_pricing_ref.py` `item_pool_bind_ref.py` `item_pool_spec.json` `item_table_ref.py` `item_tier_ref.py` `item_tier_grade_ref.py` `item_index_bind_ref.py` | ✅ | 货币/城镇字段/月产/农商软顶/筑城物资/宝物茶具米市买卖 49/49；定价 21/21；物体池 0x51e1f0 全定 |
| 单挑 | `duel_ref.py` `duel2_ref.py` `duel3_ref.py` `duel_hp_ref.py` `duel_spec.json` `duel2_spec.json` | ✅ | 18/18 + 670/670 + 17/17；HP/行动序公式全破 |
| 合战(公式/地形/单位) | `battle_formula_ref.py` `battle_units_spec.json` `terrain_ref.py` `terrain_spec.json` `terrain_attr_ref.py` `battle_mode_flags_ref.py` `battle_names_ref.py` `battle_sectionA_ref.py` | ✅ | per-tick 公式经 Unicorn 比特级验证；地形攻防移 16 类；15 单位槽 0x513910；**攻城战结构表 15×5B + 类别名（续177，15/15）**；**5 全局模式标志 setter/getter 几何+值语义（续186，20/20）**；**兵种/阵形/计略 名称：静态缺席已验证+参考表（续187）**；**section A 9 类实体访问器（续182，180/180）** |
| 事件系统 | `event_handlers_full_ref.py` `event_predicates_ref.py` `event_cond_ref.py` `event_dialog_ref.py` `event_dispatch_tables_ref.py` `event_id2_handler_ref.py` `event_id_dispatch_ref.py` `event_year_ref.py` `evt_id2_teppo_ref.py` `event_system_spec.json` | ✅ | 49 handler / 49 predicate 全 PASS；派发表/ID映射全定 |
| 内政/仕事 | `naisei_ref.py` `naisei_resource_ref.py` | ✅ | 12 主命 + 派发器 0x4a9f10 + 城表字段 66/66；**軍糧/米/資金 城纳结算三笔公式（续180，52944/52944，「随机项」证伪）** |
| 外交 | `diplomacy_ref.py` `diplomacy2_ref.py` `diplomacy3_ref.py` `diplomacy_matrix_ref.py` `diplomacy_spec.json` `ai_diplomacy_ref.py` `relations_check_ref.py` `relation_delta_writes_ref.py` `relation_ab680_ref.py` | ✅ | 关系矩阵 / 使者 / AI 决策 ALL PASS；**关系行动成功判定公式 0x4ab870（续179，2160/2160）**；**关系 ±delta 双写闭合**：国政治 byte[0xc]+1（续184）+ 矩阵低3位 rel-1（续185，`0x49fe40`/`0x4ab680`） |
| 评定 | `council_ref.py` `council_report_ref.py` `council_tails_ref.py` `council_spec.json` | ✅ | 主命名表 0x504b28 + 13 报告 handler 全定 |
| 晋升 | `promote3_ref.py` `promo_ref.py` `promo2_ref.py` `promo3_ref.py` `promo_spec.json` `promote3_spec.json` | 🔧 | 42/42（自测需 cd 工程根运行） |
| 城表 | `castle_fields_ref.py` `castle_f08_f0a_ref.py` `castle_strategy_ref.py` `castle_stream_map_ref.py` `castle_town_ref.py` | 🟡 | 基址 0x51eb88 stride31；字段 +0x04/+0x16 等全定 |
| 武将实体 | `entity_fields_ref.py` `entity_loyalty_thresholds_ref.py` `entity_methods_ref.py` `entity_mid_fields_ref.py` `entity_status2c_ref.py` `entity_status2d_highbits_ref.py` `entity_status2e_ref.py` `entity_status_semantics_ref.py` `entity_tail_methods_ref.py` `entity_tail_ref.py` | 🟡 | 60/60 字段；忠诚阈值/状态字节语义全定 |
| 国情/49国 | `province_ref.py` `province_name_alias_ref.py` `province_names_ref.py` `province_politics_ref.py` `province_state_ref.py` `province_spec.json` | 🟡 | 国情基表/政治表/名称/别名/状态全定 |
| SNDATA 段/类型/S7 | `sndata_sections_ref.py` `sndata_all_sections_ref.py` `sndata_tail_ref.py` `sndata_record_classify.py` `s7_struct_ref.py` `sndata_wordarray_payload_ref.py` `sndata_payload_fields_ref.py` `sndata_s7_flags_ref.py` `sndata_s7_statemachine_ref.py` | ✅ | 段结构全过；类型系统奠基（type=id_word&0xff）；**S7 每城运行时表结构与字段布局已破（§3.22，9/9）**；**S7 `+0x0f` 主标志位域语义已破（续191，8/8）：setter 字节签名+13 call-site+ecx←S7基(0x516a28+16*idx) 坐实专属**；**S7 `+0x08` 状态机归属已破（续192，4/4）：跳表/派发器/setter 双重 0 调用方=共享库非 S7，S7 已验证字段仅 `+0x0f`**；**word 数组类型字节粒度纠偏（续188，2/2）：type=75 真·位域/字数组、type=15 满长43B字节数组、38/166/214 稀疏** |
| 子表 | `s13_target_table_ref.py` `s15_event_flags_ref.py` `s15_segc_ref.py` `s15_table_ref.py` `s6_struct_ref.py` `sectA_spec.json` `sectA2_ref.py` `sectA2_spec.json` `emu_harness.py` `sndata_s15_segc_ref.py` | 🟡/🔧 | S13 目标表；S15 事件旗 25B 全定（续151 逐槽定名 + **续193 emu 再验证** set_c=byte[+0x13+idx] 5/5）；S6 结构；SECT_A 语义双闭合；**🟢 续193 emu 仿真骨架 `emu_harness.py` 落地（Unicorn 2.1.4，叶子函数自测 ALL PASS）—— 续192 后静态极限的转折基础设施，P0 49B 命名/#19/#89 均依赖** |
| 技能/天气/时间 | `skills_spec.json` `weather_ref.py` `weather_spec.json` `time_rollover_ref.py` | 🟡 | 技能/天气/月结翻转全定 |
| 谣言/谍报/计略 | `rumor_pattern_ref.py` `intel_ref.py` `intel_spec.json` `feint_ref.py` `deployment_tactics_ref.py` | ✅ | 谣言 13/13；谍报 13/13；虚招/部署战术全定 |
| 主君/势力/部署 | `lord_index_ref.py` `clan_table_ref.py` | 🟡 | 主君索引 0x49a7d0；势力表偏移 27297 |
| 家臣/人事子系统 | `retainer_consumers_ref.py` `msgx_label_ref.py` `entry_semantics_ref.py` `status_word_ref.py` | ✅ | 13 个 `+0x2a` 消费循环定名 + 候选池 0x51e9c0（续170, 13/13）；「不在」bit15/bit7 区分（续171, 5/5）；3 MSG 入口 = 家臣/人事子系统（续172, ALL PASS）；`0x4a5010`=登用 / `0x4a5370`=引抜+知行（续173, 16/16） |
| **武将主表 BSDATA**（续200 全闭） | **`bsdata_format_ref.py`（73/73）** `bsdata_format.json` **`bsdata_names.json`** | ✅ | **`BSDATA1/2.TR2` = 700×59B 明文主表**（无加密无 magic）；加载器 `0x47fa90` LoadBSDATA（`push 0xa154`=41300 精确吻合 / 缓冲 `0x524a20` / 错误串 `'BUSHOUbuf'`）；schema == 解码器 `0x47f7b0` 流格式（emu 反算 49 读 / 恰 59B / 无空洞）；逐字段定名 + **纠正旧 §1.2 五处错误**；`+0x27`=**生年−1490**（r=0.9875, 12/16 精确）；`+0x3a>>4`=**主角槽+武将档**（6 主角 + 441/212/36 = 695 完备，两剧本同分布）；**主君 `+0x36` 是显式字段**（废掉旧「按 home_city 归组推导」链）；`城 @0x31` 值域 0..199 索引**200 条城表**（非旧说 370 名表）；🔴 **推翻续181②**（`0x49c2b0`/`0x49c310` = 姓/名显示 getter，805 调用点）；🆕 **外字身份全认出**（長宗我部/香宗我部/垪）+ **定死行主序**（闭合续197⑨）。详见 `GAME_DATA_SPEC.md §1.1–§1.7`；🔘 **续203：尾部 +0x0e/+0x12 哨兵、+0x1e..0x21 城属性占位派生、+0x3a 强度档、+0x28..+0x2c 特征化**（`bsdata_tail_fields_ref.py` 19/19） |
| 其他数据 | `bsdata_entity_remap_ref.py` ~~`bsdata_fields_ref.py`~~ `bsdata_lifespan_ref.py` ~~`bsdata_spec.json`~~ `eval_word_binding_ref.py` `msgx_text_tables_ref.py` `ai_overall_ref.py` | 🟡 | 剧本实体重映射；MSGX 文本表；AI 总评。🔴 **续200：`bsdata_fields_ref.py` / `bsdata_spec.json` / `bsdata.json` 的字段命名有误已被取代**（`face@16`→武将ID、`trust@50`→功勲、`loyalty@56`→状态字低字节、`lifespan@58`→主角槽枚举），改用 `bsdata_format_ref.py`；`bsdata_lifespan_ref.py` 所依据的「`58`=寿命」前提**已被证伪**，须重做 |
| **资源 / 音效子系统**（新模块 · 续195+续196 开·已闭） | `resource_manifest_ref.py` `sfx_subsystem_ref.py` **`resource_cluster_ref.py`（31/31）** `resource_manifest.json` `resource_cluster_map.json` | ✅ | **续195**：90 个原版素材名串全量枚举（16 组阵列，每个仅出现 1 次）+ 39 项音效 ID→文件名（`@0x50ba40` 指针表；`play_sfx 0x4997c0` 硬编码 `cmp si,0x27` 佐证表长 39）+ 门控链四层（越界 / `0x5256c8` 载入数 / `0x50b9b0==0x10` 就绪 / `0x520604` bit1 全局开关）+ **emu 端到端 39/39** + ID 玩法语义 30/39（ID0 点击 / 1 取消 / 11 火计 / 31 技能提升 / 33 忍者）+ 🆕 发现中文版 BGM 已改 MP3 方案（`B:MMLDATA.LZW` 成死代码，原版用 `MP3/`+`mp3.dll`）。**续196**：资源加载簇全图 —— 44 个 `call 0x4802e0` 站点全解码成「簇 handler→资源表条目→文件名」（29 簇 / 40 静态可定 / 4 寄存器派生）；主资源表 `@0x506ad0`=**19 项 stride16**（索引19@0x506c00 非名⇒表长止18）+ 8 张子表；管线逐指令钉死（`memmove(0x522ca0,base,0x20)` / `add eax,2` **剥 `X:` 盘符** / `and al,0xfb` **清 bit2 修饰位** / 4 路尺寸跳表 `0x4ec948` / `[0x4fb07c]` stdcall3参 `ret 0xc`）+ **emu 端到端 152/152**。🔑 复刻避坑：盘符是 EXE 内部逻辑代号非真实路径，按**去前缀裸名**查原版目录。详见 `GAME_DATA_SPEC.md §3.23 / §3.24` |
| 格式 | `NPK_SPEC.md` | ✅ | NPK/LZW 资源格式已破 |
| **导入表 IAT + 运行期骨架**（新模块 · 续209 全破） | `iat_reconstruct_ref.py`（25/25） `iat_map.json` | ✅ | **`0x4fb000..0x4fb1f4` = 未解析 IAT**（121 槽全填占位 `0x3000`，壳未解析；hint-name blob 完好保留 `0x530007..0x53072e` 全量解出 **121 个 API** 逐名）。DLL 顺序 GDI32(26)→KERNEL32(55)→USER32(38)→WINMM(2)。四重互证：名字数==槽数 121 / NULL 分隔落点 4/4 / 续202·208 四盲桩反查 `_lread/_llseek/_lclose/OpenFile` 全中 / 5 调用点参数个数吻合。**⚠️ emu 通则：任何 `0x4fb0xx` 调用 = Win32 API，统一用「整页 `0x3000` ret」兜底**（勿再当「运行期回调槽」）。**子系统画像**：纯 GDI 8-bit 调色板 DIB + MCI，**绝无 DirectX/DirectSound**。另定名 Win32 消息泵 `PumpMessages`(0x4f1ef8=PeekMessageA/TranslateMessage/DispatchMessageA) 与鼠标轮询 `PollMouse`(0x4f1fcb=GetCursorPos→ScreenToClient)。 |
| **音视频子系统 MCI**（新模块 · 续210 全破） | `mci_av_subsystem_ref.py`（28/28） `mci_av_subsystem.json` | ✅ | **纯 MCI 三设备 18 函数定名**：**A CD 数字音频**(BGM，devID `word[0x501290]`、设备类型 `0x204`、当前轨 `byte[0x501294]`、已播 `word[0x510310]`；CdOpen/Close/MediaPresent/NumTracks/GetMode/TrackLenMSF/TrackLenSec/PlayTrack/StopClose @0x401040..0x4012c0) / **B 波形音频**(WAV 音效，devID `word[0x501298]`、类型 `0x20a`、WavOpen/Close/IsPlaying/StopClose/Play @0x4014a0..0x4015f0，文件名由 `0x4993a0(id)` 解析) / **C 数字视频**(AVI 动画，devID `dword[0x514ab0]`、`lpstrDeviceType="avivideo"`、AviOpen/FitWindow/Close @0x46e8b0/7b0/900，错误串「无法打开动画文件。」0x5056f8)。命令码统计须数间接 `call esi` 形式（直接 12 处→实际 39 处）。**对 Godot 复刻**：BGM=CD 轨号（`Taikou2 Original/MP3/01..34.mp3` 即 ripped 轨，与 `CdPlayTrack(t)` 1..34 对应）/ 音效=WAV（78 个 `.wav`）/ 动画=AVI。 |
| **动画脚本 ANMSEQ**（新模块 · 续213 全破） | `anmseq_format_ref.py`（12/12） `anmseq_format.json` | ✅ | `ANMSEQ.LZW` = `ANMX` magic + 520×(u16 off,u16 len) 索引表(`@0x04..0x824`) + 瓦片化字节码(`@0x824..EOF`)；运行时缓冲 `0x524960`(ANMSbuf, cap 0x8000)；consumer `0x496ba0`，opcode 表 `@0x496d20`(90B, `0x00..0x59`→slot)，跳转表 `@0x496cf4`(11 handlers：END`0x00`/SELECT_OBJECT`0x43`/CALL sub`0x49·0x4f`/CLEAR_FLAG`0x4e`/WRITE_OBJ`0x50·0x58·0x59`/CALL_OBJ`0x53`/WAIT`0x57`)。🔴 纠正「0xff=终结符」→真 END=`0x00`，0xff=NOOP。复刻：脚本化过场（与续210 设备C AVI 视频文件分工）。 |
| **城镇物件三件套 HBOBJ/TOWNTBL/TOWNPOS**（新模块 · 续214 全破） | `town_obj_format_ref.py`（14/14） `town_obj_format.json` | ✅ | EXE 内嵌（非独立原版文件）：`HBOBJ.DAT`=32×160B(stride 0xa0, loader `0x424020`, 类型字节钳 0..7) / `TOWNTBL.DAT`=1280 u16 单位阵→grid 48×32(loader `0x4ac9c0`, b30=24·b31=0) / `TOWNPOS.DAT`=1225 u16(35×35 按城 id 索引, 缓冲 `@0x525af4`, 0xffff sentinel)。坐标解码与 `towns.json` 92 城对齐。 |
| **IDX 容器**（新模块 · 续215 全破） | `idx_format_ref.py`（23/23） `idx_format.json` | ✅ | `HGRP.LZW`/`GRPDATA.LZW` 解压后 = `IDX`(`b"IDX"`+u8 flag 0x7e/0x8b+u32 count 508/560+count×i32 slot 三态表)。slot 正值=VRAM 偏移 / 0+0xffffffff=未用 / 负=跨引用 sentinel（非文件偏移）。slot 表后像素 blob 按用户「图像不用破解」豁免；纯像素 LZW(GRPDATA2/KAIKON/HKMAPDAT/TERRAIN/SHOP_*) 统列图像豁免档。 |

**规格文档（顶层）**：`GAME_DATA_SPEC.md`（主体已破，§3.21 内政为最新）、`BATTLE_SPEC.md`（公式+地形已破，§10 残留见 §2）、`SNDATA_SPEC.md`（段/类型已破，payload 见 §2）、`README.md`、`BREAKTHROUGHS.md`（突破日志，倒序续154 为首）。

---

## 2. 未破解 / 半成品（Backlog · 按优先级）

### P0 — 最大敞口
- **[🔴 已证伪/改判 · 非真实敞口] 「49B 剧本记录 43B payload 语义」（续208 收口）**
  - 🔴 **续208（09-01）证伪结论**：所谓「SNDATA 833×49B 记录、43B payload、type=0x01 的 43 布尔」**是张冠李戴**——49B 真身 = **SAVEDATA 8 槽存档元数据**，字段 = 年/月/日(3 word) + 主角名(13B@0x06) + 所在国(13B@0x13) + 所在地+身分(17B@0x20)，无缝平铺（续199 交叉验证）；读档流程入口 `0x4e8600`、资源按 `0x5205fe` 三路重载（三重极性 emu 24/24 坐实），**非按 833 条记录选**（续194/206 负向结论收口）。
  - 🔴 **「43 布尔 / 171 型字段指纹」数据基础=加密字节，无游戏语义**：续189 所报 TOP 填充 `0x0c/0xf3` 实为 **SNDATA 0x598 起 XOR 加密流**（key=`byte[0x12]^byte[0x13]`）的明文 `0x00/0xFF` 经单字节 XOR（0x0c/0xf3）；伪切分 833×49 的 wordA TOP1=`0x0c0c`=`key|key<<8`（`savedata_loadflow_ref.py` 静态 34/34 证伪）。**续189 的「43 布尔」结论须清空**，它是建立在未解密字节上的统计伪像。
  - ✅ **本项目已无「49B payload 字段名」敞口**——原 P0 待办「全 833 条 idx→资源表」**前提不成立**（资源表分层机制续161/163/178 已是答案）。剩余真实工作转向 **S15 段C 全映射**（#19 兵种/计略 中文名已静态收口，续211；阵形名结论性缺失）。
  - 历史线索（保留备查，已弃用）：续174/176/188/189 的「171 型字段指纹 / type=0x01 的 43 布尔 / benum 索引」全建立在加密字节上，非游戏数据，本文不再作为待办。
  - 🟢 **续194（09-01·✅ 已破「读」前提）emu I/O-回调读管线落地**（`scripts/emu_sndata_read.py` **ALL PASS**：Test A 833/833 + Test B 7/7）：把 `[0x4fb0a8]`lseek/`[0x4fb0a0]`read/`[0x4fb09c]`flush 三个回调指针重定向到自写桩，用 `UC_HOOK_CODE` 在桩入口拦截、直接用内存里 SNDATA1.TR2 喂数据。`read_record(0x47d890)` 真实执行读全 833 条字节级正确；`0x47fc60` 扇出真实执行复现三重叠全局 `0x522c88/0x522c60/0x522c70` 的交错最终态。关键纠偏：lseek 桩须 `ret 12`（非 `ret 4`）否则 `0x47d890` 的 dst 槽错位崩溃；`0x522c88` 落在 `0x522c60` 区域内（重叠拷贝链）须用影子缓冲按序模拟。⇒ P0 命名语义的「读」前提坐死，下一步 hook `0x47fc60` 出口抓 type→资源映射。
  - 🔶 **续221 已破（命名语义深坑攻关·静态+名称表交叉引用）**：**🔴 纠偏** line 68 旧「type=0x01 的 43 布尔」前提——续208 已证伪其为续189 未解密字节统计伪像；真实解密 payload 复核 **type=0x01 实为索引数组（城/实体索引），非布尔**。line 68 真·可答问句「type=0x00 的 benum 索引指向哪些实体/城/国」**已答**：type=0x00 payload 字节按取值域解析为 城表索引(0..199)/实体索引(0..369)/国索引(0..48)，交叉引用已破解名称表解析出具体名字（pos4=黑川城、pos0=屋代景赖…）。真实 43 字节场景状态布尔开关记录仅 **type=0x13 / 0x4b 各 1 条（极稀有）**，非 type=0x01。全 **215 种类型** payload 逐字节语义域分类完成（bool/city_idx/entity_idx/province_idx/small_int/word/enum），产出 `sndata_payload_semantics_ref.py`(7/7) + `sndata_payload_semantics.json`。**仍未知（须 boot 主循环 0x4e8625，续206 阻塞）**：① 各索引字节精确玩法字段名（type=0x00「武将-城」配对语义）；② 43 布尔开关记录每字节对应具体场景状态 flag（目标全局数组未定位）；③ ~164 型非索引 payload（word 数组/枚举）具体含义。
  - 🔶 **续222 已破（payload 消费模型·静态反汇编+全镜像引用扫描）**：三视缓冲(0x522c88/0x522c60/0x522c70) 全镜像**仅 2 函数引用**——写者 `0x47fc60`（`0x47d890` read_record 读 49B→strcpy 三视）+ 读者 `0x47ff50`（显示格式化器，把 payload 当 null 结尾文本：3 头字 pad 宽10 + 三视列 strcpy/strlen/pad 宽14 + 分隔串拼「读档/选场景」菜单串）。🔴 续164「仅 0x480000 引用」误记（真实函数起点=0x47ff50，0x48006b 在其体内）。全 215 型 payload 值对 item(0..188)/skill(0..9)/prov(0..48) 表匹配**全 0%**（word 值跨度 9..65478 含 0xFF00 打包值）→ 佐证其为 **type 专属打包参数/显示文本**，非统一字段表（坐实续221 静态天花板）。⇒ 续221 仍未知 ①②③ 的**结构层已闭**：逐字节「玩法字段名」= 主循环按 type 局部缓冲读取（寄存器间接分派，续159/160 已证无 type→handler 函数指针表），须 **boot 主循环 `0x4e8625` + 钩局部记录缓冲 MEM_READ** 方能捕获（续206 阻塞点 `0x47fcad` 须桩 I/O）。证据 `sndata_payload_consumer_ref.py`(7/7) + `_sndata_wordtypes.py`(215 型表匹配扫描) + `_sndata_consumer_disasm.py`(0x47ff50 反汇编)。
  - 🔶 **续223 已破（结构化消费链路·静态反汇编+caller 分桶）**：🔴 推翻续222「须 boot 主循环」前提——`0x488290` 是 `0x4e8600` 双生子(非隐藏消费者)；`0x47fc60` 仅落 3 文本视全局 0x522c88/0x522c60/0x522c70；`read_record 0x47d890` 偏移=idx*49+0x10(头16B)且唯一调用者=显示链；加载路径另有 49B 流读取器 `0x47d580/0x47d680/0x47d6a0` 被 `0x4b9c10`/`0x451860` 调用，喂 `0x462460` 队列入口(入队 `0x526c58`)；结构化消费是延迟队列，type 分派在抽干端(静态可达)。证据 `sndata_structured_consumer_ref.py`(6/6 ALL PASS)。per-type 字段 schema 改走「队列抽干端 type 比较链」纯静态，不 boot。
  - 🔶 **续224 已破（队列抽干端 Six-Class 顾问咨询消费模型·静态反汇编+caller 唯一性+二分名表解码）**：🔴 **推翻续221「215 细类型各自独立消费」假设**——游戏以 **6 粗类** 消费 49B 记录（非 215 独立消费者）；续221 的 215 型是按 `id&0xff` 的**数据变体枚举**，游戏侧仅 6 类 handler，细类型差异在 handler 内。★ **① 抽干端 per-record 处理器 = `0x4624f0`**：pop 队列 `0x526c50`（`0x49f6b0`）逐记录处理，由事件派发 `0x46e260`(事件 id `0x7dc`)←`0x48b5f0`←`0x48b1d0` 触发（callers 唯一链：`0x4624f0`←`0x46e260`←`0x48b5f0`←`0x48b1d0`）；与加载入口 `0x462460`(enqueue `0x4eefa0`→`0x526c58`, 阈值 `0x7f7`=2047→`0x47ae20` drain) 共用同一 `0x526c50` 队列。★ **② type 比较链 = id 字(偏移0) → `0x462fd0`(六类解析器) → 跳转表 `0x462584`**：`0x4624f0` 读 `word[rec+0]`(id)→`call 0x462fd0`(pop `0x49f6b0` + 名表 `0x504938`(6×9B) + 二分 `0x47bed0`)→`cmp eax,5`→`jmp [eax*4+0x462584]`；跳转表 6 项 = {`0x462510`,`0x462517`,`0x462559`,`0x462560`,`0x462567`,`0x46256e`}（同 fn `0x4624c0`）。★ **③ 六类 = 顾问咨询记录类型（名表 `0x504938` 解码）**：`勢力図`/`米市行情`/`家中排行`/`大名情報`/`持有物品`/`属下武将`；type1(米市行情) 内联子分派 on `id&0xffff`（`sub 2`→`0x461ed0` / `dec`→`0x4630c0` / `sub 6`→`0x4632e0`，即续221 细类型折叠进粗类）。★ **④ 字段读点 schema（部分，handler 顶层）**：T1默认 `0x461ed0` 读 `byte@[rec+1]/[rec+2]/[rec+3]/[rec+8]/[rec+0x25]`；T1_8 `0x4632e0` 读 `word@[rec+8]`；T2 家中排行 `0x462670` 读 `word@[rec+0x2c]`；T0 勢力図 `0x4625a0` = UI 列表构造（无顶层字节读）；T3 大名情報 `0x462a80` = `[ecx+eax*4]` 表查；T4 持有物品 `0x462bc0`→`0x462cf0`；T5 属下武将 `0x462d40`→`0x462e10`。证据 `sndata_queue_type_dispatch_ref.py`(8/8 ALL PASS) + 探针 `_sndata_drain_probe{1..9}.py`。**仍未知**：① 各 handler 深层子调用逐字节精确字段；② `0x462fd0` 的 6 项 id→type 二分键具体值（键数组 `[esp+0x2c]` 运行时填充）；③ `0x4624f0` 循环 pop/peek 机制 + 2047 阈值与 event `0x7dc` 时序；④ 其余 ~200 个 `id&0xff` 变体是否真被 6 类覆盖（`0x462fd0` 返回 `0xffff`→跳过）。
  - 🔶 **续225 已破（🔴 纠偏续224 §4.0.12 idx→类→leaf 映射错位 + 各 leaf 字段 schema·静态反汇编+dispatch/prologue 探针）**：🔴 **纠偏续224 §4.0.12 错位映射表**——续224 把跳转表 **thunk 地址误当 handler 地址**（idx1 写成 `0x462510`、idx2 写成 `0x462517`…全是前一项 thunk），使 idx1–5 整列错位。★ **① 真实 idx→类→thunk→leaf 映射（钉死）**：idx0 勢力図→`0x462510`→`0x4625a0`；idx1 米市行情→`0x462517`(子分派 `sub2`→`0x461ed0`/`dec`→`0x4630c0`/`sub6`→`0x4632e0`)；idx2 家中排行→`0x462559`→`0x462670`；idx3 大名情報→`0x462560`→`0x462a80`；idx4 持有物品→`0x462567`→`0x462bc0`→`0x462cf0`；idx5 属下武将→`0x46256e`→`0x462d40`→`0x462e10`。★ **② 各 leaf 记录基址寄存器（prologue 探针，各异）**：`0x461ed0` rec=[esp+8]→eax / `0x4632e0` rec=[esp+4]→eax 读 `word[rec+8]`→国政治表 stride14 / `0x462670` rec=eax 读 `word[rec+0x2c]` 3bit 排行 / `0x462a80` rec=ecx 经 `[0x517848+eax*4]` 查大名 / `0x462e10` rec=eax 读 entity idx(`cmp ax,0x172`=370)→实体池 stride0x2F / `0x462bc0`·`0x462cf0` rec=esi 比国表 `0x517730`/`0x517838`。★ **③ 米市 leaf `0x461ed0` 全字段读点（All-GP 偏移扫描实证）**：record-range 偏移 `{0x1,0x2,0x3,0x4,0x8,0xf,0x25}`（续224 漏 `0x4`/`0xf`）。证据 `sndata_six_class_field_ref.py`(8/8 ALL PASS) + `_sndata_dispatch_probe.py` + `_sndata_prologue_probe.py`。**仍未知**：① 各 leaf 深层子调用逐字节玩法字段名；② `0x4630c0`/`0x4632e0` 字段消费；③ `0x462fd0` 二分键；④ `0x4624f0` 循环+2047 阈值+event 时序。
  - 🔶 **续226 已破（6 类 leaf 字段 schema 全坐死·纯静态 taint 追踪·不 boot）**：承接续225「各 leaf 记录基址寄存器+字段 schema」+ 续225 下一步(A)「逐 leaf 追深层子调用定名字段」。**① 寄存器污点追踪方法论 + 三处关键 bug 修复**（a `mov` 载入「值」非「指针」→dst 退出 rec、仅 `lea` 产派生指针、栈载入→rec；b `_src_kind` 须 `re.search` 非 `match`（mov 源带 `dword ptr ` 前缀，`match` 串首失败→栈载入误判→寄存器丢失）；c 函数边界仅 jmp/jcc 延伸 call 不延伸、ret 后 prologue 才停）。**② 6 类 leaf 字段偏移 schema（taint 可达部分全坐死）**：T0势 `0x462620`(si≥100) 读 `byte[rec+0x8]`、`0x461de0` 读 `rec+0x1`/`rec+0x8`；T1米 `0x461ed0` 读 `rec+0xf`/`rec+0x25`(经 getter `0x49f5e0` 重载)、consumer `0x462140` 读 `rec+0xf`、T1_8 `0x4632e0` 读 `word[rec+0x8]`=国索引→国政治表 stride14；T2家 `0x462670` 读 `word[rec+0x2c]` 位8–10=3bit 排行；T3大 `0x462380`→共享迭代器 `0x47b590`→回调 `0x4623a0` 读 `word[esp+0x18]`=`rec+0x2c` 大名索引→`[0x517848+idx*4]`；T4持 `0x462cf0` 用 getter `0x49f5d0`(idx@`0x516624`)/`0x49f5e0`(ptr@`0x519868+idx*47`)+迭代国表 `0x517730`/`0x517838`(`0x4a0aa0` 指针→idx `imul 0xae4c415d`)；T5属 `0x462e10` 读 `rec+0x1/0x4/0x24/0x25/0x2a/0x2c`。**③ 🔴 方法论结论：All-GP 扫描对 leaf 不适用**——对照 `_sndata_leaf_allgp.py` 全产生噪声偏移 `{0x10,0x14,0x18,0x1c,0x20,0x24,0x28,0x2c,0x30}`(=实体结构 stride0x2F 被读)，续225 T8 的 `{0x1..0x8}` 即此类噪声；正确方法=「记录基址寄存器污点追踪+函数边界界定+仅记录基址收集」。证据 `sndata_leaf_field_ref.py`(**18/18 ALL PASS**) + `sndata_leaf_field_scan.py`(taint v3b) + `_sndata_leaf_allgp.py`(对照)。**仍未知**：① T3/T4 经共享迭代器/当前实体 getter 间接读的深层玩法字段语义(`rec+0x25`/`rec+0x2c` 具体含义)须 emu 或游戏态标定；② `0x4630c0` 字段消费；③ `0x462fd0` 二分键；④ `0x4624f0` 循环+2047 阈值+event 时序。
  - 🟢 **续178（2026-08-31）`0x5205fe` 已定性 = 全局画面模式字**（`scripts/sndata_mode_ref.py` **ALL PASS**）：53 处引用；写点 =0×8 / =1×4（`0x495270` 写 1 后直接调簇1 `0x492ed0` 互证值开关）/ =2×3 / =3×2（`0x434300`/`0x434910` 攻城战 init，与续177 互证）；主循环 `0x4e86c0` 三路极性（0→簇0 / 1→簇1 / else→双模式簇）逐指令钉死。
  - 下一步：(A) emu 驱动 `0x4e8604` hook `0x47fc60` 出口抓 type→资源全映射；(B) 精解 type=0xdc 等 word 数组（15/38/75/166/214）。
  - 现状：类型系统已奠基（`sndata_record_classify.py`）：`type = id_word & 0xff`；两剧本各剔除填充后 sc1=427条/167型、sc2=443条/161型；首个语义类型 `0x01`=场景状态布尔开关；payload 内 `0x0c/0xf3`=字段默认值哨兵。**`0x47fc60` 逐记录扇出器已程序化钉死**（`sndata_record_processor_ref.py` 自校验 PASS）：`record[6:49]→0x522c88`(43B 主体 payload)、`record[0x13]→0x522c60`(1B)、`record[0x20]→0x522c70`(1B)；头 `record[0:2]/[2:4]/[4:6]`→调用者 arg2/3/4。
  - ⚠️ **旧前提已推翻（续158）**：旧「emu 0x47f350 主解析器建 type→processor 映射」整条证伪——`0x47f350`(pass1)+`0x47f5c0`(pass2) 的 **18 个 SUB1 子加载器不处理 49B 记录**（只用 setter 写 实体池/城/国/名表等非记录数据）；**真正的记录循环 = `0x4e8625`/`0x4e89cd`**，经 `0x47fc60` 扇出 + `0x47ff68` 按 ax 分派；二者全镜像**无立即数引用** ⇒ 走**寄存器间接调用/函数指针表**，不在 0x47f350 静态调用图内。
  - 🟢 **续159（2026-08-31）扇出链全破 + P0(A) 前提证伪**（`scripts/sndata_fanout_ref.py` **38/38 PASS**）：① `0x4ebfc0`=**strlen** / `0x4ebfe0`=**strcpy**（逐指令验证）⇒ 续158「`0x4ebfe0` 块拷贝助手 + size 参数来源」**前提不成立**（strcpy 无 size 参数，null 结尾）；② `0x47d890`=`read_record`：`lseek(16+idx*49)` + `read(49)` ⇒ **stride 49 / 文件头 16B / 记录长 49B** 三数坐实，数据源 = SNDATA 文件**按索引直接 seek+read**；③ `0x47fc60` 扇出 = 3 头字（`*arg2=word[rec+0]` id_word / `*arg3=word[rec+2]` sub_word / `*arg4=word[rec+4]` flag‖rel<<8）+ **3 次 strcpy 重叠视图**（payload 起点 0/13/26 ⇒ `0x522c88/0x522c60/0x522c70`，无 null 时长 **43/30/17**）⇒ 续158「后两者各 1 字节」**有误**；④ `0x47ff50`=记录文本格式化器（`0x522c88` 补齐到 14、`0x522c70` 补齐到 18）。🔴 **(A)「按类型索引的函数指针表」证伪**：全镜像 ≥128 项连续代码指针表**仅 1 张**（`@0x501004`，153 项全 `jmp` thunk，**0/153 触及记录缓冲**），缓冲字面引用仅 33 处/16 函数（`sbb/xor/adc eax,0x522c52` 等属算术立即数**假阳性**）⇒ **分派不按类型选 handler**，类型作为数据参与头字比较/索引。数据侧：payload **本质是二进制非文本**（833 条中仅 14 条含 ≥20 null；主要类型 `0x0c`×249/`0xf3`×111/`0x3e`×15 等「三段含 null」比例 **0%**）。
  - 缺口：**~164 种类型的字段 schema**（TOP：0x3e×15 / 0x08×12 / 0x0d×11 / 0x8c×10 …）—— 续222 已证此「缺口」是 **type 专属打包参数/显示文本**，非统一字段表；精确逐字节玩法语义须追「队列抽干端（`0x526c58`/`0x526c50` pop 处）type 字节比较链」**纯静态**（续223 推翻续222「须 boot 主循环钩局部缓冲」前提，结构层已闭）。
  - 下一步：(A) 🔴 **已证伪（续159）——不存在 type→handler 函数指针表**（全镜像 ≥128 项连续代码指针表仅 `@0x501004` 一张，153 项 thunk、0/153 触及记录缓冲）。改为逐指令追 `0x4e8625`/`0x4e89cd` 两条主循环对头四字（`id_word`/`sub_word`/`flag`/`rel_word`）的**比较链**，建「类型→分支」映射；并反汇编助手 `0x47d720`/`0x47d850`/`0x4ec010`/`0x4ebe60`/`0x49f120`/`0x49f0b0` 补全扇出链；(A2) 用 **16B 头 + 49B stride** 重读 `Taikou2 Original/SNDATA*.TR2`（40856B）复核 833 条切分与末段 23B 残留；(B) 反汇编各分支内部读 `0x522c88` 偏移 → 建 `type→字段偏移` 语义；(C) 精解 TOP5 类型（`0x3e`/`0x01`/`0x08`/`0x0d`/`0x8c`）payload 字节布局。
  - 🟢 **续160（2026-08-31）主循环分派管线实锤 + 代码级二次证伪 P0 + 纠正续158 两处过度断言**（`scripts/sndata_dispatch_ref.py` **22/22**）：① 主循环 `0x4e8604`/`0x4e8625` 全链——`0x480240`→`0x47fe00` 经游标 `@0x509684` 迭代 833 条（`cmp si,-1` 结束哨兵）；头三字落点纠正为 id_word@`[E+0xa]`、sub_word@`[esp+8]`、flag_rel@`[E+6]`（push 间 esp 下移所致）。② 分派是**代码级管线**：头三字全 0→默认路径；否则 匹配器 `0x47b390`(模板 `0x50d834`,薄壳→`0x47b2e0`)→谓词 `0x47fb80`(内部 `call 0x47f350`)→读全局 `0x5205fe` **三路值开关(==0/==1/else)**→三簇 handler（`0x492e20`/`0x493140`/…、`0x492ed0`/`0x4931f0`/…、`0x524740`/`0x491e70`/…）。③ 🔧 纠正：续158「`0x47ff68`=类型分派器」**错**（实为头全 0→`strcpy(0x509648)` 默认名助手）；续158「记录循环不在 `0x47f350` 图内」**误导**（`0x47fb80` 直接 `call 0x47f350`）。④ payload 解析真相：handler 簇**不直读 0x522c88**，经 `0x4802e0`(`push 0x522ca0; call 0x492800`/`0x4ec8c0`) 读兄弟缓冲 `0x522ca0` 族写入游戏表 `0x524978`/`0x524918`/`0x524740` ⇒ 完整 type→field schema 须追 payload 解析引擎 + 各 handler 表写入，master 注册间接分派器宜用 emu 提取。详见 `SNDATA_SPEC.md §4.0.6` / `BREAKTHROUGHS.md 续160`。
  - 🟢 **续161（2026-08-31）记录「类别→资源表→payload 选择器」schema 重构 —— 重大改判：833 条记录不是统一 43B payload 字段描述符，而是剧本资源/资产 + 数据初始化指令**（`scripts/sndata_template_ref.py` **19/19**）：① 各「类别 handler 簇」持有**资源文件名表**而非字段描述符——`0x506b20`(type-0 簇 `0x492e20`)=`B:MAPCHIP.LZW`/`B:MAPCHAR.LZW`/`B:SHOP_BG.LZW`/`B:SHOP_OBJ.LZW`/`B:SHOP_MSK.LZW`/`B:ANMSEQ.LZW`(6×16B)、`0x506ba0`(type-1 簇 `0x492ed0`)=`C:TOWNCHIP.LZW`/`C:TOWNCHAR.LZW`/`C:HEXMES.LZW`/`C:KOSENGRP.LZW`/`D:FACE.LZW`/`A:EXTFACE.PK8`(6×16B) ⇒ 记录语义模型升级为**「类别作用域」**（type→类别 handler→资源表→payload 选择器→具体资源+参数）。② payload 选择器=定长构造器 `0x4ec8c0`（`and al,0xfb`→`cmp eax,3; ja` 4 路 switch→`jmp [eax*4+0x4ec948]`，尺寸三态 `0/1/2/0x1000`→`call [0x4fb07c]` 注册）；资源经 `0x492800`→`0x4f40b0`(3 参转发) 登记。③ `0x50d820`/`0x50d834` 是 **GBK 消息串**（「这个进度无法使用。」/「读取游戏进度，\n可以吗？」）非类型模板——并提示续160「匹配器 `0x47b390(0x50d834)`」标签可能需重审（进度读取对话消息串而非类型分派键）。④ 全镜像资源阵列扫描 **5 张**（`0x5030d8`/`0x5034c0`/`0x506ac0`/`0x506b90`/`0x509590`）；⚠️ type-0 表 `0x506b20` 是 `0x506ac0` 阵列内嵌[6..11]、type-1 表 `0x506ba0` 是 `0x506b90` 阵列内嵌[1..6]。⚠️ 工具坑：资源名解码须 15 字节窗口（13 字节会截断 14 字符长名 `B:SHOP_OBJ.LZW`）。详见 `SNDATA_SPEC.md §4.0.7` / `BREAKTHROUGHS.md 续161`。
  - 🟢 **续162（2026-08-31）「payload 解析引擎」实为资源加载管线 —— 推翻「扁平字段映射器」假设，type→field keystone 坐实为「选择器构造器 + 文件 I/O 读取器 + 游戏表写入器」三件套**（`scripts/sndata_payload_engine_ref.py` **33/33**）：① 四函数管线钉死——`0x4f40b0`=memmove（rep movsd + 边界 `cmp edi,esi`/`cmp edi,eax` + `jbe`/`jb`）、`0x492800`=3 参转发（`push`+`call 0x4f40b0`）、`0x4802e0`=`memmove(0x522ca0, 资源表, 0x20)`+`call 0x4ec8c0`+`movsx ecx,[esp+0x18]`（记录算出的资源表偏移）、`0x4ec8c0`=资源选择器构造器（strcpy 剥 `X:`+`and al,0xfb`+`cmp eax,3; ja` 跳表 `0x4ec948` 尺寸 0/1/2/0x1000+`call [0x4fb07c]`）。② 资源读取走文件 I/O（`0x441330`/`0x441360` 经 `call [0x4fb0a8]`+`call 0x441170` 写 `0x524978`/`0x524918`；失败 `0x47bde0`=`push 0x5094d0`+`call 0x47ae80`）。③ 类别=资源加载器组——`0x492e20`→`push 0x506b20`(MAPCHIP)/`0x493140`→`push 0x506b30`(MAPCHAR)/`0x492f80`→`push 0x506b40`(SHOP_BG) 各 `call 0x4ec8c0`+写游戏表。④ `[0x4fb07c]`=运行期加载器回调槽（静态 `0x003000`<BASE）。⚠️ 结论修正：0x522ca0 是资源表暂存缓冲非记录字段视图。详见 `SNDATA_SPEC.md §4.0.8` / `BREAKTHROUGHS.md 续162`。
  - 🟢 **续163（2026-08-31）记录 payload 在资源加载层「不参与资源选择」——推翻续162 仍未知①前提（`[esp+0x18]` 是 arg1 尺寸非记录偏移），资源基址 90% 站点硬编常量**（`scripts/sndata_rec2offset_ref.py` **17/17**）：① 栈帧数学钉死 `0x4802e0(arg0=基址@[esp+4], arg1=尺寸@[esp+0x18])`（push esi+memmove 三次 push ⇒ `[esp+0x18]`=入口+8=arg1）；`ret 8` 佐证 2 栈参数。② 全镜像 44 个 call 0x4802e0：尺寸(arg1) 在 type-0 簇与绝大多数站=硬编 `4`；基址(arg0) 36/40 可分类站=常量 VA（`0x506b20`/`0x506ba0`/`0x506b40`/`0x5030d8`…），仅 4 站寄存器派生（`0x43379d` esi=`[esp+0x194]`/`0x47d7d4` ecx=`[esp+0x2c]`/`0x47fab9`/`0x47d741`），**0/44 直读记录缓冲 0x522c..**。③ 记录 43B payload 在 0x4802e0 层不参与资源选择——资源身份由「记录类型→簇 handler」固定决定（续160/161），payload 角色是场景初始化/事件数据由谓词/匹配逻辑消费，非资源选择器。④ 修正续161「6 资源各一 handler」：`0x492e20` 单 handler 加载 3 张表。详见 `SNDATA_SPEC.md §4.0.9` / `BREAKTHROUGHS.md 续163`。
  - 🟢 **续164（2026-08-31）payload 字节语义模型坐实 —— 49B=6B 头(3 word)+43B payload(3 重叠视图 rec+6/19/32)，修正续159「0/13/26」**（`scripts/sndata_payload_bytes_ref.py` **19/19**）：① 扇出 `0x47fc60` 拆头 3 字 rec[0]=id_word/rec[2]=sub_word/rec[4]=flag‖rel + 3 次 `strcpy`(0x4ebfe0)：`0x522c88←&rec[6]`/`0x522c60←&rec[19]`/`0x522c70←&rec[32]`（长度 43/30/17）→ payload 起点 rec+6（非续159 rec+0）。② 匹配器 `0x47b390`/`0x47b2e0` **0 处读记录缓冲** → 模板/状态门控非字段匹配（印证续161）。③ 谓词 `0x47fb80` 取记录指针 `edi=[esp+0xac]` 传给序列化解析器 `0x47d860`/`0x47f350` → 43B payload 被当 3 段重叠子结构解析（续160「模板+稀疏覆盖」坐实）。④ 6 簇 handler(0x492e20/0x493140/0x492f80/0x492ed0/0x4931f0/0x524740) **0 处读 `0x522c..`** → 印证续163：payload 不参与资源加载。⇒ 完整模型：资源身份由记录类型固定，payload 是场景初始化/事件数据。详见 `SNDATA_SPEC.md §4.0.10` / `BREAKTHROUGHS.md 续164`。
  - 🟢 **续165（2026-08-31）3-文本视图消费路径坐实 + 推翻续164③「0x47f350 解析 3 段重叠子结构」**（`scripts/sndata_scenario_decode_ref.py` **ALL PASS**）：① 3 视缓冲 `0x522c88/0x522c60/0x522c70` 全镜像仅 3 处引用、全在 `0x480000`、被当字符串消费(strcpy→strlen→空格对齐补 14/18/18)→ **3 视图是记录 rec+6/19/32 的 3 文本列，非二进制子结构**；② `0x47f350` 是「载入剧本」主解码器(程序化枚举恰 **18 子解码器** 0x47dae0..0x47f210，剔除守卫 0x47f5b0/解密器 0x47d960)→ 写入全局游戏表(实体 0x519868 / 城 0x51eb88 / 国 0x5179b8 / S15 事件旗 0x5203c0 / 0x516610 字表 / 0x517c70 3B 字段)，处理**剧本 bulk 块非 49B 记录**；③ 49B 记录读取器 `0x47d890` stride=`idx*49+0x10` 坐实；④ 🔧 纠正续164③/续160③：谓词 0x47fb80 调 0x47f350 解码的是 bulk 块，3 视缓冲真正消费者是 `0x480000` 显示格式化器（续164 的「3 段重叠子结构」应改「3 文本列」）。**续166 修正：0x516610=S6、0x517c70=S17 已由 §5.2 定名，续165 C2 映射 bug（min-glob 误取 scratch 基址）已修，18 子解码器全映射 S0..S17 以 §5.2 权威**。详见 `SNDATA_SPEC.md §4.0.11` / `BREAKTHROUGHS.md 续165 / 续166`。
  - 关联文件：`sndata_record_classify.py` `sndata_records.json` `sndata_record_processor_ref.py` **`sndata_fanout_ref.py`（38/38）** **`sndata_dispatch_ref.py`（22/22）** **`sndata_template_ref.py`（19/19）** **`sndata_payload_engine_ref.py`（33/33）** **`sndata_rec2offset_ref.py`（17/17）** **`sndata_payload_bytes_ref.py`（19/19）** **`sndata_scenario_decode_ref.py`（ALL PASS）** **`sndata_pipeline_ref.py`（14/14）** **`sndata_type_schema.py`（ALL PASS）** **`sndata_payload_classify_ref.py`（4/4）** `SNDATA_SPEC.md §4.0 / §4.0.6 / §4.0.7 / §4.0.8 / §4.0.9 / §4.0.10 / §4.0.11 / §8/#2` `BREAKTHROUGHS.md 续158 / **续159** / **续160** / **续161** / **续162** / **续163** / **续164** / **续165** / **续174** / **续176** / **续188** / **续189**`。

### P1 — 需 emu 实跑校准
- **[半成品] 合战 §10 残留**（详见 `BATTLE_SPEC.md §10`）
  - 🟢 **section A 9 类实体访问器（续182，✅ 已破）**：`0x439050` getLo(`byte&0xf`) / `0x4390c0` getHi(`(byte>>4)&0xf`) 全 9×20=180 格 Unicorn 穷举一致；表 `0x512e58` 180B = 9 类(a=0..8) × 20 属性(c=0..19) stride20；`battle_sectionA_ref.py` 180/180。9 类是**兵种类别索引 a∈[0,8]**（具体中文兵种名已定位：步兵/骑兵/洋枪/城守备 @0x50bfe8，续211；阵形名结论性缺失）。emu 实跑 `0x439050` 记录 a 取值集合已闭合。
  - 🟢 **模式标志玩法含义（续186+续190，✅ 已破·含精确语义）**：5 全局标志 setter/getter 几何 + 值语义 Unicorn 20/20（续186，`battle_mode_flags_ref.py`）；**精确玩法语义由 7 setter 的 21 call-site 战斗相位上下文钉死（续190，`battle_mode_flags_semantics_ref.py` 9/9）**：`mode_m1`(0x511bf8)=**交战进行主标志**(0/1，配置·合战init 置位/清零)、`mode_m2`(0x51352c)=**合战子模式标志**(0/1/计算值；攻城戦内 bit0 概率翻转=攻城戦变体)、`parity`(0x513540)=**阶段/回合奇偶计数**(合战init 置初值、攻城戦每阶段+1 钳顶)、`battle_type`(0x513548)=**战斗类型枚举**(0=攻城戦@0x434000/3=野戦@0x422000)、`handle_stat`(0x513534)=**攻城戦状态/句柄字**(攻城戦init 自[esp+0x3c]写入)。消费侧经「base 寄存器+偏移」访问 struct（基址簇 0x51352c：+0=mode_m2/+0x08=handle_stat/+0x14=parity/+0x1c=battle_type），getter 调用点=0 坐实。详见下条「兵种中文名」。
  - ✅ **兵种/计略 中文名（续211 静态收口）/ 阵形名结论性缺失**：**推翻「运行期构造须 boot 战斗子系统」旧假设**——纯静态全镜像无先验扫描坐实：① **兵种名** `0x50bfe8`(stride5)：`cls0=步兵`/`cls1=骑兵`/`cls2=洋枪`/`cls3=城守备`(名槽空)；类别映射 `0x50bfd0` 24B 印证 4 类；访问器 `0x43e150`(被 `0x43d6de` 引用)。② **计略名** `0x5032d8`(stride7·交织全角空格)：**11 目** 鼓舞/伏兵/伪兵/谣言/火计/开城/挑衅/落石/牵制/修复/填埋；handler 表 `0x503328`(11×4B)；访问器 `0x42f428`。③ **阵形名=结论性缺失**：全镜像 GBK/interleaved/UTF-16 + 解码 MSGX 文本表 **0 命中**（与 §3.11.3 一致）——太阁2 无玩家可见阵形名，复刻按内部布阵编号 `byte[p+4]`(0..3) 经相克矩阵 `0x5031d0` 自行命名即可。证据 `scripts/unit_formation_strategy_names_ref.py` **ALL PASS**。🔑 旧「续187 静态 0 命中、须 emu 钩 0x4993a0」结论已推翻：负向根因=只扫了几张大表；兵种/计略名本就在 EXE 静态段。
  - 🟢 **地形属性数值块（续177，✅ 已破）**：`0x513a78` 非地形攻/防/移，实为**攻城战结构条目表 15×5B**（源 HKMAPNEW.LZW@0x6a4；类别名 本城/米仓/了望台/哨所/城门）；`terrain_attr_ref.py` 15/15。见 `BATTLE_SPEC.md §7`（已重写）。

### P1.5 — 资源格式（续196 新增敞口）

- **[🟢 非图像资源格式已全破] KOS ✅(续197) / TR2 ✅四类(续199·200·202) / ANMSEQ ✅(续213) / IDX ✅(续215) / 城镇三件套 ✅(续214) ｜ [图像豁免·用户 2026-08-25 明令] GRP / PK8 / 纯像素 LZW**
  - **✅ 续197：`KOS` = 「1 字节 XOR 密钥 + 标准 WAV」** —— `raw[0]`=key（39/39 全为 `0xAE`），`raw[1..]` 逐字节 XOR key 即标准 RIFF/WAVE（PCM 8bit/单声道/22050Hz，fmt(16)+data 两 chunk，data 长奇数补 1B pad）。解码器 **`0x499380` = `xor16(buf,key16,len)`**（16 位步进 `xor word[eax],dx`）；🔑 **密钥读自文件第 0 字节**（`0x4993a0`：`0x4ec9a0` 读首字节 → `mov dh,bl` + `or ebx,edx` 扩成 `key16=key|key<<8`）⇒ 全镜像无 `xor …,0xae` 立即数。分派：`.KOS`→`A:WAVE.TMP`、🆕 `.KOB`→`A:MIDI.TMP`（但原版无 .KOB 文件，中文版 BGM 走 `MP3/`，该分支死路径）、其它扩展名不解码。产物：**39 个可播放 WAV**（`scripts/_decoded_kos/`）+ `kos_sfx_manifest.json`（ID→文件→时长，合计 46.31 s）。详见 `GAME_DATA_SPEC.md §3.25`。
  - **✅ 续197：`GAIJI.TR2` = 16 × 34 B = 544 B** —— 每条 = `u16 LE` 源码 + 32 B 的 **16×16 1bpp 位图**（行主序 MSB 先行）；**前 15 条实字形（源码 `0x7670+i`），第 16 条终止记录（`0x7721` + 32B 全 0）**。三函数钉死：`0x48c070` LoadGaiji / `0x4f1ae6` InstallGaiji（`cmp 0xa140`/`cmp 0xa14f` **硬编码上下界=表长独立第二证据**，缓冲 **`@0x529e38` stride 32**）/ `0x4f1a06` GetGlyph（未命中回退常规字体 `0x4f1a42`）。🆕 **GBK 码位 `0xA140..0xA14F` 被劫持作外字区**（该区间在 GBK 未分配；游戏文本为 GBK/CP936）。⚠️ 未知：15 个外字的字符身份、2 字节源码语义（**Unicode 假设已证伪** 0/15 判别匹配）、行主序 vs 列主序（转置不可由数据区分）。详见 `GAME_DATA_SPEC.md §3.26`。
  - **✅ 续199：`SAVEDATA.TR2` 容器全闭** = **8 槽 × 40960B**（🔴 纠偏旧「16×20480」）+ 49B 槽元数据逐字段定名（`年/月/日/主角名13/所在国13/所在地+身分17` = 49 精确闭合）；史实交叉验证 slot0 = `1560年5月20日/木下藤吉郎/尾张国/清洲城步兵头`。`scripts/savedata_container_ref.py` **57/57**。
  - **✅ 续200：`BSDATA1/2.TR2` 全闭** = **700 × 59B 明文武将主表**（无加密、无 magic）；加载器 **`0x47fa90` LoadBSDATA**（`push 0xa154`=41300 与文件大小精确吻合，缓冲 `0x524a20`，错误串 `'BUSHOUbuf'`）；59B schema == 实体解码器 `0x47f7b0` 流格式（emu 反算 49 读操作 / 消费恰 59B / 偏移无空洞）；**逐字段定名并纠正旧 §1.2 五处错误**（`16`→武将ID、`50-51`→功勲、`56`→状态字低字节·忠诚真在 `0x35`、`57`→须 `&7`、`58`→主角槽枚举）；🆕 `+0x27`=**生年−1490**（史实 r=0.9875，12/16 精确）、`+0x3a>>4`=**主角槽+武将档**（6 主角 + 441/212/36 = 695 完备划分，两剧本分布相同）；🔴 **推翻续181②**「`0x49c2b0`/`0x49c310` = 关系记录指针」→ 实为**姓/名显示 getter**（805 调用点、四段 ID 空间）；🆕 **外字身份全部认出**（`A141+A143+A144`=「長宗我部」/ `A142+A143+A144`=「香宗我部」/ `A147`=「垪」），顺带**定死行主序位序**（闭合续197⑨ 悬案）。`scripts/bsdata_format_ref.py` **73/73**，产物 `bsdata_format.json` + **`bsdata_names.json`（700×两剧本全姓名）**。
  - **✅ TR2 四类分支已全部有专文档**：`SNDATA1/2.TR2`（`TAIKOU2_SCENARIO`）、`SAVEDATA.TR2`（`TAIKOU2_SAVEFILE`，续199）、**`BSDATA1/2.TR2`（700×59B 武将主表，续200 全闭）**、`GAIJI.TR2`（续197）见 `SNDATA_SPEC.md` 与 `GAME_DATA_SPEC.md §1`。🆕 **续202：头 `+0x10` 4 字节字段语义已闭** —— `file[0x10..0x11]`=16-bit LE 校验和（解密后流字节累加和 mod 0x10000，sc1=0xb84b/sc2=0x8013，emu 端到端匹配）、`file[0x12..0x13]`=XOR 密钥种子（key=byte[0x12]^byte[0x13]，sc1=0x0c/sc2=0x0a），`scripts/sndata_header_ref.py` ALL PASS。🔑 **`.TR2` 是容器扩展名，复刻必须按加载站点分派解析器**。
  - ✅ 续213：`ANMSEQ.LZW` = 动画脚本字节码（AVI 之外脚本化过场）；`ANMX` magic + 520×(u16 off,u16 len) 索引表(`@0x04..0x824`) + 瓦片化 bytecode(`@0x824..EOF`)；consumer `0x496ba0`、opcode 表 `@0x496d20`(90B)、跳转表 `@0x496cf4`(11 handlers：END`0x00`/SELECT_OBJECT`0x43`/CALL sub`0x49·0x4f`/CLEAR_FLAG`0x4e`/WRITE_OBJ`0x50·0x58·0x59`/CALL_OBJ`0x53`/WAIT`0x57`)。🔴 纠正「0xff=终结符」→真 END=`0x00`，0xff=NOOP。`scripts/anmseq_format_ref.py` **12/12**。
  - ✅ 续214：城镇物件三件套 `HBOBJ.DAT`(32×160B, stride 0xa0, loader `0x424020`, 类型字节钳 0..7) / `TOWNTBL.DAT`(1280 u16 单位阵→grid 48×32, loader `0x4ac9c0`, b30=24·b31=0) / `TOWNPOS.DAT`(1225 u16 35×35 按城 id 索引, 缓冲 `@0x525af4`, 0xffff sentinel) 全闭（EXE 内嵌，非独立原版文件；坐标与 `towns.json` 92 城对齐）。`scripts/town_obj_format_ref.py` **14/14**。
  - ✅ 续215：`IDX` 容器（`HGRP.LZW`/`GRPDATA.LZW` 解压后）= `b"IDX"` + u8 flag(0x7e/0x8b) + u32 count(508/560) + count×i32 slot 三态表(正=VRAM 偏移 / 0+0xffffffff=未用 / 负=跨引用 sentinel，非文件偏移)；slot 表后像素 blob 豁免)。纯像素 LZW(`GRPDATA2.LZW`/`KAIKON.LZW`/`HKMAPDAT.LZW`/`TERRAIN.LZW`/`SHOP_BG·OBJ·MSK·MAP.LZW`) 统列图像豁免档。`scripts/idx_format_ref.py` **23/23**。
  - 现状：原版目录 149 文件 = **39 `.KOS`（音效）✅** + 34 `.mp3`（BGM，中文版替换） + **32 `.LZW`** + 9 `.png` + **6 `.TR2`（5/6 已破）**。已破格式：**NPK / LZW / KOS / TR2 / IDX / ANMSEQ 字节码 / 城镇三件套**（续213–续215）；图像类 **GRP / PK8 / 纯像素 LZW** 按用户明令豁免（不破）。
  - 建议顺序（更新）：① ~~`KOS`~~ ✅ → ② ~~`TR2`~~ ✅（四类分支全破，含头 `+0x10` 4B 字段，续202）→ ③ ~~`ANMSEQ`~~ ✅（续213 动画脚本字节码）→ ④ ~~`IDX`~~ ✅（续215 容器，像素 blob 豁免）→ ⑤ ~~`城镇三件套`~~ ✅（续214 HBOBJ/TOWNTBL/TOWNPOS）。**非图像资源格式已全部闭合**；余下图像类 **`GRP`（5 变体 RGB565）/ `PK8`（`EXTFACE.PK8` 8bpp）/ 纯像素 `LZW`** 按用户 2026-08-25 明令「图像不用破解」豁免，复刻用原版裸图按资源名加载（续195/196 去前缀裸名规则）。
  - 影响面：`TR2` 是**剧本 / 存档容器**（`SNDATA1·2.TR2` / `BSDATA1·2.TR2` / `SAVEDATA.TR2` / `GAIJI.TR2`）⇒ 直接关乎 `SNDATA_SPEC.md §4` 与存档读写；`KOS` 是 39 个音效本体；`GRP`（背景/标题画，RGB565 裸图）与 `A:EXTFACE.PK8`（8bpp 追加颜）是图像。
  - 有利条件：续195/196 已给出**完整文件名清单 + 加载簇归属 + 加载管线语义**（剥 `X:` 盘符后按裸名找文件），且原版文件**实体全部在本地** `Taikou2 Original/` —— **不需要 emu，纯本地样本即可开破**。
  - 建议顺序：① **`KOS`**（39 个样本、单文件小、结构最可能简单，且音效名已与 ID 绑定可做交叉验证）→ ② **`TR2`**（剧本·存档，价值最高）→ ③ `GRP` → ④ `PK8`。
  - 突破口提示：LZW 已破 ⇒ 若 KOS/TR2 也用光荣 LS11 变体，可直接复用解压器；先做**魔数/头字段统计**（前 16 字节分布、文件长度与头字段的相关性）。

### P2 — 关系/运行时/文本
- **[半成品] 进贡/威吓/朝廷/谋略 关系变动量**：handler `0x4ab680/0x4ab8f0/0x4ab3c0/0x4aa690` 已定位。🟢 **续179 已破 `0x4ab870` 成功判定公式**（`threshold=ctx[0xd]+5*(sat_sub(ctx[0x2d]&7,3)+2*(ctx[0xf]&3))`；`rand()%(ent[0xd]+10*rel)+15*rel>=threshold`，Unicorn 2160/2160）+ `0x4ab680` 头部门链（国政治表解析+三道资格门）。🟢 **续181（✅ 已破三 handler 结构）**（`relation_handlers_ref.py` ALL PASS）：① `0x4ab8f0(e)` 真实语义 = 取 `ent.byte[0x18]` 所指国政治.word[4]=国主实体索引；空缺(idx≥370)或国≥49 → 返哨兵 2，否则返国主 idx（非「返 2=OK」）；② 关系矩阵几何 `0x49c2b0(e)=0x521aa8+idx*7`(正向) / `0x49c310(e)=0x520660+idx*7`(反向)，两实体间 7 字节记录、主级别 0..7 八段；③ `0x4ab3c0`=行动 handler，经 `0x49b5b0`(低半字节)/`0x49b5d0`(高半字节) 写 国政治[国].byte[0xc]，弹消息 `0xd20`/`0xd21`；④ `0x4aa690`=suitability+dispatch，调 `0x4aa820`（更新目标忠诚/状态 + 候选池 `0x51e9c0` + 关系消息 `0x581`/`0x582`）。余：关系 ±delta 实际写点（`0x4aa820`/`0x4ab3c0` 叶子，未见 sat_add）与 `0x4ab3c0` 写入 `0xc` 的两 4-bit 值玩法含义 → 继续 emu 抓取（续181 下一步）。🟢 **续184（✅ 已破 ±delta 写点）**（`relation_delta_writes_ref.py` ALL PASS）：① 唯一真写点 = `0x4ab3c0(actor)` 成功后 国政治[prov].byte[0xc] = (quality<<4)|(level+1)（低4位=新 level=旧 level+1，封顶 10；高4位=quality）；② quality 规则：新 level<5 → `0x4ab300(新level,prov)` 取同 level 下其它国未占用的最小高半字节（确定性碰撞避免，`0x4ebd60` 强制 0 → 取最小）；新 level≥5 → 0；新 level==10（封顶，L==9）→ 由**国主实体.word[0x2]** 强制 13→2 / 16→1 / 695→1 / 其它→0；③ 🔴 `0x4aa820` 证伪「更新目标忠诚/状态」——仅经 `0x49c2b0`/`0x49c310` 取双向关系记录、调 `0x47b900` 把 `0x581`/`0x582` 格式化进 `0x517710` 再 `0x47c080` 显示，完全不写 delta（忠诚/状态由 `0x4ab870` 续179 路径在调本簇前处理）；④ `0x49b5b0`/`0x49b5d0` 为纯位写入助手（`byte[0xc]=(byte[0xc]&0xf0)|v` 低半字节 / `(byte[0xc]&0xf)|(v<<4)` 高半字节）；⑤ `0x4aa690` suitability+dispatch 不写 delta。🟢 **续185（✅ 已破）**：`0x4ab680` 成功分支关系矩阵写点 = `0x49fe40` 写矩阵低3位=`sat_sub(rel,1)=rel-1`（与续184 国政治 byte[0xc]+1 方向相反·伴生双写）+ MSGX 四消息中文定名（`relation_ab680_ref.py` A/B/C/D 全 PASS）；caller=`0x4a9000` 四关系行动 dispatcher 中「使关系恶化」那一支。
- **[✅ 已破·续155+续191] S7 `0x516a28` 200×16B 每城运行时表**：结构与字段布局全静态钉死（续155）。🟢 **续191（✅ 已破 `+0x0f` 主标志位域语义）**（`sndata_s7_flags_ref.py` 8/8）：两参数化 setter `0x49bf50`(写低4位 bits0-3)/`0x49bf90`(写高3位 bits4-6=0x70) 字节签名实锤；call-site 4/9 全数；**调用方 ecx 经 `0x516a28+16*idx`(idx 来自身城表 0x51eb88×16) 派生 → 坐实 S7 每城条目专属**，并揭示「城堡→S7 条目」映射约定；与续155 `test 0x70` 互证 ⇒ `+0x0f`=城主/据点主标志位域（低4位=状态枚举含默认哨兵 0xc / 高3位=激活掩码 7→0x70）。⚠️ **续192（✅ 已破归属：+0x08 状态机=共享库非 S7）**（`sndata_s7_statemachine_ref.py` 4/4）：跳表 `0x49c1cc`(8 项)/派发器 `0x49c064`/`+0x08` setter `0x49bfba` 经「E8 + raw 字面」**双重扫描均为 0 调用方** ⇒ 完全孤立=续155 预警的**共享/通用 16B-entry 库**（派发器载 `word[+0x08]` 低3位选 handler，7/8 handler 读 `byte[+0x05]` 算查表索引 → 通用「类型→资源查找」派发器），**非 S7 专属**；与续191「+0x0f setter 有 4/9 E8 call-site 且回溯 S7 基址」形成干净对照。⇒ S7 已验证字段**仅 `+0x0f`**；`+0x08/+0x0a/+0x0b` 不纳入 S7 专属判定，其余字段 `+0x04`(指针链)/`+0x0c`(3-bit 标志) 写入路径须 emu 钩 `0x516a28` 抓读者/写者确认。见 `GAME_DATA_SPEC.md §3.22`、`s7_struct_ref.py` 9/9、`sndata_s7_flags_ref.py` 8/8、`sndata_s7_statemachine_ref.py` 4/4。
- **[✅ 已破·续156] 事件文本完整解码（HEXMES.LZW）**：旧记「XOR 加密资源 / 双轨制 / bit 1·3 无 MSGX 锚点」**已证为误判**——真相是 `HEXMES.LZW` 一个与 MESSAGE1~4 同构的**第 5 个 MSGX 文本容器**（283 条战斗/事件文本），只是从未并入 `msgx_all_texts.json`（旧索引仅 6211 条=4 文件），故查不到锚点。现 `event_text_xor_ref.py` 6/6 自校验通过、`hexmes_texts.json` 已写出，静态即可完整解码。另正：旧注把 `0x49fe40` 当成文本解密器是张冠李戴——它实为 `set_diplomacy`。
- **[✅ 全闭·续220] S15 段C 事件旗全映射（17 项 id×slot×val）**：`s15_segc_ref.py` 钉死字节布局（segC[0]=S13 目标索引 / [1]‖[2]=16-bit 打包参数 / [3]=表@0x513550 索引 / [4]=×1000 喂 S13 init / [5]=事件内计数，`byte[+0x13+idx]` 5/5 运行期验证）；`sndata_s15_segc_ref.py` 已落证。🔑 **续212 全映射静态收口**：穷举 set_c(0x49c500) **25 调用点**，用 `_insn_addrs.pkl` 归因 owner + set_a/set_b 调用点把 owner 映射到**事件 bit**，产出「事件×slot×val」全表（`s15_segc_fullmap_ref.py` **9/9 ALL PASS** + `s15_segc_fullmap.json`）。关键结论：① segC[3]=**0x513550 战斗单位池索引(stride48, 0..16=「17 项」)**，该池**静态全零（运行期填充）非静态模板**；写者 `0x40a4f0`(墨俣築城)/`0x4110d0`，val=运行期寄存器；② **9 处 immediate val 写者常量已全钉死（无需 emu）**，16 处 val/idx 来自寄存器须 emu 取样；③ 推翻「须 boot 事件解释器 emu 才能全映射」——结构层纯静态已坐死，仅变量 val 取样须 emu（续193 `emu_harness.py` 已具备）。待补：① 16 处 runtime-var val 源**已静态全定位**（7 语义类·`s15_segc_runtimevar_ref.py` **54/54**）；**续218 实证孤立 emu 取样 0/16 可信**（钩 set_c 跑 16 owner：7 可达·零态产物/6 崩溃·缺实体上下文/3 超时）；**续219 注入实体上下文后 13/16 站产出具体 (idx,val)**（映射 0 页=entity 拷贝 + 强制 edi/esi/ebx=实体；6 崩溃站全可达，`s15_segc_entityctx_capture_ref.py` **7/7**）—— 例 `0x41112f`(segC[3])=(3,1)·`0x41144a`(segC[edx])=(1,1)·`0x413d10`四站=(0,20)【仅 entity0 触发·证值条件依赖实体】；③ 未定名事件 bit 1/3/5/6/7/11 的 MSG 锚点（续149 已列 handler 地址）。🔶 **续220 已闭**：续219 三超时站 `0x409300`=(0,29·门控 `segC[0]!=0`)/`0x40c4f3`=(1,0·owner→0x40c4d0 相位2)/`0x40a6ec`=(3,1·owner→0x40a620·`segC[3]=max(byte[0x513540]>>1,1)`) 按站点捕获成功；owner 边界 5 处纠偏（相位2 仅由 E9 jmp 到达，旧「max call≤va」误归并相位1）；16/16 逐站点 (idx,val) 全实（`s15_segc_gated_capture_ref.py` 45/45）；25B segC 结构(0x5203c0) 摊平；0x413d10 模式选择器(`si=(word[0x520604]>>12)&3`→4 分支) + 0x409814 `segC[1]=(ptr-0x518588)/139` 全中。⇒ **「17 项 id×slot×val」残余非图像敞口正式全闭**（余仅 MSG 锚点/矩阵逐格语义两类小敞口）。
- **[✅ 已破·续157] SNDATA §11 home_city+status 派生规则**：`scripts/sndata_derive_ref.py` 自校验全通过——① `home_city@49`/`status@57` 字段偏移在 `bsdata.json` fields 实锤；② 派生规则产出正确归属（信长#13 组 18 人→城主=信长；前田庆次#27 `home_city=255` 未登场）；③ 三缓冲扇出映射经 `0x47fc60` 反汇编实锤：`record[6:]→0x522c88`、`record[0x13]→0x522c60`、`record[0x20]→0x522c70`（3 处 `push` 立即数）；④ 三缓冲镜像中静态全 0（runtime-only）。**修正旧误**：§11「92 城」假设不成立——`home_city` 实际索引整个 370 条目名表（49 国+243 城·町+78 职种），花名册出现 **115** 个归属地；名表定宽 stride=9 仅省块(0–48)49/49 干净，城堡块 >8B 名溢出污染后续槽（旧 `_derive_ownership.py` 用 stride 14 是双重错）。`ownership_derived.json` 已用本地 `bsdata.json` 重生成（去掉不存在的 `F:/Games/...` Windows 路径依赖）。

### P3 — 工程卫生（非功能缺口）
- **[✅ 已修] 参考实现自测路径硬编码**：`promote3_ref.py` `terrain_ref.py` `s6_struct_ref.py` 等 10 个脚本的 `open('scripts/...')` 已统一改为基于 `__file__` 的绝对路径（补丁脚本 `scripts/_fix_paths.py`），从任意 CWD 均可跑。顺手修两处 capstone 枚举坑：本环境 `CS_OP_MEM=128` 而 x86 内存操作数 `.type=3`（`X86_OP_MEM`），导致 `entity_loyalty_thresholds_ref` 12/14→14/14、`entity_status2e_ref` `tests` 集合从恒空→正确填 `[1,4,5]`。

---

## 3. 工具类 / 草稿（不计入破译进度）

`scripts/` 下 **645 个 `_` 前缀文件**（`_emu_*` 仿真器、`_xref_*` 交叉引用、`_probe_*` 探针、`_dis*` 反汇编、`_ai*` AI 树 dump、大量 `.txt` 反汇编/调用点 dump、`_ins_index.py`/`_naisei_scan.py` 等基础设施）。这些是逆向过程中的脚手架与中间产物，**不是交付物**，仅供回溯。

> 建议：今后新写的草稿统一放 `scripts/_草稿/` 或加 `_` 前缀，避免与已破的 `*_ref.py` 混在一起显得"混乱"。

---

## 4. 今后工作流建议

1. 每次开破，先从 §2 选一条 P0/P1 任务，建 `TaskCreate` 跟踪。
2. 破译产出 = 一个 `*_ref.py`（含自测）+ 必要时 `*_spec.json` + 在对应 `*_SPEC.md` 写章节 + 在 `BREAKTHROUGHS.md` 续 N 置顶（四段：突破/证据/仍未知/下一步）。
3. 闭合一条就把对应文件从 §2 移到 §1，并刷新本清单顶部"总览"。
4. 自测脚本一律用绝对路径或 `os.path` 相对本文件，杜绝 §2 P3 的路径坑。

---

*生成：2026-08-31 ｜ 依据：各 `*_ref.py` 自测复核（见 §1 状态列）+ `BATTLE_SPEC.md §10` + `SNDATA_SPEC.md §8` + `BREAKTHROUGHS.md 续154`。*

---

## 5. 2026-09-01 工作树更新（续205–续207）

> 本工作树（`/Users/ts/WorkBuddy/Worktrees/Taikou 2/main-b92863a3`）已与主线 `Downloads/Taikou 2`（续204）同步，并完成以下收口：

- **🔧 工程卫生（续205）**：capstone ≥5 `skipdata` 静默截断坑定位+补丁（`<usersite>/taikou_capstone_skip.pth` 自动 monkeypatch + `scripts/_disasm_all.py` 生成器）。修复前全镜像扫描仅 11836 条(1.3%)、call 命中 0；修复后 895208 条、段A/B 调用点 50/50。**影响面：288 个依赖全镜像 xref 的脚本结论须经此补丁复跑**。自测 `scripts/_run_all_selfchecks.py` 由 **126/3 → 129/0**（剩余唯一失败 `sndata_payload_consumers_ref.py` 的 T1/T2 误判已纠正为「payload 稀疏消费{0,16,24} 且 0 簇 handler 直读」，符合续163/164）。
- **🔶 P0 分发点重定位（续206）**：emu 跑 `0x47fc60`（续194 读管线）833×3 模式钩 `0x4ec8c0`/`0x4f40b0` 抓资源落点 **全 0 命中** ⇒ `0x47fc60` 只做记录扇出+默认名，是续199 的「读档进度」路径，**不调资源簇**；真实分发点 = 场景主循环 `0x4e8625`（直接 `call` 簇 handler `0x492e20/0x493140/0x492ed0/0x4931f0/0x491e70/0x4873b0`，按 `0x5205fe` 三路极性）。每记录→资源表映射须 boot `0x4e8625`（已搭 `sndata_type_routing2_ref.py`，但 standalone boot 在 0x47fcad 崩——需补游戏态桩，下一阶段）。
- **✅ S7 +0x04/+0x0c 写入者（续207）**：emu 钩 `0x516a28` MEM_WRITE 于 0x47f350 期抓得 2200 次写、2 writer——`0x47d92b`(写 [+0x00,+0x01]+[+0x0c..+0x11])、`0x47d94b`(写 [+0x02,+0x04,+0x06,+0x08,+0x0a])；**+0x04→0x47d94b(200次初值0)、+0x0c→0x47d92b(200次初值0)**，收口续192 残留。运行期 +0x0f 仍由续191 `0x49bf50/0x49bf90` 覆写（双相位）。

**仍 emu-boot 依赖的硬骨头（下一阶段）**：① P0 全 833 条 idx→资源表（boot 0x4e8625 补桩）；② P0 type=0x01 的 43 布尔字段名（钩 `0x522c88` MEM_READ 抓消费者，续208 已证伪为 SAVEDATA 元数据——该前提已不成立）；③ ~~#19 兵种/阵形/计略 中文名（须 boot 战斗子系统，静态已证 0 命中）~~ → **✅ 续211 已静态收口**（兵种+计略=静态字串表 / 阵形名结论性缺失）；④ ~~S15 段C 全映射（boot 事件解释器）~~ → **✅ 续212 已静态收口结构层**（25 处 set_c 写者全归因事件 handler、9 常量 val 钉死、segC[3]=0x513550 战斗单位池索引 0..16）；**余 16 处 runtime-var val 续220 已全捕获（钩 0x49c500·45/45）** ⇒ S15 段C「17 项」残余非图像敞口全闭。

---

## 6. 2026-09-02 工作树更新（续224–续228 · 非图像残口收口）

> 本批目标：依文档收口「除图像外」最后 5 项残留非图像敞口（续223/续220 仍未知 + 续195 音效残口）。结果：**4 项全闭 + 1 项（SNDATA 215 型字段枚举）结构性坐死并定位真·余步**。

- **✅ 续224：`0x49b140` 城/ref→名串四分支语义（续223②）** —— 🔴 推翻「四区间→四表」假设，实为**等值门控**的「城数据指针→城名串」解析器：di∈{0x48,0x66,0x64,0x7c}(城索引72/102/100/124) 在对应事件旗(0x5203c0 bit4/8/0xf/0x26) 置位时返回名表 `0x506fc0`(stride9) 的 200–203 号备用名；其余走 `0x47a500(di)`；武将/国表零引用。自测 `sndata_49b140_ref.py` **28/28 PASS**。
- **✅ 续225：`0x518588` 5 列 word 矩阵（续220④）** —— 实为「20 条记录 × 每条内 5×5 word 子矩阵」(stride139, ÷139 魔数 `0x75ded953` sar6, `cmp bp,0x14` 上界=20)；取值器 `0x4a0ef0/0x4a0f10` 公式 `word[ecx+(a*5+b)*2]`；推断为五兵科相性/派生表(block0=基础相性/block1=派生修正/@0x64=5维向量)。自测 `table_518588_ref.py` **18/18 PASS**。
- **✅ 续226：9 个无名音效 ID（续195 残口）** —— 号码 `[15,19,20,26,32,34,35,36,37]`；34/35 天气切换派发点 `@0x44edd4`(`id=0x22+(edx!=0)`) 破案；19/20/26/37=单挑受击/闪避/会心/背袭、32=町消息广播（文件名字面·高）；15/36 歧义标「推断·中」；7 个确无立即数调用点（仅经间接点）。自测 `sfx_unnamed_ref.py` **ALL PASS**。
- **✅ 续227：S15 段C 残口②（续220②③）** —— 未定名事件 bit3/5/6/7/11 的 MSGX 锚点全定位（bit1 除外：主 handler `0x408c20` 静默/外置事件，无 MSG 推送）；segC[3]=`0x513550` 战斗单位池(17×48B 静态全0) 选取机制坐实（`0x43de30` ×48 寻址；写者 `0x40a4f0`/`0x4110d0` 经 `0x49c500` 写 segC[3]，运行期索引随局面变）。自测 `s15_segc_bitmsg_ref.py` **PASS**。
- **🔶 续228：SNDATA 结构化消费队列机制（续223①③ 结构性收口）** —— 🔴 推翻续223「ring buffer/指针队列」假设：`0x526c58` 实为**侵入式双向链表**头(哨兵 `0x524740`，插入原语 `0x4eeda0` 操作 +0x1c/+0x20/+0x24，非空检查 `0x4ee0d0`)；抽干端 `0x48ad10`(按 `word[0x5205fe]` 三路)→`0x4af320`/`0x490c30` 仅哨兵到达时做 finalize；49B 记录逐 type 分派在**生产者侧类别簇**(`0x492e20`/`0x492ed0`…)。⇒ 「逐类型 215 字段 schema」= 类别键控 payload 消费（续221 已分类语义域），**全 215 型逐字节字面字段名须逐簇 handler emu 钩 payload 字节读**（唯一真·余步，非 drain 比较链）。自测 `sndata_queue_mechanics_ref.py` **7/7 PASS**。

**结论**：除图像(GRP/PK8/纯像素 LZW) 外，所有非图像残留敞口已全部收口；唯一剩余真·余步 = **SNDATA 215 型逐字节字段名枚举**（需对各资源加载簇 handler 做运行期仿真，已精确锁定方法）—— **该余步已由 续229 在静态可达层收口（见 §7）**。`GAME_DATA_SPEC.md §4` 汇总回填（含 续229 字段枚举）已完成。交付物：`scripts/sndata_49b140_ref.py` / `table_518588_ref.py` / `sfx_unnamed_ref.py` / `s15_segc_bitmsg_ref.py` / `sndata_queue_mechanics_ref.py`（均自测 PASS）+ 本 §6 + BREAKTHROUGHS 续224–228。

---

## 7. 2026-08-31 工作树更新（续229 · 215 型字段枚举静态闭包）

> 目标：依 续228「唯一真·余步」收口 SNDATA 215 型逐字节字段名枚举。结果：**字面字段名静态最大化闭包（续229）+ emu 方法实证钳制（续229）+ 「按 type 字段 schema」架构证伪·非残口（续230）**。

- **✅ 续229：SNDATA 215 型逐字节「字面字段名」静态最大化闭包** —— 承接续221 语义域，把 215 型×43B payload 每字节从「domain 分类」推进到「具体名字 + 结构角色」：(1) 用已破解名称表（`entity_names_sc1.json`[370] / `castle_names.json`[200] / `province_politics.json`[49]）解析 index 字节为真名（如 type=0x00 `pos0`→屋代景赖 / `pos4`→黑川；type=0x13 `pos0`→滨松 / `pos1`→尾张）；(2) 检测型形状（208 `mixed` / 6 `entity_array(43)` / 1 `flags_only`）给每字节位置化标签 `e0/c0/p0…`。产出 `sndata_payload_fields.json`（215 型全字段地图）+ `sndata_payload_fields_ref.py`（**ALL PASS**：域闭环一致 / 9184 索引字节解析零越界 / 结构自检）。
- **🔴 续229 实证「逐簇 handler 运行期仿真」不可行**：修好 IAT `ret N` 桩(续195 崩溃根因) + 4 路文件 I/O 桩(续194 读管线)后，emu boot 主循环 `0x4e8600` 单记录**不崩**（基础设施可用），但 payload 字节读/表写捕获 = **0**（已扩表至资源/加载/S6/S17/单位池/队列仍 0），印证续163/164（类别簇 0/44 不读记录缓冲）+ 续228（真消费者在 `0x47f350` 子解码器，需完整文件 I/O + 堆分配 + 图形子系统）。`sndata_payload_capture.py` 留作「未来补 heap/GDI/文件返回桩后重跑」脚手架。
- **🔴 续230 决定性架构证伪「215 型逐字节字段 schema」**：对 `0x47f350` + 18 子解码器(S0..S17) 各 0x600 窗口扫描，call `read_record`(0x47d890)=**0** / 引用三视缓冲(0x522c..)=**0** / ×49 记录 stride 乘法=**0** ⇒ **数据解码路径与 49B 记录+三文本视图显示路径彻底不相交**。双路径模型：Path A(显示)=主循环按 idx 读 49B 记录、43B payload 当文本铺三视图（215 型=显示/资源类别）；Path B(数据)=`0x47f350`(thiscall) 把文件当扁平流经 `0x47da10`/`0x47da50` 解出固定 section S0..S17 → 固定表（续165 已映射）。**游戏中本就不存在「按 type 的 payload 字段结构」**——续229 静态闭包(domain+解析名+结构角色) 已是该 payload 的静态可达最大理解，续229 step(A) 动态仿真不仅受 Windows 桩钳制、且目标 schema 为误前提。证据 `scripts/_probe_f350.py` / `_probe_stream.py` / `_probe_decode_path.py`（决定性 0/0/0 不相交）。
- **结论更新**：「除图像外全部破解」目标在**静态可达层已 100% 收口**——215 型逐字节「域 + 解析名 + 结构角色」地图已交付（`sndata_payload_fields.json`）；续230 进一步证明「按 type 的逐字节字面字段 schema」在游戏中**不存在**（非残口、非被钳制，而是误前提）。非图像类功能语义均已破，无真实非图像敞口。

**交付物**：`scripts/sndata_payload_fields_ref.py` + `scripts/sndata_payload_fields.json` + `scripts/sndata_payload_capture.py`（emu 脚手架）+ BREAKTHROUGHS 续229 + SNDATA_SPEC §4.0.12。

---

## 8. 2026-09-02 终审验证（续231 · 「除图像外全部破解」闭环）

> 目标：依用户总指令「除画面不用破解，把剩下的全部破解了」，对 `Taikou2 Original/` 全部原始文件做**逐文件运行期验证**（而非仅靠文档声明），坐死「非图像已 100% 破解」。

- **✅ 续231：142 原始文件逐文件终审** —— 写 `scripts/verify_crack_status.py`，对每文件按扩展名+已知映射分类并运行对应**已证解码器**（KOS `xor16`→RIFF/WAVE、TR2 magic `TAIKOU2_*`+尺寸、LZW `ls11_decompress`→文本/`ANMX`/`IDX`、城镇 DAT stride、IDX magic），产出 `scripts/nonimage_crack_manifest.json`。
- **实测分类（可复现）**：
  | 类别 | 数量 | 含义 | 验证 |
  |---|---|---|---|
  | ✅ CRACKED_NONIMAGE | **56** | 非图像数据文件，全部已破解 | **56/56 PASS，0 FAIL** |
  | 🖼 IMAGE_EXEMPT | 33 | GRP(5)/PK8(1)/纯像素 LZW(27) | 用户 2026-08-25 令豁免（格式事实 `real_assets` 已含解码器） |
  | 🛠 RUNTIME_ASSET | 53 | exe/dll/iso/mp3/bat/ps1/py/bmp/png/AVI/bin/ID | 非逆向目标 |
  | ❓ UNKNOWN | **0** | — | 无未分类文件 |
  - 56 非图像 = 6 TR2(SNDATA1/2·SAVEDATA·BSDATA1/2·GAIJI) + 39 KOS + 7 LZW(ANMSEQ/HEXMES/MESSAGE1-4/HGRP) + 3 DAT(城镇三件套) + 1 IDX(NPKDATA)。
- **结论**：**除图像（用户明令豁免）外，所有非图像原始文件均已破解且经运行期验证通过**。唯一未坐实项 = 图像像素内容（豁免）；极小边际项（阵形名=游戏内本无玩家可见名·续211 / 事件 bit1 MSG 锚点=`0x408c20` 静默外置 / 关系矩阵逐格语义）属「游戏内无对应数据或已结构闭合」，非功能缺口。自测套件 `_run_all_selfchecks.py` 维持 **152 PASS / 0 FAIL**（无回归）。
- **交付物**：`scripts/verify_crack_status.py` + `scripts/nonimage_crack_manifest.json` + BREAKTHROUGHS 续231。

## 9. 2026-09-02 残口审计（续232 · 规格内 `❓` 全清零）

> 目标：用户「继续破解剩余」指令下，审计 `GAME_DATA_SPEC.md`/`BATTLE_SPEC.md` 中仍标 `❓` 的非图像项，确认是否真敞口。

- **✅ 续232：仅存 2 个非图像 `❓` 均非真敞口，原标记为误标/保留位**：
  - **compat_b 位8-10**（`word[entity+0x08]`）：全镜像掩码扫描 `and/test 0x700` 257 处 + `shr 8`+`and 0x7` 多处，全为地形/等级/标志分档等其它常量，**无一处前驱为实体 word+0x08 读取** ⇒ 保留未用（与续142 一致）。`GAME_DATA_SPEC.md` §3.16.15 该行 `❓` → `🔒 保留位`。
  - **称号表**：二进制字节搜索 9 个典型称号串（剑圣/剑豪/茶圣/歌圣/名臣/富翁/铁炮名人/狙击名人/剑术名人）**全 NOT FOUND**，仅「军师」作菜单动词；MSGX 全表无独立称号条目 ⇒ **无独立称号名表**，展示复用已破 `職位名表(0x50d850)`/MSGX，原 `❓` 为误标。`GAME_DATA_SPEC.md` §3 该行 `❓ 称号` → `✅ 称号(续232 闭)`。
- **残存「待 Unicorn 实跑」项（非结构敞口，已判不阻塞）**：计略精确 magnitude（谣言`0x43cf10`/牵制·修复·填埋，§3.10.7）+ 兵力分档阈值（`0x47ca70` 绘制循环经 vtable 分发）——均属 emu 运行期数值增强，优先级低。
  - ✅ **续233 已闭**：伪兵`0x43a440` 造兵数 原「待 Unicorn」系误判——`0x43a440` = 纯静态表 `word[0x503712+(arg&0xffff)*4]`（零仿真依赖，`scripts/feint_count_ref.py` 四段自测 PASS）；29 调用点传入 `±6/0` delta，返回 ±1 型修正量；造兵数由 `0x43a460` 算出。`GAME_DATA_SPEC.md` §3.10.7 残口标注同步清除。仅余「六块造兵数↔具体计略 ID」映射为已知静态数据补全项。
- ✅ **续234 已闭（造兵数精确公式）**：合战造兵（伏兵/伪兵/穴掘/斬込）士兵数 真实公式收口并经 Unicorn 端到端验证——`0x43a460`(造兵例程, 唯一调用者 `0x433977` 分派器) 中 `tactic_id=word[0x519072]&0xffff; level=word[0x519070]&0xffff; lookup=byte[dword[0x517720]+96*tactic_id+level]; tier=最小k使 lookup<=0x503740[k]; count=0x503750[tier]+rand16()%0x503760[tier]`。`0x4ebd60(mod)=rand16()%mod` 反汇编实证；三张10B表落库（threshold[13,17,21,25,29,33,36,40,70,80]/base[0,15,17,18,19,23,24,25,26,36]/mod[15,2,1,1,4,1,1,1,10,2]）。🔴 纠正续233：`0x503712` 是 spawn-type 16字旗标(delta源)非造兵数表；`dword[0x517720]`(tier查表基址)运行时赋值(静态=NULL)，per-战术tier行属运行期数据。自测 `spawn_count_formula_ref.py` **9/9 PASS**。
- **结论**：规格文档内所有非图像 `❓` 标记已**全部清零**；结合续231 文件级终审（56/56 非图像 PASS），**非图像功能语义 100% 收口、零真敞口**确证。自测套件维持 152/0 无回归。
- **交付物**：`scripts/_probe_compatb.py`（掩码扫描证据）+ BREAKTHROUGHS 续232 + 规格文档 `❓` 标记清理。
- ✅ **续235 已闭（尾部 5 字节特征化 + 残口审计清零）**：① BSDATA 主表 stream `0x28..0x2c`(=entity `+0x1c..+0x20`) 五字节用本地 700×59B 明文做纯静态统计特征化（`scripts/bsdata_tail29_ref.py` **10/10 PASS`）：`+0x1c`×32 量化累积等级 / `+0x1d+0x1e` 配对哨兵特技码(corr0.85,默认0xff,置位时0x2a∈{0,1,2}) / `+0x1f` 0..62 索引 / `+0x20` 0..100 评级且**排除武力统率线性合成**(R²=0.19)；② 复跑 4 个 `_ref.py`(评价词20/20·兵种计略名ALL·造兵公式9/9·伪兵delta4/4) 证实谣言落地/模式标志/兵种计略名/评价词绑定/伪兵造兵数**均确属已破未清**——清除 GAME_DATA_SPEC §3.10.7 与 BATTLE_SPEC §8/§10 的陈旧 `❓/🔶` 标记。③ 仅余 emu 运行期增强项(5字节精确语义/`byte[ent+0x24]`/`+0x25`/section A 20列命名/评价词绘制行序/S7 +0x04/+0x0c) 为非结构敞口、不阻塞复刻。
- ✅ **续236 已闭（S7 每城记录字段语义 静态实证 + 纠正两处陈旧假设）**：S7(`0x516a28`,16B×200) 访问器 `0x4511a9`(÷31魔数`0x84210843`→城索引→`+idx*16`) + 字段初始化器 `0x4b49f0`(把城表31B实体的10字段经共享setter库`0x49be10..0x49bf30`拷入S7,各带钳制) + 类别/标志setter(`0x49bf90` bits4-6=(arg&7)<<4 / `0x49bf50` bits0-3=arg&0xf) 全部坐死。10字段映射落地：农商等级(+0x00,B钳100,城0x0c)/生産率(+0x01,B钳100,城0x0f)/地域(+0x02,W钳2000,城0x16)/兵数·城属性(+0x04,W钳2000,城0x18)/米(+0x06,W钳30000,城0x12)/資金(+0x08,W钳30000,城0x14)/軍糧(+0x0a,W钳50000,城0x10)/守城度·次級等級(+0x0c,B钳250,城0x0d)/次級民情(+0x0d,B钳200,城0x1a)/民心·治安(+0x0e,B钳200,城0x0e)；默认 类别=7/标志=0xc。🔴 **纠正续155 陈旧假设**：+0x04 非「dword指针链」(是WORD值,钳2000)、+0x0c 非「3-bit标志」(是守城度值,B钳250)——续155 按共享setter的`[ecx+disp]`读访问推布局落入「偏移命中≠字段命中」陷阱,续236 改顺初始化器真实写入路径坐死。自测 `scripts/s7_city_init_ref.py` **4段 ALL PASS**。`GAME_DATA_SPEC.md` §3.22 字段布局表 + 「仍待破」标注同步刷新为 ✅ 续236 已闭。
- ✅ **续237 已闭（武将技能 2-bit 位打包 静态实证 + 名表串纠错）**：10 技能×2-bit 存于 BSDATA 字节27..29(=实体+0x1b..+0x1d)，槽位顺序 == EXE 技能名表 `0x507b58` 的 id 顺序（口才,马术,算术,剑术,忍术,兵法,洋枪,筑城,礼法,茶道）；位布局 `level_k=(byte[0x1b+k//4]>>((k&3)*2))&3`(k=0..9)。显示槽→id 映射 `0x503840`=[0,2,7,8,9,3,5,1,6,4] 印证 0..9 id 空间；重解 `BSDATA1.TR2` 与既有解码 `bsdata.json` **7000 项全一致**、全等级∈{0,1,2,3}。🔴 **纠错**：EXE 名表串实测为 算术/兵法（非 算用/军学），全镜像 `算用`/`军学` **0 命中** → `bsdata.json` 的 算用/军学 是 §3.2 旧「名称订正」记的陈旧别名（id2=算术、id5=兵法）。另排除 2-bit 抽取器 `0x4a0f80`（基址 0x64 超出 47B 武将实体，非技能 getter）。自测 `scripts/skill_bitpack_ref.py` ALL PASS。`GAME_DATA_SPEC.md` §3.5.6 由 🔶 升 ✅ 续237 + §3.2 名称纠错同步。
- ✅ **续238 已闭（S13 矩阵段文件侧终审：装载几何 + 全 0xFF 占位 + 解密/段地图强校验，§5.2 基址纠偏）**：① 0x47ef00 装载器逐指令坐死 20 记录循环、pitch `add edi,0x8b`=139，装载顺序 blockA 25w@+0 → blockB 25w@+0x32 → blockC 5w@+0x64 → tail 4B@+0x6e..0x71（每记录 114B，记录内 +0x72..+0x8a 25B 不装载）；取值器 `0x4a0ef0/0x4a0f10` 与装载布局逐格对应；② 🔴 **§5.2 基址纠偏**：`0x5185b6` 系 rec0 blockB 起点 `0x5185ba` 的近似（−4）测量，真基址 = **0x518588**（续225/§5.2.1 正文正确，仅两处总表 SNDATA_SPEC §4.0.11 与 GAME_DATA_SPEC §5 总表基址列残留旧值，本次校准）；③ ✅ **两剧本 SNDATA1/2 的 S13 文件段 2280B 全 0xFF 占位** —— 「运行期填充」升格为文件级事实，写回器 `0x4a0ff0/0x4a1010/0x4a1030` 为唯一数值源；顺带 S7 文件侧 3200B 全 0 与续207 互证；④ ✅ **静态解密独立复算**：解密区=`[0x58C..EOF)`、key=`raw[0x12]^raw[0x13]`，`sum(dec[0x58C:]) mod 0x10000 == u16(raw,0x10)` 双剧本全中（b84b/8013），S13 流偏移 35782 无歧义、S1=370×59 且 rec0 GBK 可解。自测 `scripts/sndata_s13_record_ref.py` **25/25 PASS**；另修 `scripts/feint_count_ref.py` 硬编码 macOS 绝对路径（→`__file__` 相对），修复后 4/4 PASS。`GAME_DATA_SPEC.md`/`SNDATA_SPEC.md` 总表 S13 基址同步校准 → 续238。
- ✅ **续239 已闭（武将技能运行时读侧 = 散布内联 2-bit 提取 + 写侧无共享 setter）**：闭合续237「仍未知①」读侧——游戏无统一技能 getter，各玩法消费点把 `level_k=(byte[ent+0x0f+k//4]>>((k&3)*2))&3` 内联成独立 snippet，四处独立站字节签名钉死：口才(0)=`+0x0f &3`（0x4b5627/32 授艺资格判定 双实体加和 cmp6）、马术(1)=`+0x0f >>2&3`（0x43e23d 合战 kind==1 骑兵）、洋枪(6)=`+0x10 >>4&3`（0x43e250 合战 kind==2）、茶道(9)=`+0x11 >>2&3`（0x4b3122/0x4b3160/0x4b318e 茶会主/客，(lv+1)×host）。基址佐证：`0x43e110`=unit→主将实体（word[unit+0xa]→×47→0x519868，37B 签名）、授艺站 sibling 五维 +0xd/+0xe、茶会站槽指针 [0x525c44/4c/50/58] + 国索引 [0x525c40]+0x24<49。写侧负向审计：全镜像 `and/or/xor byte[reg+0x0f..0x11],imm`（2-bit 掩码族）**0 命中** ⇒ 无共享 setter/无单指令掩码 RMW（S7 0x49bfxx 旗标 setter 等候选逐一排除）；读点基线 +0xf/+0x10/+0x11 = 214/453/73（提取子集 146/175/57，锚点全含）。自测 `scripts/skill_runtime_consumers_ref.py` **20/20 PASS**；套件 162/0 无回归。`GAME_DATA_SPEC.md` §3.5.6 新增运行时读取表 + §1.2 skills 行注 → 续239。
- ✅ **续240 已闭（技能写侧 = cap-3 递增器族 `0x4a3040+k*0x20` + §4.3.3 mode0–2 精确站点 静态闭合）**：续239「仍未知②」（写侧精确站点属 emu 增强）消除——技能位**无共享 setter 但写器族可枚举**：10 个 32B 对齐 helper（k=0..9=技能 id），thiscall `ecx=实体+0x0f`，对 `byte[ecx+k>>2]` 的 `(k&3)*2` 位「读级→未满 3 则写回 +1」。驱动 `0x45fca0`（唯一调用方 0x45f3eb）mode 映射 = mode0(学习内政)→口才 k0@0x45fdc8 / mode1(学习外交)→筑城 k7@0x45fd8d / mode2(学习魅力)→兵法 k5@0x45fd3d（各带概率门 + 成功置位）；成功后 功勲 += (旧级+1)×500（0x45fe18 lea×3+shl2 → `0x4a3210` 饱和加 `word[+0x26]` 上限 60000，与 setter 0x49a770 互证）+ MSG 名索引 {0→0,1→7,2→5}→0x507b58+id*5。同映射双胞胎 `0x4de0e0`（授艺推进，技能写向 0x49f5e0() 玩家对象）：mode2→k5@0x4de136/mode1→k7@0x4de152/mode0→k0@0x4de16c，递增后回读 `[+0xf]&3`/`[+0x10]>>6`/`[+0x10]>>2&3` 双向自洽。全镜像族调用点 = **19 处**（k0..k9 分组，逐站 ecx 约定审计 mem 型 11 处 disp 全 0xf）⇒ 技能位无其它写者。自测 `scripts/skill_inc_writer_ref.py` **83/83 PASS**；套件 163/0 无回归。`GAME_DATA_SPEC.md` §3.5.6 写侧段落 + §1.2 行注 + §4.3.3 mode0–2 行 → 续240。
- ✅ **续241 已闭（技能写器族 13 消费站逐一玩法归位 =「技能增长完整事件表」落库）**：闭合续240「仍未知③」——k1 马术=**马屋打工**（0x45d7a0→0x45d830→0x45d950；体力双方≥45 门；级<3 且 rand(5×新级)==0；同伴站另需主角本回合未升=单名额；工钱 min((30−时)/4,5)×(同伴?2:1)；升级提示 0x4704e0+名表 0x507b5d）；k2 算术=**商家学做生意**（0x458e20 1 参 cdecl(学者=主角)；门 0x459000(商人资本 N=byte[NPC+0x07],级 L)：L0≥40/L1≥70/L2≥100/L3 拒 msg570；学费 0x4590d0: 90−N/2·600−3N·5000−2N 贯 ×10 文；**交费必然 +1**，同伴级≤学者级才 +1 否则 msg568/569；时 24−时→次日 0 点+14h）；k3 剑术=**道场试合**（0x448990；**前置体力满 +0x21==+0x20**；胜 0x640/惜败 0x642 且级<3 → +1，败 0x643 不加；耗 4h；唯一站）；k4 忍术=**忍里修业 leaf 0x451f90 无条件 +1**（3 调用方=合格流 0x451f23/0x451f40 + 再指导流 0x4521a7「师父 2729 你还得再费费劲」）；k6 洋枪=**铁炮锻冶打工**（0x447110 自含循环→0x447230(主角,同伴) **2 参非 4 参**；主角/同伴**各自独立判定**可双升；日薪 200 文；jmp 回循环连续打工）；k8 礼法=**寺庙 30 日修行**（cmp di,0x1e fast-forward；**主角必然 +1**；同伴=五维资格→必然，否则 rand(10)==0 且级<3；**⚠ 唯一无 0x4704e0 提示/无 0x507b80 名表引用的写站**）；k9 茶道=**茶人品茶**（0x442a80(主角级)→茶具品质上限 0/6/13、品质=byte[项+0x08]>>3&0xf→0x442d70(级,品质)；主角 rand(10/25/30)<品质；**级==3 转调满级分支 0x442f70**：同伴 rand(600)<五维字节和(dword[+0xa]+byte[+0xe])）。新结构事实：实体 **+0x20=最大体力/+0x21=现在体力**。自测 `scripts/skill_writer_consumers_ref.py` **82/82 PASS**（A 13 站点/B 19 字节窗口/C 28 门结构/D 名表审计含 f8 负向/E leaf xref/F 消息链锚）。`GAME_DATA_SPEC.md` §3.5.6 新增「技能增长完整事件表」→ 续241。
