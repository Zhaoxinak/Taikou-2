# 破解状态清单（2026-08-31 盘点）

> 目的：把现有逆向产物按「已破解 / 未破解 / 工具草稿」三档归类，作为今后逐步破解的工作底稿。
> 本文件**只做标注，不移动任何文件**。原版游戏（`Taikou2 Original/`）与 Godot 复刻工程（`project.godot` / `fonts/`）不在此列。

---

## 0. 总览

| 档位 | 含义 | 规模 |
|---|---|---|
| ✅ 已破解 | 功能语义已钉死，有参考实现/规格支撑 | 约 21 个系统模块（续199 SAVEDATA 容器、续200 BSDATA 武将主表新增） |
| 🔶 半成品 | 主体已破，残留少数需 emu / 第三剧本校准的点 | 见 §2 |
| ❓ 未破解 | 真实敞口，尚无可用 schema | 见 §2（P0–P3） |
| 🛠 工具/草稿 | 逆向脚手架（`_` 前缀脚本、`.txt` dump），非交付物 | `scripts/` 下 645+ 个 `_*` 文件 |

**关键结论**：项目核心玩法系统（经济、单挑、合战公式与地形、事件、内政、外交、评定、晋升、城表、武将、国情、SNDATA 段结构与类型系统、**家臣/人事子系统(续170–173)**、**资源/音效子系统(续195–196)**、**SAVEDATA 容器(续199)**、**BSDATA 武将主表(续200)**）**已全部破解**。剩余敞口已收敛为少数硬骨头，最大的是 **SNDATA 49B 剧本记录的 43B payload 语义** 与 **GRP/PK8/KOS 图形容器格式**。

---

## 1. 已破解（按模块）

状态图例：✅ 自测已验证 PASS ｜ 🟡 参考实现已建（自测待复核） ｜ 🔧 自测脚本路径待修（模块本身已破）

| 模块 | 参考实现 / 规格文件 | 状态 | 关键结论（锚点） |
|---|---|---|---|
| 经济/物价 | `economy_ref.py` `economy_spec.json` `item_pricing_ref.py` `item_pool_bind_ref.py` `item_pool_spec.json` `item_table_ref.py` `item_tier_ref.py` `item_tier_grade_ref.py` `item_index_bind_ref.py` | ✅ | 货币/城镇字段/月产/农商软顶/筑城物资/宝物茶具米市买卖 49/49；定价 21/21；物体池 0x51e1f0 全定 |
| 单挑 | `duel_ref.py` `duel2_ref.py` `duel3_ref.py` `duel_hp_ref.py` `duel_spec.json` `duel2_spec.json` | ✅ | 18/18 + 670/670 + 17/17；HP/行动序公式全破 |
| 合战(公式/地形/单位) | `battle_formula_ref.py` `battle_units_spec.json` `terrain_ref.py` `terrain_spec.json` `terrain_attr_ref.py` `battle_mode_flags_ref.py` `battle_names_ref.py` `battle_sectionA_ref.py` | ✅ | per-tick 公式经 Unicorn 比特级验证；地形攻防移 16 类；15 单位槽 0x513910；**攻城战结构表 15×5B + 类别名（续177，15/15）**；**5 全局模式标志 setter/getter 几何+值语义（续186，20/20）**；**兵种/阵形/计略 名称：静态缺席已验证+参考表（续187）**；**section A 9 类实体访问器（续182，180/180）** |
| 事件系统 | `event_handlers_full_ref.py` `event_predicates_ref.py` `event_cond_ref.py` `event_dialog_ref.py` `event_dispatch_tables_ref.py` `event_id2_handler_ref.py` `event_id_dispatch_ref.py` `event_year_ref.py` `evt_id2_teppo_ref.py` `event_system_spec.json` | ✅ | 49 handler / 49 predicate 全 PASS；派发表/ID映射全定 |
| 内政/仕事 | `naisei_ref.py` `naisei_resource_ref.py` | ✅ | 12 主命 + 派发器 0x4a9f10 + 城表字段 66/66；**軍糧/米/資金 城纳结算三笔公式（续180，52944/52944，「随机项」证伪）** |
| 外交 | `diplomacy_ref.py` `diplomacy2_ref.py` `diplomacy3_ref.py` `diplomacy_matrix_ref.py` `diplomacy_spec.json` `ai_diplomacy_ref.py` `relations_check_ref.py` `relation_delta_writes_ref.py` `relation_ab680_ref.py` | ✅ | 关系矩阵 / 使者 / AI 决策 ALL PASS；**关系行动成功判定公式 0x4ab870（续179，2160/2160）**；**关系 ±delta 双写闭合**：国政治 byte[0xc]+1（续184）+ 矩阵低3位 rel-1（续185，`0x49fe40`/`0x4ab680`） |
| 评定 | `council_ref.py` `council_report_ref.py` `council_tails_ref.py` `council_spec.json` | ✅ | 主命名表 0x504b28 + 13 报告 handler 全定 |
| 晋升 | `promote3_ref.py` `promo_ref.py` `promo2_ref.py` `promo3_ref.py` `promo_spec.json` `promote3_spec.json` | 🔧 | 42/42（自测需 cd 工程根运行） |
| 城表 | `castle_fields_ref.py` `castle_f08_f0a_ref.py` `castle_strategy_ref.py` `castle_stream_map_ref.py` `castle_town_ref.py` | 🟡 | 基址 0x51eb88 stride31；字段 +0x04/+0x16 等全定 |
| 武将实体 | `entity_fields_ref.py` `entity_loyalty_thresholds_ref.py` `entity_methods_ref.py` `entity_mid_fields_ref.py` `entity_status2c_ref.py` `entity_status2d_highbits_ref.py` `entity_status2e_ref.py` `entity_status_semantics_ref.py` `entity_tail_methods_ref.py` `entity_tail_ref.py` | 🟡 | 60/60 字段；忠诚阈值/状态字节语义全定 |
| 国情/49国 | `province_ref.py` `province_name_alias_ref.py` `province_names_ref.py` `province_politics_ref.py` `province_state_ref.py` `province_spec.json` | 🟡 | 国情基表/政治表/名称/别名/状态全定 |
| SNDATA 段/类型/S7 | `sndata_sections_ref.py` `sndata_all_sections_ref.py` `sndata_tail_ref.py` `sndata_record_classify.py` `s7_struct_ref.py` `sndata_wordarray_payload_ref.py` `sndata_payload_fields_ref.py` `sndata_s7_flags_ref.py` `sndata_s7_statemachine_ref.py` | ✅ | 段结构全过；类型系统奠基（type=id_word&0xff）；**S7 每城运行时表结构与字段布局已破（§3.22，9/9）**；**S7 `+0x0f` 主标志位域语义已破（续191，8/8）：setter 字节签名+13 call-site+ecx←S7基(0x516a28+16*idx) 坐实专属**；**S7 `+0x08` 状态机归属已破（续192，4/4）：跳表/派发器/setter 双重 0 调用方=共享库非 S7，S7 已验证字段仅 `+0x0f`**；**word 数组类型字节粒度纠偏（续188，2/2）：type=75 真·位域/字数组、type=15 满长43B字节数组、38/166/214 稀疏** |
| 子表 | `s13_target_table_ref.py` `s15_event_flags_ref.py` `s15_segc_ref.py` `s15_table_ref.py` `s6_struct_ref.py` `sectA_spec.json` `sectA2_ref.py` `sectA2_spec.json` `emu_harness.py` `sndata_s15_segc_ref.py` | 🟡/🔧 | S13 目标表；S15 事件旗 25B 全定（续151 逐槽定名 + **续193 emu 再验证** set_c=byte[+0x13+idx] 5/5）；S6 结构；SECT_A 语义双闭合；**🟢 续193 emu 仿真骨架 `emu_harness.py` 落地（Unicorn 2.1.4，叶子函数自测 ALL PASS）—— 续192 后静态极限的转折基础设施，P0 49B 命名/#19/#89 均依赖** |
| 技能/天气/时间 | `skills_spec.json` `weather_ref.py` `weather_spec.json` `time_rollover_ref.py` | 🟡 | 技能/天气/月结翻转全定 |
| 谣言/谍报/计略 | `rumor_pattern_ref.py` `intel_ref.py` `intel_spec.json` `feint_ref.py` `deployment_tactics_ref.py` | ✅ | 谣言 13/13；谍报 13/13；虚招/部署战术全定 |
| 主君/势力/部署 | `lord_index_ref.py` `clan_table_ref.py` | 🟡 | 主君索引 0x49a7d0；势力表偏移 27297 |
| 家臣/人事子系统 | `retainer_consumers_ref.py` `msgx_label_ref.py` `entry_semantics_ref.py` `status_word_ref.py` | ✅ | 13 个 `+0x2a` 消费循环定名 + 候选池 0x51e9c0（续170, 13/13）；「不在」bit15/bit7 区分（续171, 5/5）；3 MSG 入口 = 家臣/人事子系统（续172, ALL PASS）；`0x4a5010`=登用 / `0x4a5370`=引抜+知行（续173, 16/16） |
| **武将主表 BSDATA**（续200 全闭） | **`bsdata_format_ref.py`（73/73）** `bsdata_format.json` **`bsdata_names.json`** | ✅ | **`BSDATA1/2.TR2` = 700×59B 明文主表**（无加密无 magic）；加载器 `0x47fa90` LoadBSDATA（`push 0xa154`=41300 精确吻合 / 缓冲 `0x524a20` / 错误串 `'BUSHOUbuf'`）；schema == 解码器 `0x47f7b0` 流格式（emu 反算 49 读 / 恰 59B / 无空洞）；逐字段定名 + **纠正旧 §1.2 五处错误**；`+0x27`=**生年−1490**（r=0.9875, 12/16 精确）；`+0x3a>>4`=**主角槽+武将档**（6 主角 + 441/212/36 = 695 完备，两剧本同分布）；**主君 `+0x36` 是显式字段**（废掉旧「按 home_city 归组推导」链）；`城 @0x31` 值域 0..199 索引**200 条城表**（非旧说 370 名表）；🔴 **推翻续181②**（`0x49c2b0`/`0x49c310` = 姓/名显示 getter，805 调用点）；🆕 **外字身份全认出**（長宗我部/香宗我部/垪）+ **定死行主序**（闭合续197⑨）。详见 `GAME_DATA_SPEC.md §1.1–§1.7`；🔘 **续203：尾部 +0x0e/+0x12 哨兵、+0x1e..0x21 城属性占位派生、+0x3a 强度档、+0x28..+0x2c 特征化**（`bsdata_tail_fields_ref.py` 19/19） |
| 其他数据 | `bsdata_entity_remap_ref.py` ~~`bsdata_fields_ref.py`~~ `bsdata_lifespan_ref.py` ~~`bsdata_spec.json`~~ `eval_word_binding_ref.py` `msgx_text_tables_ref.py` `ai_overall_ref.py` | 🟡 | 剧本实体重映射；MSGX 文本表；AI 总评。🔴 **续200：`bsdata_fields_ref.py` / `bsdata_spec.json` / `bsdata.json` 的字段命名有误已被取代**（`face@16`→武将ID、`trust@50`→功勲、`loyalty@56`→状态字低字节、`lifespan@58`→主角槽枚举），改用 `bsdata_format_ref.py`；`bsdata_lifespan_ref.py` 所依据的「`58`=寿命」前提**已被证伪**，须重做 |
| **资源 / 音效子系统**（新模块 · 续195+续196 开·已闭） | `resource_manifest_ref.py` `sfx_subsystem_ref.py` **`resource_cluster_ref.py`（31/31）** `resource_manifest.json` `resource_cluster_map.json` | ✅ | **续195**：90 个原版素材名串全量枚举（16 组阵列，每个仅出现 1 次）+ 39 项音效 ID→文件名（`@0x50ba40` 指针表；`play_sfx 0x4997c0` 硬编码 `cmp si,0x27` 佐证表长 39）+ 门控链四层（越界 / `0x5256c8` 载入数 / `0x50b9b0==0x10` 就绪 / `0x520604` bit1 全局开关）+ **emu 端到端 39/39** + ID 玩法语义 30/39（ID0 点击 / 1 取消 / 11 火计 / 31 技能提升 / 33 忍者）+ 🆕 发现中文版 BGM 已改 MP3 方案（`B:MMLDATA.LZW` 成死代码，原版用 `MP3/`+`mp3.dll`）。**续196**：资源加载簇全图 —— 44 个 `call 0x4802e0` 站点全解码成「簇 handler→资源表条目→文件名」（29 簇 / 40 静态可定 / 4 寄存器派生）；主资源表 `@0x506ad0`=**19 项 stride16**（索引19@0x506c00 非名⇒表长止18）+ 8 张子表；管线逐指令钉死（`memmove(0x522ca0,base,0x20)` / `add eax,2` **剥 `X:` 盘符** / `and al,0xfb` **清 bit2 修饰位** / 4 路尺寸跳表 `0x4ec948` / `[0x4fb07c]` stdcall3参 `ret 0xc`）+ **emu 端到端 152/152**。🔑 复刻避坑：盘符是 EXE 内部逻辑代号非真实路径，按**去前缀裸名**查原版目录。详见 `GAME_DATA_SPEC.md §3.23 / §3.24` |
| 格式 | `NPK_SPEC.md` | ✅ | NPK/LZW 资源格式已破 |

**规格文档（顶层）**：`GAME_DATA_SPEC.md`（主体已破，§3.21 内政为最新）、`BATTLE_SPEC.md`（公式+地形已破，§10 残留见 §2）、`SNDATA_SPEC.md`（段/类型已破，payload 见 §2）、`README.md`、`BREAKTHROUGHS.md`（突破日志，倒序续154 为首）。

---

## 2. 未破解 / 半成品（Backlog · 按优先级）

### P0 — 最大敞口
- **[🔶 已收口架构，payload 逐字段语义仍待 emu] 49B 剧本记录 43B payload 语义**
  - 🟢 **续174（2026-08-31）处理架构最终收口**（`scripts/sndata_pipeline_ref.py` **14/14**）：① 主循环 `0x4e8604` 全链 = `0x480240`(取索引)→`cmp si,-1`→`0x47fc60`(扇出)→头三字全0跳过→`0x47b390`(匹配器,模板 `0x50d834`)→`0x47fb80`(谓词)→`0x5205fe` 全局值开关→簇0/簇1/else。② **非 per-type 分派**——簇0(`0x492e20/0x493140/...`)与簇1(`0x492ed0/0x4931f0/...`)由**全局 `0x5205fe`** 选（非每条 id_word 比较），else 簇(`0x491e70/0x4873b0/...`)两模式均执行。⇒ 续159「~164 种类型 payload 字段 schema」前提不成立。③ **payload = 二进制非文本**（833×3 视图=2499，干净日文文本视图=0；`0x0c/0xf3` 填充占 43%；「可读」串全是 ASCII mojibake；续165「3 文本列」对多数类型不成立）。④ `0x47d860` = 文件 seek(idx×40960+0x198)，非 payload 字段解析。⇒ **P0 收敛为「粗粒度 category→resource-set + 记录为场景/资产二进制数据」**。
  - 🟢 **续176（2026-08-31）payload 确证纯二进制（自纠续175）**（`scripts/sndata_type_schema.py` **ALL PASS** + `sndata_type_schema.json`）：对 171 类型建结构指纹全表；**用 `bsdata.json` 699 个真实武将名交叉比对，SNDATA 提取的 1956 个「名段」0 命中** ⇒ 续175 初判「131 类型含嵌入 GBK 名」是过度断言（那些 `妈楞`/`鸟断`/`鰄` 全是结构字节巧合 GBK mojibake）。kind 分布 = **二进制 164 / word数组 5 / 状态开关 2**（word 数组 id = {15,38,75,166,214}，其中 id=15 最常见共 34 条，`sndata_payload_types_ref.py` ALL PASS，续183 精确化）。⇒ **payload 无真实名/文本，纯静态已到极限**。
  - 🟢 **续188（2026-08-31→09-01 纠偏·✅ 已破表征）**：**纠正续183「word 数组 {15,38,75,166,214}」标签**——5 类型实为**字节粒度数组 + 偶发 0x0c/0xf3 填充**，「word」是填充字节配对假象；仅 `type=75` 是真·位域/字数组（0x0000/0x0001/0x0101），`type=15`(34条) 为满长 43B 字节数组（值多为 0..15 小计数、偶有 ≤253），`type=38/166/214` 越稀疏（`sndata_payload_fields_ref.py` **2/2 PASS** + `sndata_payload_fields.json` 真实字段位图）。下一步：emu 驱动 `0x492e20`/`0x492ed0` 簇抓 type→字节位消费点定字段名。
  - 🟢 **续189（09-01·✅ 已破结构层）全 833 条 per-type 字段指纹 + 三栏文本/二进制分类**：**推翻「164 类二进制无结构黑箱」笼统印象**——建立 171 型可操作字段假设表（每型 per-byte 假设 const/small_int/nibble/byte_enum/wide）。关键：① `type=0x01`(n=13) **全部 43 字节∈{0,1}** → 43 字节独立布尔开关数组（最细粒度坐实续174「场景状态布尔开关」）；② `type=0x00/0x95/0x62` = 索引+标志混合（benum 位=实体/城/国索引，sint/nibble=小枚举，偶发 const=0x0c 默认哨兵）；③ 三栏文本经严格 cp932 解码（拒 PUA/mojibake/半角片假名巧合）**全量 0 可读日文** → payload 为二进制/自定义编码非显示标签（印证续174/176）；④ TOP 型 0x0c(249,fill0.54)/0xf3(111,fill0.64) 为填充占位、0x0f(34) 为小计数数组（`sndata_payload_classify_ref.py` **4/4 PASS** + `sndata_payload_classify.json`）。缺口收窄为「171 个有结构指纹的待命名字段表」。下一步：emu 驱动 `0x47fc60` 逐记录 hook 追 type=0x01 的 43 布尔运行时消费者 + 对照游戏 dump type=0x00/0x95/0x62 的 benum 索引落点定首义。
  - 🟢 **续194（09-01·✅ 已破「读」前提）emu I/O-回调读管线落地**（`scripts/emu_sndata_read.py` **ALL PASS**：Test A 833/833 + Test B 7/7）：把 `[0x4fb0a8]`lseek/`[0x4fb0a0]`read/`[0x4fb09c]`flush 三个回调指针重定向到自写桩，用 `UC_HOOK_CODE` 在桩入口拦截、直接用内存里 SNDATA1.TR2 喂数据。`read_record(0x47d890)` 真实执行读全 833 条字节级正确；`0x47fc60` 扇出真实执行复现三重叠全局 `0x522c88/0x522c60/0x522c70` 的交错最终态。关键纠偏：lseek 桩须 `ret 12`（非 `ret 4`）否则 `0x47d890` 的 dst 槽错位崩溃；`0x522c88` 落在 `0x522c60` 区域内（重叠拷贝链）须用影子缓冲按序模拟。⇒ P0 命名语义的「读」前提坐死，下一步 hook `0x47fc60` 出口抓 type→资源映射。
  - 缺口：**各型具体字段名/玩法含义仍未知**（type=0x01 的 43 布尔各对应哪个场景状态；type=0x00 的 benum 索引指向哪些实体/城/国；须 emu 跑消费点 / 对照游戏运行期 dump）。结构层（字段边界/值域）已由续188+续189 钉死，剩「命名语义」深坑。
  - 🟢 **续178（2026-08-31）`0x5205fe` 已定性 = 全局画面模式字**（`scripts/sndata_mode_ref.py` **ALL PASS**）：53 处引用；写点 =0×8 / =1×4（`0x495270` 写 1 后直接调簇1 `0x492ed0` 互证值开关）/ =2×3 / =3×2（`0x434300`/`0x434910` 攻城战 init，与续177 互证）；主循环 `0x4e86c0` 三路极性（0→簇0 / 1→簇1 / else→双模式簇）逐指令钉死。
  - 下一步：(A) emu 驱动 `0x4e8604` hook `0x47fc60` 出口抓 type→资源全映射；(B) 精解 type=0xdc 等 word 数组（15/38/75/166/214）。
  - 现状：类型系统已奠基（`sndata_record_classify.py`）：`type = id_word & 0xff`；两剧本各剔除填充后 sc1=427条/167型、sc2=443条/161型；首个语义类型 `0x01`=场景状态布尔开关；payload 内 `0x0c/0xf3`=字段默认值哨兵。**`0x47fc60` 逐记录扇出器已程序化钉死**（`sndata_record_processor_ref.py` 自校验 PASS）：`record[6:49]→0x522c88`(43B 主体 payload)、`record[0x13]→0x522c60`(1B)、`record[0x20]→0x522c70`(1B)；头 `record[0:2]/[2:4]/[4:6]`→调用者 arg2/3/4。
  - ⚠️ **旧前提已推翻（续158）**：旧「emu 0x47f350 主解析器建 type→processor 映射」整条证伪——`0x47f350`(pass1)+`0x47f5c0`(pass2) 的 **18 个 SUB1 子加载器不处理 49B 记录**（只用 setter 写 实体池/城/国/名表等非记录数据）；**真正的记录循环 = `0x4e8625`/`0x4e89cd`**，经 `0x47fc60` 扇出 + `0x47ff68` 按 ax 分派；二者全镜像**无立即数引用** ⇒ 走**寄存器间接调用/函数指针表**，不在 0x47f350 静态调用图内。
  - 🟢 **续159（2026-08-31）扇出链全破 + P0(A) 前提证伪**（`scripts/sndata_fanout_ref.py` **38/38 PASS**）：① `0x4ebfc0`=**strlen** / `0x4ebfe0`=**strcpy**（逐指令验证）⇒ 续158「`0x4ebfe0` 块拷贝助手 + size 参数来源」**前提不成立**（strcpy 无 size 参数，null 结尾）；② `0x47d890`=`read_record`：`lseek(16+idx*49)` + `read(49)` ⇒ **stride 49 / 文件头 16B / 记录长 49B** 三数坐实，数据源 = SNDATA 文件**按索引直接 seek+read**；③ `0x47fc60` 扇出 = 3 头字（`*arg2=word[rec+0]` id_word / `*arg3=word[rec+2]` sub_word / `*arg4=word[rec+4]` flag‖rel<<8）+ **3 次 strcpy 重叠视图**（payload 起点 0/13/26 ⇒ `0x522c88/0x522c60/0x522c70`，无 null 时长 **43/30/17**）⇒ 续158「后两者各 1 字节」**有误**；④ `0x47ff50`=记录文本格式化器（`0x522c88` 补齐到 14、`0x522c70` 补齐到 18）。🔴 **(A)「按类型索引的函数指针表」证伪**：全镜像 ≥128 项连续代码指针表**仅 1 张**（`@0x501004`，153 项全 `jmp` thunk，**0/153 触及记录缓冲**），缓冲字面引用仅 33 处/16 函数（`sbb/xor/adc eax,0x522c52` 等属算术立即数**假阳性**）⇒ **分派不按类型选 handler**，类型作为数据参与头字比较/索引。数据侧：payload **本质是二进制非文本**（833 条中仅 14 条含 ≥20 null；主要类型 `0x0c`×249/`0xf3`×111/`0x3e`×15 等「三段含 null」比例 **0%**）。
  - 缺口：**~164 种类型的字段 schema 未知**（TOP：0x3e×15 / 0x08×12 / 0x0d×11 / 0x8c×10 …）。
  - 下一步：(A) 🔴 **已证伪（续159）——不存在 type→handler 函数指针表**（全镜像 ≥128 项连续代码指针表仅 `@0x501004` 一张，153 项 thunk、0/153 触及记录缓冲）。改为逐指令追 `0x4e8625`/`0x4e89cd` 两条主循环对头四字（`id_word`/`sub_word`/`flag`/`rel_word`）的**比较链**，建「类型→分支」映射；并反汇编助手 `0x47d720`/`0x47d850`/`0x4ec010`/`0x4ebe60`/`0x49f120`/`0x49f0b0` 补全扇出链；(A2) 用 **16B 头 + 49B stride** 重读 `Taikou2 Original/SNDATA*.TR2`（40856B）复核 833 条切分与末段 23B 残留；(B) 反汇编各分支内部读 `0x522c88` 偏移 → 建 `type→字段偏移` 语义；(C) 精解 TOP5 类型（`0x3e`/`0x01`/`0x08`/`0x0d`/`0x8c`）payload 字节布局。
  - 🟢 **续160（2026-08-31）主循环分派管线实锤 + 代码级二次证伪 P0 + 纠正续158 两处过度断言**（`scripts/sndata_dispatch_ref.py` **22/22**）：① 主循环 `0x4e8604`/`0x4e8625` 全链——`0x480240`→`0x47fe00` 经游标 `@0x509684` 迭代 833 条（`cmp si,-1` 结束哨兵）；头三字落点纠正为 id_word@`[E+0xa]`、sub_word@`[esp+8]`、flag_rel@`[E+6]`（push 间 esp 下移所致）。② 分派是**代码级管线**：头三字全 0→默认路径；否则 匹配器 `0x47b390`(模板 `0x50d834`,薄壳→`0x47b2e0`)→谓词 `0x47fb80`(内部 `call 0x47f350`)→读全局 `0x5205fe` **三路值开关(==0/==1/else)**→三簇 handler（`0x492e20`/`0x493140`/…、`0x492ed0`/`0x4931f0`/…、`0x524740`/`0x491e70`/…）。③ 🔧 纠正：续158「`0x47ff68`=类型分派器」**错**（实为头全 0→`strcpy(0x509648)` 默认名助手）；续158「记录循环不在 `0x47f350` 图内」**误导**（`0x47fb80` 直接 `call 0x47f350`）。④ payload 解析真相：handler 簇**不直读 0x522c88**，经 `0x4802e0`(`push 0x522ca0; call 0x492800`/`0x4ec8c0`) 读兄弟缓冲 `0x522ca0` 族写入游戏表 `0x524978`/`0x524918`/`0x524740` ⇒ 完整 type→field schema 须追 payload 解析引擎 + 各 handler 表写入，master 注册间接分派器宜用 emu 提取。详见 `SNDATA_SPEC.md §4.0.6` / `BREAKTHROUGHS.md 续160`。
  - 🟢 **续161（2026-08-31）记录「类别→资源表→payload 选择器」schema 重构 —— 重大改判：833 条记录不是统一 43B payload 字段描述符，而是剧本资源/资产 + 数据初始化指令**（`scripts/sndata_template_ref.py` **19/19**）：① 各「类别 handler 簇」持有**资源文件名表**而非字段描述符——`0x506b20`(type-0 簇 `0x492e20`)=`B:MAPCHIP.LZW`/`B:MAPCHAR.LZW`/`B:SHOP_BG.LZW`/`B:SHOP_OBJ.LZW`/`B:SHOP_MSK.LZW`/`B:ANMSEQ.LZW`(6×16B)、`0x506ba0`(type-1 簇 `0x492ed0`)=`C:TOWNCHIP.LZW`/`C:TOWNCHAR.LZW`/`C:HEXMES.LZW`/`C:KOSENGRP.LZW`/`D:FACE.LZW`/`A:EXTFACE.PK8`(6×16B) ⇒ 记录语义模型升级为**「类别作用域」**（type→类别 handler→资源表→payload 选择器→具体资源+参数）。② payload 选择器=定长构造器 `0x4ec8c0`（`and al,0xfb`→`cmp eax,3; ja` 4 路 switch→`jmp [eax*4+0x4ec948]`，尺寸三态 `0/1/2/0x1000`→`call [0x4fb07c]` 注册）；资源经 `0x492800`→`0x4f40b0`(3 参转发) 登记。③ `0x50d820`/`0x50d834` 是 **GBK 消息串**（「这个进度无法使用。」/「读取游戏进度，\n可以吗？」）非类型模板——并提示续160「匹配器 `0x47b390(0x50d834)`」标签可能需重审（进度读取对话消息串而非类型分派键）。④ 全镜像资源阵列扫描 **5 张**（`0x5030d8`/`0x5034c0`/`0x506ac0`/`0x506b90`/`0x509590`）；⚠️ type-0 表 `0x506b20` 是 `0x506ac0` 阵列内嵌[6..11]、type-1 表 `0x506ba0` 是 `0x506b90` 阵列内嵌[1..6]。⚠️ 工具坑：资源名解码须 15 字节窗口（13 字节会截断 14 字符长名 `B:SHOP_OBJ.LZW`）。详见 `SNDATA_SPEC.md §4.0.7` / `BREAKTHROUGHS.md 续161`。
  - 🟢 **续162（2026-08-31）「payload 解析引擎」实为资源加载管线 —— 推翻「扁平字段映射器」假设，type→field keystone 坐实为「选择器构造器 + 文件 I/O 读取器 + 游戏表写入器」三件套**（`scripts/sndata_payload_engine_ref.py` **33/33**）：① 四函数管线钉死——`0x4f40b0`=memmove（rep movsd + 边界 `cmp edi,esi`/`cmp edi,eax` + `jbe`/`jb`）、`0x492800`=3 参转发（`push`+`call 0x4f40b0`）、`0x4802e0`=`memmove(0x522ca0, 资源表, 0x20)`+`call 0x4ec8c0`+`movsx ecx,[esp+0x18]`（记录算出的资源表偏移）、`0x4ec8c0`=资源选择器构造器（strcpy 剥 `X:`+`and al,0xfb`+`cmp eax,3; ja` 跳表 `0x4ec948` 尺寸 0/1/2/0x1000+`call [0x4fb07c]`）。② 资源读取走文件 I/O（`0x441330`/`0x441360` 经 `call [0x4fb0a8]`+`call 0x441170` 写 `0x524978`/`0x524918`；失败 `0x47bde0`=`push 0x5094d0`+`call 0x47ae80`）。③ 类别=资源加载器组——`0x492e20`→`push 0x506b20`(MAPCHIP)/`0x493140`→`push 0x506b30`(MAPCHAR)/`0x492f80`→`push 0x506b40`(SHOP_BG) 各 `call 0x4ec8c0`+写游戏表。④ `[0x4fb07c]`=运行期加载器回调槽（静态 `0x003000`<BASE）。⚠️ 结论修正：0x522ca0 是资源表暂存缓冲非记录字段视图。详见 `SNDATA_SPEC.md §4.0.8` / `BREAKTHROUGHS.md 续162`。
  - 🟢 **续163（2026-08-31）记录 payload 在资源加载层「不参与资源选择」——推翻续162 仍未知①前提（`[esp+0x18]` 是 arg1 尺寸非记录偏移），资源基址 90% 站点硬编常量**（`scripts/sndata_rec2offset_ref.py` **17/17**）：① 栈帧数学钉死 `0x4802e0(arg0=基址@[esp+4], arg1=尺寸@[esp+0x18])`（push esi+memmove 三次 push ⇒ `[esp+0x18]`=入口+8=arg1）；`ret 8` 佐证 2 栈参数。② 全镜像 44 个 call 0x4802e0：尺寸(arg1) 在 type-0 簇与绝大多数站=硬编 `4`；基址(arg0) 36/40 可分类站=常量 VA（`0x506b20`/`0x506ba0`/`0x506b40`/`0x5030d8`…），仅 4 站寄存器派生（`0x43379d` esi=`[esp+0x194]`/`0x47d7d4` ecx=`[esp+0x2c]`/`0x47fab9`/`0x47d741`），**0/44 直读记录缓冲 0x522c..**。③ 记录 43B payload 在 0x4802e0 层不参与资源选择——资源身份由「记录类型→簇 handler」固定决定（续160/161），payload 角色是场景初始化/事件数据由谓词/匹配逻辑消费，非资源选择器。④ 修正续161「6 资源各一 handler」：`0x492e20` 单 handler 加载 3 张表。详见 `SNDATA_SPEC.md §4.0.9` / `BREAKTHROUGHS.md 续163`。
  - 🟢 **续164（2026-08-31）payload 字节语义模型坐实 —— 49B=6B 头(3 word)+43B payload(3 重叠视图 rec+6/19/32)，修正续159「0/13/26」**（`scripts/sndata_payload_bytes_ref.py` **19/19**）：① 扇出 `0x47fc60` 拆头 3 字 rec[0]=id_word/rec[2]=sub_word/rec[4]=flag‖rel + 3 次 `strcpy`(0x4ebfe0)：`0x522c88←&rec[6]`/`0x522c60←&rec[19]`/`0x522c70←&rec[32]`（长度 43/30/17）→ payload 起点 rec+6（非续159 rec+0）。② 匹配器 `0x47b390`/`0x47b2e0` **0 处读记录缓冲** → 模板/状态门控非字段匹配（印证续161）。③ 谓词 `0x47fb80` 取记录指针 `edi=[esp+0xac]` 传给序列化解析器 `0x47d860`/`0x47f350` → 43B payload 被当 3 段重叠子结构解析（续160「模板+稀疏覆盖」坐实）。④ 6 簇 handler(0x492e20/0x493140/0x492f80/0x492ed0/0x4931f0/0x524740) **0 处读 `0x522c..`** → 印证续163：payload 不参与资源加载。⇒ 完整模型：资源身份由记录类型固定，payload 是场景初始化/事件数据。详见 `SNDATA_SPEC.md §4.0.10` / `BREAKTHROUGHS.md 续164`。
  - 🟢 **续165（2026-08-31）3-文本视图消费路径坐实 + 推翻续164③「0x47f350 解析 3 段重叠子结构」**（`scripts/sndata_scenario_decode_ref.py` **ALL PASS**）：① 3 视缓冲 `0x522c88/0x522c60/0x522c70` 全镜像仅 3 处引用、全在 `0x480000`、被当字符串消费(strcpy→strlen→空格对齐补 14/18/18)→ **3 视图是记录 rec+6/19/32 的 3 文本列，非二进制子结构**；② `0x47f350` 是「载入剧本」主解码器(程序化枚举恰 **18 子解码器** 0x47dae0..0x47f210，剔除守卫 0x47f5b0/解密器 0x47d960)→ 写入全局游戏表(实体 0x519868 / 城 0x51eb88 / 国 0x5179b8 / S15 事件旗 0x5203c0 / 0x516610 字表 / 0x517c70 3B 字段)，处理**剧本 bulk 块非 49B 记录**；③ 49B 记录读取器 `0x47d890` stride=`idx*49+0x10` 坐实；④ 🔧 纠正续164③/续160③：谓词 0x47fb80 调 0x47f350 解码的是 bulk 块，3 视缓冲真正消费者是 `0x480000` 显示格式化器（续164 的「3 段重叠子结构」应改「3 文本列」）。**续166 修正：0x516610=S6、0x517c70=S17 已由 §5.2 定名，续165 C2 映射 bug（min-glob 误取 scratch 基址）已修，18 子解码器全映射 S0..S17 以 §5.2 权威**。详见 `SNDATA_SPEC.md §4.0.11` / `BREAKTHROUGHS.md 续165 / 续166`。
  - 关联文件：`sndata_record_classify.py` `sndata_records.json` `sndata_record_processor_ref.py` **`sndata_fanout_ref.py`（38/38）** **`sndata_dispatch_ref.py`（22/22）** **`sndata_template_ref.py`（19/19）** **`sndata_payload_engine_ref.py`（33/33）** **`sndata_rec2offset_ref.py`（17/17）** **`sndata_payload_bytes_ref.py`（19/19）** **`sndata_scenario_decode_ref.py`（ALL PASS）** **`sndata_pipeline_ref.py`（14/14）** **`sndata_type_schema.py`（ALL PASS）** **`sndata_payload_classify_ref.py`（4/4）** `SNDATA_SPEC.md §4.0 / §4.0.6 / §4.0.7 / §4.0.8 / §4.0.9 / §4.0.10 / §4.0.11 / §8/#2` `BREAKTHROUGHS.md 续158 / **续159** / **续160** / **续161** / **续162** / **续163** / **续164** / **续165** / **续174** / **续176** / **续188** / **续189**`。

### P1 — 需 emu 实跑校准
- **[半成品] 合战 §10 残留**（详见 `BATTLE_SPEC.md §10`）
  - 🟢 **section A 9 类实体访问器（续182，✅ 已破）**：`0x439050` getLo(`byte&0xf`) / `0x4390c0` getHi(`(byte>>4)&0xf`) 全 9×20=180 格 Unicorn 穷举一致；表 `0x512e58` 180B = 9 类(a=0..8) × 20 属性(c=0..19) stride20；`battle_sectionA_ref.py` 180/180。9 类是**兵种类别索引 a∈[0,8]**（具体中文兵种名仍属 §8 未定位，见下条）。emu 实跑 `0x439050` 记录 a 取值集合已闭合。
  - 🟢 **模式标志玩法含义（续186+续190，✅ 已破·含精确语义）**：5 全局标志 setter/getter 几何 + 值语义 Unicorn 20/20（续186，`battle_mode_flags_ref.py`）；**精确玩法语义由 7 setter 的 21 call-site 战斗相位上下文钉死（续190，`battle_mode_flags_semantics_ref.py` 9/9）**：`mode_m1`(0x511bf8)=**交战进行主标志**(0/1，配置·合战init 置位/清零)、`mode_m2`(0x51352c)=**合战子模式标志**(0/1/计算值；攻城戦内 bit0 概率翻转=攻城戦变体)、`parity`(0x513540)=**阶段/回合奇偶计数**(合战init 置初值、攻城戦每阶段+1 钳顶)、`battle_type`(0x513548)=**战斗类型枚举**(0=攻城戦@0x434000/3=野戦@0x422000)、`handle_stat`(0x513534)=**攻城戦状态/句柄字**(攻城戦init 自[esp+0x3c]写入)。消费侧经「base 寄存器+偏移」访问 struct（基址簇 0x51352c：+0=mode_m2/+0x08=handle_stat/+0x14=parity/+0x1c=battle_type），getter 调用点=0 坐实。详见下条「兵种中文名」。
  - 🟢 **兵种/阵形/计略 中文名（续187，部分破）**：**静态文本路径已验证走不通**——EXE + 111 个原始资源文件 cp932 扫描，9 阵形/兵种/计略 的**具体名称字符串 0 命中**（`battle_names_ref.py` 负向自测 PASS），坐实「非 EXE 静态、非 MSGX」；附 TK2 常见名称**参考表**（外部领域知识，非逆向产物，须运行时确认）。索引→名称绑定须 emu 捕获战斗渲染路径字符串指针 / 精灵 label（下一步）。
  - 🟢 **地形属性数值块（续177，✅ 已破）**：`0x513a78` 非地形攻/防/移，实为**攻城战结构条目表 15×5B**（源 HKMAPNEW.LZW@0x6a4；类别名 本城/米仓/了望台/哨所/城门）；`terrain_attr_ref.py` 15/15。见 `BATTLE_SPEC.md §7`（已重写）。

### P1.5 — 资源格式（续196 新增敞口）

- **[🟢 2/4 已破] KOS ✅ / TR2 ✅（GAIJI 分支）/ GRP ❓ / PK8 ❓**
  - **✅ 续197：`KOS` = 「1 字节 XOR 密钥 + 标准 WAV」** —— `raw[0]`=key（39/39 全为 `0xAE`），`raw[1..]` 逐字节 XOR key 即标准 RIFF/WAVE（PCM 8bit/单声道/22050Hz，fmt(16)+data 两 chunk，data 长奇数补 1B pad）。解码器 **`0x499380` = `xor16(buf,key16,len)`**（16 位步进 `xor word[eax],dx`）；🔑 **密钥读自文件第 0 字节**（`0x4993a0`：`0x4ec9a0` 读首字节 → `mov dh,bl` + `or ebx,edx` 扩成 `key16=key|key<<8`）⇒ 全镜像无 `xor …,0xae` 立即数。分派：`.KOS`→`A:WAVE.TMP`、🆕 `.KOB`→`A:MIDI.TMP`（但原版无 .KOB 文件，中文版 BGM 走 `MP3/`，该分支死路径）、其它扩展名不解码。产物：**39 个可播放 WAV**（`scripts/_decoded_kos/`）+ `kos_sfx_manifest.json`（ID→文件→时长，合计 46.31 s）。详见 `GAME_DATA_SPEC.md §3.25`。
  - **✅ 续197：`GAIJI.TR2` = 16 × 34 B = 544 B** —— 每条 = `u16 LE` 源码 + 32 B 的 **16×16 1bpp 位图**（行主序 MSB 先行）；**前 15 条实字形（源码 `0x7670+i`），第 16 条终止记录（`0x7721` + 32B 全 0）**。三函数钉死：`0x48c070` LoadGaiji / `0x4f1ae6` InstallGaiji（`cmp 0xa140`/`cmp 0xa14f` **硬编码上下界=表长独立第二证据**，缓冲 **`@0x529e38` stride 32**）/ `0x4f1a06` GetGlyph（未命中回退常规字体 `0x4f1a42`）。🆕 **GBK 码位 `0xA140..0xA14F` 被劫持作外字区**（该区间在 GBK 未分配；游戏文本为 GBK/CP936）。⚠️ 未知：15 个外字的字符身份、2 字节源码语义（**Unicode 假设已证伪** 0/15 判别匹配）、行主序 vs 列主序（转置不可由数据区分）。详见 `GAME_DATA_SPEC.md §3.26`。
  - **✅ 续199：`SAVEDATA.TR2` 容器全闭** = **8 槽 × 40960B**（🔴 纠偏旧「16×20480」）+ 49B 槽元数据逐字段定名（`年/月/日/主角名13/所在国13/所在地+身分17` = 49 精确闭合）；史实交叉验证 slot0 = `1560年5月20日/木下藤吉郎/尾张国/清洲城步兵头`。`scripts/savedata_container_ref.py` **57/57**。
  - **✅ 续200：`BSDATA1/2.TR2` 全闭** = **700 × 59B 明文武将主表**（无加密、无 magic）；加载器 **`0x47fa90` LoadBSDATA**（`push 0xa154`=41300 与文件大小精确吻合，缓冲 `0x524a20`，错误串 `'BUSHOUbuf'`）；59B schema == 实体解码器 `0x47f7b0` 流格式（emu 反算 49 读操作 / 消费恰 59B / 偏移无空洞）；**逐字段定名并纠正旧 §1.2 五处错误**（`16`→武将ID、`50-51`→功勲、`56`→状态字低字节·忠诚真在 `0x35`、`57`→须 `&7`、`58`→主角槽枚举）；🆕 `+0x27`=**生年−1490**（史实 r=0.9875，12/16 精确）、`+0x3a>>4`=**主角槽+武将档**（6 主角 + 441/212/36 = 695 完备划分，两剧本分布相同）；🔴 **推翻续181②**「`0x49c2b0`/`0x49c310` = 关系记录指针」→ 实为**姓/名显示 getter**（805 调用点、四段 ID 空间）；🆕 **外字身份全部认出**（`A141+A143+A144`=「長宗我部」/ `A142+A143+A144`=「香宗我部」/ `A147`=「垪」），顺带**定死行主序位序**（闭合续197⑨ 悬案）。`scripts/bsdata_format_ref.py` **73/73**，产物 `bsdata_format.json` + **`bsdata_names.json`（700×两剧本全姓名）**。
  - **✅ TR2 四类分支已全部有专文档**：`SNDATA1/2.TR2`（`TAIKOU2_SCENARIO`）、`SAVEDATA.TR2`（`TAIKOU2_SAVEFILE`，续199）、**`BSDATA1/2.TR2`（700×59B 武将主表，续200 全闭）**、`GAIJI.TR2`（续197）见 `SNDATA_SPEC.md` 与 `GAME_DATA_SPEC.md §1`。🆕 **续202：头 `+0x10` 4 字节字段语义已闭** —— `file[0x10..0x11]`=16-bit LE 校验和（解密后流字节累加和 mod 0x10000，sc1=0xb84b/sc2=0x8013，emu 端到端匹配）、`file[0x12..0x13]`=XOR 密钥种子（key=byte[0x12]^byte[0x13]，sc1=0x0c/sc2=0x0a），`scripts/sndata_header_ref.py` ALL PASS。🔑 **`.TR2` 是容器扩展名，复刻必须按加载站点分派解析器**。
  - 现状：原版目录 149 文件 = **39 `.KOS`（音效）✅** + 34 `.mp3`（BGM，中文版替换） + **32 `.LZW`** + 9 `.png` + **6 `.TR2`（5/6 已破）**。已破格式：**NPK / LZW / KOS / TR2**。
  - 建议顺序（更新）：① ~~`KOS`~~ ✅ → ② ~~`TR2`~~ ✅（四类分支全破，含头 `+0x10` 4B 字段，续202）→ ③ **`GRP`**（5 个：ACERTWP/END/KOEILOGO/PRESS/SMODE；从 `0x50a208` `B:GRPDATA2.LZW` 消费者入手）→ ④ `PK8`（`EXTFACE.PK8`）。
  - 影响面：`TR2` 是**剧本 / 存档容器**（`SNDATA1·2.TR2` / `BSDATA1·2.TR2` / `SAVEDATA.TR2` / `GAIJI.TR2`）⇒ 直接关乎 `SNDATA_SPEC.md §4` 与存档读写；`KOS` 是 39 个音效本体；`GRP`（背景/标题画，RGB565 裸图）与 `A:EXTFACE.PK8`（8bpp 追加颜）是图像。
  - 有利条件：续195/196 已给出**完整文件名清单 + 加载簇归属 + 加载管线语义**（剥 `X:` 盘符后按裸名找文件），且原版文件**实体全部在本地** `Taikou2 Original/` —— **不需要 emu，纯本地样本即可开破**。
  - 建议顺序：① **`KOS`**（39 个样本、单文件小、结构最可能简单，且音效名已与 ID 绑定可做交叉验证）→ ② **`TR2`**（剧本·存档，价值最高）→ ③ `GRP` → ④ `PK8`。
  - 突破口提示：LZW 已破 ⇒ 若 KOS/TR2 也用光荣 LS11 变体，可直接复用解压器；先做**魔数/头字段统计**（前 16 字节分布、文件长度与头字段的相关性）。

### P2 — 关系/运行时/文本
- **[半成品] 进贡/威吓/朝廷/谋略 关系变动量**：handler `0x4ab680/0x4ab8f0/0x4ab3c0/0x4aa690` 已定位。🟢 **续179 已破 `0x4ab870` 成功判定公式**（`threshold=ctx[0xd]+5*(sat_sub(ctx[0x2d]&7,3)+2*(ctx[0xf]&3))`；`rand()%(ent[0xd]+10*rel)+15*rel>=threshold`，Unicorn 2160/2160）+ `0x4ab680` 头部门链（国政治表解析+三道资格门）。🟢 **续181（✅ 已破三 handler 结构）**（`relation_handlers_ref.py` ALL PASS）：① `0x4ab8f0(e)` 真实语义 = 取 `ent.byte[0x18]` 所指国政治.word[4]=国主实体索引；空缺(idx≥370)或国≥49 → 返哨兵 2，否则返国主 idx（非「返 2=OK」）；② 关系矩阵几何 `0x49c2b0(e)=0x521aa8+idx*7`(正向) / `0x49c310(e)=0x520660+idx*7`(反向)，两实体间 7 字节记录、主级别 0..7 八段；③ `0x4ab3c0`=行动 handler，经 `0x49b5b0`(低半字节)/`0x49b5d0`(高半字节) 写 国政治[国].byte[0xc]，弹消息 `0xd20`/`0xd21`；④ `0x4aa690`=suitability+dispatch，调 `0x4aa820`（更新目标忠诚/状态 + 候选池 `0x51e9c0` + 关系消息 `0x581`/`0x582`）。余：关系 ±delta 实际写点（`0x4aa820`/`0x4ab3c0` 叶子，未见 sat_add）与 `0x4ab3c0` 写入 `0xc` 的两 4-bit 值玩法含义 → 继续 emu 抓取（续181 下一步）。🟢 **续184（✅ 已破 ±delta 写点）**（`relation_delta_writes_ref.py` ALL PASS）：① 唯一真写点 = `0x4ab3c0(actor)` 成功后 国政治[prov].byte[0xc] = (quality<<4)|(level+1)（低4位=新 level=旧 level+1，封顶 10；高4位=quality）；② quality 规则：新 level<5 → `0x4ab300(新level,prov)` 取同 level 下其它国未占用的最小高半字节（确定性碰撞避免，`0x4ebd60` 强制 0 → 取最小）；新 level≥5 → 0；新 level==10（封顶，L==9）→ 由**国主实体.word[0x2]** 强制 13→2 / 16→1 / 695→1 / 其它→0；③ 🔴 `0x4aa820` 证伪「更新目标忠诚/状态」——仅经 `0x49c2b0`/`0x49c310` 取双向关系记录、调 `0x47b900` 把 `0x581`/`0x582` 格式化进 `0x517710` 再 `0x47c080` 显示，完全不写 delta（忠诚/状态由 `0x4ab870` 续179 路径在调本簇前处理）；④ `0x49b5b0`/`0x49b5d0` 为纯位写入助手（`byte[0xc]=(byte[0xc]&0xf0)|v` 低半字节 / `(byte[0xc]&0xf)|(v<<4)` 高半字节）；⑤ `0x4aa690` suitability+dispatch 不写 delta。🟢 **续185（✅ 已破）**：`0x4ab680` 成功分支关系矩阵写点 = `0x49fe40` 写矩阵低3位=`sat_sub(rel,1)=rel-1`（与续184 国政治 byte[0xc]+1 方向相反·伴生双写）+ MSGX 四消息中文定名（`relation_ab680_ref.py` A/B/C/D 全 PASS）；caller=`0x4a9000` 四关系行动 dispatcher 中「使关系恶化」那一支。
- **[✅ 已破·续155+续191] S7 `0x516a28` 200×16B 每城运行时表**：结构与字段布局全静态钉死（续155）。🟢 **续191（✅ 已破 `+0x0f` 主标志位域语义）**（`sndata_s7_flags_ref.py` 8/8）：两参数化 setter `0x49bf50`(写低4位 bits0-3)/`0x49bf90`(写高3位 bits4-6=0x70) 字节签名实锤；call-site 4/9 全数；**调用方 ecx 经 `0x516a28+16*idx`(idx 来自身城表 0x51eb88×16) 派生 → 坐实 S7 每城条目专属**，并揭示「城堡→S7 条目」映射约定；与续155 `test 0x70` 互证 ⇒ `+0x0f`=城主/据点主标志位域（低4位=状态枚举含默认哨兵 0xc / 高3位=激活掩码 7→0x70）。⚠️ **续192（✅ 已破归属：+0x08 状态机=共享库非 S7）**（`sndata_s7_statemachine_ref.py` 4/4）：跳表 `0x49c1cc`(8 项)/派发器 `0x49c064`/`+0x08` setter `0x49bfba` 经「E8 + raw 字面」**双重扫描均为 0 调用方** ⇒ 完全孤立=续155 预警的**共享/通用 16B-entry 库**（派发器载 `word[+0x08]` 低3位选 handler，7/8 handler 读 `byte[+0x05]` 算查表索引 → 通用「类型→资源查找」派发器），**非 S7 专属**；与续191「+0x0f setter 有 4/9 E8 call-site 且回溯 S7 基址」形成干净对照。⇒ S7 已验证字段**仅 `+0x0f`**；`+0x08/+0x0a/+0x0b` 不纳入 S7 专属判定，其余字段 `+0x04`(指针链)/`+0x0c`(3-bit 标志) 写入路径须 emu 钩 `0x516a28` 抓读者/写者确认。见 `GAME_DATA_SPEC.md §3.22`、`s7_struct_ref.py` 9/9、`sndata_s7_flags_ref.py` 8/8、`sndata_s7_statemachine_ref.py` 4/4。
- **[✅ 已破·续156] 事件文本完整解码（HEXMES.LZW）**：旧记「XOR 加密资源 / 双轨制 / bit 1·3 无 MSGX 锚点」**已证为误判**——真相是 `HEXMES.LZW` 一个与 MESSAGE1~4 同构的**第 5 个 MSGX 文本容器**（283 条战斗/事件文本），只是从未并入 `msgx_all_texts.json`（旧索引仅 6211 条=4 文件），故查不到锚点。现 `event_text_xor_ref.py` 6/6 自校验通过、`hexmes_texts.json` 已写出，静态即可完整解码。另正：旧注把 `0x49fe40` 当成文本解密器是张冠李戴——它实为 `set_diplomacy`。
- **[✅ 已破·续157] SNDATA §11 home_city+status 派生规则**：`scripts/sndata_derive_ref.py` 自校验全通过——① `home_city@49`/`status@57` 字段偏移在 `bsdata.json` fields 实锤；② 派生规则产出正确归属（信长#13 组 18 人→城主=信长；前田庆次#27 `home_city=255` 未登场）；③ 三缓冲扇出映射经 `0x47fc60` 反汇编实锤：`record[6:]→0x522c88`、`record[0x13]→0x522c60`、`record[0x20]→0x522c70`（3 处 `push` 立即数）；④ 三缓冲镜像中静态全 0（runtime-only）。**修正旧误**：§11「92 城」假设不成立——`home_city` 实际索引整个 370 条目名表（49 国+243 城·町+78 职种），花名册出现 **115** 个归属地；名表定宽 stride=9 仅省块(0–48)49/49 干净，城堡块 >8B 名溢出污染后续槽（旧 `_derive_ownership.py` 用 stride 14 是双重错）。`ownership_derived.json` 已用本地 `bsdata.json` 重生成（去掉不存在的 `F:/Games/...` Windows 路径依赖）。

### P3 — 工程卫生（非功能缺口）
- **[✅ 已修] 参考实现自测路径硬编码**：`promote3_ref.py` `terrain_ref.py` `s6_struct_ref.py` 等 10 个脚本的 `open('scripts/...')` 已统一改为基于 `__file__` 的绝对路径（补丁脚本 `scripts/_fix_paths.py`），从任意 CWD 均可跑。顺手修两处 capstone 枚举坑：本环境 `CS_OP_MEM=128` 而 x86 内存操作数 `.type=3`（`X86_OP_MEM`），导致 `entity_loyalty_thresholds_ref` 12/14→14/14、`entity_status2e_ref` `tests` 集合从恒空→正确填 `[1,4,5]`。

---

## 3. 工具类 / 草稿（不计入破译进度）

`scripts/` 下 **645 个 `_` 前缀文件**（`_emu_*` 仿真器、`_xref_*` 交叉引用、`_probe_*` 探针、`_dis*` 反汇编、`_ai*` AI 树 dump、大量 `.txt` 反汇编/调用点 dump、`_ins_index.py`/`_naisei_scan.py` 等基础设施）。这些是逆向过程中的脚手架与中间产物，**不是交付物**，仅供回溯。

> 建议：今后新写的草稿统一放 `scripts/_草稿/` 或加 `_` 前缀，避免与已破的 `*_ref.py` 混在一起显得"混乱"。

---

## 4. 今后工作流建议

1. 每次开破，先从 §2 选一条 P0/P1 任务，建 `TaskCreate` 跟踪。
2. 破译产出 = 一个 `*_ref.py`（含自测）+ 必要时 `*_spec.json` + 在对应 `*_SPEC.md` 写章节 + 在 `BREAKTHROUGHS.md` 续 N 置顶（四段：突破/证据/仍未知/下一步）。
3. 闭合一条就把对应文件从 §2 移到 §1，并刷新本清单顶部"总览"。
4. 自测脚本一律用绝对路径或 `os.path` 相对本文件，杜绝 §2 P3 的路径坑。

---

*生成：2026-08-31 ｜ 依据：各 `*_ref.py` 自测复核（见 §1 状态列）+ `BATTLE_SPEC.md §10` + `SNDATA_SPEC.md §8` + `BREAKTHROUGHS.md 续154`。*
