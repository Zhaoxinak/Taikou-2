# HJMAPDAT.DAT — 合战（战斗）数据结构规范

> 跨 AI 接手用。本文档描述《太阁立志传2》合战系统的核心数据文件 `C:HJMAPDAT.DAT` 的二进制结构。
> 破解依据：EXE 反汇编战斗初始化函数 `0x43a490`（打开 `0x5036f0`="C:HJMAPDAT.DAT"，按 `offset=variant×1700` 读记录，三次 `call 0x4411b0` 拆分写入 `0x512e58`/`0x512868`/`0x512b60`）。

## 文件级

| 项 | 值 |
|---|---|
| 路径(游戏内) | `C:HJMAPDAT.DAT` |
| 原始大小 | 64600 字节（精确 = 38 × 1700） |
| 记录数 | **38**（每记录 = 一场合战/剧本的战斗布局） |
| 记录大小 | **1700 字节** |
| 记录布局 | `[180 兵种参数][760 地形图][760 部署图]` |

## 记录内三段

### A. 兵种/阵形/计略 参数表（偏移 0..179，180 字节）
- 结构：**9 行 × 20 字节** = 9 个「兵种槽位/单位类别」× 每槽 20 个属性字节。
- 运行时载入 `0x512e58`。战斗代码 `0x439063` 读取：
  `lea eax,[ut*5]; mov al,[ecx + ut*20 + 0x512e58]; and eax,0xf`
  → 兵种 id `ut∈[0..8]`、属性下标 `ecx∈[0..19]`，**每字节低4位取值 0-7**（高4位恒 0）。
- 全 38 战 nibble 统计：0×11243、1×761、2×887、3×271、4×183、5×128、6×153、7×54；8-15 为 0。
- **含义待定**：20 属性各字段（攻/防/HP/机动/命中/阵形/计略…）需结合 per-tick 公式反推。见 `battle_combat_data.json` 或 `hjmapdat_battles.json`。

### B. 地形图（偏移 180..939，760 字节 = 40×19）
- 每字节 = **低4位 地形类型(0-15，共16种) + 高4位 修饰位(0-5)**。
- 16 种地形类型：0空 1平地 2荒地 3草地 4森林 5河流 6山地 7桥 8-?B(?8/?9/?A/?B/?C) ?D=城 ?E=阵 ?F=?F。
  （注：`?8`..`?C`、`?F` 为中文定名待补，建议对照游戏内地形观感与 `0x513a78` 地形属性表。）
- 运行时载入 `0x512868`（战斗模拟簇 `0x438c00-0x43a700` 读取，与单位属性叠加算伤害）。

### C. 部署图（偏移 940..1699，760 字节 = 40×19，ASCII）
- 每个单元格一个 ASCII 字符，表示布阵单位/将/地形修饰。
- 实测字符集：`A J K F G I H 8 9 2 + . - ( % ' , / 1 7 5 3` 等（左/右/中军将兵、特例、空地等）。
- 运行时载入 `0x512b60`。

## 运行时缓冲区对应（战斗簇 0x438c00-0x43a700）

| 缓冲地址 | 内容 | 来源段 |
|---|---|---|
| `0x512e58` | 兵种/阵形/计略 参数表（9×20，值0-7） | A |
| `0x512868` | 地形图（40×19，16种+修饰位） | B |
| `0x512b60` | 部署图（40×19，ASCII） | C |
| `0x517720` | 场景大缓冲（含地形循环，由 B:TERRAIN.LZW 另填） | — |

> 注意：HKMAPNEW.LZW（解压 1765B=26×68）是**视觉战图块**，非数值表，别混淆。

## 提取脚本
- `scripts/_extract_hjmapdat.py` → 输出 `hjmapdat_battles.json`（38 战全量：unit_table / terrain[type,mod] / deploy）。
- `scripts/_scan_all_battle_files.py` → 全文件周期表扫描（已证无明文 96B 表，数值在 HJMAPDAT 内）。

## 待破（接手者）
1. section A 每兵种 20 字节字段语义（反推 0x439063 等读取点的 ecx 含义）。
2. 9 个兵种 id ↔ 足轻/骑兵/铁炮/弓/枪/水军/忍者/僧兵/攻城 等具体对应。
3. 地形 16 种中文定名补全（?8-?F）。
4. 部署 ASCII 字符 → 单位/将 映射。
5. 解码 MESSAGE*.LZW(MSGX) 拿兵种/阵形/计略中文名，与上述 id 对表。
